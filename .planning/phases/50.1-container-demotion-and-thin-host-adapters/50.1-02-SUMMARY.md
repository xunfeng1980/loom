---
phase: 50.1-container-demotion-and-thin-host-adapters
plan: 02
subsystem: ingress
tags: [rust, vortex, parquet, lance, thin-adapter, sidecar-stub, container-demotion]

# Dependency graph
requires:
  - phase: 50.1-container-demotion-and-thin-host-adapters
    provides: "RESEARCH.md, PATTERNS.md — research and pattern analysis for ingress degradation"
provides:
  - "Degraded Vortex ingress to thin host adapter (fact extraction + sidecar stubs)"
  - "Degraded Parquet ingress to thin host adapter (fact extraction + sidecar stubs)"
  - "Degraded Lance ingress to thin host adapter (fact extraction + sidecar stubs)"
affects: [50-sidecar-overlay, loom-core, loom-ffi]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Thin adapter degradation: remove emission path, add sidecar/hash stubs, gate oracle to cfg(test)"
    - "Public API: source_facts_from_* + extract_sidecar_bytes_from_* + bind_content_hash_to_*"
    - "Phase 50.1 module doc: thin host adapter note in each lib.rs"

key-files:
  created: []
  modified:
    - ingress/loom-vortex-ingress/src/source_contract.rs
    - ingress/loom-vortex-ingress/src/lib.rs
    - ingress/loom-parquet-ingress/src/source_contract.rs
    - ingress/loom-parquet-ingress/src/lib.rs
    - ingress/loom-lance-ingress/src/source_contract.rs
    - ingress/loom-lance-ingress/src/lib.rs

key-decisions:
  - "Removed all emit_source_ingress_lmc2_from_* and source_ingress_report_from_* from three ingress crates"
  - "Gated oracle batch functions (Arrow materialization) to #[cfg(test)] in all three crates"
  - "Added extract_sidecar_bytes_from_* and bind_content_hash_to_* as Phase 50 placeholder stubs"
  - "Kept all fact extraction functions (*_source_facts_from_*) and conversion helpers public"

patterns-established:
  - "Thin adapter pattern: each ingress crate exposes only fact extraction + sidecar stubs + hash-binding stubs"
  - "cfg(test) gate: Arrow/oracle materialization restricted to test contexts, enforced at Rust compile time"

requirements-completed: []

# Metrics
duration: 13min
completed: 2026-06-11
status: complete
---

# Phase 50.1 Plan 02: Container Demotion — Ingress Adapter Degradation Summary

**Degraded Vortex, Parquet, and Lance ingress crates to thin host adapters — removed all LMC2/LMA1 Arrow emission paths from public API, added sidecar-extraction and content-hash-binding stubs for Phase 50, and gated oracle batch materialization to cfg(test).**

## Performance

- **Duration:** 13 min
- **Started:** 2026-06-11T05:02:41Z
- **Completed:** 2026-06-11T05:16:22Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Vortex ingress: removed ~225 lines of emission code, added extract_sidecar + bind_content_hash stubs, gated vortex_arrow_oracle_batches_from_buffer to #[cfg(test)]
- Parquet ingress: removed ~476 lines of emission/dead code, added stubs, gated parquet_arrow_oracle_batches_from_path to #[cfg(test)]
- Lance ingress: removed ~459 lines of emission/dead code, added async extract_sidecar stub + bind_content_hash stub, gated lance_native_oracle_batches_from_path to #[cfg(test)]
- All three crates keep their fact extraction functions public and all fact conversion helpers intact

## Task Commits

Each task was committed atomically:

1. **Task 1: Degrade Vortex ingress** - `8ea6a8e` (feat)
2. **Task 2: Degrade Parquet ingress** - `0c0e041` (feat)
3. **Task 3: Degrade Lance ingress** - `dc96a9a` (feat)

## Files Modified

- `ingress/loom-vortex-ingress/src/source_contract.rs` - Removed emission path, added stubs, gated oracle to cfg(test)
- `ingress/loom-vortex-ingress/src/lib.rs` - Updated re-exports to thin adapter API + doc note
- `ingress/loom-parquet-ingress/src/source_contract.rs` - Removed emission path, added stubs, gated oracle to cfg(test)
- `ingress/loom-parquet-ingress/src/lib.rs` - Updated re-exports to thin adapter API + doc note
- `ingress/loom-lance-ingress/src/source_contract.rs` - Removed emission path, added stubs, gated oracle to cfg(test)
- `ingress/loom-lance-ingress/src/lib.rs` - Updated re-exports to thin adapter API + doc note

## Decisions Made

- Chose to remove all dead helper functions (loom_artifact_from_batches, direct_lma1_artifact_from_batches, legacy_lmc1, arrow_oracle_evidence, source_verification_failed_report, source_oracle_failed_report) from all three ingress crates — they were tightly coupled to the removed emission path
- Kept non_negative_i64_to_u64 in Parquet (still used by fact extraction)
- Gated import-level dead code (Arc, SchemaRef, vortex session extensions) behind #[cfg(test)] rather than removing them entirely to preserve the oracle functions

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed import cleanup cascading beyond plan**
- **Found during:** Tasks 2, 3
- **Issue:** Removing the emission path functions in Parquet and Lance also removed callers of helper functions (legacy_lmc1, raw_bytes_from_batches, layout_from_batches, etc.), creating cascading dead code and unused imports
- **Fix:** Systematically removed all dead helper functions across Parquet (~290 lines) and Lance (~285 lines) beyond the plan-specified emission path; gated remaining test-only imports behind #[cfg(test)]
- **Files modified:** ingress/loom-parquet-ingress/src/source_contract.rs, ingress/loom-lance-ingress/src/source_contract.rs
- **Verification:** cargo check -p loom-parquet-ingress, cargo check -p loom-lance-ingress both pass with zero warnings
- **Committed in:** 0c0e041, dc96a9a

**2. [Rule 1 - Bug] Fixed crate::opened_buffer_or_report duplicate import in Vortex ingress**
- **Found during:** Task 1
- **Issue:** The non-cfg(test) import of `opened_buffer_or_report` was accidentally removed during the initial edit, leaving only the cfg(test) import
- **Fix:** Re-added the non-cfg(test) import since `extract_sidecar_bytes_from_vortex_buffer` uses it outside cfg(test)
- **Files modified:** ingress/loom-vortex-ingress/src/source_contract.rs
- **Verification:** cargo check -p loom-vortex-ingress compiles clean
- **Committed in:** 8ea6a8e

**3. [Rule 2 - Missing Critical] Added proper error conversion in Vortex extract_sidecar stub**
- **Found during:** Task 1
- **Issue:** The original stub used `?` directly on `opened_buffer_or_report` which returns `Result<VortexFile, VortexIngressReport>`, but the function returns `Result<Option<Vec<u8>>, SourceIngressReport>`
- **Fix:** Wrapped with `.map_err(source_report_from_vortex_ingress_report)?` for proper error conversion
- **Files modified:** ingress/loom-vortex-ingress/src/source_contract.rs
- **Verification:** cargo check passes
- **Committed in:** 8ea6a8e

---

**Total deviations:** 3 auto-fixed (1 blocking, 1 bug, 1 missing critical)
**Impact on plan:** All auto-fixes necessary for correctness and clean compilation. No scope creep.

## Issues Encountered

None — all deviations were auto-fixed using deviation rules.

## Known Stubs

| Stub | File | Reason |
|------|------|--------|
| `extract_sidecar_bytes_from_vortex_buffer` returns `Ok(None)` | vortex-ingress/src/source_contract.rs:62-67 | Phase 50 sidecar overlay contract not yet defined |
| `extract_sidecar_bytes_from_parquet_path` returns `Ok(None)` | parquet-ingress/src/source_contract.rs:75-87 | Phase 50 sidecar overlay contract not yet defined |
| `extract_sidecar_bytes_from_lance_path` returns `Ok(None)` | lance-ingress/src/source_contract.rs:87-107 | Phase 50 sidecar overlay contract not yet defined |
| `bind_content_hash_to_vortex_data` returns `Ok(())` | vortex-ingress/src/source_contract.rs:70-74 | Phase 50 hash binding contract not yet defined |
| `bind_content_hash_to_parquet_data` returns `Ok(())` | parquet-ingress/src/source_contract.rs:89-97 | Phase 50 hash binding contract not yet defined |
| `bind_content_hash_to_lance_data` returns `Ok(())` | lance-ingress/src/source_contract.rs:109-115 | Phase 50 hash binding contract not yet defined |

All stubs are intentional Phase 50 placeholders. Consumers must treat `None` as "no sidecar present — fall back to host reader."

## Next Phase Readiness

- All three ingress crates degraded: ready for Phase 50 sidecar overlay contract
- Fact extraction remains functional — backward compat maintained
- Oracle batch functions available in test context for kloom/verifier fixture generation
- No unused-import warnings remain in any ingress crate

---
*Phase: 50.1-container-demotion-and-thin-host-adapters*
*Completed: 2026-06-11*

## Self-Check: PASSED

All files confirmed present on disk. All 3 task commits verified in git log. All acceptance criteria pass.
