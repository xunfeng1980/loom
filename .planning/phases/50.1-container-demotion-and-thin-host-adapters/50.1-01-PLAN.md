---
phase: 50.1-container-demotion-and-thin-host-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/arrow_semantic_codec.rs
  - crates/loom-core/src/arrow_semantic.rs
  - crates/loom-core/src/artifact_verifier.rs
  - crates/loom-core/src/native_arrow_semantic.rs
  - ingress/loom-source-ingress/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "LMC2/LMA1 codec module docs state out-of-TCB, dev-time-only status"
    - "ArtifactVerificationFacts carries tcb_status and artifact_role fields"
    - "LMC2 verifier facts returned by verify_arrow_semantic_container_artifact set tcb_status='out-of-tcb' and artifact_role='dev-time-reference-packaging'"
    - "LMA1 verifier facts returned by verify_arrow_semantic_artifact set tcb_status='out-of-tcb' and artifact_role='dev-time-reference-packaging'"
    - "All encode/decode functions in arrow_semantic_codec.rs remain callable (backward compat)"
    - "DuckDB loom_scan still accepts LMC2 artifacts (backward compat — no code change)"
    - "Native execution still accepts LMC2/LMA1 with deprecation comment (backward compat)"
  artifacts:
    - path: "crates/loom-core/src/arrow_semantic_codec.rs"
      provides: "LMA1/LMC2 codec — demoted to dev-time packaging"
      contains: "OUT-OF-TCB"
    - path: "crates/loom-core/src/artifact_verifier.rs"
      provides: "ArtifactVerificationFacts with tcb_status + artifact_role fields"
      contains: "tcb_status"
    - path: "ingress/loom-source-ingress/src/lib.rs"
      provides: "SourceIngressAcceptedArtifact with deprecation notice"
      contains: "deprecated"
  key_links:
    - from: "crates/loom-core/src/artifact_verifier.rs"
      to: "crates/loom-core/src/arrow_semantic_codec.rs"
      via: "verify_arrow_semantic_container_artifact / verify_arrow_semantic_artifact sets facts fields"
      pattern: "facts\\.tcb_status"
    - from: "crates/loom-ffi/src/duckdb_runtime.rs"
      to: "crates/loom-core/src/arrow_semantic_codec.rs"
      via: "is_arrow_semantic_container / decode_arrow_semantic_container_payload — unchanged"
      pattern: "is_arrow_semantic_container"
---

<objective>
Demote LMC2/LMA1 from top-level distribution format to optional lineage evidence + dev-time reference packaging. This is a role/claim/semantics change — NOT a code deletion. The codec, verifier, and DuckDB paths remain intact for backward compat.

Purpose: Fulfill Repositioning Decision Two (§8 item 4) first slice: mark LMC2/LMA1 as out-of-TCB so that the sidecar overlay (Phase 50) is the sole in-TCB artifact. All existing gates pass because this is a marker/doc change only.
Output: Updated module docs in arrow_semantic_codec.rs; new tcb_status + artifact_role fields on ArtifactVerificationFacts; deprecation notice on SourceIngressAcceptedArtifact; backward-compat comments in native_arrow_semantic.rs.
</objective>

<execution_context>
@/Users/macintoshhd/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/50.1-container-demotion-and-thin-host-adapters/50.1-PATTERNS.md
@.planning/phases/49-independent-l2core-decode-ir-codec-and-content-hash-identity/49-SUMMARY.md
@crates/loom-core/src/arrow_semantic_codec.rs
@crates/loom-core/src/artifact_verifier.rs
@crates/loom-core/src/arrow_semantic.rs
@crates/loom-core/src/native_arrow_semantic.rs
@ingress/loom-source-ingress/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LMC2/LMA1 codec module docs to out-of-TCB</name>
  <files>crates/loom-core/src/arrow_semantic_codec.rs</files>
  <read_first>
    - crates/loom-core/src/arrow_semantic_codec.rs (current module docs at lines 1-2)
    - crates/loom-core/src/container_codec.rs (analog module doc pattern for comparison)
  </read_first>
  <action>
    Update the module-level doc comment in arrow_semantic_codec.rs (line 1-2 currently "//! `LMA1` Arrow semantic payload codec and `LMC2` wrapper.") to add Phase 50.1 out-of-TCB status.

    Replace current module docs with the expanded version from PATTERNS.md lines 316-326:
    - Keep all existing docs
    - Add section: "**Phase 50.1: OUT-OF-TCB — dev-time reference packaging only.**"
    - State that codecs are retained for kloom/verifier test fixtures and backward-compat decode
    - State that production emission paths do NOT use LMC2/LMA1
    - State that encode functions are documented as dev-time only

    Add `Dev-time only.` doc comment to these public functions (above their existing doc lines):
    - `encode_arrow_semantic_payload` (line 46)
    - `encode_arrow_semantic_container_payload` (line 79)
    - `wrap_arrow_semantic_payload` (line 86)

    Do NOT add `#[cfg(test)]` gates — per RESEARCH.md, these functions stay callable for backward compat. Do NOT change any function signatures or implementations.

    Per security: no new attack surface — this is documentation-only.
  </action>
  <verify>
    <automated>
      grep -q "OUT-OF-TCB" crates/loom-core/src/arrow_semantic_codec.rs || echo "MISSING: out-of-TCB marker"
      grep -q "Dev-time only" crates/loom-core/src/arrow_semantic_codec.rs || echo "MISSING: dev-time-only marker"
      cargo check -p loom-core 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] Module docs contain "OUT-OF-TCB" and "dev-time reference packaging"
    - [ ] encode_arrow_semantic_payload has "Dev-time only" in its doc comment
    - [ ] wrap_arrow_semantic_payload has "Dev-time only" in its doc comment
    - [ ] cargo check -p loom-core passes
    - [ ] No function signatures changed
  </acceptance_criteria>
  <done>LMC2/LMA1 codec documented as out-of-TCB dev-time-only; all functions remain callable.</done>
</task>

<task type="auto">
  <name>Task 2: Add tcb_status + artifact_role to ArtifactVerificationFacts</name>
  <files>crates/loom-core/src/artifact_verifier.rs</files>
  <read_first>
    - crates/loom-core/src/artifact_verifier.rs (struct definition at lines 149-170, impl new() at lines 172-180)
    - The full PATTERNS.md section for artifact_verifier.rs (lines 345-403)
  </read_first>
  <action>
    Add two new fields to ArtifactVerificationFacts struct (insert before the closing `}` at line 169, after `lowering_ready`):

    ```rust
    /// TCB status of the artifact kind. "in-tcb" for L2Core IR artifacts;
    /// "out-of-tcb" for LMC2/LMA1 (demoted to dev-time packaging in Phase 50.1).
    /// None for LMC1 artifacts (inherits default: L1 structural only).
    pub tcb_status: Option<String>,

    /// Production role of the artifact kind.
    /// "dev-time-reference-packaging" for LMC2/LMA1;
    /// "distribution-container" for LMC1 (backward compat);
    /// None when unspecified.
    pub artifact_role: Option<String>,
    ```

    Add corresponding initializers in `ArtifactVerificationFacts::new()` (after the `lowering_ready` field initialization):
    ```rust
    tcb_status: None,
    artifact_role: None,
    ```

    Set fields in `verify_arrow_semantic_container_artifact` (after existing field assignments at line ~410, before `ArtifactVerificationReport::accepted(facts)`):
    ```rust
    facts.tcb_status = Some("out-of-tcb".to_string());
    facts.artifact_role = Some("dev-time-reference-packaging".to_string());
    ```

    Set fields in `verify_arrow_semantic_artifact` (after existing field assignments at line ~445, before `ArtifactVerificationReport::accepted(facts)`):
    ```rust
    facts.tcb_status = Some("out-of-tcb".to_string());
    facts.artifact_role = Some("dev-time-reference-packaging".to_string());
    ```

    Do NOT touch the LMC1 verification path (`verify_artifact` function starting at line 270) — LMC1 is not affected by this demotion. Do NOT modify any other facts fields.

    Per security (T-50.1-01): demoted LMC2 code path re-enters TCB through production code — mitigated by explicit out-of-tcb marker + ongoing audit by artifact_verifier tests.
  </action>
  <verify>
    <automated>
      cargo check -p loom-core 2>&1
      cargo test -p loom-core --lib -- artifact_verifier 2>&1
      grep -c 'tcb_status' crates/loom-core/src/artifact_verifier.rs | grep -v '^#' | xargs -I{} sh -c '[ {} -ge 5 ]' || echo "MISSING: tcb_status field usage"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] ArtifactVerificationFacts has `tcb_status: Option<String>` field
    - [ ] ArtifactVerificationFacts has `artifact_role: Option<String>` field
    - [ ] new() initializes both fields to None
    - [ ] LMC2 verifier path sets tcb_status="out-of-tcb" and artifact_role="dev-time-reference-packaging"
    - [ ] LMA1 verifier path sets tcb_status="out-of-tcb" and artifact_role="dev-time-reference-packaging"
    - [ ] LMC1 path NOT modified (no tcb_status set)
    - [ ] cargo test -p loom-core --lib -- artifact_verifier passes
  </acceptance_criteria>
  <done>ArtifactVerificationFacts carries tcb_status/artifact_role; LMC2/LMA1 facts marked out-of-TCB.</done>
</task>

<task type="auto">
  <name>Task 3: Add deprecation and compat annotations in arrow_semantic.rs, native_arrow_semantic.rs, and loom-source-ingress</name>
  <files>
    crates/loom-core/src/arrow_semantic.rs
    crates/loom-core/src/native_arrow_semantic.rs
    ingress/loom-source-ingress/src/lib.rs
  </files>
  <read_first>
    - crates/loom-core/src/arrow_semantic.rs (LMC2_MAGIC/LMA1_MAGIC constants at lines 16-19)
    - crates/loom-core/src/native_arrow_semantic.rs (artifact kind checks at lines 401-409, 495-506, 623-633)
    - ingress/loom-source-ingress/src/lib.rs (SourceIngressAcceptedArtifact at lines 552-560)
    - 50.1-PATTERNS.md sections for arrow_semantic.rs (lines 406-412), native_arrow_semantic.rs (lines 422-442), loom-source-ingress (lines 280-300)
  </read_first>
  <action>
    THREE SMALL CHANGES across three files:

    1. **crates/loom-core/src/arrow_semantic.rs**: Add a module-level comment before the constants (after the existing module doc if any) stating Phase 50.1 out-of-TCB status. The LMC2_MAGIC and LMA1_MAGIC constants stay unchanged for backward-compat decode. Minimal: one comment line.

    2. **crates/loom-core/src/native_arrow_semantic.rs**: Add a deprecation comment at EACH of the three artifact_kind check sites (lines 401, 495, 623 approximately — grep for `"LMC2" | "LMA1"`) that reads:
       ```
       // Phase 50.1: LMC2/LMA1 kept for backward compat with existing test fixtures.
       // Phase 50 will re-anchor native execution to sidecar overlay.
       // DO NOT remove LMC2/LMA1 acceptance until sidecar-native track is production-ready.
       ```
       Add immediately above each of the three `if !matches!(facts.artifact_kind.as_str(), "LMC2" | "LMA1")` blocks.
       Do NOT change any logic — only comments.

    3. **ingress/loom-source-ingress/src/lib.rs**: Add `#[deprecated(since = "0.1.0", note = "LMC2/LMA1 demoted to dev-time packaging in Phase 50.1; production paths use sidecar overlay")]` annotation on `pub struct SourceIngressAcceptedArtifact` (around line 552). Keep the struct and all fields unchanged.

    Per security (T-50.1-03): demoted code paths remain accessible — mitigated by deprecation annotations + docs update in this phase.
  </action>
  <verify>
    <automated>
      cargo check -p loom-core 2>&1
      cargo check -p loom-source-ingress 2>&1
      cargo build 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] arrow_semantic.rs has out-of-TCB comment near LMC2_MAGIC/LMA1_MAGIC
    - [ ] native_arrow_semantic.rs has Phase 50.1 deprecation comment at all 3 artifact_kind check sites
    - [ ] SourceIngressAcceptedArtifact has #[deprecated] annotation
    - [ ] cargo build passes (deprecation warnings expected, not errors)
    - [ ] No logic changes in any file
  </acceptance_criteria>
  <done>Deprecation annotations and compat comments added; full cargo build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loom-core codec → production callers | LMC2/LMA1 encode functions remain callable but are out-of-TCB |
| artifact_verifier → downstream consumers | tcb_status field now carries in-tcb/out-of-tcb classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.1-01 | Elevation of Privilege | arrow_semantic_codec.rs | mitigate | Explicit "OUT-OF-TCB" module docs + tcb_status field on all LMC2/LMA1 facts; Phase 50.1-02 removes the production emission path that could silently promote demoted containers |
| T-50.1-02 | Tampering | artifact_verifier.rs | mitigate | tcb_status set per artifact kind with fail-closed default (None = unspecified); downstream consumers must check tcb_status before making in-TCB claims |
| T-50.1-03 | Information Disclosure | native_arrow_semantic.rs | accept | No PII/sensitive data; deprecation comments are advisory only; native execution still accepts LMC2 for backward compat |
| T-50.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
<automated>
  cargo build 2>&1
  cargo test -p loom-core --lib -- artifact_verifier 2>&1
  cargo test -p loom-core --test artifact_verifier 2>&1
</automated>
</verification>

<success_criteria>
- [ ] arrow_semantic_codec.rs module docs state OUT-OF-TCB, dev-time-only
- [ ] ArtifactVerificationFacts carries tcb_status and artifact_role
- [ ] LMC2 and LMA1 verifier paths set both fields
- [ ] LMC1 path unchanged
- [ ] SourceIngressAcceptedArtifact deprecated
- [ ] native_arrow_semantic.rs has compat comments
- [ ] cargo build succeeds
- [ ] All existing artifact_verifier tests pass
- [ ] No production code paths broken
</success_criteria>

<output>
Create `.planning/phases/50.1-container-demotion-and-thin-host-adapters/50.1-01-SUMMARY.md` when done
</output>
