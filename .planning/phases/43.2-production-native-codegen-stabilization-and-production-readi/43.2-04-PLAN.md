---
phase: 43.2-production-native-codegen-stabilization-and-production-readi
plan: 04
type: execute
wave: 2
depends_on:
  - 43.2-01
  - 43.2-02
  - 43.2-03
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-native-melior/src/jit.rs
  - crates/loom-native-melior/tests/production_arrow_semantic_soak.rs
  - .planning/phases/43.2-production-native-codegen-stabilization-and-production-readi/43.2-PERF-RESOURCE-REPORT.md
autonomous: true
requirements:
  - CODEGEN-STABLE-04
must_haves:
  truths:
    - "D-43.2-10: Repeated native-codegen execution does not rely on leaked state, stale cache evidence, or skipped tooling."
    - "D-43.2-11: Cancellation latency, memory ownership, and release/lifetime assumptions are measured or explicitly bounded before ABI freeze."
    - "D-43.2-12: Native-vs-interpreter timing evidence is recorded as bounded production-readiness data, not a GA performance promise."
---

<objective>
Collect resource, soak, cancellation, cache replay, and bounded performance
evidence for the real native-codegen path so Phase 44 can freeze ownership and
runtime contracts with production behavior in view.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add repeated execution and cache replay tests</name>
  <files>crates/loom-native-melior/tests/production_arrow_semantic_soak.rs</files>
  <action>Add a bounded soak test that repeatedly executes supported Phase 35 artifacts through the real production route, validates every output, records cache hit/miss/replay behavior, and asserts stable replay evidence with no stale acceptance after intentional drift.</action>
  <acceptance_criteria>
    - Repeated execution validates every iteration.
    - Cache hit/miss behavior is deterministic for unchanged vs drifted inputs.
    - The test is bounded enough for normal CI and does not require external services.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --features melior --test production_arrow_semantic_soak replay_soak</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add cancellation and resource evidence</name>
  <files>crates/loom-native-melior/src/jit.rs, crates/loom-core/src/native_arrow_semantic.rs</files>
  <action>Expose route-level timing/resource evidence needed for production readiness: cancellation checkpoints, cancellation diagnostic latency where measurable, output buffer ownership/lifetime classification, and release assumptions. Keep values deterministic enough for tests by using bounded counters or status fields rather than wall-clock assertions where possible.</action>
  <acceptance_criteria>
    - Cancellation before support, before JIT, and before validation has distinct diagnostics.
    - Output buffers are documented as owned Rust `Vec<u8>` evidence before any future host ABI handoff.
    - Resource evidence does not expose raw pointers as identity.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --features melior --test production_arrow_semantic_soak cancellation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Record bounded native-vs-interpreter timing evidence</name>
  <files>.planning/phases/43.2-production-native-codegen-stabilization-and-production-readi/43.2-PERF-RESOURCE-REPORT.md</files>
  <action>Add a report with bounded timing/resource observations for representative Phase 35 rows. Treat timing as readiness evidence only; do not claim a stable speedup target or GA performance guarantee.</action>
  <acceptance_criteria>
    - Report names dataset shapes, row counts, native path, interpreter/reference comparison path, host/toolchain identity, and caveats.
    - Report distinguishes measured evidence from performance promises.
    - Report names any resource/ownership findings that must feed Phase 44.
  </acceptance_criteria>
  <verify>
    <manual>Review report against CODEGEN-STABLE-04 and Phase 44 ABI inputs.</manual>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-native-melior --features melior --test production_arrow_semantic_soak
cargo test -p loom-native-melior --features melior --test production_arrow_semantic_route
git diff --check
</verification>

<success_criteria>
Plan 43.2-04 is complete when repeated real native-codegen execution, cache
replay, cancellation, ownership, and bounded timing evidence are recorded without
turning readiness data into unsupported GA performance claims.
</success_criteria>

<output>
Create `43.2-04-SUMMARY.md` when done.
</output>

