---
phase: 43.2-production-native-codegen-stabilization-and-production-readi
plan: 02
type: execute
wave: 1
depends_on:
  - 43.2-01
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-native-melior/src/jit.rs
  - crates/loom-native-melior/tests/production_arrow_semantic_route.rs
  - crates/loom-core/tests/native_arrow_semantic_codegen_stability.rs
autonomous: true
requirements:
  - CODEGEN-STABLE-02
  - CODEGEN-STABLE-04
must_haves:
  truths:
    - "D-43.2-04: Positive evidence comes from a production route that prepares support, executes the real JIT/native backend, validates output, and admits runtime/cache identity."
    - "D-43.2-05: Cancellation, missing toolchain, unsupported shape, divergent output, and cache mismatch fail closed or take explicit fallback without partial positive evidence."
    - "D-43.2-06: The production route does not count helper-only execution as production native-codegen evidence."
---

<objective>
Close the gap between helper-local JIT tests and production-route evidence:
supported artifacts must run through one route that prepares, JITs, validates,
decides runtime disposition, and admits cache identity under strict policy.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add production route tests</name>
  <files>crates/loom-native-melior/tests/production_arrow_semantic_route.rs</files>
  <action>Add feature-enabled route tests that run supported `LMC2(LMA1)` and direct `LMA1` artifacts through a production native-codegen route helper: support preparation, real `ExecutionEngine` output, Phase 40 validation, runtime decision, replay evidence, and cache key admission.</action>
  <acceptance_criteria>
    - Positive route evidence is emitted only after real JIT output and Phase 40 validation.
    - The route records backend/toolchain/pipeline identity used by the JIT.
    - Direct `LMA1` remains explicit bridge evidence, not the default source artifact path.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --features melior --test production_arrow_semantic_route positive</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add production route helper</name>
  <files>crates/loom-native-melior/src/jit.rs, crates/loom-core/src/native_arrow_semantic.rs</files>
  <action>Add a route helper that composes the existing Phase 43.1 pieces without weakening them. It must return a structured route report with status, diagnostics, support evidence, JIT output identity, validation outcome, runtime decision, cacheability, replay evidence, and fallback disposition.</action>
  <acceptance_criteria>
    - Route helper does not duplicate verifier/native eligibility policy in host-specific code.
    - Route helper rejects before JIT if support extraction fails.
    - Route helper rejects before cache admission if validation or replay evidence fails.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --features melior --test production_arrow_semantic_route</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Add route negative and fallback tests</name>
  <files>crates/loom-native-melior/tests/production_arrow_semantic_route.rs</files>
  <action>Add tests for cancellation, unsupported shapes, missing non-feature backend, validation divergence, explicit interpreter fallback, strict fail-closed behavior, and cache-key mismatch. Assert no negative route produces cacheable native evidence.</action>
  <acceptance_criteria>
    - Cancellation is distinguishable from validation failure and toolchain failure.
    - Strict policy fails closed on divergence.
    - Fallback-enabled policy records interpreter fallback without native cache admission.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --test production_arrow_semantic_route</automated>
    <automated>cargo test -p loom-native-melior --features melior --test production_arrow_semantic_route negative</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-native-melior --test production_arrow_semantic_route
cargo test -p loom-native-melior --features melior --test production_arrow_semantic_route
cargo test -p loom-core --test native_arrow_semantic_codegen_stability
git diff --check
</verification>

<success_criteria>
Plan 43.2-02 is complete when production-route evidence, not helper-only
evidence, is the positive native-codegen path, and all negative route modes
fail closed or fallback explicitly without native cache admission.
</success_criteria>

<output>
Create `43.2-02-SUMMARY.md` when done.
</output>

