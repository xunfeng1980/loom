---
phase: 43.2-production-native-codegen-stabilization-and-production-readi
plan: 03
type: execute
wave: 2
depends_on:
  - 43.2-01
  - 43.2-02
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/tests/native_arrow_semantic_codegen_adversarial.rs
autonomous: true
requirements:
  - CODEGEN-STABLE-02
  - CODEGEN-STABLE-03
must_haves:
  truths:
    - "D-43.2-07: Native output validation rejects malformed Arrow buffer contracts before positive validation evidence."
    - "D-43.2-08: Boundary shapes in the Phase 35 matrix, including zero-row and one-row batches, are explicitly covered."
    - "D-43.2-09: Direct LMA1 bridge inputs and LMC2(LMA1) defaults share validation strength while preserving distinct artifact identity."
---

<objective>
Attack the native output validator at the real Arrow buffer boundary so Phase 44
does not freeze an ABI around under-specified buffer, bitmap, offset, null-count,
schema, or trace behavior.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add malformed buffer contract tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen_adversarial.rs</files>
  <action>Add tests that mutate native output columns with too-short and too-long value buffers, too-short validity bitmaps, mismatched null counts, missing validity for nullable columns, extra validity for non-null columns where disallowed, wrong value-buffer kind, wrong field name, wrong data type, and swapped column order.</action>
  <acceptance_criteria>
    - Every malformed output returns a deterministic diagnostic code/path.
    - No malformed output is Phase 40 validated.
    - No malformed output can produce replay/cache evidence.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_adversarial malformed</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Add bitmap, offset, and null boundary tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen_adversarial.rs</files>
  <action>Add tests for Boolean value bitmap corruption, validity bitmap drift, sliced Arrow buffers with non-zero offsets, zero-row batches, one-row batches, all-null nullable columns, no-null nullable columns, and mixed nullable/non-null multi-column matrices.</action>
  <acceptance_criteria>
    - Sliced buffer support preserves exact logical rows and bitmap offsets.
    - Zero-row and one-row supported artifacts are accepted or rejected with explicit documented diagnostics.
    - Bitmap drift is caught by output equality or trace validation before cache admission.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_adversarial bitmap_boundary</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Add artifact and trace adversarial tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen_adversarial.rs</files>
  <action>Add tests for malformed `LMC2`, malformed direct `LMA1`, multi-batch payloads, schema drift after support extraction, trace-fingerprint divergence, and direct-bridge/default-wrapper identity confusion.</action>
  <acceptance_criteria>
    - Malformed artifacts reject before native backend execution.
    - Trace divergence rejects after output rebuilding but before native cache admission.
    - Direct bridge evidence cannot masquerade as default `LMC2(LMA1)` source evidence.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_adversarial artifact_trace</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic_codegen_adversarial
cargo test -p loom-core --test native_arrow_semantic_codegen
cargo test -p loom-core --test native_arrow_semantic_codegen_stability
git diff --check
</verification>

<success_criteria>
Plan 43.2-03 is complete when adversarial buffer, bitmap, schema, trace, and
artifact mutations fail closed with deterministic diagnostics and cannot produce
positive validation or cache evidence.
</success_criteria>

<output>
Create `43.2-03-SUMMARY.md` when done.
</output>

