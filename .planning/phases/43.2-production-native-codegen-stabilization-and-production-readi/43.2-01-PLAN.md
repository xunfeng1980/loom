---
phase: 43.2-production-native-codegen-stabilization-and-production-readi
plan: 01
type: execute
wave: 1
depends_on:
  - 43.1-01
  - 43.1-02
  - 43.1-03
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/tests/native_arrow_semantic_codegen_stability.rs
autonomous: true
requirements:
  - CODEGEN-STABLE-01
  - CODEGEN-STABLE-04
must_haves:
  truths:
    - "D-43.2-01: Production native-codegen identity is deterministic for unchanged artifact, schema, projection, predicate, split, backend, toolchain, and pipeline inputs."
    - "D-43.2-02: Drift in any native-codegen identity input produces a cache miss or fail-closed diagnostic before native cache admission."
    - "D-43.2-03: Output buffer digests and Phase 40 trace fingerprints are recorded as first-class production-readiness evidence."
---

<objective>
Make Phase 43.1 production native-codegen output replayable and drift-aware:
support reports, output digests, validation traces, and cache keys must be
stable for unchanged inputs and visibly change or fail closed when inputs drift.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add deterministic replay tests for supported artifacts</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen_stability.rs</files>
  <action>Add tests that build the same `LMC2(LMA1)` and direct `LMA1` supported artifacts repeatedly, prepare codegen support, validate identical native output buffers, and assert stable schema fingerprints, support fingerprints, output buffer digests, Phase 40 trace fingerprints, runtime decisions, and cache keys.</action>
  <acceptance_criteria>
    - Repeated runs over unchanged `LMC2(LMA1)` inputs produce identical replay records.
    - Repeated runs over explicit direct `LMA1` bridge inputs produce identical replay records while retaining direct-bridge identity.
    - Tests use non-zero and nullable data so accidental zero-buffer or null-dropping behavior fails.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_stability replay</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add production native-codegen replay evidence</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs</files>
  <action>Introduce replay/identity evidence helpers that compute stable support fingerprints, output-buffer digests, validation trace fingerprints, and cache-admission fingerprints from the existing support and execution reports. Keep the schema deterministic and independent of host addresses or allocation order.</action>
  <acceptance_criteria>
    - Fingerprints include artifact digest, artifact kind, payload kind, schema fingerprint, row/column count, backend identity, pipeline identity, and validation trace fingerprints.
    - Fingerprints exclude pointer addresses, allocation IDs, wall-clock time, and host-local temporary paths.
    - Unsupported or unvalidated reports cannot produce positive replay evidence.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_stability replay</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Add drift detection tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen_stability.rs</files>
  <action>Add tests that intentionally vary projection, predicate, split, backend identity, pipeline identity, artifact bytes, schema, and output buffers. Assert each drift changes the replay/cache evidence or fails closed before cache admission.</action>
  <acceptance_criteria>
    - Projection/predicate/split drift changes cache identity.
    - Backend/toolchain/pipeline drift changes cache identity.
    - Artifact/schema/output drift cannot accidentally hit a prior native cache key.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen_stability drift</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic_codegen_stability
cargo test -p loom-core --test native_arrow_semantic_codegen
git diff --check
</verification>

<success_criteria>
Plan 43.2-01 is complete when production native-codegen identity is replayable,
deterministic, and drift-aware, and no unsupported or unvalidated execution can
produce positive cache/replay evidence.
</success_criteria>

<output>
Create `43.2-01-SUMMARY.md` when done.
</output>

