---
phase: 29-starrocks-duckdb-dual-query-surface
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
files_modified:
  - crates/loom-dual-query-surface/src/query_surface.rs
  - crates/loom-dual-query-surface/tests/query_surface_negative.rs
  - crates/loom-dual-query-surface/tests/dependency_boundary.rs
  - scripts/dual-query-surface-test.sh
autonomous: true
requirements:
  - PHASE-29
must_haves:
  truths:
    - "D-08/D-15: stale binding evidence, identity drift, sidecar-only success, separate artifact formats, and unsupported query features fail closed with no accepted descriptor/evidence."
    - "D-02/D-09/D-17: live StarRocks runtime smoke is optional, env-gated, and never required for the deterministic release proof."
    - "D-10/D-11/D-12: scope guards continue to prevent StarRocks credentials/network/catalog routes, public SQL/C ABI creep, and Phase 30 Vortex semantic expansion."
  artifacts:
    - path: crates/loom-dual-query-surface/tests/query_surface_negative.rs
      provides: "Mismatch, unsupported feature, and no-accepted-evidence negative matrix"
    - path: crates/loom-dual-query-surface/tests/dependency_boundary.rs
      provides: "Expanded scope guards for StarRocks runtime/client/credential/query creep"
    - path: scripts/dual-query-surface-test.sh
      provides: "Focused gate with negative checks and optional StarRocks runtime-smoke skip semantics"
  key_links:
    - from: crates/loom-dual-query-surface/tests/query_surface_negative.rs
      to: crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
      via: "Phase 28 mismatch fail-closed pattern reuse"
      pattern: "assert_no_accepted_bytes"
    - from: scripts/dual-query-surface-test.sh
      to: crates/loom-dual-query-surface/tests/query_surface_negative.rs
      via: "focused negative matrix test invocation"
      pattern: "query_surface_negative"
    - from: scripts/dual-query-surface-test.sh
      to: scripts/mvp0-verify.sh
      via: "ordering guard prepared for Plan 29-05 wiring"
      pattern: "dual-query-surface-test"
---

<objective>
Harden the dual query-surface contract with negative coverage and optional StarRocks runtime handling.

Purpose: make the StarRocks-compatible proof fail closed on trust/scope drift while keeping live StarRocks runtime checks optional and visibly non-canonical.
Output: negative Rust tests, expanded dependency/API guards, and a focused gate that distinguishes descriptor evidence from optional runtime smoke.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-starrocks-duckdb-dual-query-surface/29-CONTEXT.md
@.planning/phases/29-starrocks-duckdb-dual-query-surface/29-RESEARCH.md
@.planning/phases/29-starrocks-duckdb-dual-query-surface/29-PATTERNS.md
@.planning/phases/29-starrocks-duckdb-dual-query-surface/29-03-SUMMARY.md
@crates/loom-dual-query-surface/src/query_surface.rs
@crates/loom-dual-query-surface/tests/dependency_boundary.rs
@crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
@scripts/dual-query-surface-test.sh
@scripts/iceberg-binding-test.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed query-surface negative tests</name>
  <files>crates/loom-dual-query-surface/src/query_surface.rs, crates/loom-dual-query-surface/tests/query_surface_negative.rs</files>
  <behavior>
    - Mutating table UUID, schema ID, snapshot ID, artifact SHA-256, row count, or projection prevents accepted StarRocks-compatible descriptor output.
    - Sidecar-only, manifest-only, stale source evidence, forged oracle evidence, and malformed artifact paths return unsupported or rejected reports with no accepted descriptor/evidence.
    - Unsupported query features such as join, freeform SQL, external table DDL, remote catalog, credentials, nested field, nullable expansion, distributed execution, and predicate pushdown return unsupported diagnostics.
  </behavior>
  <action>Create `tests/query_surface_negative.rs` and extend `query_surface.rs` as needed so all trust and scope mismatches fail closed per D-05/D-08/D-12/D-15. Reuse the Plan 29-02 fixture generator plus Phase 28 mismatch patterns to mutate identity and evidence one dimension at a time. Assert that negative cases return `Unsupported` or `Rejected`, carry actionable diagnostics, and expose no accepted descriptor, no query result evidence, and no alternate artifact bytes. Unsupported query cases should be represented through typed unsupported requests rather than freeform accepted SQL.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test query_surface_negative</automated>
  </verify>
  <done>The dual-surface contract rejects identity/evidence drift and unsupported features without accepted descriptor or evidence leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand dependency and public-scope guards</name>
  <files>crates/loom-dual-query-surface/tests/dependency_boundary.rs, scripts/dual-query-surface-test.sh</files>
  <behavior>
    - Direct default dependencies on StarRocks, JDBC, ODBC, MySQL, HTTP/REST clients for StarRocks, catalog SDKs, object-store SDKs, credential helpers, and Docker orchestration are rejected.
    - Public headers, `duckdb-ext`, CLI code, `loom-core`, `loom-ffi`, `loom-source-ingress`, and `loom-iceberg-binding` remain free of StarRocks-compatible query-surface vocabulary except where intentionally allowed by comments in planning docs.
    - Scope guards avoid self-matching by scanning non-comment code lines and constructing forbidden markers from pieces.
  </behavior>
  <action>Harden `dependency_boundary.rs` and `scripts/dual-query-surface-test.sh` per D-09/D-10/D-11/D-12/D-15. The guards must reject new StarRocks runtime/client/catalog/credential dependencies, public SQL route names, public C ABI symbols, CLI routes, external table DDL controls, remote catalog settings, object-store credentials, query-engine framework markers, and Phase 30 semantic-compatibility terms in production code. Keep StarRocks-compatible vocabulary allowed only inside `crates/loom-dual-query-surface`, `scripts/dual-query-surface-test.sh`, and Phase 29 planning/report artifacts. Use filtered `awk`/`rg` checks rather than unfiltered count gates.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test dependency_boundary && bash -n scripts/dual-query-surface-test.sh</automated>
  </verify>
  <done>Dependency and public-surface guards prevent Phase 29 from becoming a live StarRocks connector, generic engine framework, public ABI expansion, or Phase 30 compatibility phase.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional StarRocks runtime-smoke skip semantics</name>
  <files>scripts/dual-query-surface-test.sh</files>
  <action>Extend `scripts/dual-query-surface-test.sh` with explicit optional StarRocks runtime-smoke handling per D-02/D-07/D-09/D-17. By default, the gate must print that optional StarRocks runtime smoke is skipped and that this skip is not accepted StarRocks runtime evidence. If `LOOM_STARROCKS_RUNTIME_SMOKE=1` is set, require complete env inputs before attempting any runtime command: `STARROCKS_MYSQL`, `STARROCKS_HOST`, `STARROCKS_PORT`, `STARROCKS_USER`, `STARROCKS_PASSWORD`, `STARROCKS_DATABASE`, and `STARROCKS_TABLE`. When all are present, run a supplemental query equivalent to the bounded descriptor matrix against the operator-provided table and compare count/sum/rows to the canonical evidence; report the result as supplemental runtime smoke, not as the deterministic acceptance root. If env is incomplete while the opt-in flag is set, fail with a clear diagnostic. Do not launch Docker, create FE/BE/CN services, write credentials, create external catalogs, or require StarRocks for `scripts/mvp0-verify.sh` per D-09/D-16.</action>
  <verify>
    <automated>bash scripts/dual-query-surface-test.sh && bash -c 'set +e; LOOM_STARROCKS_RUNTIME_SMOKE=1 bash scripts/dual-query-surface-test.sh >/tmp/loom-starrocks-missing-env.out 2>&1; status=$?; test "$status" -ne 0; rg -q "STARROCKS_" /tmp/loom-starrocks-missing-env.out'</automated>
  </verify>
  <done>The focused gate clearly distinguishes deterministic offline descriptor evidence from optional live StarRocks runtime smoke and remains reproducible without a cluster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted binding identity -> descriptor identity | Descriptor mutation must not preserve accepted status. |
| supported query allowlist -> unsupported feature requests | Unsupported features must not turn into accepted freeform SQL. |
| developer environment -> optional StarRocks runtime smoke | Env-provided credentials/endpoints are untrusted and optional. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Spoofing | descriptor identity fields | mitigate | Negative tests mutate table UUID, schema, snapshot, hash, row count, and projection and assert no accepted descriptor per D-06/D-08. |
| T-29-04-02 | Tampering | sidecar/source/oracle evidence | mitigate | Negative tests reuse Phase 28 stale/forged evidence patterns and assert no accepted output per D-05/D-15. |
| T-29-04-03 | Elevation of Privilege | unsupported query features | mitigate | Joins, freeform SQL, DDL, remote catalogs, credentials, nested/nullable expansion, distributed execution, and pushdown return unsupported diagnostics per D-08/D-12. |
| T-29-04-04 | Information Disclosure | optional StarRocks credentials | mitigate | Runtime smoke is opt-in, env-only, never stored, never required, and fails on incomplete env only when explicitly requested per D-09/D-17. |
| T-29-04-05 | Repudiation | skipped StarRocks runtime smoke | mitigate | Gate output states skipped runtime smoke is not accepted runtime evidence; later report must preserve that distinction per D-07/D-17. |
| T-29-SC | Tampering | Cargo dependency graph | mitigate | Expanded dependency tests and shell guards reject service/client/catalog dependency creep. |
</threat_model>

<verification>
Overall plan checks:
- `cargo test -p loom-dual-query-surface --test query_surface_negative`
- `cargo test -p loom-dual-query-surface --test dependency_boundary`
- `bash -n scripts/dual-query-surface-test.sh`
- `bash scripts/dual-query-surface-test.sh`
- `bash -c 'set +e; LOOM_STARROCKS_RUNTIME_SMOKE=1 bash scripts/dual-query-surface-test.sh >/tmp/loom-starrocks-missing-env.out 2>&1; status=$?; test "$status" -ne 0; rg -q "STARROCKS_" /tmp/loom-starrocks-missing-env.out'`
</verification>

<success_criteria>
Plan 29-04 is complete when the dual-query surface fails closed on stale binding evidence, descriptor identity drift, sidecar-only claims, separate artifact formats, unsupported query features, and scope/dependency creep, while optional StarRocks runtime smoke remains explicitly opt-in and non-required.
</success_criteria>

<output>
Create `.planning/phases/29-starrocks-duckdb-dual-query-surface/29-04-SUMMARY.md` when done.
</output>
