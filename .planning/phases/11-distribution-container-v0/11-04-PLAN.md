---
phase: 11-distribution-container-v0
plan: "04"
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - README.md
  - README-zh.md
  - scripts/mvp0-verify.sh
  - scripts/container-negative-test.sh
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/phases/11-distribution-container-v0/11-01-SUMMARY.md
  - .planning/phases/11-distribution-container-v0/11-02-SUMMARY.md
  - .planning/phases/11-distribution-container-v0/11-03-SUMMARY.md
  - .planning/phases/11-distribution-container-v0/11-04-SUMMARY.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, DIST-05]
must_haves:
  truths:
    - "D-11-11: Phase 11 docs distinguish LMC1 container from internal LMP1/LMT1 payloads"
    - "D-11-12: final gate includes negative container cases"
    - "D-11-13: Phase 12/13/14 remain placeholders only"
  artifacts:
    - path: "scripts/container-negative-test.sh"
      provides: "negative container release gate"
      contains: "unknown required feature"
    - path: "README.md"
      provides: "Phase 11 public docs"
      contains: "LMC1"
---

<objective>
Close Phase 11 with public documentation, negative container release-gate coverage, final verification, requirement closure, and phase summaries.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add container documentation and negative gate</name>
  <files>README.md, README-zh.md, scripts/container-negative-test.sh, scripts/mvp0-verify.sh</files>
  <read_first>
    - README.md
    - README-zh.md
    - scripts/mvp0-verify.sh
    - scripts/verifier-negative-test.sh
    - .planning/phases/11-distribution-container-v0/11-RESEARCH.md
  </read_first>
  <action>
    Document `LMC1` as the Phase 11 distribution container v0 and explain that `LMP1`/`LMT1` remain internal wrapped payloads. Add a negative container script that creates curated malformed containers or invokes a test fixture path for unknown required feature, unsupported version, duplicate payload section, truncated section, and offset overflow. Wire that script into `scripts/mvp0-verify.sh`.
  </action>
  <verify>
    <automated>bash scripts/container-negative-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - README and README-zh name `LMC1` and show inspect/decode/smoke commands.
    - Docs do not claim formal verification, native speed, URI/signature support, or real Vortex file ingestion.
    - Negative container gate passes and is called from `scripts/mvp0-verify.sh`.
  </acceptance_criteria>
  <done>Phase 11 behavior is documented and malformed containers are part of the release gate.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 11 verification gates</name>
  <files>workspace</files>
  <read_first>
    - scripts/mvp0-verify.sh
    - scripts/duckdb-smoke-test.sh
  </read_first>
  <action>
    Run the full Phase 11 gate suite after all container changes: focused container tests, CLI inspect/decode on generated container fixtures, DuckDB smoke, full `scripts/mvp0-verify.sh`, fixture hygiene grep, loom-core dependency guard, and `git diff --check`. Do not close `DIST-*` requirements until all required gates pass.
  </action>
  <verify>
    <automated>cargo test -p loom-core container_codec verifier</automated>
    <automated>cargo test --workspace</automated>
    <automated>bash scripts/container-negative-test.sh</automated>
    <automated>bash scripts/duckdb-smoke-test.sh</automated>
    <automated>bash scripts/mvp0-verify.sh</automated>
    <automated>test "$(cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}')" = "0"</automated>
    <automated>rg -n 'vortex_file|vortex-file|\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - All listed gates pass.
    - DuckDB smoke output covers container-wrapped fixtures.
    - No Vortex/FastLanes dependency enters `loom-core`.
    - Fixture hygiene grep has no forbidden file-backed Vortex API matches.
  </acceptance_criteria>
  <done>Phase 11 implementation is verified end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Close requirements, roadmap, state, and summaries</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/11-distribution-container-v0/*-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/PROJECT.md
    - .planning/phases/11-distribution-container-v0/11-01-PLAN.md
    - .planning/phases/11-distribution-container-v0/11-02-PLAN.md
    - .planning/phases/11-distribution-container-v0/11-03-PLAN.md
    - .planning/phases/11-distribution-container-v0/11-04-PLAN.md
  </read_first>
  <action>
    After final verification passes, mark `DIST-01` through `DIST-05` complete in requirements, mark all Phase 11 plans complete in ROADMAP, update STATE to reflect Phase 11 completion, keep Phase 12/13/14 as placeholders only, and write concise `11-01-SUMMARY.md` through `11-04-SUMMARY.md` with files changed, verification commands, commit hashes, and residual follow-up notes.
  </action>
  <verify>
    <automated>rg -n 'DIST-0.*Complete|Distribution Container v0|11-04-SUMMARY|Placeholder' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md .planning/phases/11-distribution-container-v0</automated>
  </verify>
  <acceptance_criteria>
    - Requirements show `DIST-01` through `DIST-05` complete.
    - ROADMAP shows Phase 11 plans `4/4`.
    - STATE current position advances away from incomplete Phase 11 work.
    - Four Phase 11 summary files exist.
    - Phase 12/13/14 remain placeholders only.
  </acceptance_criteria>
  <done>Phase 11 is documented, verified, and closed in planning state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public docs -> future implementers | Docs must avoid overclaiming beyond v0 container behavior. |
| Negative fixtures -> release gate | Malformed container classes must stay visible in the one-command gate. |
| Planning state -> future phases | Phase 12/13/14 placeholders must not imply accidental execution scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-11 | Repudiation | docs | mitigate | Summaries record exact verification commands and commits. |
| T-11-12 | Tampering | negative gate | mitigate | Curated malformed containers fail closed in `mvp0-verify.sh`. |
| T-11-13 | Spoofing | future scope | mitigate | Phase 12/13/14 remain labeled placeholders in ROADMAP/STATE. |

No high open threats remain if release gates pass and docs preserve scope boundaries.
</threat_model>

<verification>
- `cargo test -p loom-core container_codec verifier`
- `cargo test --workspace`
- `bash scripts/container-negative-test.sh`
- `bash scripts/duckdb-smoke-test.sh`
- `bash scripts/mvp0-verify.sh`
- `test "$(cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}')" = "0"`
- `rg -n 'vortex_file|vortex-file|\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures`
- `git diff --check`
</verification>

<success_criteria>
- Public docs describe `LMC1` accurately without overclaiming final Loom capabilities.
- The release gate covers container success and negative cases.
- `DIST-01` through `DIST-05` are closed only after final gates pass.
- Phase 12/13/14 remain placeholders.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `container-negative-test.sh` | release gate | `scripts/` |
| Phase 11 docs | public docs | `README.md`, `README-zh.md` |
| Phase 11 summaries | execution summaries | `.planning/phases/11-distribution-container-v0/` |

