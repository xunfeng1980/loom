---
phase: 19
plan: 19-05
title: CLI, release gate, and solver verifier closeout
type: execute
wave: 5
depends_on:
  - 19-04
files_modified:
  - crates/loom-cli/src/main.rs
  - scripts/solver-verifier-test.sh
  - scripts/mvp0-verify.sh
  - README.md
  - README-zh.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-REPORT.md
  - .planning/phases/19-solver-backed-full-artifact-verifier/19-SUMMARY.md
autonomous: true
requirements: []
---

<objective>
Close Phase 19 with reviewer-visible solver-backed verifier status, a release-gated Bitwuzla solver script, public/planning documentation, and an explicit Phase 20 handoff.
</objective>

<context>
By this point Phase 19 should have core solver report types, deterministic `QF_BV` SMT-LIB scripts, a Bitwuzla backend, and artifact-verifier integration. The closeout must make the evidence visible without claiming production native lowering or full proof-object checking.
</context>

<threat_model>
- Threat: CLI hides skipped solver evidence.
  Mitigation: print `discharged`, `failed`, `unknown`, or `skipped` explicitly with backend name and strict-mode hint.
- Threat: release gate passes without Bitwuzla and looks like proof.
  Mitigation: default mode may skip, but strict mode must fail; docs must distinguish skip from proof.
- Threat: Phase 20 consumes collected-only facts.
  Mitigation: final report and docs state Phase 20 may consume only discharged facts.
</threat_model>

<tasks>
  <task id="1" type="execute">
    <title>Expose solver-backed status in CLI</title>
    <read_first>
      <file>crates/loom-cli/src/main.rs</file>
      <file>crates/loom-core/src/artifact_verifier.rs</file>
      <file>crates/loom-solver-smt/src/lib.rs</file>
    </read_first>
    <files>crates/loom-cli/src/main.rs</files>
    <action>
      Extend `loom verify-artifact` with solver-backed output. Recommended shape: existing structural verification remains default; an option or mode runs solver-backed verification through `loom-solver-smt`. Output must show backend kind, script logic, required obligation count, discharged/failed/unknown/skipped counts, and whether facts are production-discharge-ready. Include Bitwuzla primary wording.
    </action>
    <verify>
      <automated>cargo test -p loom-cli</automated>
      <automated>cargo run --bin loom -- verify-artifact --help | rg -n "solver|bitwuzla|discharge"</automated>
    </verify>
    <acceptance_criteria>
      <criterion>CLI exposes solver status without hiding skipped evidence.</criterion>
      <criterion>CLI output names Bitwuzla when solver-backed verification is requested.</criterion>
      <criterion>CLI reports `CollectedOnly` vs `Discharged` distinctly.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <title>Wire Phase 19 gate into release verification</title>
    <read_first>
      <file>scripts/solver-verifier-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>scripts/artifact-verifier-test.sh</file>
    </read_first>
    <files>scripts/solver-verifier-test.sh, scripts/mvp0-verify.sh</files>
    <action>
      Finalize `scripts/solver-verifier-test.sh` and wire it into `scripts/mvp0-verify.sh` after the Phase 17/18 verifier/reader gates and before native expansion checks. Normal mode may skip unavailable Bitwuzla with `[SKIP]`; `LOOM_REQUIRE_SOLVER=1` must require Bitwuzla-backed evidence.
    </action>
    <verify>
      <automated>bash scripts/solver-verifier-test.sh</automated>
      <automated>LOOM_REQUIRE_SOLVER=1 bash scripts/solver-verifier-test.sh || true</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>rg -n "solver-verifier-test|LOOM_REQUIRE_SOLVER|Phase 19" scripts/mvp0-verify.sh scripts/solver-verifier-test.sh</automated>
    </verify>
    <acceptance_criteria>
      <criterion>`scripts/mvp0-verify.sh` invokes `scripts/solver-verifier-test.sh`.</criterion>
      <criterion>Default release gate remains usable on machines without Bitwuzla by printing explicit skip evidence.</criterion>
      <criterion>Strict solver gate fails unless Bitwuzla-backed discharge evidence is available.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <title>Write final report and update docs</title>
    <read_first>
      <file>.planning/phases/19-solver-backed-full-artifact-verifier/19-RESEARCH.md</file>
      <file>.planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-CONTRACT.md</file>
      <file>README.md</file>
      <file>README-zh.md</file>
      <file>.planning/PROJECT.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/STATE.md</file>
    </read_first>
    <files>README.md, README-zh.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-REPORT.md, .planning/phases/19-solver-backed-full-artifact-verifier/19-SUMMARY.md</files>
    <action>
      Write `19-SOLVER-REPORT.md` with sections named `Scope`, `Solver Architecture`, `Backend Contract`, `Bitwuzla Evidence`, `SMT-LIB Emission`, `Artifact Verifier Integration`, `CLI and Release Gate`, `Commands Run`, `Deferred Work`, and `Phase 20 Handoff`. Write `19-SUMMARY.md` with shipped files, commands, deviations, residual risks, and `Self-Check: PASSED` only after final verification. Update public docs concisely to say Phase 19 adds solver-backed verifier evidence, not production native execution.
    </action>
    <verify>
      <automated>rg -n "Solver Architecture|Backend Contract|Bitwuzla Evidence|SMT-LIB Emission|Artifact Verifier Integration|Phase 20 Handoff|Self-Check" .planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-REPORT.md .planning/phases/19-solver-backed-full-artifact-verifier/19-SUMMARY.md</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Final report states Bitwuzla is the primary backend.</criterion>
      <criterion>Final report states Z3/cvc5 are declarations or optional cross-check paths.</criterion>
      <criterion>Final report states Phase 20 may only trust discharged facts.</criterion>
      <criterion>Docs avoid claiming production native execution or checked proof objects.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="execute">
    <title>Run final Phase 19 verification</title>
    <read_first>
      <file>scripts/solver-verifier-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>.planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-REPORT.md</file>
    </read_first>
    <files>.planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-REPORT.md, .planning/phases/19-solver-backed-full-artifact-verifier/19-SUMMARY.md</files>
    <action>
      Run and record final verification commands: `cargo test -p loom-core --test solver_contract`, `cargo test -p loom-core --test smtlib_emitter`, `cargo test -p loom-core --test artifact_solver_discharge`, `cargo test -p loom-solver-smt`, `bash scripts/solver-verifier-test.sh`, `bash scripts/mvp0-verify.sh`, and `git diff --check`. Record whether strict Bitwuzla evidence passed or was unavailable on the local machine.
    </action>
    <verify>
      <automated>cargo test -p loom-core --test solver_contract</automated>
      <automated>cargo test -p loom-core --test smtlib_emitter</automated>
      <automated>cargo test -p loom-core --test artifact_solver_discharge</automated>
      <automated>cargo test -p loom-solver-smt</automated>
      <automated>bash scripts/solver-verifier-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>All final non-strict verification commands exit 0.</criterion>
      <criterion>`19-SOLVER-REPORT.md` records exact commands run.</criterion>
      <criterion>Strict Bitwuzla availability is recorded accurately as pass or unavailable/fail, not silently ignored.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<must_haves>
- CLI makes solver-backed status reviewer-visible.
- Release gate records Phase 19 solver evidence.
- Strict mode requires Bitwuzla-backed discharge.
- Phase 20 handoff forbids trusting `CollectedOnly` obligations.
</must_haves>

<verification>
- `cargo test -p loom-core --test solver_contract`
- `cargo test -p loom-core --test smtlib_emitter`
- `cargo test -p loom-core --test artifact_solver_discharge`
- `cargo test -p loom-solver-smt`
- `bash scripts/solver-verifier-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Phase 19 has solver-backed verifier evidence, Bitwuzla primary backend support, CLI visibility, release-gate coverage, and a clear Phase 20 discharged-facts handoff.
</success_criteria>

