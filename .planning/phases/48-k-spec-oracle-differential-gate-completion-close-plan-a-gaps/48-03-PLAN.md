---
phase: 48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps
plan: 03
type: execute
wave: 3
depends_on: ["48-01", "48-02"]
files_modified:
  - contrib/kloom/scripts/kloom-diff.sh
  - contrib/kloom/scripts/kloom-llvm-feasibility.sh
  - contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md
  - contrib/kloom/docs/SEMANTICS.md
  - contrib/kloom/README.md
  - scripts/model-rust-interpreter-consistency-test.sh
  - .github/workflows/ci.yml
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "kloom-diff.sh and the CI kloom job run strict (do NOT set LOOM_ALLOW_K_ORACLE_SKIP) so K-absence or divergence fails CI"
    - "Local/dev gate paths can pass without K installed by setting LOOM_ALLOW_K_ORACLE_SKIP=1, which records a skip"
    - "A skip-aware LLVM-backend feasibility script kompiles kloom.k with --backend llvm and compares the 12-test corpus traces against the Haskell backend, recording a findings doc"
    - "The LLVM-backend gate is NOT wired strict into the release pipeline (skip-aware, no production wiring)"
    - "contrib/kloom README and SEMANTICS no longer describe the stale v0/pure-append model and reflect v4 coverage plus the four-state oracle outcome taxonomy"
    - "STATE.md and ROADMAP.md mark Phase 48 complete and the ROADMAP goal/non-goals match the narrowed scope actually delivered"
  artifacts:
    - path: "contrib/kloom/scripts/kloom-llvm-feasibility.sh"
      provides: "Skip-aware LLVM-backend kompile + Haskell-vs-LLVM trace parity over the 12-test corpus"
      min_lines: 30
    - path: "contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md"
      provides: "Findings doc recording whether LLVM backend kompiled, trace parity result, and explicit unknowns"
      min_lines: 15
  key_links:
    - from: ".github/workflows/ci.yml (kloom job)"
      to: "contrib/kloom/scripts/kloom-diff.sh"
      via: "strict invocation (LOOM_ALLOW_K_ORACLE_SKIP unset)"
      pattern: "kloom-diff.sh"
---

<objective>
Wire the typed K-oracle outcome and skip convention from plans 48-01/48-02 into the gate scripts and CI: keep kloom-diff.sh and the CI kloom job strict (never set LOOM_ALLOW_K_ORACLE_SKIP), let local/dev gate paths opt into skip, and keep the model-rust-interpreter-consistency-test passing. Add a skip-aware LLVM-backend feasibility script plus a findings doc (NOT a strict release gate, no production wiring). Sync the stale contrib/kloom docs to v4 coverage and the four-state outcome taxonomy. Close out STATE.md and ROADMAP.md for the narrowed scope actually delivered.

Purpose: Closes gap #4 (LLVM-backend feasibility evidence) and the doc-sync / skip-convention wiring that makes the new outcome taxonomy operational across the gates, completing Plan-A's reconciliation core within the user-narrowed scope.

Output: skip-convention wiring in kloom-diff.sh + ci.yml + the consistency script, a new kloom-llvm-feasibility.sh + LLVM-BACKEND-FEASIBILITY.md, refreshed SEMANTICS.md/README.md, and Phase 48 closeout in STATE.md/ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-CONTEXT.md
@.planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-RESEARCH.md
@.planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-01-PLAN.md
@.planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan (48-03) produces:
- `contrib/kloom/scripts/kloom-llvm-feasibility.sh` — skip-aware LLVM-backend kompile + corpus trace parity script.
- `contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md` — findings doc (records availability, parity result, explicit unknowns A1-A4 from RESEARCH).
- Strict-mode confirmation/comments in `contrib/kloom/scripts/kloom-diff.sh` and `.github/workflows/ci.yml` (LOOM_ALLOW_K_ORACLE_SKIP intentionally unset).
- Skip-convention documentation/handling in `scripts/model-rust-interpreter-consistency-test.sh`.
- Doc sync: `contrib/kloom/docs/SEMANTICS.md`, `contrib/kloom/README.md`.
- Closeout edits to `.planning/STATE.md` and `.planning/ROADMAP.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire skip convention strictly into kloom-diff.sh, CI, and the consistency script</name>
  <files>contrib/kloom/scripts/kloom-diff.sh, .github/workflows/ci.yml, scripts/model-rust-interpreter-consistency-test.sh</files>
  <read_first>
    - contrib/kloom/scripts/kloom-diff.sh (full: Step 0 tool checks ~52-58, kompile ~62-70, krun corpus ~75-115, cargo test invocations ~121/130)
    - .github/workflows/ci.yml lines 204-246 (kloom job: Nix install, kompile haskell, krun corpus, kloom-diff.sh invocation)
    - scripts/model-rust-interpreter-consistency-test.sh (full: grep assertions ~39-43 reference kloom_trace_for_program / kloom_harness / "K spec-oracle"; cargo test ~35)
    - scripts/toolchain-common.sh lines ~20-30 (LOOM_ALLOW_NATIVE_TOOL_SKIP "1" check + skip messaging pattern to mirror)
    - .planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-RESEARCH.md Q4 (skip convention: kloom-diff.sh + CI strict / do NOT set the var; local verify scripts may set it), Pitfall 2 (definition-dir-absent must be hard fail in strict CI)
  </read_first>
  <action>
    kloom-diff.sh: confirm it stays STRICT — it must NOT set or export LOOM_ALLOW_K_ORACLE_SKIP, so the harness treats krun-absence/definition-missing as hard fail from this gate (Step 0 already hard-fails when kompile/krun are missing — keep that). Add a comment documenting that this gate is strict and LOOM_ALLOW_K_ORACLE_SKIP is intentionally NOT set. ci.yml kloom job: confirm it does NOT set LOOM_ALLOW_K_ORACLE_SKIP for the kloom-diff.sh step or the cargo test steps (strict); add a short comment near the differential-gate step recording the strict intent. If the consistency-test grep assertions reference symbols this phase renamed/added, update them so the script still passes and optionally add an assertion for the new typed-outcome wiring (e.g. KOracleOutcome present in native_arrow_semantic.rs/kloom_harness.rs); keep all existing passing assertions intact. model-rust-interpreter-consistency-test.sh runs cargo test -p loom-core --test native_arrow_semantic — ensure it still passes; document the LOOM_ALLOW_K_ORACLE_SKIP=1 local escape hatch in a comment but do NOT set it unconditionally (strict by default). Do NOT weaken any existing strict gate.
  </action>
  <verify>
    <automated>cd /Users/macintoshhd/loom-demo && bash -n contrib/kloom/scripts/kloom-diff.sh && bash -n scripts/model-rust-interpreter-consistency-test.sh && grep -qi "kloom-diff.sh" .github/workflows/ci.yml && echo STRICT_OK</automated>
  </verify>
  <acceptance_criteria>
    - bash -n passes for both shell scripts (no syntax errors).
    - kloom-diff.sh does NOT contain an assignment LOOM_ALLOW_K_ORACLE_SKIP=1: strict.
    - ci.yml kloom job does NOT set LOOM_ALLOW_K_ORACLE_SKIP and still invokes kloom-diff.sh.
    - consistency-test grep assertions still pass against the post-48-01/02 source (run if krun available, else document skip).
  </acceptance_criteria>
  <done>The differential gate and CI kloom job remain strict (no skip env var set), the consistency-test assertions match the new source, and the LOOM_ALLOW_K_ORACLE_SKIP=1 local escape hatch is documented but not enabled by default.</done>
</task>

<task type="auto">
  <name>Task 2: LLVM-backend feasibility script + findings doc + contrib/kloom doc sync</name>
  <files>contrib/kloom/scripts/kloom-llvm-feasibility.sh, contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md, contrib/kloom/docs/SEMANTICS.md, contrib/kloom/README.md</files>
  <read_first>
    - contrib/kloom/scripts/kloom-diff.sh (kompile/krun invocation patterns, trace-extraction awk over the events cell ~88-100, tool-check + fail() helper to reuse)
    - contrib/kloom/tests/semantics/ (the 12 anchor .kloom tests the parity loop iterates)
    - contrib/kloom/docs/SEMANTICS.md (stale "Known Limitations (v0)" section listing readInput/letScalar as missing — kloom.k v4 now implements them)
    - contrib/kloom/README.md (stale architecture position; any reference to a deleted l2_core_reference_executor leg)
    - .planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-RESEARCH.md Q5 (LLVM backend script shape, .build-llvm output dir, explicit unknowns), Assumptions Log A1-A4, Open Question 1 (LLVM backend may be unavailable in Nix; skip gracefully and the findings doc must say so), Open Question 3 (doc-sync items)
    - .planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-CONTEXT.md (LLVM evidence is skip-aware, does NOT gate release strictly, no production code touched; deferred items must NOT be re-introduced into docs as in-scope)
  </read_first>
  <action>
    Create contrib/kloom/scripts/kloom-llvm-feasibility.sh: (1) detect kompile/krun; if absent OR --backend llvm unsupported AND LOOM_ALLOW_K_ORACLE_SKIP=1, emit a skip note and exit 0 (skip-aware, mirrors the optional-toolchain pattern); if absent and the var is unset, exit non-zero with a clear message (runnable strict on demand, but NOT added to the release pipeline). (2) kompile src/kloom.k --backend llvm -o .build-llvm. (3) For each tests/semantics/*.kloom: run krun against .build (Haskell) and .build-llvm (LLVM), extract the events trace with the same awk used in kloom-diff.sh, and diff; any divergence fails. (4) Print "N tests compared, all identical => feasibility confirmed". Do NOT add this script to ci.yml as a strict step and do NOT call it from kloom-diff.sh's failure path (manual or non-blocking note only). Create contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md recording: how to run the script, the result captured (or "LLVM backend not available in this environment; feasibility UNVERIFIED" if it could not run), and the explicit unknowns A1-A4 from RESEARCH (token-spacing parity, --gen-bison-parser need, MAP/LIST syntax support, Nix LLVM-backend availability). SEMANTICS.md + README.md doc sync: replace stale "v0"/"pure-append"/"Known Limitations (v0)" wording with the actual v4 coverage (readInput, letScalar, forRange, cursorLoop, appendValue/appendNull, failClosed) and add a short section describing this phase's four-state K-oracle outcome taxonomy (compared / diverged-hard-fail / referee-absent-skip / unsupported-program-skip) and the per-shape native-route disable behavior. Remove any reference to the deleted Rust reference-executor leg. Do NOT describe deferred items (near-exhaustive corpus, sync-checklist gate, real min/max rules, extracted LLVM interpreter) as in-scope or delivered.
  </action>
  <verify>
    <automated>cd /Users/macintoshhd/loom-demo && bash -n contrib/kloom/scripts/kloom-llvm-feasibility.sh && test -s contrib/kloom/docs/LLVM-BACKEND-FEASIBILITY.md && echo FEASIBILITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - bash -n passes for kloom-llvm-feasibility.sh; the script is skip-aware (skip with LOOM_ALLOW_K_ORACLE_SKIP=1 when K/LLVM-backend absent) and is NOT referenced as a strict step in ci.yml.
    - LLVM-BACKEND-FEASIBILITY.md exists and records the result (or UNVERIFIED + reason) plus the A1-A4 unknowns.
    - SEMANTICS.md and README.md no longer contain stale "Known Limitations (v0)" / "pure-append" descriptions, mention the v4 coverage, the four-state outcome taxonomy, and per-shape disable; no deleted reference-executor leg referenced; deferred items not described as delivered.
  </acceptance_criteria>
  <done>A skip-aware LLVM-backend feasibility script and findings doc exist (NOT a strict release gate, no production wiring); contrib/kloom docs reflect v4 coverage, the four-state oracle taxonomy, and per-shape disable, with stale v0 wording and the deleted reference-executor leg removed.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 48 closeout in STATE.md and ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (front-matter progress block, Current Position, Progress Snapshot)
    - .planning/ROADMAP.md lines ~1314-1326 (Phase 48 entry — Goal currently describes the PRE-narrowing scope: Rust interpreter leg, near-exhaustive corpus, four-place sync checklist; Requirements TBD; Plans 0 placeholder)
    - .planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-CONTEXT.md (the narrowed scope of record: no Rust interpreter leg, two-way K↔native mirror reconciliation, reconciliation-core-only, near-exhaustive corpus + sync-checklist DEFERRED)
  </read_first>
  <action>
    ROADMAP.md Phase 48 entry: rewrite the Goal to match the narrowed scope actually delivered (two-way K↔native per-builder-event mirror reconciliation with K as sole reference executor — NO Rust interpreter leg; krun-absent/timeout recorded skip that never blocks the production gate; per-shape native-route disable on divergence; placeholder-hole programs classified unsupported-for-K-oracle; skip-aware LLVM-backend feasibility evidence) and move the near-exhaustive corpus + sync-checklist into the Non-goals/Deferred line. Update the Plans count to 3 and list the three plans with one-line objectives. Mark the phase complete (checkbox/marker consistent with how other completed phases are recorded). STATE.md: update the front-matter progress (completed_phases / completed_plans / percent — phase 48 adds 3 completed plans), set Current Position / stopped_at / last_activity to reflect Phase 48 complete (Plan-A reconciliation core landed: typed K-oracle outcome, per-shape disable, skip convention, LLVM-backend feasibility evidence), and note the explicit deferrals carried forward. Keep edits consistent with the existing STATE.md format; do not invent new schema keys.
  </action>
  <verify>
    <automated>cd /Users/macintoshhd/loom-demo && grep -qi "mirror reconciliation" .planning/ROADMAP.md && grep -qi "48" .planning/STATE.md && echo CLOSEOUT_OK</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 48 Goal reflects the narrowed delivered scope (mirror reconciliation, no Rust interpreter leg) and lists 3 plans; near-exhaustive corpus + sync-checklist appear only as deferred/non-goals.
    - STATE.md progress block and Current Position reflect Phase 48 complete with 3 added completed plans and the carried-forward deferrals.
    - grep checks pass.
  </acceptance_criteria>
  <done>ROADMAP.md and STATE.md record Phase 48 as complete under the narrowed scope, with the three plans listed and the deferred items (near-exhaustive corpus, sync-checklist gate, real min/max rules, extracted LLVM interpreter) carried forward, not described as delivered.</done>
</task>

</tasks>

<verification>
- `bash -n` passes for kloom-diff.sh, kloom-llvm-feasibility.sh, and model-rust-interpreter-consistency-test.sh.
- LLVM-BACKEND-FEASIBILITY.md exists and is non-empty.
- ci.yml kloom job remains strict (no LOOM_ALLOW_K_ORACLE_SKIP) and invokes kloom-diff.sh; the LLVM feasibility script is NOT a strict CI step.
- ROADMAP.md and STATE.md reflect Phase 48 complete under the narrowed scope.
- If K is available locally: `bash contrib/kloom/scripts/kloom-diff.sh` exits 0 and `bash scripts/model-rust-interpreter-consistency-test.sh` exits 0.
</verification>

<success_criteria>
The K-oracle skip convention is operational across gates: kloom-diff.sh and CI run strict (krun-absence/divergence fails), while local/dev runs may opt into skip with LOOM_ALLOW_K_ORACLE_SKIP=1. A skip-aware LLVM-backend feasibility script and findings doc capture whether kloom.k kompiles under --backend llvm with corpus trace parity, without touching production or gating release strictly. The contrib/kloom docs reflect v4 coverage, the four-state outcome taxonomy, and per-shape disable. STATE.md and ROADMAP.md record Phase 48 complete under the user-narrowed scope with deferred items carried forward.
</success_criteria>

<output>
Create `.planning/phases/48-k-spec-oracle-differential-gate-completion-close-plan-a-gaps/48-03-SUMMARY.md` when done.
</output>
