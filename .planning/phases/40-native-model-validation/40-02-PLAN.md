---
phase: 40-native-model-validation
plan: 40-02
title: Fail Closed Routing And TCB Record
depends_on:
  - 40-01
requirements:
  - LINEAGE-10
decisions:
  - D-40-06
  - D-40-07
  - D-40-08
  - D-40-09
  - D-40-10
summary: >
  Make native route/cache eligibility depend on successful native/model
  validation, wire a focused gate into the broad verifier path, and record
  permanent MLIR/LLVM TCB assumptions and non-claims.
---

## Tasks

<task id="40-02-T1">
  <name>Add validation-aware runtime/cache helpers</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Add runtime decision/cache-key helpers that require a successful native/model validation report. Divergent or unsupported reports must fail closed and cannot seed native cache identity.</action>
  <acceptance>
    <automated>cargo test -p loom-core --test native_arrow_semantic native_model</automated>
    <criterion>`rg -n "validated.*runtime|validated.*cache|native/model validation" crates/loom-core/src/native_arrow_semantic.rs crates/loom-core/tests/native_arrow_semantic.rs` shows validation-aware routing.</criterion>
  </acceptance>
</task>

<task id="40-02-T2">
  <name>Wire focused and broad gates</name>
  <files>scripts/native-model-validation-test.sh, scripts/full-verifier-test.sh</files>
  <action>Create a focused Phase 40 gate and wire it into the verified-lineage broad gate after model/Rust consistency. Add markers for trace comparison, fail-closed divergence, and TCB/non-claim language.</action>
  <acceptance>
    <automated>bash scripts/native-model-validation-test.sh</automated>
    <automated>bash scripts/full-verifier-test.sh</automated>
    <criterion>`rg -n "native-model-validation-test|native/model|translation validation|TCB" scripts/full-verifier-test.sh scripts/native-model-validation-test.sh` shows gate wiring and markers.</criterion>
  </acceptance>
</task>

<task id="40-02-T3">
  <name>Close requirements and docs</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/40-native-model-validation/40-02-SUMMARY.md</files>
  <action>Close LINEAGE-09/10, mark Phase 40 complete, record MLIR/LLVM as permanent TCB, and state non-claims. Move state to Phase 41 ready only after all gates pass.</action>
  <acceptance>
    <automated>git diff --check</automated>
    <criterion>`rg -n "LINEAGE-09|LINEAGE-10|Phase 40.*Complete|Phase 41.*READY|per-run translation validation|permanent TCB" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/40-native-model-validation/40-02-SUMMARY.md` shows closeout.</criterion>
  </acceptance>
</task>

## Verification

```sh
bash scripts/native-model-validation-test.sh
bash scripts/full-verifier-test.sh
git diff --check
```

## Completion Criteria

Plan 40-02 is complete when successful native/model validation is required for
validated native route/cache eligibility, divergent validation fails closed, the
focused gate is wired into the broad verified-lineage gate, and Phase 40 docs
state permanent toolchain TCB and non-claims.
