---
phase: 40-native-model-validation
plan: 40-01
title: Native Model Trace Check
depends_on:
  - Phase 35
  - Phase 39
requirements:
  - LINEAGE-09
decisions:
  - D-40-01
  - D-40-02
  - D-40-03
  - D-40-04
  - D-40-05
summary: >
  Add full supported-matrix native/model builder-event trace validation for
  native Arrow semantic execution, using the Phase 39 reference executor as the
  model side of the comparison.
---

## Tasks

<task id="40-01-T1">
  <name>Extend model scalar tags for Float32/Float64</name>
  <files>formal/lean/LoomCore.lean, crates/loom-core/src/l2_core.rs, crates/loom-core/src/full_verifier.rs, crates/loom-core/src/l2_core_reference_executor.rs, crates/loom-core/tests/l2_core_reference_executor.rs</files>
  <action>Add Float32/Float64 scalar type tags and bit-pattern constants sufficient for append-value trace typing. Do not add float arithmetic. Preserve Lean no-sorry and Lean/Rust correspondence gates.</action>
  <acceptance>
    <automated>lean formal/lean/LoomCore.lean</automated>
    <automated>cargo test -p loom-core --test l2_core_reference_executor</automated>
    <criterion>`rg -n "Float32|Float64|float32|float64" formal/lean/LoomCore.lean crates/loom-core/src/l2_core.rs crates/loom-core/src/l2_core_reference_executor.rs` shows model tag coverage.</criterion>
  </acceptance>
</task>

<task id="40-01-T2">
  <name>Add native/model validation report</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Build a reference L2Core append/null trace from supported Arrow semantic batches, build a native trace from native output, compare exactly, and expose a native/model validation report with stable diagnostics.</action>
  <acceptance>
    <automated>cargo test -p loom-core --test native_arrow_semantic native_model</automated>
    <criterion>`rg -n "native.*model|reference_trace|native_trace|native-model-trace-mismatch" crates/loom-core/src/native_arrow_semantic.rs crates/loom-core/tests/native_arrow_semantic.rs` shows trace comparison and diagnostics.</criterion>
  </acceptance>
</task>

<task id="40-01-T3">
  <name>Cover supported matrix and divergence</name>
  <files>crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Add tests for default LMC2 and direct LMA1 validation, full nullable primitive matrix including Float32/Float64, and injected native output divergence.</action>
  <acceptance>
    <automated>cargo test -p loom-core --test native_arrow_semantic</automated>
    <criterion>`rg -n "Float32|Float64|direct.*LMA1|injected.*divergence|native-model-trace-mismatch" crates/loom-core/tests/native_arrow_semantic.rs` shows required coverage.</criterion>
  </acceptance>
</task>

## Verification

```sh
lean formal/lean/LoomCore.lean
cargo test -p loom-core --test l2_core_reference_executor
cargo test -p loom-core --test native_arrow_semantic
git diff --check
```

## Completion Criteria

Plan 40-01 is complete when native Arrow semantic output can be checked against
the reference executor trace for every Phase 35 supported primitive type, and
trace divergence produces stable mismatch diagnostics without claiming compiler
verification.
