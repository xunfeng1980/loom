---
phase: 43-starrocks-live-runtime-integration
plan: 43-03
type: execute
wave: 2
depends_on:
  - 43-01
  - 43-02
requirements:
  - ENGINE-02
autonomous: true
files_expected:
  - .planning/phases/43-starrocks-live-runtime-integration/43-ABI-FINDINGS.md
  - scripts/mvp2-verify.sh
---

# 43-03 Plan: ABI-Shape Findings and MVP2 Gate Wiring

## Objective

Document the second-consumer ABI findings that Phase 44 must consume, then wire
the Phase 43 focused gate into MVP2 without overstating live evidence.

## Tasks

1. Write `43-ABI-FINDINGS.md`.
   - Compare StarRocks runtime needs with the current DuckDB-driven runtime ABI
     and public surfaces.
   - Classify each finding as fixed-now, accepted asymmetry, or Phase 44 input.
   - Cover path scan assumptions, lifecycle/ownership, projection/predicate
     contract, diagnostics, concurrency, and Arrow/raw-buffer output.

2. Wire broad gates.
   - Add Phase 43 focused gate invocation to `scripts/mvp2-verify.sh` after
     Phase 42.
   - Ensure broad verification does not require a local live cluster by default
     and does fail closed when strict live evidence is requested.

3. Close planning state.
   - Update `ENGINE-01..03`, ROADMAP, STATE, and summaries.
   - If live runtime evidence was unavailable locally, leave that fact visible
     and do not claim success criterion 1 as fully satisfied.

## Acceptance Criteria

- `bash scripts/starrocks-live-runtime-test.sh` passes.
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp2-verify.sh` passes.
- Strict live mode fails closed without live StarRocks env/client.
- `git diff --check` passes.

## Handoff

Phase 44 consumes `43-ABI-FINDINGS.md` and the Phase 43 runtime evidence status
to freeze only the ABI claims actually supported by two-engine evidence.
