---
phase: 13-full-loom-verifier
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - crates/loom-core/src/lib.rs
  - crates/loom-core/src/l2_core.rs
  - crates/loom-core/src/l2_core/constraints.rs
  - crates/loom-core/tests/l2_core_model.rs
  - .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md
autonomous: true
requirements: [VERIFIER-03, VERIFIER-04, VERIFIER-05, VERIFIER-07, VERIFIER-10]
must_haves:
  truths:
    - "D-13-02: Rust verifier is the executable boundary"
    - "D-13-03: SMT is an automation backend, not the foundation"
    - "loom-core must keep #![forbid(unsafe_code)]"
  artifacts:
    - path: "crates/loom-core/src/l2_core.rs"
      provides: "L2Core syntax and facts model"
      contains: "VerifiedArtifactFacts"
    - path: "crates/loom-core/src/l2_core/constraints.rs"
      provides: "SMT-ready constraint IR"
      contains: "LoomConstraint"
---

<objective>
Add the Rust `L2Core` model and SMT-ready constraint IR that later verifier tasks can interpret, test, diagnose, and hand to Phase 14 as `VerifiedArtifactFacts`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add the L2Core syntax and fact model</name>
  <files>crates/loom-core/src/lib.rs, crates/loom-core/src/l2_core.rs</files>
  <read_first>
    - crates/loom-core/src/lib.rs
    - crates/loom-core/src/verifier.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/arrow_builder_output.rs
    - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
    - .planning/phases/13-full-loom-verifier/13-PATTERNS.md
  </read_first>
  <action>
    Add a public `l2_core` module under `loom-core`. Define a tiny `L2CoreProgram` model with concrete identifiers from the spec: input slices, scalar expressions, bounded `ForRange` loop, monotone `CursorLoop`, `ReadInput`, `AppendValue`, `AppendNull`, and `FailClosed` operations. Define `Capability`, `ResourceBudget`, `ArrowEventType`, `OutputBuilderState`, and `VerifiedArtifactFacts` structs/enums. Export the module from `crates/loom-core/src/lib.rs`. Keep all new code safe Rust under the existing `#![forbid(unsafe_code)]`.
  </action>
  <verify>
    <automated>rg -n "pub mod l2_core|L2CoreProgram|ForRange|CursorLoop|Capability|ResourceBudget|ArrowEventType|VerifiedArtifactFacts" crates/loom-core/src/lib.rs crates/loom-core/src/l2_core.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/loom-core/src/lib.rs` contains `pub mod l2_core`.
    - `crates/loom-core/src/l2_core.rs` defines `L2CoreProgram`, `Capability`, `ResourceBudget`, `ArrowEventType`, and `VerifiedArtifactFacts`.
    - `cargo check -p loom-core` exits 0 after the new model compiles.
  </acceptance_criteria>
  <done>`loom-core` has a concrete L2Core model for verifier implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add SMT-ready constraint IR</name>
  <files>crates/loom-core/src/l2_core.rs, crates/loom-core/src/l2_core/constraints.rs</files>
  <read_first>
    - crates/loom-core/src/l2_core.rs
    - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
    - .planning/phases/13-full-loom-verifier/13-RESEARCH.md
  </read_first>
  <action>
    Add `crates/loom-core/src/l2_core/constraints.rs` and expose it from `l2_core`. Define `LoomConstraint` variants for `Le`, `Lt`, `Eq`, `AddNoOverflow`, `MulNoOverflow`, `InRange`, `Decreases`, `NonNegative`, and `FeatureImplies`. Define `ConstraintSet` with methods `new`, `push`, `iter`, `is_empty`, and `to_smtlib_comments`. The output is intentionally SMT-ready but must not add a Z3 dependency or make SMT a required build-time tool.
  </action>
  <verify>
    <automated>rg -n "enum LoomConstraint|AddNoOverflow|MulNoOverflow|InRange|Decreases|FeatureImplies|struct ConstraintSet|to_smtlib_comments" crates/loom-core/src/l2_core.rs crates/loom-core/src/l2_core/constraints.rs</automated>
  </verify>
  <acceptance_criteria>
    - `LoomConstraint` includes every variant named in the action.
    - `ConstraintSet::to_smtlib_comments` returns deterministic text suitable for diagnostics.
    - `cargo check -p loom-core` exits 0 without adding an external SMT dependency.
  </acceptance_criteria>
  <done>L2Core has a stable local proof-obligation representation.</done>
</task>

<task type="auto">
  <name>Task 3: Add model and constraint tests</name>
  <files>crates/loom-core/tests/l2_core_model.rs, .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - crates/loom-core/src/l2_core.rs
    - crates/loom-core/src/l2_core/constraints.rs
    - crates/loom-core/tests/safety_contract.rs
    - .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md
  </read_first>
  <action>
    Add `crates/loom-core/tests/l2_core_model.rs` with focused tests proving that a sample bounded copy program can be represented, `VerifiedArtifactFacts` records row count/range/builder facts, and `ConstraintSet::to_smtlib_comments` emits stable names for `AddNoOverflow`, `InRange`, and `Decreases`. Update `13-PROOF-OBLIGATIONS.md` so `VERIFIER-03`, `VERIFIER-04`, `VERIFIER-05`, `VERIFIER-07`, and `VERIFIER-10` cite this model evidence as in-progress or complete for the model layer.
  </action>
  <verify>
    <automated>cargo test -p loom-core --test l2_core_model</automated>
    <automated>rg -n "VERIFIER-03|VERIFIER-04|VERIFIER-05|VERIFIER-07|VERIFIER-10|l2_core_model" .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p loom-core --test l2_core_model` exits 0.
    - Tests assert at least one `InRange`, one `AddNoOverflow`, and one `Decreases` constraint appears in deterministic diagnostic output.
    - The obligation matrix references `l2_core_model.rs`.
  </acceptance_criteria>
  <done>The L2Core model and constraint IR are tested and traced to Phase 13 obligations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Spec -> Rust model | Rust model must not silently omit spec concepts needed by later verification. |
| Constraint IR -> SMT backend | Constraint names and operands must be stable before any solver integration. |
| Verified facts -> Phase 14 | Facts must be explicit data, not implicit verifier side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-04 | Tampering | L2Core model | mitigate | Tests construct a sample program and inspect facts/constraints. |
| T-13-05 | Repudiation | constraint diagnostics | mitigate | `to_smtlib_comments` emits deterministic constraint labels. |
| T-13-06 | Elevation of privilege | capability model | mitigate | Model includes explicit `Capability` and input-range facts before verifier work begins. |

No high open threats remain if the model compiles under `#![forbid(unsafe_code)]` and tests pass.
</threat_model>

<verification>
- `cargo check -p loom-core`
- `cargo test -p loom-core --test l2_core_model`
- `rg -n "pub mod l2_core|L2CoreProgram|LoomConstraint|VerifiedArtifactFacts" crates/loom-core/src/lib.rs crates/loom-core/src/l2_core.rs crates/loom-core/src/l2_core/constraints.rs`
- `git diff --check`
</verification>

<success_criteria>
- L2Core syntax and fact model exists in `loom-core`.
- SMT-ready constraint IR exists without adding a solver dependency.
- Focused model tests pass.
- Proof-obligation matrix cites model evidence.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `l2_core` | Rust module | `crates/loom-core/src/lib.rs` |
| `L2CoreProgram` | Rust struct/enum family | `crates/loom-core/src/l2_core.rs` |
| `Capability` | Rust enum/struct | `crates/loom-core/src/l2_core.rs` |
| `ResourceBudget` | Rust struct | `crates/loom-core/src/l2_core.rs` |
| `ArrowEventType` | Rust enum | `crates/loom-core/src/l2_core.rs` |
| `VerifiedArtifactFacts` | Rust struct | `crates/loom-core/src/l2_core.rs` |
| `LoomConstraint` | Rust enum | `crates/loom-core/src/l2_core/constraints.rs` |
| `ConstraintSet` | Rust struct | `crates/loom-core/src/l2_core/constraints.rs` |
| `l2_core_model.rs` | Rust tests | `crates/loom-core/tests/` |

