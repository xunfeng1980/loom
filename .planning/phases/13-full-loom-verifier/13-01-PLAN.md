---
phase: 13-full-loom-verifier
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
  - .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md
autonomous: true
requirements: [VERIFIER-01, VERIFIER-02, VERIFIER-03, VERIFIER-04, VERIFIER-05, VERIFIER-10]
must_haves:
  truths:
    - "D-13-01: Phase 13 uses a layered verifier stack rather than one formalism"
    - "D-13-07: Phase 13 MVP is a tiny L2Core vertical slice"
    - "Phase 13 does not implement MLIR/native lowering or real Vortex ingress"
  artifacts:
    - path: ".planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md"
      provides: "normative L2Core verifier/spec target"
      contains: "L2Core"
    - path: ".planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md"
      provides: "Phase 13 obligation matrix"
      contains: "VERIFIER-01"
---

<objective>
Define the normative Phase 13 verifier target: a tiny `L2Core` language subset, capability/resource model, Arrow builder event semantics, verifier facts, explicit exclusions, and proof-obligation matrix.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write the L2Core verifier specification</name>
  <files>.planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md</files>
  <read_first>
    - .planning/phases/13-full-loom-verifier/13-CONTEXT.md
    - .planning/phases/13-full-loom-verifier/13-RESEARCH.md
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-PROOF.md
    - README.md
    - design.md
  </read_first>
  <action>
    Create `13-VERIFIER-SPEC.md` with sections named `Scope`, `L2Core Syntax`, `Static Semantics`, `Dynamic Semantics`, `Capability Model`, `Resource Model`, `Arrow Builder Event Semantics`, `VerifiedArtifactFacts`, `Lowering Preconditions`, and `Explicit Exclusions`. Define the Phase 13 `L2Core` subset concretely: finite input slices, bounded `for i in 0..N`, monotone cursor loop, bounded scalar integer arithmetic, and typed Arrow builder events. State that semantic correctness of arbitrary producer intent, MLIR/native lowering implementation, real Vortex ingress, signatures, attestation, remote fetch, and geometric-algebra-based verifier design are out of scope.
  </action>
  <verify>
    <automated>rg -n "L2Core Syntax|Static Semantics|Dynamic Semantics|Capability Model|Resource Model|Arrow Builder Event Semantics|VerifiedArtifactFacts|Lowering Preconditions|Explicit Exclusions" .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-VERIFIER-SPEC.md` contains the exact section headings listed in the verify command.
    - The spec explicitly names `Lean/Rocq`, `Rust abstract interpretation`, `SMT`, and `TLA+`.
    - The spec states that Phase 13 does not implement MLIR/native lowering or real Vortex ingress.
  </acceptance_criteria>
  <done>The Phase 13 verifier target is precise enough for code and proof plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 13 proof-obligation matrix</name>
  <files>.planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
    - .planning/phases/13-full-loom-verifier/13-CONTEXT.md
    - .planning/phases/13-full-loom-verifier/13-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `13-PROOF-OBLIGATIONS.md` with one row for each `VERIFIER-01` through `VERIFIER-10`. Each row must include claim, layer (`Spec`, `Rust`, `SMT`, `Lean/Rocq`, `TLA+`, or `Gate`), source evidence, executable evidence, gate evidence, status, and gap. Initialize `VERIFIER-01` through `VERIFIER-05` as `Planned in 13-01`, `VERIFIER-06` through `VERIFIER-08` as `Planned in 13-02/13-03`, `VERIFIER-09` as `Planned in 13-04`, and `VERIFIER-10` as `Planned in 13-01/13-05`.
  </action>
  <verify>
    <automated>for id in VERIFIER-01 VERIFIER-02 VERIFIER-03 VERIFIER-04 VERIFIER-05 VERIFIER-06 VERIFIER-07 VERIFIER-08 VERIFIER-09 VERIFIER-10; do rg -q "$id" .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Every `VERIFIER-01` through `VERIFIER-10` ID appears at least once.
    - The matrix separates proof layers instead of treating a single formalism as authoritative.
    - The matrix includes `VerifiedArtifactFacts` as the Phase 14 handoff claim for `VERIFIER-10`.
  </acceptance_criteria>
  <done>All Phase 13 requirements have a proof-obligation tracking row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Spec -> implementation | Ambiguous spec language can let the Rust verifier prove the wrong thing. |
| Phase 12 -> Phase 13 | Phase 13 must not silently reuse current-boundary proof claims for the future language. |
| Phase 13 -> Phase 14 | Lowering preconditions must be explicit facts, not prose assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-01 | Tampering | verifier spec | mitigate | Use exact named semantics sections and proof-obligation rows for every `VERIFIER-*` ID. |
| T-13-02 | Spoofing | proof claims | mitigate | Separate Spec/Rust/SMT/Lean/TLA layers in `13-PROOF-OBLIGATIONS.md`. |
| T-13-03 | Repudiation | lowering handoff | mitigate | Define `VerifiedArtifactFacts` and `Lowering Preconditions` explicitly. |

No high open threats remain if the spec and matrix make scope boundaries explicit.
</threat_model>

<verification>
- `rg -n "L2Core Syntax|Static Semantics|Dynamic Semantics|Capability Model|Resource Model|Arrow Builder Event Semantics|VerifiedArtifactFacts|Lowering Preconditions|Explicit Exclusions" .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md`
- `for id in VERIFIER-01 VERIFIER-02 VERIFIER-03 VERIFIER-04 VERIFIER-05 VERIFIER-06 VERIFIER-07 VERIFIER-08 VERIFIER-09 VERIFIER-10; do rg -q "$id" .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md; done`
- `git diff --check`
</verification>

<success_criteria>
- Normative Phase 13 L2Core spec exists.
- Proof-obligation matrix covers `VERIFIER-01` through `VERIFIER-10`.
- Phase 13 scope is explicit and does not claim MLIR/native lowering or real Vortex ingress.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `13-VERIFIER-SPEC.md` | verifier specification | `.planning/phases/13-full-loom-verifier/` |
| `13-PROOF-OBLIGATIONS.md` | proof-obligation matrix | `.planning/phases/13-full-loom-verifier/` |

