---
phase: 13-full-loom-verifier
plan: "04"
type: execute
wave: 3
depends_on: ["13-01"]
files_modified:
  - formal/lean/LoomCore.lean
  - specs/tla/LoomVerifierPipeline.tla
  - specs/tla/LoomVerifierPipeline.cfg
  - scripts/full-verifier-test.sh
  - .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md
autonomous: true
requirements: [VERIFIER-01, VERIFIER-03, VERIFIER-04, VERIFIER-05, VERIFIER-09, VERIFIER-10]
must_haves:
  truths:
    - "D-13-04: Lean/Rocq handles language semantics and soundness"
    - "D-13-05: TLA+ is for workflow invariants only"
    - "D-13-06: geometric algebra is not the verifier core"
  artifacts:
    - path: "formal/lean/LoomCore.lean"
      provides: "Lean L2Core soundness scaffold"
      contains: "accepted_program_safe"
    - path: "specs/tla/LoomVerifierPipeline.tla"
      provides: "TLA+ verifier lifecycle invariant model"
      contains: "LoweredImpliesVerified"
---

<objective>
Add formal scaffolding for Phase 13: a Lean L2Core semantics/soundness artifact, a TLA+ lifecycle invariant model, and a full-verifier gate script that checks docs, Rust tests, Lean/TLA artifacts, and proof-obligation IDs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Lean L2Core semantics scaffold</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
    - .planning/phases/13-full-loom-verifier/13-CONTEXT.md
    - .planning/phases/13-full-loom-verifier/13-RESEARCH.md
  </read_first>
  <action>
    Create `formal/lean/LoomCore.lean` as the Phase 13 mechanized scaffold. Define minimal `L2Ty`, `Capability`, `ArrowEvent`, `Stmt`, `Program`, `Verified`, and `Safe` terms for the tiny `L2Core` subset. Include theorem names `builder_events_well_formed` and `accepted_program_safe`. The theorem may be a small proved scaffold over simplified predicates, but it must not claim complete final Loom verifier soundness. Add comments that Rocq remains the fallback if extraction becomes mandatory.
  </action>
  <verify>
    <automated>rg -n "inductive L2Ty|inductive Capability|inductive ArrowEvent|inductive Stmt|structure Program|def Verified|def Safe|builder_events_well_formed|accepted_program_safe" formal/lean/LoomCore.lean</automated>
    <automated>if command -v lean >/dev/null 2>&1; then lean formal/lean/LoomCore.lean; else echo "lean not installed; syntax check skipped"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `formal/lean/LoomCore.lean` defines the exact symbols listed in the first verify command.
    - If `lean` is installed, `lean formal/lean/LoomCore.lean` exits 0.
    - File comments state that this is a Phase 13 scaffold, not complete final Loom soundness.
  </acceptance_criteria>
  <done>Phase 13 has a mechanized Lean proof scaffold for the core semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add TLA+ lifecycle pipeline model</name>
  <files>specs/tla/LoomVerifierPipeline.tla, specs/tla/LoomVerifierPipeline.cfg</files>
  <read_first>
    - .planning/phases/13-full-loom-verifier/13-VERIFIER-SPEC.md
    - .planning/phases/13-full-loom-verifier/13-CONTEXT.md
    - .planning/phases/13-full-loom-verifier/13-RESEARCH.md
  </read_first>
  <action>
    Create `specs/tla/LoomVerifierPipeline.tla` and `specs/tla/LoomVerifierPipeline.cfg`. Model artifact states `Raw`, `Parsed`, `Verified`, `Rejected`, `Lowerable`, `Lowered`, and `Invalidated`. Include variables for `requiredFeaturesAccepted`, `resourceBounded`, and `verifiedFactsPresent`. Define invariant `LoweredImpliesVerified` such that any `Lowered` state implies verified artifact status, accepted required features, resource bounds, and verifier facts. The model must keep TLA+ scoped to lifecycle/pipeline invariants, not L2 type soundness.
  </action>
  <verify>
    <automated>rg -n "Raw|Parsed|Verified|Rejected|Lowerable|Lowered|Invalidated|requiredFeaturesAccepted|resourceBounded|verifiedFactsPresent|LoweredImpliesVerified" specs/tla/LoomVerifierPipeline.tla specs/tla/LoomVerifierPipeline.cfg</automated>
    <automated>if command -v tlc >/dev/null 2>&1; then tlc -config specs/tla/LoomVerifierPipeline.cfg specs/tla/LoomVerifierPipeline.tla; else echo "tlc not installed; TLA model execution skipped"; fi</automated>
  </verify>
  <acceptance_criteria>
    - TLA file defines the exact state names and invariant listed in the verify command.
    - Config file names `LoweredImpliesVerified` as an invariant.
    - TLA comments state that lifecycle invariants are separate from L2 language soundness.
  </acceptance_criteria>
  <done>Phase 13 has a TLA+ pipeline invariant model.</done>
</task>

<task type="auto">
  <name>Task 3: Add full-verifier gate script and update obligations</name>
  <files>scripts/full-verifier-test.sh, .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - scripts/safety-proof-test.sh
    - scripts/mvp0-verify.sh
    - .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md
    - formal/lean/LoomCore.lean
    - specs/tla/LoomVerifierPipeline.tla
    - specs/tla/LoomVerifierPipeline.cfg
  </read_first>
  <action>
    Add `scripts/full-verifier-test.sh` using the established shell-gate style from `scripts/safety-proof-test.sh`. The script must check that `13-VERIFIER-SPEC.md`, `13-PROOF-OBLIGATIONS.md`, `formal/lean/LoomCore.lean`, and `specs/tla/LoomVerifierPipeline.tla` exist; check every `VERIFIER-01` through `VERIFIER-10` ID appears in the obligation matrix; run `cargo test -p loom-core --test l2_core_model` if that test file exists; run `cargo test -p loom-core --test full_verifier` if that test file exists; run `lean formal/lean/LoomCore.lean` when `lean` is installed; and run TLC when `tlc` is installed. Update `13-PROOF-OBLIGATIONS.md` so `VERIFIER-09` cites Lean evidence and `VERIFIER-10` cites TLA/lowering-fact evidence.
  </action>
  <verify>
    <automated>bash scripts/full-verifier-test.sh</automated>
    <automated>rg -n "VERIFIER-09|VERIFIER-10|accepted_program_safe|LoweredImpliesVerified|full-verifier" .planning/phases/13-full-loom-verifier/13-PROOF-OBLIGATIONS.md scripts/full-verifier-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/full-verifier-test.sh` exits 0 in an environment without Lean/TLC by reporting skipped optional tool checks.
    - If Lean/TLC are installed, the script attempts their checks.
    - Obligation matrix cites Lean and TLA artifacts for `VERIFIER-09` and `VERIFIER-10`.
  </acceptance_criteria>
  <done>The formal artifacts are checked by a repeatable Phase 13 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Formal scaffold -> proof claims | Scaffold must not overclaim complete Loom soundness. |
| TLA lifecycle -> language proof | TLA model must not be mistaken for L2 type soundness. |
| Optional tools -> release gate | Absence of Lean/TLC must be explicit in output rather than silently ignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-11 | Spoofing | Lean proof | mitigate | File comments and obligation matrix label it as a Phase 13 scaffold. |
| T-13-12 | Tampering | TLA lifecycle | mitigate | `LoweredImpliesVerified` is checked by rg and TLC when available. |
| T-13-13 | Repudiation | optional formal tools | mitigate | Gate script prints whether Lean/TLC checks ran or were skipped. |

No high open threats remain if the gate script exposes optional-tool status and the docs avoid complete-proof claims.
</threat_model>

<verification>
- `bash scripts/full-verifier-test.sh`
- `if command -v lean >/dev/null 2>&1; then lean formal/lean/LoomCore.lean; else echo "lean not installed; syntax check skipped"; fi`
- `if command -v tlc >/dev/null 2>&1; then tlc -config specs/tla/LoomVerifierPipeline.cfg specs/tla/LoomVerifierPipeline.tla; else echo "tlc not installed; TLA model execution skipped"; fi`
- `git diff --check`
</verification>

<success_criteria>
- Lean scaffold exists with accepted-program safety theorem name.
- TLA+ lifecycle model exists with `LoweredImpliesVerified`.
- Full-verifier gate checks Phase 13 docs, obligations, Rust tests, and optional formal tools.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `LoomCore.lean` | Lean proof scaffold | `formal/lean/` |
| `accepted_program_safe` | Lean theorem | `formal/lean/LoomCore.lean` |
| `builder_events_well_formed` | Lean theorem | `formal/lean/LoomCore.lean` |
| `LoomVerifierPipeline.tla` | TLA+ model | `specs/tla/` |
| `LoomVerifierPipeline.cfg` | TLA+ config | `specs/tla/` |
| `LoweredImpliesVerified` | TLA+ invariant | `specs/tla/LoomVerifierPipeline.tla` |
| `full-verifier-test.sh` | release gate | `scripts/` |

