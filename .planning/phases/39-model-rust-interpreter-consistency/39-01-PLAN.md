---
phase: 39-model-rust-interpreter-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/l2_core_reference_executor.rs
  - crates/loom-core/src/lib.rs
  - crates/loom-core/tests/l2_core_reference_executor.rs
  - .planning/phases/39-model-rust-interpreter-consistency/39-LEAN-EXTRACTION-NOTE.md
autonomous: true
requirements:
  - LINEAGE-07
must_haves:
  truths:
    - "D-39-01: The Rust reference executor transcribes the Phase 38 Lean modeled semantics one-to-one."
    - "D-39-02: The reference executor is visibly separate from production execution code."
    - "D-39-03: The reference executor emits stable trace records for reads, append events, terminal status, and fail-closed diagnostics."
    - "D-39-10: Lean extraction is evaluated as optional additive evidence."
---

<objective>
Implement a separate Rust reference executor that faithfully transcribes the
Phase 38 Lean modeled executor and emits deterministic trace records for later
comparison against the production interpreter path.
</objective>

<tasks>
<task type="tdd">
  <name>Task 1: Add reference-executor trace tests and corpus seed</name>
  <files>crates/loom-core/tests/l2_core_reference_executor.rs</files>
  <read_first>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-CONTEXT.md</file>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/tests/full_verifier.rs</file>
    <file>crates/loom-core/src/l2_core.rs</file>
  </read_first>
  <action>Add focused tests for the reference executor over accepted copy, append-null, `letScalar` append, loop/cursor, fail-closed, missing input, missing output, invalid bounds, non-monotone cursor, and output type mismatch cases. Expected traces must include stable event ordering and terminal/fail-closed status.</action>
  <acceptance_criteria>
    <criterion>`cargo test -p loom-core --test l2_core_reference_executor` exits 0.</criterion>
    <criterion>`rg -n "reference.*trace|append-value|append-null|fail-closed|terminal|matrix-|fuzz-" crates/loom-core/tests/l2_core_reference_executor.rs` shows trace/corpus coverage.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement the separate Rust reference executor</name>
  <files>crates/loom-core/src/l2_core_reference_executor.rs, crates/loom-core/src/lib.rs</files>
  <read_first>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/src/l2_core.rs</file>
    <file>crates/loom-core/src/full_verifier.rs</file>
  </read_first>
  <action>Add a `loom_core::l2_core_reference_executor` module with reference-only trace types and execution functions mirroring Phase 38 Lean `execProgram`: scalar type environment, concrete read in-bounds classification, modeled read trace rows, append-value/null trace rows, bounded loop/cursor status, and fail-closed terminal rows. Keep the module clearly documented as differential oracle only.</action>
  <acceptance_criteria>
    <criterion>`rg -n "ReferenceExecutor|ReferenceTrace|reference oracle|differential oracle|execute_reference|fail_closed" crates/loom-core/src/l2_core_reference_executor.rs` shows the reference-only API and boundary wording.</criterion>
    <criterion>`cargo test -p loom-core --test l2_core_reference_executor` exits 0.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Evaluate Lean extraction and write note</name>
  <files>.planning/phases/39-model-rust-interpreter-consistency/39-LEAN-EXTRACTION-NOTE.md</files>
  <read_first>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-CONTEXT.md</file>
    <file>formal/lean/LoomCore.lean</file>
  </read_first>
  <action>Evaluate whether Lean extraction is practical for this phase's oracle path. If adopted, document how it is gated. If deferred, record the concrete reason and why the Rust transcription remains acceptable for Phase 39 differential validation.</action>
  <acceptance_criteria>
    <criterion>`rg -n "Lean extraction|adopted|deferred|reason|Rust transcription" .planning/phases/39-model-rust-interpreter-consistency/39-LEAN-EXTRACTION-NOTE.md` shows an explicit extraction decision.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
cargo test -p loom-core --test l2_core_reference_executor
rg -n "ReferenceExecutor|ReferenceTrace|reference oracle|differential oracle|execute_reference|fail_closed" crates/loom-core/src/l2_core_reference_executor.rs
rg -n "reference.*trace|append-value|append-null|fail-closed|terminal|matrix-|fuzz-" crates/loom-core/tests/l2_core_reference_executor.rs
rg -n "Lean extraction|adopted|deferred|reason|Rust transcription" .planning/phases/39-model-rust-interpreter-consistency/39-LEAN-EXTRACTION-NOTE.md
git diff --check
</verification>

<success_criteria>
Plan 39-01 is complete when a separate reference executor can emit stable
modeled traces for the seeded corpus, tests pass, and Lean extraction has an
explicit adopted/deferred decision.
</success_criteria>

<output>
Create `39-01-SUMMARY.md` when done.
</output>

