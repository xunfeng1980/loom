---
phase: 39-model-rust-interpreter-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - crates/loom-core/src/l2_core_reference_executor.rs
  - crates/loom-core/tests/l2_core_interpreter_consistency.rs
  - scripts/model-rust-interpreter-consistency-test.sh
  - scripts/full-verifier-test.sh
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - LINEAGE-07
  - LINEAGE-08
must_haves:
  truths:
    - "D-39-04: The production interpreter under test is the subject, not adjusted to match the oracle."
    - "D-39-05: Any production trace hook is observer-only and its boundary is documented."
    - "D-39-06: The gate compares full builder-event traces and fail-closed behavior, not final values only."
    - "D-39-08: Trace divergence fails closed."
    - "D-39-12: Phase 39 is per-run differential validation, not proof of all-program equivalence."
---

<objective>
Add trace-level differential validation between the Rust production interpreter
surface under test and the separate reference executor, wire the fail-closed gate,
and close Phase 39 without claiming native/model validation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Define production trace subject and observer-only hook</name>
  <files>crates/loom-core/src/l2_core_reference_executor.rs, crates/loom-core/tests/l2_core_interpreter_consistency.rs</files>
  <read_first>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-CONTEXT.md</file>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-01-SUMMARY.md</file>
    <file>crates/loom-core/src/l2_core_reference_executor.rs</file>
    <file>crates/loom-core/src/l2_core.rs</file>
  </read_first>
  <action>Define the narrow production/interpreter surface under test. If the current repo lacks an independent L2Core runtime interpreter, add an explicitly named production trace subject for the current modeled slice and document the boundary. The trace hook must be observer-only and must not call the reference executor.</action>
  <acceptance_criteria>
    <criterion>`rg -n "production.*trace|observer-only|subject under test|does not call reference|interpreter surface" crates/loom-core/src crates/loom-core/tests/l2_core_interpreter_consistency.rs` shows the subject boundary.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compare reference and production traces over deterministic corpus</name>
  <files>crates/loom-core/tests/l2_core_interpreter_consistency.rs</files>
  <read_first>
    <file>crates/loom-core/src/l2_core_reference_executor.rs</file>
    <file>crates/loom-core/tests/l2_core_reference_executor.rs</file>
    <file>crates/loom-core/tests/full_verifier.rs</file>
  </read_first>
  <action>Add deterministic corpus tests comparing reference and production traces exactly. Include positive append-value traces, append-null traces, fail-closed traces, loop/cursor traces, and generated/fuzz-like cases. Divergence must fail with stable diff output or clear assertion messages.</action>
  <acceptance_criteria>
    <criterion>`cargo test -p loom-core --test l2_core_interpreter_consistency` exits 0.</criterion>
    <criterion>`rg -n "trace divergence|reference_trace|production_trace|append-value|append-null|fail-closed|fuzz-" crates/loom-core/tests/l2_core_interpreter_consistency.rs` shows full-trace comparison coverage.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire consistency gate into verifier path</name>
  <files>scripts/model-rust-interpreter-consistency-test.sh, scripts/full-verifier-test.sh</files>
  <read_first>
    <file>scripts/full-verifier-test.sh</file>
    <file>crates/loom-core/tests/l2_core_interpreter_consistency.rs</file>
  </read_first>
  <action>Add `scripts/model-rust-interpreter-consistency-test.sh` and wire it into `scripts/full-verifier-test.sh`. The script must run the consistency test, marker-check the observer/reference separation, and fail on divergence.</action>
  <acceptance_criteria>
    <criterion>`bash scripts/model-rust-interpreter-consistency-test.sh` exits 0.</criterion>
    <criterion>`bash scripts/full-verifier-test.sh` exits 0.</criterion>
    <criterion>`rg -n "model-rust-interpreter-consistency-test|l2_core_interpreter_consistency" scripts/full-verifier-test.sh scripts/model-rust-interpreter-consistency-test.sh` shows release-gate wiring.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Close LINEAGE requirements, docs, roadmap, and summary</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/39-model-rust-interpreter-consistency/39-02-SUMMARY.md</files>
  <read_first>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-CONTEXT.md</file>
    <file>.planning/phases/39-model-rust-interpreter-consistency/39-01-SUMMARY.md</file>
    <file>.planning/phases/36-verified-lineage-contract-and-tcb-declaration/36-VERIFIED-LINEAGE-CONTRACT.md</file>
    <file>.planning/PROJECT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <action>Add or complete `LINEAGE-07` for the reference executor and `LINEAGE-08` for trace-level differential validation. Mark Phase 39 complete and Phase 40 ready only after verification passes. Update docs/state to say Phase 39 validates model-to-Rust interpreter consistency per run, not native/model validation or all-program proof. Create `39-02-SUMMARY.md` recording verification, corpus, extraction decision, residual risks, and Phase 40 handoff.</action>
  <acceptance_criteria>
    <criterion>`rg -n "LINEAGE-07|LINEAGE-08" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/39-model-rust-interpreter-consistency/39-02-SUMMARY.md` shows both requirements represented and closed.</criterion>
    <criterion>`rg -n "Phase 39.*Complete|Phase 40.*READY|model.*Rust interpreter" .planning/STATE.md .planning/ROADMAP.md .planning/PROJECT.md` shows Phase 39 complete and Phase 40 ready/current.</criterion>
    <criterion>`rg -n "not.*native|Phase 40|per-run|differential validation|not.*proof" .planning/PROJECT.md .planning/STATE.md .planning/phases/39-model-rust-interpreter-consistency/39-02-SUMMARY.md` shows non-claims remain explicit.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
cargo test -p loom-core --test l2_core_reference_executor
cargo test -p loom-core --test l2_core_interpreter_consistency
bash scripts/model-rust-interpreter-consistency-test.sh
bash scripts/full-verifier-test.sh
rg -n "production.*trace|observer-only|subject under test|does not call reference|interpreter surface" crates/loom-core/src crates/loom-core/tests/l2_core_interpreter_consistency.rs
rg -n "LINEAGE-07|LINEAGE-08" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/39-model-rust-interpreter-consistency/39-02-SUMMARY.md
git diff --check
</verification>

<success_criteria>
Plan 39-02 is complete when reference and production traces match exactly over
the deterministic corpus, divergence fails the release gate, LINEAGE-07/08 are
closed, and Phase 40 native/model scope remains separate.
</success_criteria>

<output>
Create `39-02-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

- Rust reference executor for the modeled L2Core semantics
- Production trace consistency tests and gate
- Optional Lean extraction decision note
- Requirement IDs: `LINEAGE-07`, `LINEAGE-08`
