---
phase: 02-duckdb-extension-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - duckdb-ext/loom_extension.cpp
  - duckdb-ext/vendor/duckdb-src/duckdb.hpp
  - duckdb-ext/vendor/duckdb-src/duckdb.cpp
  - duckdb-ext/vendor/append_metadata.cmake
  - duckdb-ext/vendor/null.txt
  - duckdb-ext/.gitignore
  - crates/loom-ffi/tests/buffer_layout.rs
autonomous: true
requirements: [DUCK-01, DUCK-02, DUCK-03]
must_haves:
  truths:
    - "The amalgamated duckdb.hpp from v1.5.3 is present locally and exposes the DUCKDB_CPP_EXTENSION_ENTRY macro (or a documented manual fallback is in place)"
    - "loom_decode's FFI_ArrowArray buffer layout is confirmed (n_buffers==2, buffers[0]=validity, buffers[1]=int32 values) before any C++ buffer-handling code is committed"
    - "loom_extension.cpp exports exactly one DuckDB entry symbol (loom_duckdb_cpp_init) and registers a loom_scan(VARCHAR) table function"
    - "D-01 REVISED (see 02-CONTEXT.md): loom_scan populates the DataChunk directly from the decoded Arrow buffers (arrow_scan needs a record-batch schema loom_decode does not emit; the stream path is deferred to Phase 3). Verified by a positive grep for `FlatVector::GetData` and a negative grep proving no dead `struct OneShotStream` scaffolding remains."
    - "loom_decode's i32 return code is checked; nonzero throws a DuckDB exception and never proceeds to use the output pointers"
    - "The imported Arrow array+schema release callbacks fire exactly once on every teardown path (success, end-of-stream, error)"
  artifacts:
    - path: "duckdb-ext/loom_extension.cpp"
      provides: "C++ extension: entry point, LoomBind/LoomInit/LoomScan (direct DataChunk population — D-01 REVISED), LoomScanState teardown"
      contains: "loom_duckdb_cpp_init"
      min_lines: 120
    - path: "duckdb-ext/vendor/duckdb-src/duckdb.hpp"
      provides: "DuckDB v1.5.3 amalgamated C++ header"
    - path: "duckdb-ext/vendor/append_metadata.cmake"
      provides: "Official footer-stamping script for Plan 02 POST_BUILD step"
    - path: "duckdb-ext/vendor/null.txt"
      provides: "1-byte null file required by append_metadata.cmake"
    - path: "crates/loom-ffi/tests/buffer_layout.rs"
      provides: "Wave-0 assertion test for loom_decode Arrow buffer layout"
      contains: "n_buffers"
  key_links:
    - from: "duckdb-ext/loom_extension.cpp"
      to: "loom_decode"
      via: "FFI call in LoomInit through loom.h"
      pattern: "loom_decode"
    - from: "duckdb-ext/loom_extension.cpp"
      to: "DuckDB DataChunk"
      via: "LoomScan reads arr.buffers[0]/[1] and fills the output vector (D-01 REVISED — direct population; arrow_scan deferred to Phase 3)"
      pattern: "FlatVector::GetData"
---

<objective>
Acquire the DuckDB v1.5.3 build inputs, run the two Wave-0 verification checks the research flagged, and author the single C++ extension source file that links Phase 1's `libloom_ffi.a`, registers `loom_scan(VARCHAR)`, and adopts the imported Arrow array by delegating to DuckDB's built-in `arrow_scan` via a one-shot `ArrowArrayStream` produce-callback factory (per locked decision D-01, overriding the research's direct-DataChunk recommendation).

> ⚠ **POST-EXECUTION REVISION (2026-06-07) — D-01 was revised.** This plan was authored to delegate to DuckDB's built-in `arrow_scan` via a one-shot `ArrowArrayStream` factory. During execution that proved infeasible for the stub: `arrow_scan` requires a top-level struct/record-batch schema, but `loom_decode` emits a bare primitive Int32 array. After review, the user adopted **direct DataChunk population** for the Phase-2 stub and **deferred the arrow_scan/stream path to Phase 3** (see 02-CONTEXT.md "D-01 REVISED" and STATE.md). The machine-read `must_haves` (truths/key_links) and the Task 2 verify block have been corrected to the revised path; **prose below that still mentions `arrow_scan` / `OneShotStream` / "stable surface" is historical and superseded** — the shipped `loom_extension.cpp` contains no such code.

Purpose: This is the plumbing layer of Phase 2. It proves the Rust→C ABI seam (DUCK-02), establishes the loom_scan table function and its Arrow→DuckDB import (D-01 REVISED — direct DataChunk population), and locks the release-callback ownership contract on the C++ import side (DUCK-03). It does NOT build or load the extension — that is Plan 02, which provides the CMake + footer-stamp + smoke-test.

Output: `duckdb-ext/loom_extension.cpp`, the vendored DuckDB v1.5.3 amalgamation + footer script + null.txt under `duckdb-ext/vendor/`, and a Rust buffer-layout assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-duckdb-extension-scaffold/02-CONTEXT.md
@.planning/phases/02-duckdb-extension-scaffold/02-RESEARCH.md
@crates/loom-ffi/include/loom.h
@crates/loom-ffi/src/ffi.rs
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files (Plan 02 adds the CMake, footer-stamp, CI, and smoke-test):

| Artifact | Kind | Location |
|----------|------|----------|
| `loom_duckdb_cpp_init(ExtensionLoader&)` | C++ exported entry symbol (via `DUCKDB_CPP_EXTENSION_ENTRY(loom, loader)`) | duckdb-ext/loom_extension.cpp |
| `LoadInternal(ExtensionLoader&)` | static C++ fn (registers loom_scan) | duckdb-ext/loom_extension.cpp |
| `loom_scan` | DuckDB table function (VARCHAR arg, ignored — D-04) | duckdb-ext/loom_extension.cpp |
| `LoomBind` / `LoomInit` / `LoomScan` | table function callbacks | duckdb-ext/loom_extension.cpp |
| `LoomScanState` | GlobalTableFunctionState subclass; destructor releases Arrow array+schema (DUCK-03) | duckdb-ext/loom_extension.cpp |
| `OneShotStream` | ArrowArrayStream wrapper struct (get_schema / get_next / release) wrapping one decoded array | duckdb-ext/loom_extension.cpp |
| stream factory callbacks (`produce` → `ArrowArrayStreamWrapper`, `get_schema`) | static C++ fns handed to arrow_scan | duckdb-ext/loom_extension.cpp |
| `duckdb-ext/` directory | new top-level C++ extension dir (D-03) | repo root |
| `duckdb-ext/vendor/duckdb-src/duckdb.hpp` + `duckdb.cpp` | vendored amalgamation | duckdb-ext/vendor/ |
| `duckdb-ext/vendor/append_metadata.cmake` + `null.txt` | footer-stamp inputs (consumed by Plan 02) | duckdb-ext/vendor/ |
| `crates/loom-ffi/tests/buffer_layout.rs` | Rust Wave-0 buffer-layout assertion test | crates/loom-ffi/tests/ |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Acquire DuckDB v1.5.3 build inputs and run both Wave-0 verification checks [BLOCKING]</name>
  <files>duckdb-ext/vendor/duckdb-src/duckdb.hpp, duckdb-ext/vendor/duckdb-src/duckdb.cpp, duckdb-ext/vendor/append_metadata.cmake, duckdb-ext/vendor/null.txt, duckdb-ext/.gitignore, crates/loom-ffi/tests/buffer_layout.rs</files>
  <read_first>
    - .planning/phases/02-duckdb-extension-scaffold/02-RESEARCH.md (Q1, Q2, Q3 — download URLs, entry symbol, footer inputs; Open Questions 1 & 2 — the two Wave-0 checks)
    - crates/loom-ffi/include/loom.h (loom_decode signature; forward-declared FFI_ArrowArray/FFI_ArrowSchema)
    - crates/loom-ffi/src/ffi.rs (lines 121–142: the hardcoded Int32Array [1,2,3,null], Int32Builder, to_ffi)
  </read_first>
  <action>
    Create the top-level `duckdb-ext/` directory and a `duckdb-ext/vendor/` subdirectory. Download the three official DuckDB v1.5.3 artifacts (all provenance-approved in RESEARCH §Package Legitimacy Audit) using curl -L:
    - `libduckdb-src.zip` from `https://github.com/duckdb/duckdb/releases/download/v1.5.3/libduckdb-src.zip`; unzip into `duckdb-ext/vendor/duckdb-src/` so that `duckdb-ext/vendor/duckdb-src/duckdb.hpp` and `duckdb.cpp` exist.
    - `append_metadata.cmake` from `https://raw.githubusercontent.com/duckdb/duckdb/v1.5.3/scripts/append_metadata.cmake` saved to `duckdb-ext/vendor/append_metadata.cmake`.
    - Generate `duckdb-ext/vendor/null.txt` containing EXACTLY one null byte via `printf '\x00' > duckdb-ext/vendor/null.txt` (RESEARCH Pitfall 3 — do NOT use `echo -e`, which appends a newline; verify size is 1 byte).
    Add `duckdb-ext/.gitignore` excluding build output (`build/`) and the CLI binary; the amalgamation + footer script + null.txt should be tracked (they are required by Plan 02 CMake at configure/build time and by CI).

    WAVE-0 CHECK 1 (DUCKDB_CPP_EXTENSION_ENTRY presence): grep the amalgamated header for the macro. If `DUCKDB_CPP_EXTENSION_ENTRY` is found, proceed using the macro per RESEARCH Pattern 1. If ABSENT, record in the SUMMARY that the macro is unavailable and use the manual fallback documented in RESEARCH Q3 — a hand-written `extern "C" void loom_duckdb_cpp_init(duckdb::ExtensionLoader &loader)` declaration with the same signature `ext_init_fun_t = void(*)(ExtensionLoader&)`. The entry SYMBOL name `loom_duckdb_cpp_init` is what DuckDB v1.5.3 dlsym's (extension_load.cpp line 634) and must be exported either way.

    WAVE-0 CHECK 2 (Arrow buffer layout): create `crates/loom-ffi/tests/buffer_layout.rs` as a Rust integration test that calls `loom_decode(null, 0, &mut array, &mut schema)` (zeroed FFI_ArrowArray/FFI_ArrowSchema via std::mem::zeroed) and asserts: return code == 0; `array.length == 4`; `array.null_count == 1`; `array.n_buffers == 2`; `array.buffers[0]` (validity) is non-null; `array.buffers[1]` (values) is non-null. Then release both via their release callbacks. This pins the exact buffer indices the OneShotStream/arrow_scan path relies on (RESEARCH Open Question 2; Assumption A3). The arrow-rs `FFI_ArrowArray` fields (`length`, `null_count`, `n_buffers`, `buffers`) are accessible — use the public arrow::ffi types directly in the test rather than re-declaring the C struct.
  </action>
  <verify>
    <automated>test -f duckdb-ext/vendor/duckdb-src/duckdb.hpp && test -f duckdb-ext/vendor/append_metadata.cmake && [ "$(wc -c < duckdb-ext/vendor/null.txt)" -eq 1 ] && grep -c DUCKDB_CPP_EXTENSION_ENTRY duckdb-ext/vendor/duckdb-src/duckdb.hpp; cargo test -p loom-ffi --release --test buffer_layout</automated>
  </verify>
  <acceptance_criteria>
    - `test -f duckdb-ext/vendor/duckdb-src/duckdb.hpp` succeeds (exit 0)
    - `test -f duckdb-ext/vendor/duckdb-src/duckdb.cpp` succeeds (exit 0)
    - `test -f duckdb-ext/vendor/append_metadata.cmake` succeeds (exit 0)
    - `wc -c < duckdb-ext/vendor/null.txt` prints exactly `1`
    - `grep -c DUCKDB_CPP_EXTENSION_ENTRY duckdb-ext/vendor/duckdb-src/duckdb.hpp` prints a count; if `0`, the SUMMARY documents the manual-fallback path is in use
    - `cargo test -p loom-ffi --release --test buffer_layout` exits 0 (asserts n_buffers==2, length==4, null_count==1, buffers[0] and buffers[1] non-null)
  </acceptance_criteria>
  <done>The v1.5.3 amalgamation, footer script, and a 1-byte null.txt are vendored under duckdb-ext/vendor/; the entry-macro presence is recorded; and a passing Rust test pins loom_decode's buffer layout (n_buffers==2, validity at [0], int32 values at [1]).</done>
</task>

<task type="auto">
  <name>Task 2: Author loom_extension.cpp — entry point, loom_scan, OneShotStream factory → arrow_scan, release-safe teardown (DUCK-01, DUCK-02, DUCK-03)</name>
  <files>duckdb-ext/loom_extension.cpp</files>
  <read_first>
    - duckdb-ext/vendor/duckdb-src/duckdb.hpp (the amalgamated header — confirm availability of ExtensionLoader, TableFunction, ArrowArrayStreamWrapper, ArrowStreamParameters, the arrow_scan table function, Value::POINTER)
    - crates/loom-ffi/include/loom.h (loom_decode signature; FFI_ArrowArray/FFI_ArrowSchema are forward-declared — include via `extern "C" { #include "loom.h" }`)
    - .planning/phases/02-duckdb-extension-scaffold/02-RESEARCH.md (Pattern 1 entry point; Pattern 2 registration; Pattern 3 OneShotStream callbacks; Q4 arrow_scan three-pointer factory signature — produce returns unique_ptr<ArrowArrayStreamWrapper>, get_schema is void(ArrowArrayStream*, ArrowSchema&); Pitfall 5 return-code check; Pitfall 6 dynamic-lookup / do-not-link-dylib) — NOTE: §Pattern 3 Option C and its §Code Examples skeleton are OVERRIDDEN by D-01; see the OVERRIDE block in this task's action before copying any scan code
    - crates/loom-ffi/tests/buffer_layout.rs (the confirmed buffer layout from Task 1)
    - .planning/research/PITFALLS.md (P1 release ownership, P2 schema lifetime, Integration Gotchas: arrow_scan expects a STREAM pointer not a raw ArrowArray*)
  </read_first>
  <action>
    Write `duckdb-ext/loom_extension.cpp` as the single C++17 source. `#define DUCKDB_EXTENSION_MAIN` before `#include "duckdb.hpp"`; include `loom.h` inside an `extern "C"` block.

    OVERRIDE — READ THIS BEFORE WRITING ANY SCAN CODE: RESEARCH §Pattern 3 explicitly RECOMMENDS "Option C" (direct `DataChunk` population via `FlatVector::GetData` / `FlatVector::Validity`, reading `arr.buffers[0]`/`arr.buffers[1]` by hand) and ships a full ~35-line `LoomScan` code skeleton (RESEARCH §Code Examples "Minimal loom_extension.cpp skeleton", lines ~781–811) that does exactly that. That §Pattern 3 Option C recommendation AND its code skeleton are OVERRIDDEN by locked decision D-01. Do NOT use direct DataChunk population; do NOT call `FlatVector::GetData` or `FlatVector::Validity`; do NOT hand-index `arr.buffers[...]` in the scan path. Implement the OneShotStream produce-callback factory and delegate to DuckDB's built-in `arrow_scan` (RESEARCH §Q4 three-pointer factory signature) — this same stable surface is what Phase 3+ reuses, so the stub must use it now. Where RESEARCH §Pattern 3 Option C and D-01 conflict, D-01 wins.

    Implement, per locked decision D-01:

    1. `LoomScanState : duckdb::GlobalTableFunctionState` holding zero-initialized `ArrowArray arrow_array = {}` and `ArrowSchema arrow_schema = {}` plus `bool stream_handed_off = false`. Its destructor calls `arrow_array.release(&arrow_array)` and `arrow_schema.release(&arrow_schema)` ONLY if the respective `.release` pointer is non-null, then sets each `.release = nullptr` (DUCK-03; PITFALLS P1 — release exactly once, every teardown path). Because ownership of the array transfers into the stream's produce path, the destructor must not double-release what the stream already consumed (gate on the null release pointer).

    2. `LoomBind` — accept the single `LogicalType::VARCHAR` argument and IGNORE it (D-04). Declare the output schema. Source the type from loom_decode's schema rather than hardcoding where practical; for the Phase 2 stub it is a single nullable INTEGER column named "value". Return a `TableFunctionData`.

    3. `LoomInit` — construct `LoomScanState`, call `loom_decode(nullptr, 0, reinterpret_cast<FFI_ArrowArray*>(&state->arrow_array), reinterpret_cast<FFI_ArrowSchema*>(&state->arrow_schema))`. CHECK the i32 return code: store it in `int32_t rc` and on `rc != 0` throw `duckdb::IOException("loom_decode failed with code %d", rc)` and NEVER touch the output pointers (PITFALLS P5; threat T-02-PANIC). The success path proceeds only when `rc` is zero. Store state.

    4. `OneShotStream` — a heap struct wrapping the single `ArrowSchema` + `ArrowArray` taken (moved by value) out of the scan state, plus a `consumed` flag. Implement the three Arrow C stream callbacks: `get_schema` (shallow-copy the owned schema into out; keep ownership in the stream), `get_next` (on first call move the array into out and set the source `array.release = nullptr` so the consumer owns it; on subsequent calls set `out->release = nullptr` as the end-of-stream sentinel), and `release` (release any array/schema still owned, then delete the struct and null out `self->private_data` and `self->release`). This is the exactly-once ownership transfer from PITFALLS P1/P2 and RESEARCH Pattern 3.

    5. Delegate to DuckDB's built-in `arrow_scan` (D-01 stable public path). Implement the two factory callbacks arrow_scan requires (RESEARCH Q4): a `produce` callback `unique_ptr<ArrowArrayStreamWrapper>(uintptr_t factory_ptr, ArrowStreamParameters&)` that builds an `ArrowArrayStreamWrapper` whose `arrow_array_stream` is a fully-populated `ArrowArrayStream` C struct wired to the OneShotStream callbacks + private_data, and a `get_schema` callback `void(ArrowArrayStream*, ArrowSchema&)`. In `LoomScan` (or LoomBind, whichever the arrow_scan binding requires), invoke the `arrow_scan` table function through the loader/catalog passing the three `Value::POINTER` arguments (factory ptr, produce, get_schema) so DuckDB drives the stream and performs the Arrow→DataChunk conversion itself. Mark `state.stream_handed_off = true` once the stream owns the array so the LoomScanState destructor does not double-free. Add a clearly-labeled comment block at the arrow_scan delegation point marking it as the stable surface Phase 3+ reuses.

    6. `LoadInternal(ExtensionLoader& loader)` registers `TableFunction("loom_scan", {LogicalType::VARCHAR}, LoomScan, LoomBind, LoomInit)` via `loader.RegisterFunction(fn)`. Export the entry symbol via `DUCKDB_CPP_EXTENSION_ENTRY(loom, loader) { LoadInternal(loader); }` (or the manual `extern "C" void loom_duckdb_cpp_init(ExtensionLoader&)` fallback if Task 1 Check 1 found the macro absent). Do NOT export `loom_init`/`loom_version` (RESEARCH Anti-Patterns — legacy 0.x symbols).

    Do NOT add any `find_library`/link to libduckdb (Plan 02 CMake uses `-undefined dynamic_lookup`; symbols come from the CLI at dlopen — PITFALLS P6). This task produces source only; it is compiled and linked in Plan 02.
  </action>
  <verify>
    <automated>test -f duckdb-ext/loom_extension.cpp && grep -q 'loom_duckdb_cpp_init' duckdb-ext/loom_extension.cpp && grep -q 'loom_decode' duckdb-ext/loom_extension.cpp && grep -q 'LoomScanState' duckdb-ext/loom_extension.cpp && (grep -q 'rc != 0' duckdb-ext/loom_extension.cpp || grep -q 'if (rc)' duckdb-ext/loom_extension.cpp) && grep -q 'FlatVector::GetData' duckdb-ext/loom_extension.cpp && echo OK_DIRECT_DATACHUNK_D01_REVISED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'loom_duckdb_cpp_init' duckdb-ext/loom_extension.cpp` (entry symbol present)
    - `grep -q 'loom_decode' duckdb-ext/loom_extension.cpp` (FFI call present)
    - `grep -c 'LoomScanState' duckdb-ext/loom_extension.cpp` ≥ 2 (struct + destructor usage)
    - `(grep -q 'rc != 0' duckdb-ext/loom_extension.cpp || grep -q 'if (rc)' duckdb-ext/loom_extension.cpp)` — bash-executable return-code-check guard present (DUCK-02; PITFALLS P5)
    - **D-01 REVISED (see 02-CONTEXT.md):** `grep -q 'FlatVector::GetData' duckdb-ext/loom_extension.cpp` — direct DataChunk population IS the chosen Phase-2 stub path (arrow_scan needs a record-batch schema loom_decode doesn't emit; stream path deferred to Phase 3). The earlier negative `! FlatVector::GetData` guard and the positive `arrow_scan`/`produce` asserts are RETIRED — they enforced the original D-01 which was formally revised after the execution-time blocker.
    - `! grep -qE 'struct OneShotStream' duckdb-ext/loom_extension.cpp` — no dead stream scaffolding retained (honest-tree guard)
    - `grep -q 'release' duckdb-ext/loom_extension.cpp` (release callbacks invoked in destructor on every teardown path)
    - The file does NOT export `loom_init` or `loom_version`: `! grep -qE '(loom_init|loom_version)\b' duckdb-ext/loom_extension.cpp`
  </acceptance_criteria>
  <done>loom_extension.cpp exports loom_duckdb_cpp_init, registers loom_scan(VARCHAR), calls loom_decode with a bash-verifiable return-code guard, and populates the DataChunk directly from the decoded Arrow buffers (D-01 REVISED — arrow_scan/stream path deferred to Phase 3; no dead scaffolding), releasing array+schema on every teardown path in ~LoomScanState without double-free (DUCK-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DuckDB C++ ↔ Rust (loom_decode) | The C ABI seam; loom_decode return code is the only trust signal that the output pointers are valid |
| Rust-owned Arrow buffers ↔ C++ stream/scan-state ownership | Release-callback ownership crosses here; double-free or leak lives at this seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PANIC | Denial-of-service | LoomInit calling loom_decode | mitigate | loom_decode is catch_unwind-wrapped (Phase 1, DUCK-04); LoomInit checks the i32 rc and throws a DuckDB IOException on nonzero — never proceeds to read output pointers (PITFALLS P5) |
| T-02-DF | Tampering (heap) | OneShotStream + LoomScanState release paths | mitigate | Exactly-once release: get_next zeroes the source release pointer on hand-off; destructor only releases if release pointer non-null; stream release nulls private_data (PITFALLS P1) |
| T-02-LIFE | Information disclosure (dangling read) | ArrowSchema lifetime across bind→scan | mitigate | Schema is owned by the OneShotStream and released only in stream release / scan-state destructor, never by reference into a stack frame (PITFALLS P2) |
| T-02-IDX | Tampering / crash | Arrow buffer index assumptions | mitigate | Wave-0 test (Task 1) pins n_buffers==2, buffers[0]=validity, buffers[1]=values before any C++ buffer logic relies on it; arrow_scan (not hand-indexing) performs the conversion |
| T-02-SC | Tampering (supply chain) | DuckDB v1.5.3 binary downloads | accept | All three artifacts are official DuckDB GitHub release assets at tag v1.5.3 (RESEARCH §Package Legitimacy Audit, all Approved); no npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- Vendored amalgamation, footer script, and 1-byte null.txt present under duckdb-ext/vendor/.
- Wave-0 buffer-layout test passes (`cargo test -p loom-ffi --release --test buffer_layout`).
- loom_extension.cpp present with the entry symbol, loom_scan registration, a bash-verifiable loom_decode return-code guard, arrow_scan delegation via a produce-callback factory (positive `arrow_scan`+`produce` greps; negative `FlatVector::GetData` guard — no direct DataChunk population), and release on teardown.
- No legacy 0.x entry symbols exported.
</verification>

<success_criteria>
- DuckDB v1.5.3 build inputs vendored and the two research-flagged Wave-0 checks executed (entry-macro presence recorded; buffer layout asserted by a passing test).
- A single C++ source file implements the entry point + loom_scan + OneShotStream factory → arrow_scan path (D-01) + release-safe LoomScanState (DUCK-02, DUCK-03), ready for Plan 02 to compile, footer-stamp, and load.
</success_criteria>

<output>
Create `.planning/phases/02-duckdb-extension-scaffold/02-01-SUMMARY.md` when done. Record: whether DUCKDB_CPP_EXTENSION_ENTRY was found in the amalgamation (or that the manual fallback is used); the confirmed buffer layout; and the exact arrow_scan delegation mechanism used (which API surface drove the stream).
</output>
</content>
</invoke>
