---
phase: 43.1-production-native-codegen-realization
plan: 02
type: execute
wave: 2
depends_on:
  - 43.1-01
files_modified:
  - crates/loom-core/src/arrow_buffer_lowering.rs
  - crates/loom-native-melior/src/jit.rs
  - crates/loom-native-melior/src/backend.rs
  - crates/loom-native-melior/tests/production_arrow_semantic_jit.rs
autonomous: true
requirements:
  - CODEGEN-01
must_haves:
  truths:
    - "D-43.1-04: Positive JIT tests invoke the real melior ExecutionEngine path when a compatible toolchain is available."
    - "D-43.1-05: Nullable validity bitmaps and Boolean value bitmaps are copied by generated MLIR, not by Rust builders."
    - "D-43.1-06: Toolchain skip is explicit diagnostic evidence, not positive native-codegen success."
---

<objective>
Extend the production MLIR/LLVM/JIT backend from value-only raw primitive copy
to the Phase 35 Arrow semantic buffer matrix, including Boolean and nullable
validity bitmaps.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add backend tests for primitive, Boolean, and validity buffers</name>
  <files>crates/loom-native-melior/tests/production_arrow_semantic_jit.rs</files>
  <action>Add focused JIT tests with non-zero Int32/Int64/Float32/Float64 values, Boolean bitmaps, nullable validity bitmaps, and row counts that are not multiples of eight. Require exact output buffer equality when the compatible MLIR/LLVM toolchain is present; assert fail-closed or explicit skip diagnostics otherwise.</action>
  <verify>
    <automated>cargo test -p loom-native-melior --test production_arrow_semantic_jit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extend buffer plans for Boolean and nullable columns</name>
  <files>crates/loom-core/src/arrow_buffer_lowering.rs</files>
  <action>Add Boolean bitmap buffer support and nullable validity-copy plans. Preserve existing decode-dialect raw-copy behavior while adding Arrow semantic buffer plans that can carry value and validity byte buffers.</action>
  <acceptance_criteria>
    - `PrimitiveArrowType` or successor plan type represents Boolean bitmap copy.
    - Nullable fixed-width primitives no longer fail support classification in the Arrow semantic codegen path.
    - Existing non-null raw primitive tests keep passing.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen</automated>
    <automated>cargo test -p loom-core arrow_buffer_lowering</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Generate MLIR for typed values plus bitmap byte-copy buffers</name>
  <files>crates/loom-core/src/arrow_buffer_lowering.rs, crates/loom-native-melior/src/jit.rs</files>
  <action>Generate MLIR that copies typed primitive value memrefs and byte-addressed bitmap memrefs through the real lowering pipeline. Update the JIT storage/argument model to pass input and output descriptors for value and validity buffers.</action>
  <acceptance_criteria>
    - The JIT output contains separate value and validity buffers where needed.
    - Boolean value buffers are copied as Arrow bitmaps with exact byte lengths.
    - Missing input buffers, wrong lengths, and unsupported buffer kinds fail closed.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-native-melior --test production_arrow_semantic_jit</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic_codegen
cargo test -p loom-native-melior --test production_arrow_semantic_jit
bash scripts/melior-jit-test.sh
git diff --check
</verification>

<success_criteria>
Plan 43.1-02 is complete when the backend can produce exact native output
buffers for the Phase 35 fixed-width nullable matrix through MLIR/LLVM/JIT, and
the tests distinguish real JIT output from zero buffers or Rust-side copies.
</success_criteria>

<output>
Create `43.1-02-SUMMARY.md` when done.
</output>

