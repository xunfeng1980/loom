---
phase: 43.1-production-native-codegen-realization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/src/arrow_buffer_lowering.rs
  - crates/loom-core/src/lib.rs
  - crates/loom-core/tests/native_arrow_semantic_codegen.rs
autonomous: true
requirements:
  - CODEGEN-01
  - CODEGEN-03
must_haves:
  truths:
    - "D-43.1-01: Arrow semantic codegen support starts from verifier-accepted LMC2(LMA1) or direct LMA1 bytes."
    - "D-43.1-02: Support classification covers the full Phase 35 fixed-width nullable matrix, including Boolean."
    - "D-43.1-03: Extracted native inputs contain real Arrow value and validity buffer bytes, never zero placeholders."
---

<objective>
Create the Loom-core Arrow semantic native-codegen contract: verifier-gated
support classification, deterministic buffer extraction, and matrix tests for
the artifacts that the backend must compile.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add focused support and extraction tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen.rs</files>
  <action>Add tests for default `LMC2(LMA1)` and direct `LMA1` one-batch Boolean/Int32/Int64/Float32/Float64 nullable artifacts. Assert support reports expose row count, column metadata, value-buffer bytes, validity-buffer bytes where nullable, and stable diagnostics for Utf8/logical/nested/multi-batch shapes.</action>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add Arrow semantic codegen support reports</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/src/lib.rs</files>
  <action>Introduce a production-codegen support report that verifies artifact bytes, unwraps `LMC2` to inner `LMA1` when needed, decodes the Arrow semantic payload, requires exactly one batch, and records artifact kind, payload kind, schema fingerprint, row count, and column count.</action>
  <acceptance_criteria>
    - Verifier rejection stops before buffer extraction.
    - `LMC2` and direct `LMA1` produce distinct artifact-kind evidence.
    - Unsupported shapes return deterministic diagnostic codes and paths.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen support</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Extract real Arrow buffers for the Phase 35 matrix</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/src/arrow_buffer_lowering.rs</files>
  <action>Add value/validity buffer descriptors for supported columns. Primitive value buffers should preserve little-endian Arrow value bytes; Boolean value and validity buffers should preserve Arrow bitmap bytes with exact byte lengths for the row count.</action>
  <acceptance_criteria>
    - Nullable columns carry validity bitmap bytes and null count.
    - Boolean carries bitmap value bytes, not byte-per-row pseudo-values.
    - Tests use non-zero sentinel data so zero-filled stand-ins fail.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen buffers</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic_codegen
cargo test -p loom-core --test native_arrow_semantic
git diff --check
</verification>

<success_criteria>
Plan 43.1-01 is complete when Loom-core can classify and extract real native
codegen inputs for every Phase 35 supported Arrow semantic artifact shape, while
failing closed for unsupported shapes before backend/JIT execution.
</success_criteria>

<output>
Create `43.1-01-SUMMARY.md` when done.
</output>

