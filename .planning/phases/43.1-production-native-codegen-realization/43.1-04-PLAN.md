---
phase: 43.1-production-native-codegen-realization
plan: 04
type: execute
wave: 3
depends_on:
  - 43.1-01
  - 43.1-02
  - 43.1-03
files_modified:
  - scripts/production-native-codegen-realization-test.sh
  - scripts/mvp2-verify.sh
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/43.1-production-native-codegen-realization/43.1-NATIVE-CODEGEN-REPORT.md
  - README.md
  - README-zh.md
autonomous: true
requirements:
  - CODEGEN-01
  - CODEGEN-02
  - CODEGEN-03
must_haves:
  truths:
    - "D-43.1-10: The closeout gate fails closed when production native codegen is absent, divergent, or admitted without Phase 40 validation."
    - "D-43.1-11: Phase 44 receives an explicit ABI/cache/backend identity handoff based on real native codegen."
    - "D-43.1-12: Documentation keeps MLIR/LLVM/compiler/CPU in the TCB and avoids verified-compilation claims."
---

<objective>
Close Phase 43.1 with release-gate evidence, docs, requirement state, and the
Phase 44 ABI-freeze handoff.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add focused production-native-codegen gate</name>
  <files>scripts/production-native-codegen-realization-test.sh, scripts/mvp2-verify.sh</files>
  <action>Add a focused script that runs the Loom-core codegen contract tests, loom-native-melior JIT tests, and validation/runtime/cache tests. Wire it into the MVP2 verification sequence before Phase 44 gates.</action>
  <acceptance_criteria>
    - Toolchain skip is reported explicitly and cannot satisfy the focused positive gate unless the script is run in documented skip mode.
    - The script checks for stale placeholder markers such as positive use of `reference_zeroed_value_bytes`.
    - Existing broad gates remain runnable in environments without native tooling.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/production-native-codegen-realization-test.sh</automated>
    <automated>bash scripts/mvp2-verify.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write final native-codegen report and ABI handoff</name>
  <files>.planning/phases/43.1-production-native-codegen-realization/43.1-NATIVE-CODEGEN-REPORT.md</files>
  <action>Document the supported matrix, backend/toolchain/pipeline identity, cache-key ingredients, validation evidence, fallback behavior, and non-claims. Include a Phase 44 ABI-freeze input section.</action>
  <acceptance_criteria>
    - Report names exactly which artifacts and Arrow types are positive native-codegen evidence.
    - Report names every TCB assumption inherited from Phase 36/40.
    - Report distinguishes real codegen output from interpreter fallback.
  </acceptance_criteria>
  <verify>
    <manual>Review report against CODEGEN-01 through CODEGEN-03 and Phase 44 entry criteria.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update roadmap, requirements, state, and public docs</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, README.md, README-zh.md</files>
  <action>Mark CODEGEN requirements and Phase 43.1 complete only after gates pass. Update docs to say production native codegen is realized for the bounded Phase 35 matrix and validated per run, while verified compilation remains out of scope.</action>
  <acceptance_criteria>
    - Phase 44 remains the next phase and consumes the Phase 43.1 handoff.
    - `ENGINE-01` remains suspended/pre-GA and is not silently closed.
    - Public docs do not claim broader Arrow/nested/logical/native coverage.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check</automated>
    <automated>bash scripts/production-native-codegen-realization-test.sh</automated>
  </verify>
</task>

</tasks>

<verification>
bash scripts/production-native-codegen-realization-test.sh
bash scripts/mvp2-verify.sh
git diff --check
</verification>

<success_criteria>
Plan 43.1-04 is complete when the focused gate and MVP2 gate pass, CODEGEN-01
through CODEGEN-03 are closed, Phase 43.1 has a final report, and Phase 44 can
start ABI freeze using real native-codegen evidence rather than placeholders.
</success_criteria>

<output>
Create `43.1-04-SUMMARY.md` when done.
</output>

