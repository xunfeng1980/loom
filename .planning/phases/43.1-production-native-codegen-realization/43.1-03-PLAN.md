---
phase: 43.1-production-native-codegen-realization
plan: 03
type: execute
wave: 2
depends_on:
  - 43.1-01
  - 43.1-02
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/src/runtime_abi.rs
  - crates/loom-native-melior/src/lib.rs
  - crates/loom-core/tests/native_arrow_semantic_codegen.rs
autonomous: true
requirements:
  - CODEGEN-01
  - CODEGEN-02
  - CODEGEN-03
must_haves:
  truths:
    - "D-43.1-07: A production native-codegen execution report is only supported after Phase 40 validation succeeds."
    - "D-43.1-08: Runtime/cache admission consumes validated production-codegen evidence, not Phase 35 builder-copy execution."
    - "D-43.1-09: Divergence, unsupported shapes, and toolchain mismatch fail closed or choose explicit interpreter fallback."
---

<objective>
Integrate real JIT-produced Arrow semantic output with Phase 40 model
validation, runtime decisions, fallback policy, and cache identity.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add validation-gated runtime/cache tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic_codegen.rs</files>
  <action>Add tests showing successful production-codegen validation becomes a native candidate and cacheable, while injected output divergence, unsupported shape, toolchain mismatch, and skipped toolchain cannot seed native cache identity.</action>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen validation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Assemble RecordBatch output from JIT buffers</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs</files>
  <action>Add a production-codegen execution report that rebuilds Arrow arrays from JIT value/validity buffers using the original schema metadata, row count, null count, and type descriptors. This assembly may use Arrow array construction APIs, but values/nulls must come from JIT output buffers.</action>
  <acceptance_criteria>
    - Output RecordBatch equals the decoded reference for supported artifacts.
    - Array construction rejects buffer length/schema/null-count mismatches.
    - The report records backend identity distinct from Phase 35 builder-copy execution.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen output</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Require Phase 40 validation before admission</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/src/runtime_abi.rs</files>
  <action>Call the existing Phase 40 model validation API on the JIT-produced RecordBatch. Expose runtime decision and cache-key helpers that require validated production-codegen reports before returning `NativeCandidate` or cache identity.</action>
  <acceptance_criteria>
    - Validation failure is fail-closed under strict policy.
    - Interpreter fallback is explicit under fallback-enabled policy.
    - Cache identity includes artifact digest, verifier facts, backend identity, toolchain identity, pipeline identity, target/layout facts, and trace fingerprints.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic_codegen runtime_cache</automated>
    <automated>cargo test -p loom-core --test native_arrow_semantic native_model</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic_codegen
cargo test -p loom-core --test native_arrow_semantic native_model
cargo test -p loom-native-melior --test production_arrow_semantic_jit
git diff --check
</verification>

<success_criteria>
Plan 43.1-03 is complete when real native-codegen output is admitted only after
Phase 40 model validation, and unvalidated or divergent output cannot be
reported, routed, or cached as positive native execution.
</success_criteria>

<output>
Create `43.1-03-SUMMARY.md` when done.
</output>

