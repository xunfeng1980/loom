---
phase: 52-container-split-loom-common-core-and-contrib-loom-container-legacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-common/Cargo.toml
  - crates/loom-common/src/lib.rs
  - crates/loom-common/src/artifact_types.rs
  - crates/loom-common/src/verify_layout_types.rs
  - crates/loom-common/src/arrow_semantic.rs
  - crates/loom-common/src/arrow_semantic_codec.rs
  - crates/loom-common/src/arrow_semantic_verifier.rs
  - crates/loom-common/src/native_arrow_semantic.rs
  - crates/loom-common/src/arrow_buffer_lowering.rs
  - crates/loom-common/src/native_lowering.rs
  - crates/loom-common/src/production_native_lowering.rs
  - crates/loom-common/src/decode_dialect.rs
  - crates/loom-common/src/runtime_abi.rs
  - crates/loom-common/src/l1_model.rs
  - crates/loom-common/src/l1_model/bitpack.rs
  - crates/loom-common/src/l2_kernel_registry.rs
  - crates/loom-common/src/fsst_params.rs
  - crates/loom-common/src/alp_params.rs
  - crates/loom-common/src/arrow_builder_output.rs
  - crates/loom-common/src/kloom_harness.rs
  - crates/loom-container/src/verifier.rs
  - crates/loom-container/src/artifact_verifier.rs
  - crates/loom-container/src/lib.rs
  - crates/loom-core/Cargo.toml
  - crates/loom-core/src/lib.rs
  - Cargo.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "crates/loom-common exists as a workspace member with Cargo.toml depending on loom-ir-core and arrow deps"
    - "15 clean modules moved to crates/loom-common/src/ with zero dependency on container_codec/layout_codec/table_codec"
    - "artifact_types.rs extracted from artifact_verifier.rs (lines 18-268, ~250 lines of pure type definitions) into loom-common"
    - "verify_layout function + VerificationReport/VerificationCode/VerificationDiagnostic types extracted from verifier.rs into loom-common"
    - "l1_model.rs imports verify_layout from crate::verify_layout_types (same-crate path after move)"
    - "runtime_abi.rs imports ArtifactVerificationStatus from crate::artifact_types (same-crate path after move)"
    - "production_native_lowering.rs imports from crate::artifact_types (same-crate path after move)"
    - "loom-core/Cargo.toml lists loom-common as dependency; keeps loom-container temporarily for remaining modules"
    - "loom-core/src/lib.rs re-exports moved modules from loom_common instead of loom_container"
  artifacts:
    - path: "crates/loom-common/Cargo.toml"
      provides: "New loom-common crate manifest with loom-ir-core and arrow deps"
      contains: "[package]\nname = \"loom-common\""
    - path: "crates/loom-common/src/lib.rs"
      provides: "Module declarations for all 17 moved modules"
      contains: "pub mod artifact_types;"
    - path: "crates/loom-common/src/artifact_types.rs"
      provides: "ArtifactVerificationStatus, ArtifactVerificationReport, and related diagnostics"
      exports: ["ArtifactVerificationStatus", "ArtifactVerificationReport", "ArtifactVerificationStage"]
    - path: "crates/loom-common/src/verify_layout_types.rs"
      provides: "verify_layout function, VerificationReport, VerificationCode, VerificationDiagnostic"
      exports: ["verify_layout", "VerificationReport", "VerificationCode", "VerificationDiagnostic"]
  key_links:
    - from: "crates/loom-common/src/l1_model.rs"
      to: "crates/loom-common/src/verify_layout_types.rs"
      via: "use crate::verify_layout_types::verify_layout"
      pattern: "use crate::verify_layout"
    - from: "crates/loom-common/src/runtime_abi.rs"
      to: "crates/loom-common/src/artifact_types.rs"
      via: "use crate::artifact_types::ArtifactVerificationStatus"
      pattern: "use crate::artifact_types"
    - from: "crates/loom-core/src/lib.rs"
      to: "crates/loom-common/src/lib.rs"
      via: "pub use loom_common:: module re-exports"
      pattern: "pub use loom_common::"
---

<objective>
Create `crates/loom-common` — the production-core crate — by moving 15 clean modules from
`crates/loom-container` and extracting two new type-definition modules from files that have
cross-category imports. Zero dependency on container_codec/layout_codec/table_codec.
</objective>

<execution_context>
@/Users/macintoshhd/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-container-split-loom-common-core-and-contrib-loom-container-legacy/52-RESEARCH.md
@.planning/phases/52-container-split-loom-common-core-and-contrib-loom-container-legacy/52-CONTEXT.md
@crates/loom-container/src/lib.rs
@crates/loom-container/Cargo.toml
@crates/loom-container/src/artifact_verifier.rs
@crates/loom-container/src/verifier.rs
@crates/loom-core/Cargo.toml
@crates/loom-core/src/lib.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loom-common crate, move 15 clean modules, extract 2 type modules</name>
  <files>
    crates/loom-common/Cargo.toml,
    crates/loom-common/src/lib.rs,
    crates/loom-common/src/artifact_types.rs,
    crates/loom-common/src/verify_layout_types.rs,
    crates/loom-common/src/arrow_semantic.rs,
    crates/loom-common/src/arrow_semantic_codec.rs,
    crates/loom-common/src/arrow_semantic_verifier.rs,
    crates/loom-common/src/native_arrow_semantic.rs,
    crates/loom-common/src/arrow_buffer_lowering.rs,
    crates/loom-common/src/native_lowering.rs,
    crates/loom-common/src/production_native_lowering.rs,
    crates/loom-common/src/decode_dialect.rs,
    crates/loom-common/src/runtime_abi.rs,
    crates/loom-common/src/l1_model.rs,
    crates/loom-common/src/l1_model/bitpack.rs,
    crates/loom-common/src/l2_kernel_registry.rs,
    crates/loom-common/src/fsst_params.rs,
    crates/loom-common/src/alp_params.rs,
    crates/loom-common/src/arrow_builder_output.rs,
    crates/loom-common/src/kloom_harness.rs,
    crates/loom-container/src/verifier.rs,
    crates/loom-container/src/artifact_verifier.rs,
    crates/loom-container/src/lib.rs,
    Cargo.toml
  </files>
  <action>
**A. Create crates/loom-common directory and Cargo.toml:**

Create `crates/loom-common/` directory. Write `Cargo.toml` with package name `loom-common`, description "Loom production-core types, codecs, lowering, and runtime ABI". Use workspace dependencies for arrow, arrow-array, arrow-buffer, arrow-schema, arrow-data, arrow-ipc, fsst, ron, serde, fnv (same as the existing loom-container Cargo.toml). Add `loom-ir-core = { path = "../loom-ir-core" }` dependency.

**B. Create lib.rs with module declarations:**

Write `crates/loom-common/src/lib.rs` with:
- `#![forbid(unsafe_code)]`
- The `l2_to_arrow` and `arrow_to_l2` conversion functions (copy from current loom-container/src/lib.rs lines 9-36 — these are production-core utility functions with no container dep)
- Module declarations for all 17 modules:
  ```rust
  pub mod artifact_types;
  pub mod verify_layout_types;
  pub mod fsst_params;
  pub mod alp_params;
  pub mod arrow_builder_output;
  pub mod arrow_semantic;
  pub mod arrow_semantic_codec;
  pub mod arrow_semantic_verifier;
  pub mod native_lowering;
  pub mod production_native_lowering;
  pub mod decode_dialect;
  pub mod arrow_buffer_lowering;
  pub mod runtime_abi;
  pub mod native_arrow_semantic;
  pub mod l1_model;
  pub mod l2_kernel_registry;
  pub mod kloom_harness;
  ```

**C. Move 15 clean module files:**

Copy (using `cp`, not `mv` — the originals stay in loom-container for now, removed in plan 52-02) these files from `crates/loom-container/src/` to `crates/loom-common/src/`:
- `arrow_semantic.rs` — no intra-crate imports (only loom_ir_core)
- `arrow_semantic_codec.rs` — imports only `arrow_semantic` + `arrow_semantic_verifier` (both moving)
- `arrow_semantic_verifier.rs` — imports only `arrow_semantic` (moving)
- `native_arrow_semantic.rs` — imports `arrow_builder_output`, `arrow_semantic_codec`, `artifact_verifier`, `kloom_harness`, `l2_kernel_registry`, `runtime_abi` (see import fix below)
- `arrow_buffer_lowering.rs` — imports only `decode_dialect` + `production_native_lowering` (both moving)
- `native_lowering.rs` — ZERO intra-crate imports (only loom_ir_core)
- `production_native_lowering.rs` — imports `artifact_verifier`, `decode_dialect`, `l1_model` (see import fix below)
- `decode_dialect.rs` — imports only `production_native_lowering` (moving)
- `runtime_abi.rs` — imports `artifact_verifier::ArtifactVerificationStatus` (see import fix below)
- `l1_model.rs` + `l1_model/bitpack.rs` — imports `arrow_builder_output`, `l2_kernel_registry`, `verifier::verify_layout` (see import fix below)
- `l2_kernel_registry.rs` — imports `alp_params`, `fsst_params` (both moving)
- `fsst_params.rs` — zero intra-crate imports
- `alp_params.rs` — zero intra-crate imports
- `arrow_builder_output.rs` — zero intra-crate imports
- `kloom_harness.rs` — zero intra-crate imports (only loom_ir_core)

**D. Extract artifact_types.rs from artifact_verifier.rs:**

Read `crates/loom-container/src/artifact_verifier.rs`. Lines 18-268 contain pure type/enum/struct definitions with ZERO container-layer imports (no `use crate::container_codec`). Extract these into `crates/loom-common/src/artifact_types.rs`:
- `ArtifactVerificationStage` enum + `as_str` impl
- `ArtifactVerificationStatus` enum + `as_str` impl
- `ArtifactVerificationDiagnostic` struct + `ArtifactLoweringDiagnostic` struct
- `ArtifactLoweringReadiness` struct
- `ArtifactVerificationFacts` struct
- `ArtifactVerificationOptions` struct
- `ArtifactVerificationReport` struct + `is_accepted()` + `is_rejected()` + builder methods through `accepted()`/`rejected()` constructors

The extracted types only import from `loom_ir_core::*` and std types. Verify: grep the extracted type definitions for `use crate::container_codec` before writing — must be zero.

Replace the original lines 18-268 in `crates/loom-container/src/artifact_verifier.rs` with:
```rust
pub use loom_common::artifact_types::*;
```
(The original file in loom-container keeps its container-dependent functions: `verify_artifact`, `verify_artifact_with_l2_core`, etc. which import from `container_codec`.)

**E. Extract verify_layout_types.rs from verifier.rs:**

Read `crates/loom-container/src/verifier.rs`. Extract into `crates/loom-common/src/verify_layout_types.rs`:
- `VerificationCode` enum (all variants) + `as_str` + `Display` impl (lines 25-90ish)
- `VerificationDiagnostic` struct + `code()` + `path()` + `message()` methods (lines ~92-120)
- `VerificationReport` struct + `is_clean()` + `diagnostics()` + `add_diagnostic()` + `merge()` + `is_clean_and()` (lines ~122-200)
- `verify_layout` function body (lines ~202 onward until container-dependent functions begin)
- `verify_bitpack_width` helper if it exists as standalone

The extracted types only use `LayoutDescription`, `LayoutNode`, `L2KernelRegistry`, `DataType` from arrow, `AlpParams`, `FsstParams` — all of which are in loom-common after moves. Verify: grep for `container_codec`, `table_codec` in extracted content — must be zero.

Replace extracted content in `crates/loom-container/src/verifier.rs` with:
```rust
pub use loom_common::verify_layout_types::*;
```

**F. Fix intra-crate imports in moved files:**

For each moved file, update `use crate::` imports referencing modules that are also moving (now in the same crate):
- `arrow_semantic_codec.rs`: `use crate::arrow_semantic::*` stays as-is (both in loom-common now; `crate::` works)
- `arrow_semantic_verifier.rs`: `use crate::arrow_semantic::*` stays as-is
- `l2_kernel_registry.rs`: `use crate::alp_params::*` + `use crate::fsst_params::*` stays as-is
- `l1_model.rs`: Change `use crate::verifier::verify_layout` to `use crate::verify_layout_types::verify_layout`
- `runtime_abi.rs`: Change `use crate::artifact_verifier::ArtifactVerificationStatus` to `use crate::artifact_types::ArtifactVerificationStatus`
- `production_native_lowering.rs`: Change `use crate::artifact_verifier::*` imports to `use crate::artifact_types::*` for type-only imports (ArtifactVerificationStatus, ArtifactVerificationReport, ArtifactVerificationFacts). If the file calls `verify_artifact()` (a function, not a type), keep that import but redirect: since `verify_artifact` stays in contrib, we need a different approach — CHECK the file first: if it only imports types from artifact_verifier (not functions), update. If it calls container-dependent functions, note for plan 52-02.
- `native_arrow_semantic.rs`: Change `use crate::artifact_verifier::*` to `use crate::artifact_types::*` for type imports. For the `verify_artifact` function call, check if it's used. If used, the module must also import from contrib (handled in plan 52-02 via loom-core re-exports).
- `arrow_buffer_lowering.rs`: `use crate::decode_dialect::*` + `use crate::production_native_lowering::*` stays as-is (both moving)
- `decode_dialect.rs`: `use crate::production_native_lowering::*` stays as-is

**G. Add loom-common to workspace:**

In root `Cargo.toml`, add `"crates/loom-common"` to the `[workspace] members` array.

**H. Update loom-core:**

In `crates/loom-core/Cargo.toml`: add `loom-common = { path = "../loom-common" }` to `[dependencies]`. Keep `loom-container` dependency for remaining modules (removed in plan 52-02).

In `crates/loom-core/src/lib.rs`: update re-exports. For each of the 17 modules now in loom-common, change `pub use loom_container::X` to `pub use loom_common::X`. Keep `pub use loom_container::*` for modules not yet moved (container_codec, layout_codec, table_codec, descriptor, verified_lineage, verifier functions, artifact_verifier functions). Add: `pub use loom_common::artifact_types;` and `pub use loom_common::verify_layout_types;`.

**I. Run cargo check to verify:**

```bash
cargo check -p loom-common 2>&1
cargo check -p loom-core 2>&1
```

Fix any unresolved imports. The key invariants to verify:
1. `loom-common` compiles without any `container_codec`/`layout_codec`/`table_codec` reference
2. `loom-core` re-exports resolve correctly from both loom-common and loom-container
3. `cargo tree -p loom-common | grep -c contrib` returns 0 (no contrib dep)
</action>
<verify>
  <automated>
    cargo check -p loom-common 2>&1 | grep -c 'error' | grep '^0$' &&
    cargo check -p loom-core 2>&1 | grep -c 'error' | grep '^0$' &&
    cargo tree -p loom-common | grep -c 'contrib' | grep '^0$'
  </automated>
</verify>
<done>
- crates/loom-common compiles cleanly with zero reference to container_codec/layout_codec/table_codec
- crates/loom-core re-exports modules from both loom-common and loom-container correctly
- Verify with: `cargo tree -p loom-common | grep -v 'loom-common'` shows only loom-ir-core and external arrow/fsst/ron/serde/fnv deps
</done>
</task>

<task type="auto">
  <name>Task 2: Verify downstream crate compilation and gate loom-common isolation</name>
  <files>
    crates/loom-common/src/lib.rs,
    crates/loom-core/src/lib.rs
  </files>
  <action>
Run the full workspace check to ensure no regressions from the loom-common creation:

```bash
cargo check --workspace 2>&1
```

Fix any compilation errors. Common issues:
- Modules in loom-container that imported from moved modules may have `use crate::X` that now resolves to nothing (the module declaration was removed from loom-container's lib.rs). These will be fixed in plan 52-02.
- Downstream crates importing `loom_core::X` where X was re-exported from the old path — verify loom-core/src/lib.rs updates catch all re-exports.

Run isolation verification:
```bash
# Verify loom-common has zero contrib dependency
cargo tree -p loom-common --invert 2>&1 | grep -c 'contrib'  # must be 0

# Verify loom-common has zero container_codec reference in its source
grep -rn 'container_codec\|layout_codec\|table_codec' crates/loom-common/src/ | grep -v '^Binary' | grep -v verify_layout_types | wc -l  # must be 0

# Verify artifact_types.rs has zero container imports
grep -c 'container_codec' crates/loom-common/src/artifact_types.rs  # must be 0
grep -c 'table_codec' crates/loom-common/src/artifact_types.rs     # must be 0

# Verify verify_layout_types.rs has zero container imports
grep -c 'container_codec' crates/loom-common/src/verify_layout_types.rs  # must be 0
```

If any isolation gate fails: fix before proceeding to plan 52-02.

Document the final module count in loom-common (should be 17: 11 original A + 4 moved-from-B + 2 extracted). Count with:
```bash
grep 'pub mod' crates/loom-common/src/lib.rs | wc -l
```
</action>
<verify>
  <automated>
    cargo check --workspace 2>&1 | tail -5 &&
    grep -c 'container_codec\|layout_codec\|table_codec' crates/loom-common/src/artifact_types.rs | grep '^0$' &&
    grep -rn 'container_codec\|layout_codec\|table_codec' crates/loom-common/src/ --include='*.rs' | grep -v verify_layout_types.rs | wc -l | grep '^ *0$'
  </automated>
</verify>
<done>
- Full workspace cargo check passes (modulo remaining modules in old loom-container — plan 52-02 handles those)
- loom-common has zero reference to container_codec/layout_codec/table_codec in any production source file
- artifact_types.rs and verify_layout_types.rs have zero container-layer imports
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loom-common → contrib/loom-container | loom-common must never import from contrib — enforced by Cargo resolver (circular dep = compile error) |
| loom-common public API | Must not expose container-level types (ContainerDescription, TableDescription, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | artifact_types.rs extraction | mitigate | Verify extracted types import zero container-layer modules via grep gate in Task 2 verify |
| T-52-02 | Information Disclosure | verify_layout_types.rs extraction | mitigate | Verify verify_layout function body has zero container_codec/table_codec references via grep gate |
| T-52-03 | Tampering | loom-common/public API | mitigate | loom-common must not expose `ContainerDescription` or `TableDescription` — audit pub exports |
| T-52-04 | Denial of Service | Stale loom-container references | mitigate | cargo check --workspace catches all unresolved imports at compile time |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new external packages installed; pure crate reorganization |
</threat_model>

<verification>
## Isolation Gate (must pass before plan 52-02)

```bash
# Gate 1: loom-common has zero container_codec dependency (source grep)
grep -rn 'container_codec\|layout_codec\|table_codec' crates/loom-common/src/ --include='*.rs' | wc -l | grep '^ *0$'

# Gate 2: artifact_types.rs imports zero container modules
grep -c 'container_codec\|table_codec' crates/loom-common/src/artifact_types.rs | grep '^0$'

# Gate 3: verify_layout_types.rs imports zero container modules
grep -c 'container_codec\|table_codec' crates/loom-common/src/verify_layout_types.rs | grep '^0$'

# Gate 4: loom-common has no contrib/loom-container in dependency tree
cargo tree -p loom-common | grep -c 'contrib' | grep '^0$'

# Gate 5: loom-common module count ≥ 16 (minimum for production surface)
grep -c 'pub mod' crates/loom-common/src/lib.rs | awk '$1 >= 16'
```
</verification>

<success_criteria>
1. `crates/loom-common` exists as a workspace member with 17 modules
2. Zero dependency on container_codec/layout_codec/table_codec in loom-common source or dependency tree
3. `artifact_types.rs` extracted cleanly from artifact_verifier.rs (zero container imports)
4. `verify_layout_types.rs` extracted cleanly from verifier.rs (zero container imports)
5. `loom-core` depends on `loom-common` and re-exports all moved modules
6. Full workspace `cargo check` passes with no regression
</success_criteria>

<output>
Create `.planning/phases/52-container-split-loom-common-core-and-contrib-loom-container-legacy/52-01-SUMMARY.md` when done
</output>
