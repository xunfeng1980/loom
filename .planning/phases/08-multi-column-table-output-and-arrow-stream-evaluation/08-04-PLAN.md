---
phase: 08-multi-column-table-output-and-arrow-stream-evaluation
plan: "04"
subsystem: verification
tags: [sql, docs, release-gate]
requirements: [VERIFY-05]
wave: 3
requires:
  - phase: 08-multi-column-table-output-and-arrow-stream-evaluation
    plan: "02"
    provides: multi-column fixtures and CLI
  - phase: 08-multi-column-table-output-and-arrow-stream-evaluation
    plan: "03"
    provides: DuckDB multi-column scan
---

# Phase 08-04 Plan: Multi-Column SQL Acceptance and Phase Closure

## Purpose

Close Phase 8 with a SQL acceptance gate, updated docs, and requirement closure.

## Tasks

1. Add multi-column SQL acceptance.
   - Extend `scripts/duckdb-smoke-test.sh` or add a dedicated script invoked by it.
   - Check rows, projection, filter, and aggregates.
   - Ensure existing single-column checks still run.
2. Update docs.
   - README and README-zh include table payload CLI examples.
   - Document ArrowArrayStream decision.
   - Record table payload format as MVP0/v2-internal unless promoted later.
3. Close planning docs.
   - Mark COV-02, TABLE-01/02/03, DUCK-05, STREAM-01, VERIFY-05 complete only after final verification passes.
   - Write Phase 8 summaries.
4. Run final gates.
   - workspace tests;
   - dependency isolation;
   - fixture hygiene;
   - single-column and multi-column DuckDB smoke.

## Acceptance Criteria

- `scripts/mvp0-verify.sh` passes.
- Multi-column SQL checks pass.
- Phase 8 requirements are complete in `.planning/REQUIREMENTS.md`.
- Worktree is ready for commit.

## Verification

- `cargo test --workspace`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
