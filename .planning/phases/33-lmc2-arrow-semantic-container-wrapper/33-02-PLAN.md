---
phase: 33-lmc2-arrow-semantic-container-wrapper
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - crates/loom-core/src/artifact_verifier.rs
  - crates/loom-core/tests/artifact_verifier.rs
  - crates/loom-core/tests/arrow_semantic.rs
autonomous: true
requirements:
  - PHASE-33
must_haves:
  truths:
    - "D-33-04: verify_artifact routes direct LMA1 and wrapped LMC2(LMA1) fail-closed before legacy LMC1 fallback."
    - "D-33-05: accepted LMC2 facts expose wrapper version, features, Arrow semantic payload kind, schema presence, row count, batch count when practical, and inner LMA1 acceptance summary."
    - "D-33-06: LMC2 lowering readiness remains not ready with the Arrow semantic deferral diagnostic."
    - "D-33-07: malformed wrapper diagnostics distinguish wrapper shape/version/feature/inner-payload failures."
  artifacts:
    - path: crates/loom-core/src/artifact_verifier.rs
      provides: "verify_artifact routing and facts for LMC2"
---

<objective>
Route `LMC2(LMA1)` through the unified artifact verifier with stable facts and fail-closed diagnostics.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-CONTEXT.md
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-RESEARCH.md
@crates/loom-core/src/artifact_verifier.rs
@crates/loom-core/src/arrow_semantic_codec.rs
@crates/loom-core/tests/artifact_verifier.rs
@crates/loom-core/tests/arrow_semantic.rs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add artifact verifier tests for LMC2 acceptance</name>
  <files>crates/loom-core/tests/artifact_verifier.rs, crates/loom-core/tests/arrow_semantic.rs</files>
  <read_first>
    - crates/loom-core/tests/artifact_verifier.rs
    - crates/loom-core/tests/arrow_semantic.rs
    - crates/loom-core/src/artifact_verifier.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
  </read_first>
  <action>Add tests that create a verified Arrow semantic payload, wrap it as `LMC2`, call `verify_artifact`, and assert accepted status, `facts.artifact_kind == "LMC2"`, `facts.container_version == Some(1)`, `facts.payload_kind == Some("Arrow semantic payload")`, `facts.schema_section_present == true`, and `facts.row_count_bound == Some(3)` for the scalar fixture. Also assert direct `LMA1` is still accepted.</action>
  <acceptance_criteria>
    - `crates/loom-core/tests/artifact_verifier.rs` contains `verify_artifact_accepts_lmc2_arrow_semantic_container`.
    - The test asserts both direct `LMA1` and wrapped `LMC2` accepted statuses.
    - The test checks `artifact_kind` values `LMA1` and `LMC2` separately.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test artifact_verifier verify_artifact_accepts_lmc2_arrow_semantic_container</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Add artifact verifier tests for LMC2 rejection diagnostics</name>
  <files>crates/loom-core/tests/artifact_verifier.rs</files>
  <read_first>
    - crates/loom-core/tests/artifact_verifier.rs
    - crates/loom-core/src/artifact_verifier.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
  </read_first>
  <action>Add artifact-verifier tests for malformed `LMC2` bytes that assert rejected status and diagnostic code/path/message coverage for malformed wrapper shape, unsupported version, unknown required feature, missing required `LMA1` payload, and malformed inner `LMA1` payload.</action>
  <acceptance_criteria>
    - `crates/loom-core/tests/artifact_verifier.rs` contains `verify_artifact_rejects_malformed_lmc2_container`.
    - The test asserts at least one diagnostic path beginning with `$.lmc2`.
    - The test asserts `LMC2` malformed bytes do not report the legacy `artifact container does not contain a supported LMP1 or LMT1 payload` message.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test artifact_verifier verify_artifact_rejects_malformed_lmc2_container</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement verifier routing and facts</name>
  <files>crates/loom-core/src/artifact_verifier.rs</files>
  <read_first>
    - crates/loom-core/src/artifact_verifier.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
    - crates/loom-core/src/arrow_semantic_verifier.rs
  </read_first>
  <action>Update `verify_artifact` to check `is_arrow_semantic_container(bytes)` and route `LMC2` bytes to a new `verify_arrow_semantic_container_artifact` helper before the legacy `decode_container` branch. Build accepted facts with artifact kind `LMC2`, wrapper version `Some(1)`, payload kind `Arrow semantic payload`, schema section present, row count bound from the decoded inner payload, feature names if the wrapper codec exposes them, and the same `arrow-semantic-lowering-deferred` lowering diagnostic when lowering readiness is requested.</action>
  <acceptance_criteria>
    - `verify_artifact` contains an `is_arrow_semantic_container(bytes)` branch before `decode_container(bytes)`.
    - Accepted `LMC2` facts use `ArtifactVerificationFacts::new("LMC2")`.
    - The lowering diagnostic code for `LMC2` is `arrow-semantic-lowering-deferred`.
    - Direct `LMA1` verifier behavior remains unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test artifact_verifier</automated>
    <automated>cargo test -p loom-core --test arrow_semantic</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact verifier -> execution surfaces | Downstream code trusts accepted artifact facts and diagnostics. |
| LMC2 -> legacy LMC1 verifier branch | LMC2 must not be misclassified as malformed legacy LMC1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Spoofing | artifact facts | mitigate | Facts must report `LMC2` and inner Arrow semantic payload separately from direct `LMA1`. |
| T-33-02-02 | Repudiation | diagnostics | mitigate | Rejection diagnostics identify wrapper-vs-inner-payload failure paths. |
| T-33-02-03 | Elevation of Privilege | native readiness | mitigate | Lowering readiness remains explicitly not ready for Arrow semantic artifacts. |
</threat_model>

<verification>
cargo test -p loom-core --test artifact_verifier
cargo test -p loom-core --test arrow_semantic
git diff --check
</verification>

<success_criteria>
Plan 33-02 is complete when `verify_artifact` accepts `LMC2(LMA1)`, preserves
direct `LMA1`, rejects malformed wrappers with `LMC2`-specific diagnostics, and
does not imply native lowering readiness.
</success_criteria>

<artifacts_this_phase_produces>
- `verify_arrow_semantic_container_artifact`
- `verify_artifact_accepts_lmc2_arrow_semantic_container`
- `verify_artifact_rejects_malformed_lmc2_container`
- `ArtifactVerificationFacts` for `artifact_kind = "LMC2"`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-02-SUMMARY.md` when done.
</output>

