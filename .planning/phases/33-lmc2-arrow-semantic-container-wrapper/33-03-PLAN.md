---
phase: 33-lmc2-arrow-semantic-container-wrapper
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - crates/loom-parquet-ingress/src/source_contract.rs
  - crates/loom-lance-ingress/src/source_contract.rs
  - crates/loom-vortex-ingress/src/source_contract.rs
  - crates/loom-parquet-ingress/tests/source_ingress_handoff.rs
  - crates/loom-lance-ingress/tests/source_ingress_handoff.rs
  - crates/loom-vortex-ingress/tests/source_ingress_handoff.rs
  - crates/loom-parquet-ingress/tests/full_arrow_schema_compatibility.rs
  - crates/loom-lance-ingress/tests/full_arrow_schema_compatibility.rs
  - crates/loom-vortex-ingress/tests/full_arrow_dtype_semantic_compatibility.rs
autonomous: true
requirements:
  - PHASE-33
must_haves:
  truths:
    - "D-33-08: Parquet, Lance, and Vortex accepted source emission produce verifier-accepted LMC2(LMA1) by default."
    - "D-33-09: direct LMA1 helpers or compatibility shims remain available for existing bridge evidence."
    - "D-33-10: source reports state that accepted bytes are LMC2 wrapping Arrow semantic LMA1 and keep oracle evidence separate."
    - "D-33-11: source semantic equality coverage remains unchanged except for wrapper acceptance and negative coverage."
  artifacts:
    - path: crates/loom-parquet-ingress/src/source_contract.rs
      provides: "Parquet default LMC2(LMA1) source artifact emission"
    - path: crates/loom-lance-ingress/src/source_contract.rs
      provides: "Lance default LMC2(LMA1) source artifact emission"
    - path: crates/loom-vortex-ingress/src/source_contract.rs
      provides: "Vortex default LMC2(LMA1) source artifact emission"
---

<objective>
Cut Parquet, Lance, and Vortex source-ingress accepted emission over to `LMC2(LMA1)` while preserving direct `LMA1` compatibility helpers.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-CONTEXT.md
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-RESEARCH.md
@crates/loom-parquet-ingress/src/source_contract.rs
@crates/loom-lance-ingress/src/source_contract.rs
@crates/loom-vortex-ingress/src/source_contract.rs
@crates/loom-source-ingress/src/lib.rs
@crates/loom-core/src/arrow_semantic_codec.rs
@crates/loom-core/src/artifact_verifier.rs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Update source handoff tests for default LMC2 emission</name>
  <files>crates/loom-parquet-ingress/tests/source_ingress_handoff.rs, crates/loom-lance-ingress/tests/source_ingress_handoff.rs, crates/loom-vortex-ingress/tests/source_ingress_handoff.rs</files>
  <read_first>
    - crates/loom-parquet-ingress/tests/source_ingress_handoff.rs
    - crates/loom-lance-ingress/tests/source_ingress_handoff.rs
    - crates/loom-vortex-ingress/tests/source_ingress_handoff.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
    - crates/loom-source-ingress/src/lib.rs
  </read_first>
  <action>Update source handoff tests so accepted Parquet, Lance, and Vortex artifacts assert `is_arrow_semantic_container(&artifact.bytes)` or first-four-byte magic `LMC2`, `verify_artifact` accepted status, and report summaries containing `LMC2` and `Arrow semantic payload`. Keep assertions that decoded Arrow batches equal source/oracle batches.</action>
  <acceptance_criteria>
    - Parquet handoff tests assert emitted bytes start with `LMC2`.
    - Lance handoff tests assert emitted bytes start with `LMC2`.
    - Vortex handoff tests assert emitted bytes start with `LMC2`.
    - Source reports still include oracle evidence assertions separate from artifact verifier assertions.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-parquet-ingress --test source_ingress_handoff</automated>
    <automated>cargo test -p loom-lance-ingress --test source_ingress_handoff</automated>
    <automated>cargo test -p loom-vortex-ingress --test source_ingress_handoff</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update source adapters to wrap accepted bytes by default</name>
  <files>crates/loom-parquet-ingress/src/source_contract.rs, crates/loom-lance-ingress/src/source_contract.rs, crates/loom-vortex-ingress/src/source_contract.rs</files>
  <read_first>
    - crates/loom-parquet-ingress/src/source_contract.rs
    - crates/loom-lance-ingress/src/source_contract.rs
    - crates/loom-vortex-ingress/src/source_contract.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
    - crates/loom-core/src/artifact_verifier.rs
  </read_first>
  <action>Change the default source artifact construction path so Parquet, Lance, and Vortex build direct `LMA1` bytes from `ArrowSemanticPayload`, wrap those bytes as `LMC2`, and pass the wrapped bytes to `verify_artifact`. Keep direct `LMA1` helper compatibility by retaining existing helper names as wrappers, adding explicit `_lmc2_` entry points with old names delegating, or adding documented direct helpers used only by compatibility tests.</action>
  <acceptance_criteria>
    - Default accepted Parquet artifact bytes begin with `LMC2`.
    - Default accepted Lance artifact bytes begin with `LMC2`.
    - Default accepted Vortex artifact bytes begin with `LMC2`.
    - At least one direct `LMA1` encode helper or compatibility shim remains available in each adapter where existing tests or fixture generators need it.
    - Error messages that previously said `failed to encode ... LMA1 payload` now distinguish direct payload encode from `LMC2` wrapper encode where failures can occur.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-parquet-ingress --test source_ingress_handoff</automated>
    <automated>cargo test -p loom-lance-ingress --test source_ingress_handoff</automated>
    <automated>cargo test -p loom-vortex-ingress --test source_ingress_handoff</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Preserve full Arrow semantic equality coverage</name>
  <files>crates/loom-parquet-ingress/tests/full_arrow_schema_compatibility.rs, crates/loom-lance-ingress/tests/full_arrow_schema_compatibility.rs, crates/loom-vortex-ingress/tests/full_arrow_dtype_semantic_compatibility.rs</files>
  <read_first>
    - crates/loom-parquet-ingress/tests/full_arrow_schema_compatibility.rs
    - crates/loom-lance-ingress/tests/full_arrow_schema_compatibility.rs
    - crates/loom-vortex-ingress/tests/full_arrow_dtype_semantic_compatibility.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
  </read_first>
  <action>Update full Arrow semantic compatibility tests to unwrap `LMC2` before direct `LMA1` decode where needed, while preserving the same source/oracle Arrow schema, value, null, and metadata equality assertions. Do not reduce tested source schemas or dtype coverage.</action>
  <acceptance_criteria>
    - Full Parquet schema compatibility tests pass with default `LMC2` emission.
    - Full Lance schema compatibility tests pass with default `LMC2` emission.
    - Full Vortex Arrow dtype semantic compatibility tests pass with default `LMC2` emission.
    - No test name or assertion is changed to imply broader DuckDB SQL or native execution support.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-parquet-ingress --test full_arrow_schema_compatibility</automated>
    <automated>cargo test -p loom-lance-ingress --test full_arrow_schema_compatibility</automated>
    <automated>cargo test -p loom-vortex-ingress --test full_arrow_dtype_semantic_compatibility</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source reader -> Loom artifact bytes | Source adapters convert trusted upstream Arrow materialization into untrusted-by-default Loom bytes. |
| source report -> artifact acceptance | Report summaries must not substitute for live `verify_artifact` acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Spoofing | source artifact reports | mitigate | Reports name accepted `LMC2` bytes only after `verify_artifact` accepts them. |
| T-33-03-02 | Tampering | source bytes | mitigate | Tests assert emitted magic and verifier status for every adapter. |
| T-33-03-03 | Repudiation | oracle equality evidence | mitigate | Source/oracle equality assertions remain separate from wrapper acceptance assertions. |
</threat_model>

<verification>
cargo test -p loom-parquet-ingress --test source_ingress_handoff
cargo test -p loom-lance-ingress --test source_ingress_handoff
cargo test -p loom-vortex-ingress --test source_ingress_handoff
cargo test -p loom-parquet-ingress --test full_arrow_schema_compatibility
cargo test -p loom-lance-ingress --test full_arrow_schema_compatibility
cargo test -p loom-vortex-ingress --test full_arrow_dtype_semantic_compatibility
git diff --check
</verification>

<success_criteria>
Plan 33-03 is complete when all three source adapters emit verifier-accepted
`LMC2(LMA1)` by default, direct `LMA1` compatibility remains available, and
full Arrow semantic equality coverage still passes.
</success_criteria>

<artifacts_this_phase_produces>
- Parquet default `LMC2(LMA1)` emission path
- Lance default `LMC2(LMA1)` emission path
- Vortex default `LMC2(LMA1)` emission path
- Source report summaries naming `LMC2`
- Compatibility helper or shim for direct `LMA1`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-03-SUMMARY.md` when done.
</output>

