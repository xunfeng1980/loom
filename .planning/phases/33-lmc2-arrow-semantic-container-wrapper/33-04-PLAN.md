---
phase: 33-lmc2-arrow-semantic-container-wrapper
plan: 04
type: execute
wave: 4
depends_on:
  - 33-03
files_modified:
  - crates/loom-cli/src/main.rs
  - crates/loom-parquet-ingress/src/bin/emit_duckdb_parquet_lma1_fixture.rs
  - crates/loom-lance-ingress/src/bin/emit_duckdb_lance_lma1_fixture.rs
  - crates/loom-vortex-ingress/src/bin/emit_duckdb_vortex_lma1_fixture.rs
  - scripts/lmc2-arrow-semantic-container-test.sh
  - scripts/full-arrow-semantic-compatibility-test.sh
  - scripts/duckdb-source-e2e-test.sh
autonomous: true
requirements:
  - PHASE-33
must_haves:
  truths:
    - "D-33-03: direct LMA1 compatibility remains explicit while LMC2 becomes the preferred distribution artifact."
    - "D-33-06: visible surfaces do not claim native readiness for LMC2/LMA1."
    - "D-33-08/D-33-10: gates and reports identify accepted source bytes as LMC2 wrapping Arrow semantic LMA1."
    - "D-33-11: visible gates add wrapper coverage without expanding DuckDB SQL shape support."
  artifacts:
    - path: scripts/lmc2-arrow-semantic-container-test.sh
      provides: "Focused Phase 33 LMC2 wrapper gate"
---

<objective>
Make `LMC2` visible through CLI/report output and focused verification gates without broadening DuckDB or native claims.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-CONTEXT.md
@.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-RESEARCH.md
@crates/loom-cli/src/main.rs
@scripts/full-arrow-semantic-compatibility-test.sh
@scripts/duckdb-source-e2e-test.sh
@scripts/container-negative-test.sh
@crates/loom-core/src/artifact_verifier.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI verification visibility</name>
  <files>crates/loom-cli/src/main.rs</files>
  <read_first>
    - crates/loom-cli/src/main.rs
    - crates/loom-core/src/artifact_verifier.rs
    - crates/loom-core/src/arrow_semantic_codec.rs
  </read_first>
  <action>Update `loom verify-artifact` output if needed so accepted `LMC2` artifacts print artifact kind `LMC2`, payload kind `Arrow semantic payload`, wrapper version, row-count bound, feature names when present, and the existing Arrow semantic lowering deferral when lowering readiness is requested. Do not add new public C ABI functions or new DuckDB SQL modes.</action>
  <acceptance_criteria>
    - `loom verify-artifact <lmc2-file>` output includes `artifact: LMC2` or an equivalent existing field showing `LMC2`.
    - `loom verify-artifact <lmc2-file>` output includes `payload: Arrow semantic payload` or equivalent existing payload field.
    - CLI output does not contain wording that says `native ready` for Arrow semantic artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-cli</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update source fixture generators and DuckDB bridge assertions</name>
  <files>crates/loom-parquet-ingress/src/bin/emit_duckdb_parquet_lma1_fixture.rs, crates/loom-lance-ingress/src/bin/emit_duckdb_lance_lma1_fixture.rs, crates/loom-vortex-ingress/src/bin/emit_duckdb_vortex_lma1_fixture.rs, scripts/duckdb-source-e2e-test.sh</files>
  <read_first>
    - crates/loom-parquet-ingress/src/bin/emit_duckdb_parquet_lma1_fixture.rs
    - crates/loom-lance-ingress/src/bin/emit_duckdb_lance_lma1_fixture.rs
    - crates/loom-vortex-ingress/src/bin/emit_duckdb_vortex_lma1_fixture.rs
    - scripts/duckdb-source-e2e-test.sh
    - duckdb-ext/loom_extension.cpp
    - crates/loom-ffi/src/ffi.rs
  </read_first>
  <action>Update fixture generator names/comments/output behavior and `scripts/duckdb-source-e2e-test.sh` so the bounded DuckDB source e2e path remains explicit. If DuckDB can unwrap `LMC2(LMA1)` through existing FFI after earlier plans, make the script assert `LMC2` magic and keep the same single-column SQL rows/aggregates. If DuckDB cannot support wrapped bytes in this phase without broadening SQL/FFI scope, keep a direct-`LMA1` compatibility fixture for DuckDB e2e and add a separate `LMC2` fixture assertion in the focused Phase 33 gate. In either case, script messages must distinguish `LMC2` wrapper proof from bounded DuckDB single-column SQL proof.</action>
  <acceptance_criteria>
    - `scripts/duckdb-source-e2e-test.sh` no longer silently assumes all source-backed distribution artifacts are direct `LMA1`.
    - DuckDB source e2e still checks rows `7`, `-1`, `42` and aggregate `3,48,-1,42`.
    - Any direct `LMA1` DuckDB compatibility path is labeled compatibility/bridge, not the new distribution default.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/duckdb-source-e2e-test.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add focused LMC2 wrapper gate</name>
  <files>scripts/lmc2-arrow-semantic-container-test.sh, scripts/full-arrow-semantic-compatibility-test.sh</files>
  <read_first>
    - scripts/full-arrow-semantic-compatibility-test.sh
    - scripts/container-negative-test.sh
    - scripts/duckdb-source-e2e-test.sh
    - crates/loom-core/tests/arrow_semantic.rs
    - crates/loom-core/tests/artifact_verifier.rs
  </read_first>
  <action>Create `scripts/lmc2-arrow-semantic-container-test.sh` that checks marker strings for `LMC2` wrapper codec and verifier routing, runs `cargo test -p loom-core --test arrow_semantic`, runs `cargo test -p loom-core --test artifact_verifier`, runs Parquet/Lance/Vortex source handoff tests, and checks that source adapters contain `LMC2` report wording. Update `scripts/full-arrow-semantic-compatibility-test.sh` only where needed to account for wrapped default emission while preserving its Phase 31 semantic equality purpose.</action>
  <acceptance_criteria>
    - `scripts/lmc2-arrow-semantic-container-test.sh` exists and is executable.
    - The focused gate contains fixed-string checks for `LMC2`, `is_arrow_semantic_container`, and `verify_arrow_semantic_container_artifact`.
    - The focused gate runs core wrapper tests and source handoff tests.
    - `scripts/full-arrow-semantic-compatibility-test.sh` still runs full Parquet, Lance, and Vortex semantic compatibility tests.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lmc2-arrow-semantic-container-test.sh</automated>
    <automated>bash scripts/full-arrow-semantic-compatibility-test.sh</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/report output -> reviewer trust | Reviewer-facing output must not overstate query/native capabilities. |
| focused gate -> broad release gate | The focused gate must prove wrapper behavior before broad wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Repudiation | CLI/report wording | mitigate | CLI and script output state exact artifact kind and deferrals. |
| T-33-04-02 | Spoofing | DuckDB e2e fixtures | mitigate | Script asserts expected magic and labels direct LMA1 compatibility explicitly. |
| T-33-04-03 | Tampering | gate coverage | mitigate | Focused gate checks marker strings and runs exact core/source tests. |
</threat_model>

<verification>
cargo test -p loom-cli
bash scripts/lmc2-arrow-semantic-container-test.sh
bash scripts/full-arrow-semantic-compatibility-test.sh
bash scripts/duckdb-source-e2e-test.sh
git diff --check
</verification>

<success_criteria>
Plan 33-04 is complete when reviewers can see `LMC2` in CLI/report/gate output,
the focused wrapper gate passes, and DuckDB source e2e remains bounded and
truthful.
</success_criteria>

<artifacts_this_phase_produces>
- `scripts/lmc2-arrow-semantic-container-test.sh`
- CLI artifact output for `artifact_kind = LMC2`
- Updated DuckDB source e2e wrapper/compatibility assertions
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/33-lmc2-arrow-semantic-container-wrapper/33-04-SUMMARY.md` when done.
</output>

