---
phase: 51-sidecar-duckdb-decoupling-and-loom-self-ingress
verified: 2026-06-11T19:00:00Z
status: gaps_found
score: 4/5 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Full workspace builds + tests pass"
    status: partial
    reason: "loom-lance-ingress dependency boundary test (lance_dependency_is_direct_only_in_lance_adapter_manifest) unchanged for new loom-sidecar-ffi crate — same pattern already fixed in loom-parquet-ingress test (commit 89b44af)"
    artifacts:
      - path: "ingress/loom-lance-ingress/tests/dependency_boundary.rs"
        issue: "Line 100-103: direct_parquet_manifests assertion expects only [loom-parquet-ingress] but loom-sidecar-ffi also has direct parquet dep"
    missing:
      - "Add crates/loom-sidecar-ffi/Cargo.toml to the direct_parquet_manifests expected list at line 100-103"
---

# Phase 51: Sidecar-DuckDB Decoupling and Loom Self-Ingress — Verification Report

**Phase Goal:** Decouple the DuckDB sidecar path from `loom-container` so that DuckDB can read Parquet files with embedded Loom IR sidecars using only `loom-ir-core`. Reposition `loom-container` as the exclusive handler of the Loom native `.loom` format, and introduce `loom-self-ingress` as its IO boundary.

**Verified:** 2026-06-11T19:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | `loom-sidecar-ffi` has zero dependency on `loom-container` | ✓ VERIFIED | `cargo tree -p loom-sidecar-ffi \| grep loom-container` → 0 lines; Cargo.toml deps: only `loom-ir-core`, `loom-parquet-ingress`, `parquet` |
| 2   | DuckDB extension has `LOOM_SIDECAR_ONLY` CMake option | ✓ VERIFIED | `option(LOOM_SIDECAR_ONLY ...)` at line 15; conditional cargo build, LLVM guard, sidecar include path, compile definitions all in CMakeLists.txt |
| 3   | `loom-self-ingress` crate exists and wraps `loom-container` | ✓ VERIFIED | `crates/loom-self-ingress/` exists; `cargo tree -p loom-self-ingress \| grep loom-container` → 1 match; 3 public functions (`read_loom_file`, `write_loom_file`, `verify_loom_file`) |
| 4   | CLI has lean feature (no container) and full feature | ✓ VERIFIED | `cargo tree -p loom-cli --no-default-features \| grep loom-container` → 0 lines; `sidecar embed --help` works; `inspect` returns "requires full feature"; full build includes container |
| 5   | Full workspace builds + tests pass | ⚠️ PARTIAL | Workspace build passes cleanly; 1 test failure: `loom-lance-ingress` dependency boundary test not updated for `loom-sidecar-ffi` `parquet` dep |

**Score:** 4/5 truths verified (1 partial)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/loom-sidecar-ffi/Cargo.toml` | staticlib crate manifest with deps on loom-ir-core + loom-parquet-ingress, zero container | ✓ VERIFIED | No `loom-container` or `loom-core`; has `loom-ir-core`, `loom-parquet-ingress`, `parquet` |
| `crates/loom-sidecar-ffi/src/ffi.rs` | 4 `extern "C"` entry points: extract, verify, route, free_bytes | ✓ VERIFIED | 380 lines; all 4 functions substantive with null-checking, catch_unwind, error codes, JSON routing output |
| `crates/loom-sidecar-ffi/include/loom_sidecar.h` | C header for sidecar FFI symbols | ✓ VERIFIED | 86 lines; 4 function declarations; generated by cbindgen |
| `ingress/loom-parquet-ingress/Cargo.toml` | loom-core moved to dev-dependencies only | ✓ VERIFIED | `loom-core` in `[dev-dependencies]` only; production tree has 0 loom-core |
| `crates/loom-self-ingress/Cargo.toml` | Crate manifest with sole dep on loom-container | ✓ VERIFIED | Single dep `loom-container` + workspace `arrow` |
| `crates/loom-self-ingress/src/lib.rs` | `read_loom_file`, `write_loom_file`, `verify_loom_file` public API | ✓ VERIFIED | 130 lines; all 3 functions public; `SelfIngressError` implements Display + Error |
| `crates/loom-cli/Cargo.toml` | Feature-gated dependencies with `[features]` section | ✓ VERIFIED | `default = ["full"]`; `full` enables loom-core + arrow + loom-vortex-ingress; lean uses only loom-ir-core |
| `crates/loom-cli/src/main.rs` | Feature-gated command dispatch | ✓ VERIFIED | 1047 lines; `sidecar`/`verify-l2core` always available; `inspect`/`decode`/`verify-artifact`/`ingest-vortex` gated behind `#[cfg(feature = "full")]` |
| `contrib/duckdb-ext/CMakeLists.txt` | `LOOM_SIDECAR_ONLY` CMake option | ✓ VERIFIED | `option(LOOM_SIDECAR_ONLY ... OFF)`; conditional cargo build + LLVM guard + sidecar include + compile definitions |
| `contrib/duckdb-ext/loom_extension.cpp` | Sidecar-aware scan path | ✓ VERIFIED | `#ifdef LOOM_SIDECAR_ONLY` block with `SidecarBind`/`SidecarInit`/`SidecarScan` using `loom_sidecar_extract`/`loom_sidecar_route`/`loom_sidecar_free_bytes` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `loom-sidecar-ffi/src/ffi.rs` | `loom-ir-core/src/sidecar.rs` | `use loom_ir_core::sidecar::SidecarOverlay` | ✓ WIRED | Line 27: import + used in decode/verify/route |
| `loom-sidecar-ffi/src/ffi.rs` | `ingress/loom-parquet-ingress/src/sidecar_parquet.rs` | `use loom_parquet_ingress::sidecar_parquet::extract_sidecar_from_parquet_metadata` | ✓ WIRED | Line 31: import + used in extract function |
| `loom-self-ingress/src/lib.rs` | `loom-container/src/container_codec.rs` | `use loom_container::container_codec` | ✓ WIRED | Line 23: import + used in read/write/verify |
| `loom-cli/src/main.rs` | `loom-ir-core/src/sidecar.rs` | `use loom_ir_core::sidecar::SidecarOverlay` | ✓ WIRED | Line 12: always-available import; used in sidecar embed |
| `loom_extension.cpp` (sidecar mode) | `include/loom_sidecar.h` | `#include "loom_sidecar.h"` | ✓ WIRED | Line 23: header included under `#ifdef LOOM_SIDECAR_ONLY` |
| `CMakeLists.txt` (sidecar mode) | `crates/loom-sidecar-ffi/` | `cargo build -p loom-sidecar-ffi --release` | ✓ WIRED | Lines 97-108: custom command + target for sidecar staticlib |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `ffi.rs::loom_sidecar_extract` | `overlay` | `extract_sidecar_from_parquet_metadata(metadata)` → `SidecarOverlay::decode()` | Yes — parquet file metadata | ✓ FLOWING |
| `ffi.rs::loom_sidecar_route` | `decision` | `decide_sidecar_routing(input)` → `SidecarRoutingDecision` | Yes — real routing logic | ✓ FLOWING |
| `ffi.rs::loom_sidecar_verify` | `hash` | `l2core_codec::l2core_program_hash(&program)` | Yes — FNV-1a hash of decoded IR | ✓ FLOWING |
| `lib.rs::read_loom_file` | `bytes` | `fs::read(path)` + `is_container_payload` check | Yes — file system | ✓ FLOWING |
| `lib.rs::write_loom_file` | `bytes` | `fs::write` + atomic rename after validation | Yes — file system | ✓ FLOWING |
| `main.rs::sidecar_embed_parquet` | `overlay` | `SidecarOverlay { ir_bytes, bindings }` → `embed_sidecar_into_parquet_file` | Yes — real embedding via ir-core + parquet-ingress | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Sidecar-ffi zero container deps | `cargo tree -p loom-sidecar-ffi \| grep -c loom-container` | 0 | ✓ PASS |
| Lean CLI zero container deps | `cargo tree -p loom-cli --no-default-features \| grep -c loom-container` | 0 | ✓ PASS |
| Lean CLI sidecar embed works | `cargo build -p loom-cli --no-default-features && ./target/debug/loom sidecar embed --help` | Shows usage | ✓ PASS |
| Lean CLI inspect fails gracefully | `cargo build -p loom-cli --no-default-features && ./target/debug/loom inspect /dev/null` | "requires full feature" | ✓ PASS |
| Full CLI builds with container | `cargo build -p loom-cli && cargo tree -p loom-cli \| grep -c loom-container` | 1 | ✓ PASS |
| Sidecar symbols in archive | `nm target/release/libloom_sidecar_ffi.a \| grep "T _loom_sidecar"` | 4 symbols (extract, verify, route, free_bytes) | ✓ PASS |
| Full workspace release build | `cargo build --workspace --release` | Passes (pre-existing warnings only) | ✓ PASS |
| Parquet-ingress production no loom-core | `cargo tree -p loom-parquet-ingress -e no-dev \| grep -c loom-core` | 0 | ✓ PASS |
| Existing loom-ffi path intact | `cargo tree -p loom-ffi \| grep -c loom-container` | 1 | ✓ PASS |
| CMake LOOM_SIDECAR_ONLY option | `grep "option(LOOM_SIDECAR_ONLY" CMakeLists.txt` | Found at line 15 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| SC-1 | 51-01 | `loom-sidecar-ffi` zero dependency on `loom-container` | ✓ SATISFIED | `cargo tree -p loom-sidecar-ffi \| grep loom-container` returns 0 |
| SC-2 | 51-03 | DuckDB extension has `LOOM_SIDECAR_ONLY` CMake option | ✓ SATISFIED | `option(LOOM_SIDECAR_ONLY)` in CMakeLists.txt line 15; conditional build path, LLVM guard, sidecar include, compile definitions |
| SC-3 | 51-02 | `loom-self-ingress` crate exists and wraps `loom-container` | ✓ SATISFIED | Crate at `crates/loom-self-ingress/` with `loom-container` dep; 3 public functions |
| SC-4 | 51-02 | CLI has lean feature and full feature | ✓ SATISFIED | `[features]` in Cargo.toml; lean → 0 container; full → 1 container; sidecar embed works in both modes |
| SC-5 | 51-01, 51-03 | Full workspace builds + tests pass | ⚠️ PARTIAL | Build passes; 1 test assertion stale in `loom-lance-ingress` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `ingress/loom-lance-ingress/tests/dependency_boundary.rs` | 100-103 | Stale assertion: `direct_parquet_manifests` expected list missing `loom-sidecar-ffi` | ⚠️ WARNING | Test assertion not updated for new crate; same pattern already fixed in parquet-ingress equivalent test (commit 89b44af) |

No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers found in any Phase 51-modified source files.

### Human Verification Required

None — all verification is fully automatable. No visual, real-time, or external service dependencies.

### Gaps Summary

The phase goal is substantially achieved — the DuckDB sidecar path is successfully decoupled from `loom-container`, `loom-sidecar-ffi` exports a lean C ABI surface, `loom-self-ingress` centralizes `.loom` file IO, and the CLI supports lean compilation without container dependencies.

**One gap identified:**

1. **Stale dependency boundary test in `loom-lance-ingress`** — The test `lance_dependency_is_direct_only_in_lance_adapter_manifest` at line 100-103 of `ingress/loom-lance-ingress/tests/dependency_boundary.rs` asserts only `loom-parquet-ingress` has a direct `parquet` dependency, but `loom-sidecar-ffi` (with its legitimate `parquet` dep for `ParquetRecordBatchReaderBuilder`) causes the assertion to fail. This is the same pattern that was already fixed in the parquet-ingress equivalent test in Plan 51-01 (commit `89b44af`). 

   **Fix:** Add `root.join("crates/loom-sidecar-ffi/Cargo.toml")` to the `direct_parquet_manifests` expected `vec!` at line 100-103.

---

_Verified: 2026-06-11T19:00:00Z_
_Verifier: the agent (gsd-verifier)_
