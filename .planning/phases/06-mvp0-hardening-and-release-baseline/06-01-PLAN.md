---
phase: 06-mvp0-hardening-and-release-baseline
plan: "01"
subsystem: docs
tags: [docs, planning, baseline]
requires:
  - phase: 05-fsst-l2-kernel-and-full-verification
    plan: "04"
    provides: MVP0 acceptance summary
provides:
  - consistent MVP0 complete state
  - README implementation status and run commands
  - resolved stale planning blockers
requirements: [BASE-01, DOC-01, DOC-02]
wave: 1
---

# Phase 06-01 Plan: Planning-State and README Consistency

## Purpose

Make the documentation agree with reality: MVP0 is complete, Phase 6 is active, and the current repo is an interpreter-based DuckDB demo rather than the full Loom distribution IR.

## Tasks

1. Update planning state documents:
   - `.planning/STATE.md`: remove or mark resolved stale Phase 4/5 concerns, keep the Phase 8 ArrowArrayStream note as future work, and ensure Phase 6 is current.
   - `.planning/PROJECT.md`: keep completed Phase 4/5 capabilities in Validated, keep only Phase 6 hardening items in Active, and mark key decisions with completed outcomes.
   - `.planning/REQUIREMENTS.md`: ensure BASE/DOC/VERIFY/BUILD requirements map to Phase 6.
   - `.planning/ROADMAP.md`: ensure Phase 6 appears in phase list, details, and progress.

2. Fix planning-doc text hygiene:
   - Repair mojibake sequences in `.planning/ROADMAP.md` such as `Ã¢ÂÂ` and `Ã¢ÂÂ`.
   - Do not rewrite historical plan bodies unless the corruption affects current roadmap readability.

3. Update public README docs:
   - Add a concise "Current MVP0 implementation" section or equivalent note.
   - Add exact verification commands:
     - `cargo test --workspace`
     - `bash scripts/duckdb-smoke-test.sh`
     - `cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}'`
     - fixture hygiene grep
   - Link `.planning/research/POSITIONING.md` from the positioning section.
   - Mirror the essential note in `README-zh.md`.

## Acceptance Criteria

- `rg -n 'Phase 05.*active|Phase 3.*Last updated|Pending \\|' .planning/PROJECT.md .planning/STATE.md` returns no stale active-state matches.
- `rg -n 'Ã|Â' .planning/ROADMAP.md` returns no matches.
- README and README-zh both link `.planning/research/POSITIONING.md`.
- The docs clearly state that descriptor/CLI, multi-column, verifier, MLIR, and full `.vortex` file work are not Phase 6.

## Verification

- `rg -n 'POSITIONING|Current MVP0|mvp0-verify|duckdb-smoke-test' README.md README-zh.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/STATE.md`
- `rg -n 'Ã|Â' .planning/ROADMAP.md`
- `git diff --check`
