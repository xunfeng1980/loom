---
phase: 24-duckdb-native-execution-integration-mvp
plan: 05
type: execute
wave: 5
depends_on:
  - 24-04
files_modified:
  - crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
  - scripts/duckdb-native-integration-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md
autonomous: true
requirements:
  - PHASE-24
must_haves:
  truths:
    - "D-01 through D-14 are covered by route-aware tests and final report evidence."
    - "Public SQL remains `loom_scan(path)`; test-only controls do not become documented public API."
    - "Release gate includes runtime ABI, production backend, DuckDB native integration, and existing DuckDB smoke evidence."
    - "Projection, fallback, strict fail-closed, mismatch, cancellation, Arrow release/error paths, single-worker, and single-batch behavior are test-visible."
  artifacts:
    - path: scripts/duckdb-native-integration-test.sh
      provides: Phase 24 route-aware DuckDB integration release gate
    - path: crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
      provides: Native-eligible non-null primitive DuckDB fixture
      contains: native-primitives-table
    - path: .planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md
      provides: Final Phase 24 route, policy, diagnostics, and non-goal report
  key_links:
    - from: scripts/mvp0-verify.sh
      to: scripts/duckdb-native-integration-test.sh
      via: phase gate invocation
      pattern: "duckdb-native-integration-test.sh"
    - from: scripts/duckdb-native-integration-test.sh
      to: duckdb-ext/loom_extension.cpp
      via: route-aware SQL over `loom_scan(path)`
      pattern: "loom_scan\\("
---

<objective>
Add route-aware DuckDB native integration evidence, wire it into the release gate, and close Phase 24 with a report that documents supported behavior and non-goals.

Purpose: SQL row success alone can hide fallback or skipped native routes; Phase 24 must make route, diagnostics, projection, error, cancel, and release behavior visible without changing public SQL.
Output: Native primitive fixture, `scripts/duckdb-native-integration-test.sh`, release gate wiring, and final Phase 24 report.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-CONTEXT.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-RESEARCH.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-PATTERNS.md
@duckdb-ext/loom_extension.cpp
@scripts/duckdb-smoke-test.sh
@scripts/production-backend-test.sh
@scripts/runtime-abi-test.sh
@scripts/mvp0-verify.sh
@crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
@crates/loom-ffi/tests/duckdb_runtime.rs
@crates/loom-ffi/tests/duckdb_runtime_ffi.rs
</context>

<artifacts_this_phase_produces>
- New generated fixture from `emit_duckdb_payloads`: `target/loom-duckdb-fixtures/native-primitives-table.loom`.
- New script: `scripts/duckdb-native-integration-test.sh`.
- Updated release gate: `scripts/mvp0-verify.sh` runs the Phase 24 DuckDB native integration gate after the Phase 23 backend gate and before or alongside DuckDB SQL smoke.
- New report: `.planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md`.
</artifacts_this_phase_produces>

<must_haves>
- Route-aware tests cover D-01 through D-14 and make native/fallback/fail-closed behavior visible.
- Public SQL checks use only `loom_scan(path)` for D-13.
- Test controls are prefixed `LOOM_DUCKDB_TEST_` and stay non-public for D-14.
- Release gate wiring proves runtime, backend, DuckDB native integration, and existing SQL smoke together.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add a narrow non-null primitive route fixture</name>
  <files>crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs</files>
  <read_first>
    - `.planning/phases/24-duckdb-native-execution-integration-mvp/24-CONTEXT.md` decisions D-06, D-10, D-12, D-14.
    - `crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs` existing `mixed-table.loom`, Int32, Int64, Float32, and Float64 fixture patterns.
    - `scripts/duckdb-smoke-test.sh` fixture generation and manifest assumptions.
    - `crates/loom-core/src/production_native_lowering.rs` supported non-null primitive matrix.
  </read_first>
  <action>Extend `emit_duckdb_payloads` with a generated `native-primitives-table.loom` fixture containing only non-null primitive columns supported by Phase 23 evidence: Int32, Int64, Float32, and Float64. Use deterministic all-zero or otherwise backend-comparable rows so current Phase 23 JIT seed can be route-tested without claiming broad native semantics. Add a manifest row with exact expected row count, aggregate values, min, and max. Do not add nullable native fixtures, string native fixtures, bitpack/FOR native expansion, arbitrary Vortex support, or public API controls. Keep the existing smoke fixtures unchanged.</action>
  <verify>
    <automated>cargo run -p loom-fixtures --bin emit_duckdb_payloads >/dev/null && test -f target/loom-duckdb-fixtures/native-primitives-table.loom</automated>
  </verify>
  <acceptance_criteria>
    - `target/loom-duckdb-fixtures/native-primitives-table.loom` is generated and begins with `LMC1`.
    - Manifest output contains `native-primitives-table`.
    - Fixture uses only non-null Int32, Int64, Float32, and Float64 columns.
    - Existing fixture names and expected smoke outputs remain unchanged.
  </acceptance_criteria>
  <done>A narrow primitive table fixture exists for Phase 24 native route and projection evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Create route-aware DuckDB native integration gate</name>
  <files>scripts/duckdb-native-integration-test.sh</files>
  <read_first>
    - `.planning/phases/24-duckdb-native-execution-integration-mvp/24-CONTEXT.md` decisions D-01 through D-14.
    - `.planning/phases/24-duckdb-native-execution-integration-mvp/24-RESEARCH.md` recommended release gate shape and pitfalls.
    - `scripts/duckdb-smoke-test.sh` build, CLI, SQL assertion, and temp-dir patterns.
    - `scripts/production-backend-test.sh` skip-aware native toolchain pattern.
    - `crates/loom-ffi/tests/duckdb_runtime.rs` and `crates/loom-ffi/tests/duckdb_runtime_ffi.rs` for helper-level mismatch/cancellation coverage.
  </read_first>
  <action>Create `scripts/duckdb-native-integration-test.sh` using the existing shell gate style: `set -euo pipefail`, repo-root discovery, temp-dir cleanup, DuckDB CLI discovery, cargo fixture generation, `cargo build -p loom-ffi --release`, CMake extension build, SQL `COPY` assertions, and colored pass/fail helpers. The script must use only public SQL `loom_scan(path)` for SQL checks. Add test-only environment controls only with names prefixed `LOOM_DUCKDB_TEST_`; require route report output through a temp file such as `LOOM_DUCKDB_TEST_ROUTE_REPORT`. Assert: native-eligible primitive table route is `native` when compatible tooling is available or explicitly `toolchain-skipped`/fallback when `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`; unsupported string/table routes use policy-controlled interpreter fallback; strict fail-closed route returns a DuckDB error containing stable diagnostic code/path; projection query preserves requested column order; repeated scan behavior remains single-batch; Rust helper tests cover `native-output-mismatch` and cancellation when host cancellation is not observable through DuckDB; malformed artifact/error path does not crash and preserves Arrow release ownership by completing the process successfully after the error test. Do not add public SQL mode parameters, predicate pushdown tests, parallel split tests, persistent cache tests, or ArrowArrayStream tests.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/duckdb-native-integration-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script output contains `Phase 24 DuckDB native integration gate`.
    - Script asserts route strings or diagnostics for `native`, `interpreter-fallback`, `fail-closed`, `toolchain-skipped` or `toolchain-failed`, `native-output-mismatch`, and `cancelled`.
    - `rg -n "loom_scan_native|loom_scan_interpreter|mode :=|ArrowArrayStream|predicate pushdown|parallel split" scripts/duckdb-native-integration-test.sh` returns no matches.
    - Projection SQL uses `loom_scan('${payload}')` and verifies output column order.
  </acceptance_criteria>
  <done>The route-aware gate proves DuckDB adapter behavior instead of only checking final SQL rows.</done>
</task>

<task type="auto">
  <name>Task 3: Wire release gate and write final Phase 24 report</name>
  <files>scripts/mvp0-verify.sh, .planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md</files>
  <read_first>
    - `.planning/phases/24-duckdb-native-execution-integration-mvp/24-CONTEXT.md` decisions D-01 through D-14 and deferred ideas.
    - `.planning/phases/22-host-native-runtime-abi-and-execution-policy/22-RUNTIME-ABI-REPORT.md`.
    - `.planning/phases/23-production-native-backend-implementation/23-BACKEND-REPORT.md`.
    - `scripts/mvp0-verify.sh` Phase 22, Phase 23, and DuckDB smoke gate placement.
    - `scripts/duckdb-native-integration-test.sh`.
  </read_first>
  <action>Add the Phase 24 gate to `scripts/mvp0-verify.sh` after `scripts/production-backend-test.sh` and before or adjacent to `scripts/duckdb-smoke-test.sh`, using the same `info`, `bash scripts/...`, and `ok` pattern. Create `24-DUCKDB-NATIVE-REPORT.md` documenting: bind planning, global init prepare, single-worker/single-batch semantics, direct DataChunk output, route diagnostics, projection evidence, fallback policy, mismatch fail-closed behavior, cancellation/error path coverage, Arrow release ownership, public SQL API unchanged as `loom_scan(path)`, internal test controls, and non-goals. The report must explicitly state that Phase 24 does not freeze `loom_runtime.h`, does not add ArrowArrayStream, does not implement predicate pushdown, does not implement parallel split execution, does not harden persistent native cache, and does not expand arbitrary Vortex/native semantics.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mvp0-verify.sh` invokes `bash scripts/duckdb-native-integration-test.sh`.
    - `24-DUCKDB-NATIVE-REPORT.md` names all decisions D-01 through D-14 and states how each was satisfied.
    - `24-DUCKDB-NATIVE-REPORT.md` contains the exact non-goal strings `loom_runtime.h remains unfrozen`, `no ArrowArrayStream`, `no predicate pushdown`, `no parallel split execution`, `no persistent native cache hardening`, and `no arbitrary Vortex/native expansion`.
    - Full release gate passes with `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`.
  </acceptance_criteria>
  <done>The Phase 24 gate is part of the main release verification path and the final report closes every locked decision without scope creep.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 24 | DuckDB `loom_scan(path)` wires Phase 22 runtime and Phase 23 backend over complete-reader artifacts | 01-05 | COVERED | Rust bridge, C ABI, C++ adapter, direct output, and release gate.
REQ | none | Phase requirement IDs are null/TBD | 01-05 | COVERED | `requirements: PHASE-24` used because ROADMAP has no mapped IDs.
RESEARCH | R-01 | Thin DuckDB adapter over runtime/backend contract | 01-05 | COVERED | C++ does not duplicate policy.
RESEARCH | R-02 | Projection pushdown via DuckDB init column ids | 03,05 | COVERED | `projection_pushdown` and projected SQL test.
RESEARCH | R-03 | Internal non-public bridge, no frozen `loom_runtime.h` | 02,05 | COVERED | Internal header plus public header grep gate.
RESEARCH | R-04 | Route diagnostics visible in tests | 01,02,03,05 | COVERED | Rust/C++/script diagnostics.
RESEARCH | R-05 | Release gate includes route-aware SQL test | 05 | COVERED | New script wired into main gate.
CONTEXT | D-01 | Runtime planning happens in DuckDB Bind | 01,03,05 | COVERED | Bind creates all-column plan and init finalizes DuckDB-exposed projection.
CONTEXT | D-02 | Backend prepare/JIT seed happens in global init | 01,03,05 | COVERED | Prepare helper called from `LoomInit`.
CONTEXT | D-03 | Single-worker/serialized scan | 03,05 | COVERED | `MaxThreads() == 1`; no local state.
CONTEXT | D-04 | Single-batch scan semantics | 04,05 | COVERED | `batch_emitted` route guard.
CONTEXT | D-05 | Direct DataChunk population | 04,05 | COVERED | No stream/batch ABI.
CONTEXT | D-06 | Native output adapts to same fill path | 01,04,05 | COVERED | Fixed-width native buffer fill.
CONTEXT | D-07 | Runtime policy controls fallback | 01,03,05 | COVERED | Fallback tested through route diagnostics.
CONTEXT | D-08 | Native output mismatch fails closed | 01,03,04,05 | COVERED | Mismatch diagnostic blocks fallback/output.
CONTEXT | D-09 | Host cancellation maps to backend cancellation/error | 01,03,05 | COVERED | Helper-level and host-level path where observable.
CONTEXT | D-10 | Prove projection, no predicate pushdown | 01,03,05 | COVERED | Projection tests, PredicateEnvelope::None.
CONTEXT | D-11 | Full scan/single row-range modeling only | 01,03 | COVERED | FullScan single worker; no parallel execution.
CONTEXT | D-12 | Default policy avoids accidental native execution | 01,03,05 | COVERED | Native only for runtime `NativeCandidate`.
CONTEXT | D-13 | Public SQL remains `loom_scan(path)` | 02,03,05 | COVERED | No public modes or alternate functions.
CONTEXT | D-14 | Test-only controls acceptable if not public API | 01,02,05 | COVERED | `LOOM_DUCKDB_TEST_` controls and no public docs.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated fixture to route gate | Test fixture shapes must not imply broader native support. |
| Test controls to public behavior | Route forcing/reporting must remain internal to tests. |
| Release gate to user claims | Passing SQL rows must be backed by route diagnostics and non-goal documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Spoofing | route-aware script | mitigate | Assert stable route diagnostics instead of trusting SQL rows alone. |
| T-24-05-02 | Tampering | native fixture | mitigate | Fixture limited to non-null primitive table shape supported by Phase 23; report states no arbitrary expansion. |
| T-24-05-03 | Repudiation | fallback/skipped native | mitigate | Script checks `interpreter-fallback` and toolchain diagnostics explicitly. |
| T-24-05-04 | Denial of Service | error/cancel/release paths | mitigate | Script runs malformed/error path plus Rust cancellation/mismatch tests without process crash. |
| T-24-05-05 | Elevation of Privilege | public API creep | mitigate | Grep gates block `loom_scan_native`, public mode parameters, predicate pushdown, parallel split, and ArrowArrayStream. |
| T-24-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Overall checks for this plan:
- `cargo run -p loom-fixtures --bin emit_duckdb_payloads`
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/duckdb-native-integration-test.sh`
- `bash scripts/duckdb-smoke-test.sh`
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Route-aware gate proves native candidate, fallback, strict fail-closed, mismatch, cancellation, projection, single-worker, single-batch, and release/error behavior.
- Main release gate includes Phase 24.
- Public SQL API remains `loom_scan(path)`.
- Final report covers D-01 through D-14 and explicitly excludes Phase 24 non-goals.
</success_criteria>

<output>
Create `.planning/phases/24-duckdb-native-execution-integration-mvp/24-05-SUMMARY.md` when done.
</output>
