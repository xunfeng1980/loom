---
phase: 25-native-equivalence-cache-and-fallback-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 25-03
files_modified:
  - scripts/native-hardening-test.sh
  - duckdb-ext/loom_extension.cpp
autonomous: true
requirements:
  - PHASE-25
must_haves:
  truths:
    - "Public SQL remains loom_scan(path) while native/cache/fallback evidence stays internal."
    - "Repeated identical SQL scans produce matching rows and observable cache-hit or prepare-reuse smoke evidence."
    - "Projection changes, unsupported payloads, strict fallback, malformed artifacts, cancellation, and mismatch routes emit deterministic diagnostics."
  artifacts:
    - path: scripts/native-hardening-test.sh
      provides: "Phase 25 SQL/cache/fallback release gate"
    - path: duckdb-ext/loom_extension.cpp
      provides: "Internal route-report propagation for Rust cache diagnostics if existing report output needs a small extension"
  key_links:
    - from: scripts/native-hardening-test.sh
      to: duckdb-ext/loom_extension.cpp
      via: "LOOM_DUCKDB_TEST_ROUTE_REPORT"
      pattern: "LOOM_DUCKDB_TEST_ROUTE_REPORT"
    - from: scripts/native-hardening-test.sh
      to: crates/loom-ffi/src/duckdb_runtime.rs
      via: "cache-hit/cache-miss diagnostics surfaced through internal route report"
      pattern: "cache-hit|cache-miss|cache-inserted|cache-non-cacheable"
---

<objective>
Create the Phase 25 DuckDB SQL hardening gate.

Purpose: prove cache/equivalence/fallback behavior through public loom_scan(path) where possible, while keeping cache controls internal and treating performance evidence as a smoke signal rather than a benchmark.
Output: scripts/native-hardening-test.sh and, only if needed, a tiny C++ route-report extension that forwards Rust diagnostics without duplicating policy.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-CONTEXT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-RESEARCH.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-PATTERNS.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-VERIFICATION.md
@scripts/duckdb-native-integration-test.sh
@scripts/mvp0-verify.sh
@duckdb-ext/loom_extension.cpp
@crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add native-hardening SQL and cache smoke gate</name>
  <files>scripts/native-hardening-test.sh</files>
  <action>Create scripts/native-hardening-test.sh following the style of scripts/duckdb-native-integration-test.sh. The script must generate deterministic DuckDB payloads, build loom-ffi and the DuckDB extension, set LOOM_DUCKDB_TEST_ROUTE_REPORT to a temp file, and use only public SELECT/COPY over loom_scan(path). Positive cases must include native-primitives-table all-column aggregate equality, reordered projection equality, repeated identical scan equality, and a smoke assertion that the second identical scan reports cache-hit or an equivalent prepare-reuse counter while the first reports cache-miss/cache-inserted. Projection changes must report a miss rather than reusing the previous key. Do not add benchmark timing, public SQL cache flags, or new packages.</action>
  <verify>
    <automated>bash -n scripts/native-hardening-test.sh && LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/native-hardening-test.sh</automated>
  </verify>
  <done>The Phase 25 gate passes locally and proves SQL row equality plus cache reuse/invalidation smoke evidence through internal diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Gate fallback, strict, and unsupported routes through SQL</name>
  <files>scripts/native-hardening-test.sh</files>
  <action>Extend the script with negative SQL cases for fsst-utf8 interpreter fallback, strict fail-closed over unsupported string/native lowering, nullable/compressed fixtures such as bitpack-nullable-i32 and bitpack-i32 as fallback/fail-closed evidence, malformed artifact failure followed by a successful scan, and cancellation through LOOM_DUCKDB_TEST_CANCEL_PREPARE. Append helper-level cargo test checks for native-output-mismatch and cache non-cacheable routes when SQL cannot naturally trigger the condition. Require stable diagnostic code/path text in stderr or route report. This records interpreter oracle vs broad Vortex claims: existing Vortex-backed fixtures are used as accepted fixture evidence only, not as arbitrary Vortex semantic coverage.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/native-hardening-test.sh</automated>
  </verify>
  <done>Unsupported and error routes are release-gated as deterministic fallback/fail-closed behavior with no partial row output.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve C++ as a report consumer</name>
  <files>duckdb-ext/loom_extension.cpp, scripts/native-hardening-test.sh</files>
  <action>If the existing route report already includes Rust cache diagnostics through CollectPreparedDiagnostics, leave C++ behavior unchanged and make the script assert those diagnostics. If a small C++ edit is required, limit it to forwarding existing Rust cache diagnostics or cache input into AppendTestRouteReport; do not compare cache keys, choose fallback, decide native eligibility, store cache entries, or add public SQL controls in C++. Add script grep gates for forbidden public markers: loom_scan_native, loom_scan_interpreter, cache mode flags, ArrowArrayStream, predicate pushdown, and parallel split public controls.</action>
  <verify>
    <automated>bash -n scripts/native-hardening-test.sh && rg -n "loom_scan_native|loom_scan_interpreter|ArrowArrayStream" scripts/native-hardening-test.sh duckdb-ext/loom_extension.cpp crates/loom-ffi/include/loom.h; test $? -eq 1</automated>
  </verify>
  <done>C++ remains a Rust-report consumer, public SQL remains loom_scan(path), and the script blocks API creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public SQL -> internal route report | Users query loom_scan(path); tests observe route/cache diagnostics only through internal env hooks. |
| SQL error text -> release gate | Diagnostics in stderr/route report must be stable enough for gate assertions. |
| C++ adapter -> Rust runtime policy | C++ consumes route/cache diagnostics but must not own policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Information Disclosure | public SQL/API surface | mitigate | Script greps block public cache/native/fallback controls and stream markers. |
| T-25-04-02 | Tampering | C++ route handling | mitigate | C++ may only forward Rust diagnostics; no cache/fallback decisions in C++. |
| T-25-04-03 | Repudiation | route diagnostics | mitigate | Gate requires code/path diagnostics for strict, cancellation, mismatch, and cache events. |
| T-25-SC | Tampering | package installs | mitigate | No package installs are planned; if package additions appear, stop and run package legitimacy review. |
</threat_model>

<verification>
Run:
- bash -n scripts/native-hardening-test.sh
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/native-hardening-test.sh
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/duckdb-native-integration-test.sh
</verification>

<success_criteria>
The Phase 25 gate proves SQL-visible row equality, repeated-scan cache smoke evidence, fallback/strict diagnostics, and no public SQL/API creep.
</success_criteria>

<output>
Create `.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-04-SUMMARY.md` when done.
</output>
