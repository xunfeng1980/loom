---
phase: 25-native-equivalence-cache-and-fallback-hardening
plan: 05
type: execute
wave: 5
depends_on:
  - 25-04
files_modified:
  - scripts/mvp0-verify.sh
  - .planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PHASE-25
must_haves:
  truths:
    - "The main release gate runs Phase 25 native hardening after Phase 24 and before DuckDB smoke."
    - "The final report lists supported equivalence rows, cache invalidation rules, fallback rules, performance smoke evidence, non-goals, and Phase 26 handoff assumptions."
    - "Planning docs close Phase 25 without claiming persistent cache, public API controls, new native semantics, table binding, or arbitrary Vortex compatibility."
  artifacts:
    - path: scripts/mvp0-verify.sh
      provides: "Release-gate wiring for native-hardening-test.sh"
    - path: .planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md
      provides: "Final equivalence/cache/fallback hardening report"
    - path: .planning/PROJECT.md
      provides: "Validated Phase 25 project status after execution"
    - path: .planning/STATE.md
      provides: "Phase transition state after execution"
    - path: .planning/ROADMAP.md
      provides: "Phase 25 completion and Phase 26 handoff"
  key_links:
    - from: scripts/mvp0-verify.sh
      to: scripts/native-hardening-test.sh
      via: "release gate invocation"
      pattern: "native-hardening-test\\.sh"
    - from: .planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md
      to: scripts/native-hardening-test.sh
      via: "recorded gate evidence"
      pattern: "cache hit|cache miss|interpreter oracle|loom_scan\\(path\\)"
---

<objective>
Wire the Phase 25 release gate and write the final closeout report/docs.

Purpose: make native equivalence/cache/fallback hardening part of the one-command gate before Phase 26 source-ingress contract work starts.
Output: mvp0 release wiring, Phase 25 final report, and planning docs that record bounded claims and handoff assumptions.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-CONTEXT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-RESEARCH.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-DUCKDB-NATIVE-REPORT.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-VERIFICATION.md
@scripts/mvp0-verify.sh
@scripts/native-hardening-test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 25 into the main release gate</name>
  <files>scripts/mvp0-verify.sh</files>
  <action>Add a Phase 25 native hardening step immediately after scripts/duckdb-native-integration-test.sh and before scripts/duckdb-smoke-test.sh. Keep the existing LOOM_ALLOW_NATIVE_TOOL_SKIP behavior inherited from the environment. Add bash syntax verification and a grep assertion that Phase 23, Phase 24, Phase 25, and DuckDB smoke run in that order. Do not remove or weaken any prior release gate.</action>
  <verify>
    <automated>bash -n scripts/mvp0-verify.sh && bash -n scripts/native-hardening-test.sh && LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/native-hardening-test.sh</automated>
  </verify>
  <done>mvp0-verify invokes native-hardening-test.sh in the correct order without changing earlier gate semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Write final native hardening report</name>
  <files>.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md</files>
  <action>Create the final report with sections for supported equivalence matrix, interpreter oracle scope, existing Vortex/fixture evidence used, in-process cache design, cache invalidation rules, cache non-cacheable routes, fallback/strict behavior, deterministic diagnostic codes, performance smoke evidence, public SQL/API non-creep, and Phase 26 handoff assumptions. Explicitly record tradeoffs: in-process vs persistent cache, interpreter oracle vs broad Vortex semantic claims, smoke evidence vs benchmark, and Rust-owned policy vs C++ duplication. State that public SQL remains loom_scan(path), cache controls remain internal, and no new external packages were added.</action>
  <verify>
    <automated>grep -E "in-process|persistent|interpreter oracle|Vortex|smoke|benchmark|Rust-owned|C\\+\\+|loom_scan\\(path\\)" .planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md</automated>
  </verify>
  <done>The report documents evidence and non-goals tightly enough for Phase 26 to consume without reopening native hardening decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Close planning docs and run final gates</name>
  <files>.planning/PROJECT.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Update planning docs after the implementation is verified: move Phase 25 from active/planned status to validated/complete, set Phase 26 as the next active focus, and record the native-hardening gate and report path. Keep claims bounded to Phase 25 evidence. Do not claim persistent cache, native speedup, public cache/native SQL controls, source/table-format binding, predicate pushdown, parallel split execution, new native kernels, or arbitrary Vortex semantic compatibility.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh</automated>
  </verify>
  <done>Full release verification passes and planning docs accurately close Phase 25 while preserving Phase 26 boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| focused gate -> main release gate | Phase 25 evidence becomes part of the one-command verification path. |
| implementation evidence -> docs | Reports must not overstate cache, performance, native semantics, or source/table readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-01 | Repudiation | final report | mitigate | Report records concrete commands, matrix rows, cache rules, diagnostics, and non-goals. |
| T-25-05-02 | Tampering | release gate order | mitigate | Wire Phase 25 after Phase 24 and before DuckDB smoke; verify ordering and run full gate. |
| T-25-05-03 | Information Disclosure | public docs/API claims | mitigate | Docs state public SQL remains loom_scan(path) and internal hooks are not public API. |
| T-25-SC | Tampering | package installs | mitigate | No package installs are planned; final report must state no new external packages were added. |
</threat_model>

<verification>
Run:
- bash -n scripts/native-hardening-test.sh && bash -n scripts/mvp0-verify.sh
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/native-hardening-test.sh
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh
</verification>

<success_criteria>
Phase 25 has a release-gated closeout report and planning-state handoff that hardens native execution without adding public API or table/source-format scope.
</success_criteria>

<output>
Create `.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-05-SUMMARY.md` when done.
</output>
