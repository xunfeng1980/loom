---
phase: 25-native-equivalence-cache-and-fallback-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/runtime_abi.rs
  - crates/loom-core/tests/runtime_cache_key.rs
  - crates/loom-core/tests/runtime_execution_policy.rs
autonomous: true
requirements:
  - PHASE-25
must_haves:
  truths:
    - "Runtime cache reuse requires exact RuntimeCacheKey stable_id and canonical_input equality."
    - "Cache-key mismatch diagnostics are stable and host-neutral before DuckDB-specific code sees them."
    - "Runtime cache identity includes artifact, facts, lowering, backend/toolchain, projection, predicate, split, and policy inputs."
  artifacts:
    - path: crates/loom-core/src/runtime_abi.rs
      provides: "Host-neutral cache-key compatibility helper and stable diagnostics"
    - path: crates/loom-core/tests/runtime_cache_key.rs
      provides: "Mutation matrix for cache identity and compatibility checks"
    - path: crates/loom-core/tests/runtime_execution_policy.rs
      provides: "Stable strict/fallback diagnostic assertions"
  key_links:
    - from: crates/loom-core/src/runtime_abi.rs
      to: crates/loom-ffi/src/duckdb_runtime.rs
      via: "RuntimeCacheKey and RuntimeDiagnostic vocabulary consumed by the DuckDB bridge"
      pattern: "RuntimeCacheKey|RuntimeDiagnosticCode::CacheKeyMismatch"
---

<objective>
Define the host-neutral runtime cache compatibility contract that Phase 25 cache reuse must obey.

Purpose: keep cache identity and mismatch diagnostics in Rust runtime vocabulary instead of DuckDB/C++ branches, per CONTEXT Cache Contract and Fallback And Negative Coverage.
Output: exact-key compatibility helper, expanded mutation tests, and stable runtime diagnostics for later Phase 25 plans.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-CONTEXT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-RESEARCH.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-PATTERNS.md
@.planning/phases/22-host-native-runtime-abi-and-execution-policy/22-SUMMARY.md
@crates/loom-core/src/runtime_abi.rs
@crates/loom-core/tests/runtime_cache_key.rs
@crates/loom-core/tests/runtime_execution_policy.rs
</context>

<source_audit>
SOURCE | ID | Requirement | Plan | Status | Notes
GOAL | PHASE-25-GOAL | Harden native equivalence, cache reuse/invalidation, fallback diagnostics, performance smoke, and release gate before table-format-visible work | 25-01..25-05 | COVERED | Goal decomposed across runtime contract, Rust cache, helper matrix, SQL gate, and closeout.
REQ | PHASE-25 | Roadmap phase requirement for native equivalence/cache/fallback hardening | 25-01..25-05 | COVERED | Every plan declares PHASE-25.
RESEARCH | Equivalence matrix | Interpreter-primary oracle, supported primitive/table/projection/repeated scans, Vortex evidence only where already available | 25-03, 25-04, 25-05 | COVERED | Plans avoid arbitrary Vortex semantic claims.
RESEARCH | In-process cache contract | RuntimeCacheKey exact equality, accepted prepared artifacts/evidence only, no path/mtime or persistent format | 25-01, 25-02, 25-04 | COVERED | Plan 25-01 defines compatibility; 25-02 implements Rust cache.
RESEARCH | Fallback and negative hardening | Unsupported lowering/facts, cache mismatch, cancellation, native mismatch, malformed artifact, unsupported projection/predicate/split | 25-03, 25-04 | COVERED | Negative cases are fallback/fail-closed evidence.
RESEARCH | Deterministic diagnostics | Stable code/path/message diagnostics for cache, fallback, cancellation, toolchain, mismatch | 25-01, 25-02, 25-03, 25-04 | COVERED | Diagnostics asserted in Rust, FFI, and script gates.
RESEARCH | Performance smoke | Cache hit or prepare-avoidance evidence without benchmark claims | 25-02, 25-04, 25-05 | COVERED | Smoke evidence is report/counter based.
RESEARCH | No new packages | Use existing workspace crates, scripts, DuckDB gate tooling | 25-01..25-05 | COVERED | No package manager install tasks appear.
CONTEXT | Equivalence Scope | Interpreter is primary oracle; Vortex only where already established; unsupported strings/compression/nullability are not native targets | 25-03, 25-04, 25-05 | COVERED | Plans state bounded claims and non-goals.
CONTEXT | Cache Contract | Host-neutral in-process cache keyed by RuntimeCacheKey plus backend/toolchain/lowering/query/policy identity | 25-01, 25-02 | COVERED | Persistent cache and public controls are excluded.
CONTEXT | Fallback And Negative Coverage | Phase 22 policy remains source of truth; strict mode fails closed with stable diagnostics | 25-01, 25-03, 25-04 | COVERED | C++ consumes Rust reports only.
CONTEXT | Release And Performance Evidence | Dedicated native-hardening gate, mvp0 wiring, final report with matrix/cache/fallback rules | 25-04, 25-05 | COVERED | Release gate runs after Phase 24 and before DuckDB smoke.
CONTEXT | Deferred Ideas | Persistent cache, public SQL flags, predicate pushdown, split execution, new native semantics, source/table bindings | none | EXCLUDED | Deferred ideas do not appear as implementation tasks.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add host-neutral cache compatibility checks</name>
  <files>crates/loom-core/src/runtime_abi.rs, crates/loom-core/tests/runtime_cache_key.rs</files>
  <behavior>
    - Test 1: equal RuntimeCacheKey stable_id and canonical_input returns cache-compatible hit with no diagnostics.
    - Test 2: different stable_id returns cache miss without claiming corruption.
    - Test 3: same stable_id with different canonical_input returns cache-key-mismatch at $.cache.key.
  </behavior>
  <action>Add a host-neutral cache compatibility helper beside RuntimeCacheKey in runtime_abi.rs. Use explicit status vocabulary such as hit, miss, and key-mismatch; emit RuntimeDiagnosticCode::CacheKeyMismatch only when stable_id matches but canonical_input differs. Do not add storage, eviction, path/mtime semantics, DuckDB types, native artifact types, persistent cache format, or packages. This records the in-process vs persistent cache tradeoff by making identity reusable while leaving cache storage to the Rust host bridge.</action>
  <verify>
    <automated>cargo test -p loom-core --test runtime_cache_key cache</automated>
  </verify>
  <done>Cache compatibility behavior is implemented in loom-core and all three equality/miss/mismatch cases are asserted with stable diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand cache identity mutation coverage</name>
  <files>crates/loom-core/tests/runtime_cache_key.rs</files>
  <behavior>
    - Test 1: artifact digest, facts fingerprint, solver identity, and production lowering fingerprint mutations change RuntimeCacheKey.
    - Test 2: backend name/version/toolchain/target/cpu feature mutations change RuntimeCacheKey.
    - Test 3: projection, predicate, split, fallback policy, predicate policy, and concurrency mutations change RuntimeCacheKey.
  </behavior>
  <action>Extend runtime_cache_key.rs with one-field-at-a-time mutation assertions for every RuntimeCacheKeyInput field. Keep assertions structural rather than snapshotting the whole canonical string except for targeted contains checks such as projection=columns and predicate=none. Include a short test comment explaining that interpreter oracle vs broad Vortex claims are outside this core identity layer; this task only proves identity changes are visible to cache reuse.</action>
  <verify>
    <automated>cargo test -p loom-core --test runtime_cache_key</automated>
  </verify>
  <done>Every cache-key input dimension has a focused regression assertion and RuntimeDiagnosticCode::CacheKeyMismatch remains stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve strict fallback diagnostic ownership</name>
  <files>crates/loom-core/tests/runtime_execution_policy.rs</files>
  <behavior>
    - Test 1: unsupported interpreter-only lowering with fallback disabled returns fail-closed and fallback-disabled or lowering-unsupported diagnostics.
    - Test 2: unsupported predicate and invalid split remain policy-owned runtime diagnostics, not host-adapter decisions.
  </behavior>
  <action>Add focused assertions showing Phase 22 runtime policy remains the source of fallback and unsupported-input diagnostics that Phase 25 cache and DuckDB plans consume. Do not add any DuckDB/C++ logic here. This records the Rust-owned policy vs C++ duplication tradeoff at the runtime layer.</action>
  <verify>
    <automated>cargo test -p loom-core --test runtime_execution_policy --test runtime_scan_planning</automated>
  </verify>
  <done>Runtime policy tests prove unsupported native paths produce deterministic Rust diagnostics before any host adapter can duplicate policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact facts -> runtime cache key | Untrusted artifact bytes and verifier facts become cache identity material. |
| runtime cache key -> later cache storage | A compact stable id could otherwise hide stale or mismatched canonical inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | RuntimeCacheKey compatibility | mitigate | Require canonical_input equality in addition to stable_id equality before hit status. |
| T-25-01-02 | Repudiation | Runtime diagnostics | mitigate | Assert stable code/path/message for cache-key-mismatch and fallback-disabled style diagnostics. |
| T-25-SC | Tampering | package installs | mitigate | No package installs are planned; if npm/pip/cargo add/install appears, stop and run package legitimacy review. |
</threat_model>

<verification>
Run the focused core tests:
- cargo test -p loom-core --test runtime_cache_key
- cargo test -p loom-core --test runtime_execution_policy --test runtime_scan_planning
</verification>

<success_criteria>
Runtime cache compatibility is exact-key based, mutation coverage proves all key dimensions affect identity, and fallback/cache diagnostics remain host-neutral.
</success_criteria>

<output>
Create `.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-01-SUMMARY.md` when done.
</output>
