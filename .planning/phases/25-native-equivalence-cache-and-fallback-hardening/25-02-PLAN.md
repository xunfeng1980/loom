---
phase: 25-native-equivalence-cache-and-fallback-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - crates/loom-ffi/src/duckdb_runtime.rs
  - crates/loom-ffi/tests/duckdb_runtime_cache.rs
  - crates/loom-ffi/tests/duckdb_runtime_ffi.rs
autonomous: true
requirements:
  - PHASE-25
must_haves:
  truths:
    - "Identical native-candidate plans can reuse accepted in-process preparation evidence."
    - "Projection, policy, backend/toolchain, lowering, artifact, or facts drift creates a miss or key-mismatch diagnostic before reuse."
    - "Failed, cancelled, skipped-toolchain, and native-output-mismatch routes never create reusable cache entries."
  artifacts:
    - path: crates/loom-ffi/src/duckdb_runtime.rs
      provides: "Rust-owned in-process native preparation cache and cache evidence diagnostics"
    - path: crates/loom-ffi/tests/duckdb_runtime_cache.rs
      provides: "Cache hit/miss/invalidation/non-cacheable route tests"
    - path: crates/loom-ffi/tests/duckdb_runtime_ffi.rs
      provides: "Internal FFI visibility and public header non-leakage tests"
  key_links:
    - from: crates/loom-ffi/src/duckdb_runtime.rs
      to: crates/loom-core/src/runtime_abi.rs
      via: "RuntimeCacheKey compatibility helper"
      pattern: "cache.*RuntimeCacheKey|RuntimeCacheKey"
    - from: crates/loom-ffi/src/duckdb_runtime.rs
      to: crates/loom-native-melior/src/backend.rs
      via: "Accepted NativeBackendReport artifact cache entry"
      pattern: "NativeBackendReport|NativeBackendStatus::Accepted"
---

<objective>
Implement the Phase 25 in-process native artifact cache in the Rust DuckDB runtime bridge.

Purpose: prove cache reuse and invalidation semantics without creating a persistent cache format or duplicating policy in C++.
Output: Rust-owned process-local cache, deterministic cache diagnostics, and tests for hit/miss/non-cacheable behavior.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-CONTEXT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-RESEARCH.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-PATTERNS.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-01-SUMMARY.md
@.planning/phases/24-duckdb-native-execution-integration-mvp/24-02-SUMMARY.md
@crates/loom-ffi/src/duckdb_runtime.rs
@crates/loom-ffi/tests/duckdb_runtime.rs
@crates/loom-ffi/tests/duckdb_runtime_ffi.rs
@crates/loom-ffi/include/loom_duckdb_internal.h
@crates/loom-native-melior/src/backend.rs
@crates/loom-native-melior/src/jit.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache-hit and invalidation tests</name>
  <files>crates/loom-ffi/tests/duckdb_runtime_cache.rs, crates/loom-ffi/src/duckdb_runtime.rs</files>
  <behavior>
    - Test 1: two identical native-candidate plans with accepted test JIT buffers produce miss+insert then hit diagnostics.
    - Test 2: changing projection or allow_interpreter_fallback policy produces a miss rather than reusing the prior entry.
    - Test 3: injected canonical-input mismatch for the same stable id produces cache-key-mismatch and does not reuse.
  </behavior>
  <action>Create duckdb_runtime_cache.rs as a focused integration test file. Add test-only Rust helpers in duckdb_runtime.rs for resetting the in-process cache and reading cache counters or the last cache event; keep them Rust-internal and out of loom.h. Use existing test-native-facts/test-jit buffers so the tests do not require local MLIR/LLVM tools. Do not add public SQL flags or external packages.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 cargo test -p loom-ffi --test duckdb_runtime_cache</automated>
  </verify>
  <done>Cache tests fail before implementation and describe exact hit, miss, invalidation, and mismatch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Rust-owned in-process preparation cache</name>
  <files>crates/loom-ffi/src/duckdb_runtime.rs, crates/loom-ffi/tests/duckdb_runtime_cache.rs</files>
  <behavior>
    - Test 1: cache insertion happens only after NativeBackendStatus::Accepted, no backend diagnostics, successful JIT/reference comparison, and non-empty native buffers for the requested shape.
    - Test 2: cancelled, skipped-toolchain, failed-toolchain, native-output-mismatch, missing-lowering-facts, and fallback routes report non-cacheable and leave cache counters unchanged except lookup/miss.
    - Test 3: cache hit reuses accepted backend preparation evidence but still validates output before returning native buffers.
  </behavior>
  <action>In duckdb_runtime.rs, add a process-local cache protected by std synchronization primitives already available in the standard library. Key entries by RuntimeCacheKey stable_id and validate canonical_input through the Phase 25 core helper before reuse. Store only accepted NativeBackendReport artifact/preparation evidence plus metadata needed for deterministic reporting; do not store failed outputs, C++ vector pointers, path/mtime data, or persistent files. Add deterministic diagnostics such as cache-miss, cache-inserted, cache-hit, cache-non-cacheable, and cache-key-mismatch to DuckDbPreparedRoute diagnostics. This records the in-process vs persistent cache and smoke evidence vs benchmark tradeoffs.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 cargo test -p loom-ffi --test duckdb_runtime_cache</automated>
  </verify>
  <done>Identical eligible requests show cache reuse; mutated keys miss; unsafe routes never insert; returned native buffers remain comparison-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose cache evidence through existing internal diagnostics</name>
  <files>crates/loom-ffi/tests/duckdb_runtime_ffi.rs, crates/loom-ffi/src/duckdb_runtime.rs</files>
  <behavior>
    - Test 1: existing internal prepare diagnostic accessors can read cache-miss/cache-hit/cache-non-cacheable codes.
    - Test 2: generated public loom.h still excludes loom_duckdb_, LoomDuckDb, cache, native/interpreter route functions, and ArrowArrayStream.
  </behavior>
  <action>Use the existing internal diagnostic accessors rather than adding public cache APIs. If additional internal Rust helpers are needed for tests, keep their names under duckdb_runtime and do not add them to cbindgen output. Extend duckdb_runtime_ffi.rs public-header leakage checks for cache control markers that would imply public SQL/API creep.</action>
  <verify>
    <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 cargo test -p loom-ffi --test duckdb_runtime_ffi --test duckdb_runtime_cache</automated>
  </verify>
  <done>Cache evidence is visible to internal tests and later DuckDB route reports through diagnostics, while public headers remain clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime plan -> cache lookup | A verified plan attempts to reuse process-local native preparation evidence. |
| Backend report -> cache insertion | Native backend output becomes reusable only after validation and comparison. |
| Rust cache diagnostics -> C++ route report | Internal host adapter observes cache status without owning cache policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | duckdb_runtime in-process cache | mitigate | Require exact RuntimeCacheKey compatibility and accepted backend report before hit. |
| T-25-02-02 | Elevation of Privilege | failed native output | mitigate | Mark cancelled/mismatch/toolchain failure/fallback routes non-cacheable and assert zero inserts. |
| T-25-02-03 | Information Disclosure | cache controls | mitigate | Expose cache evidence only through internal diagnostics; public loom.h leakage tests block route/cache symbols. |
| T-25-SC | Tampering | package installs | mitigate | No package installs are planned; if package additions appear, stop and run package legitimacy review. |
</threat_model>

<verification>
Run:
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 cargo test -p loom-ffi --test duckdb_runtime_cache
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 cargo test -p loom-ffi --test duckdb_runtime_ffi --test duckdb_runtime
</verification>

<success_criteria>
The Rust DuckDB bridge owns an in-process cache with deterministic hit/miss/non-cacheable diagnostics, exact key invalidation, and no public API surface.
</success_criteria>

<output>
Create `.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-02-SUMMARY.md` when done.
</output>
