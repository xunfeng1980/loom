---
phase: 28-iceberg-ref-table-binding
plan: 05
type: execute
wave: 5
depends_on:
  - 28-04
files_modified:
  - scripts/iceberg-binding-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
autonomous: true
requirements:
  - PHASE-28
must_haves:
  truths:
    - "D-13/D-14: the focused Phase 28 gate is wired into `scripts/mvp0-verify.sh` after Phase 27 and before DuckDB smoke only after it passes."
    - "D-12/D-16: the final report records release-gate evidence, concrete source/oracle evidence artifact checks, stale/forged evidence mismatches, current-phase tradeoffs, non-goals, and Phase 29 handoff."
    - "D-01/D-09/D-10/D-15: closeout gates prove no Iceberg SDK default dependency, no public SQL/C ABI/DuckDB/CLI route, no StarRocks surface, and no catalog/object-store credential creep."
  artifacts:
    - path: scripts/iceberg-binding-test.sh
      provides: "Focused Phase 28 release gate"
    - path: scripts/mvp0-verify.sh
      provides: "Main release gate ordering after Phase 27 before DuckDB smoke"
    - path: .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
      provides: "Final Phase 28 evidence, source/oracle fixture validation, tradeoffs, non-goals, and Phase 29 handoff"
  key_links:
    - from: scripts/mvp0-verify.sh
      to: scripts/iceberg-binding-test.sh
      via: "release gate invocation"
      pattern: "iceberg-binding-test\\.sh"
    - from: scripts/iceberg-binding-test.sh
      to: crates/loom-iceberg-binding
      via: "focused cargo tests"
      pattern: "loom-iceberg-binding"
    - from: .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
      to: scripts/iceberg-binding-test.sh
      via: "documented release evidence"
      pattern: "Release Gate Evidence"
    - from: scripts/iceberg-binding-test.sh
      to: crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-source-evidence.json
      via: "required evidence fixture checks"
      pattern: "accepted-table-source-evidence|stale-source-evidence|forged-oracle-evidence"
---

<objective>
Finalize Phase 28 focused release gating, main verifier wiring, and report closeout.

Purpose: make Iceberg binding evidence reproducible from the focused gate and the main release verifier while preserving dependency isolation and preventing Phase 29 query-surface creep.
Output: final `scripts/iceberg-binding-test.sh`, `scripts/mvp0-verify.sh` wiring, and updated `28-ICEBERG-BINDING-REPORT.md` release evidence.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-iceberg-ref-table-binding/28-CONTEXT.md
@.planning/phases/28-iceberg-ref-table-binding/28-RESEARCH.md
@.planning/phases/28-iceberg-ref-table-binding/28-PATTERNS.md
@.planning/phases/28-iceberg-ref-table-binding/28-01-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-02-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-03-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-04-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md
@scripts/iceberg-binding-test.sh
@scripts/lance-parquet-ingress-test.sh
@scripts/source-ingress-contract-test.sh
@scripts/mvp0-verify.sh
</context>

## Artifacts This Phase Produces

- Files: `crates/loom-iceberg-binding/Cargo.toml`, `crates/loom-iceberg-binding/src/lib.rs`, `crates/loom-iceberg-binding/src/binding_contract.rs`, `crates/loom-iceberg-binding/tests/dependency_boundary.rs`, `crates/loom-iceberg-binding/tests/binding_contract.rs`, `crates/loom-iceberg-binding/tests/binding_handoff.rs`, `crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs`.
- Fixtures: `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-metadata.json`, `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-loom-binding.json`, `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-source-evidence.json`, and local unsupported/rejected/mismatch/source-oracle JSON fixtures used by tests.
- Shared/gate files: `scripts/iceberg-binding-test.sh`, `scripts/mvp0-verify.sh`, `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`.
- Symbols: `IcebergBindingStatus`, `IcebergTableRefIdentity`, `IcebergBindingFacts`, `IcebergBindingEvidence`, `IcebergBindingReport`, `IcebergBindingAcceptedArtifact`, `iceberg_binding_facts_from_paths`, `source_ingress_report_from_iceberg_metadata_path`, `bind_iceberg_ref_from_paths`.

<tasks>

<task type="auto">
  <name>Task 1: Finalize focused Iceberg binding gate</name>
  <read_first>
    - `scripts/iceberg-binding-test.sh`
    - `scripts/lance-parquet-ingress-test.sh`
    - `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
    - `Cargo.toml`
    - `crates/loom-iceberg-binding/Cargo.toml`
    - `crates/loom-core/Cargo.toml`
    - `crates/loom-ffi/Cargo.toml`
    - `crates/loom-source-ingress/Cargo.toml`
    - `crates/loom-cli/Cargo.toml`
  </read_first>
  <files>scripts/iceberg-binding-test.sh</files>
  <action>Finalize `scripts/iceberg-binding-test.sh` as the Phase 28 focused release gate per D-13. It must check required Phase 28 context/research/patterns/report artifacts, required plan summaries `28-01-SUMMARY.md` through `28-04-SUMMARY.md`, local metadata/sidecar/source-evidence/mismatch fixture JSON files, the adapter crate files, and report markers from Plan 28-04. Required fixture checks include `accepted-table-source-evidence.json`, `stale-source-evidence.json`, and `forged-oracle-evidence.json` so the gate proves concrete evidence-artifact validation and stale/forged negative coverage. It must run `cargo test -p loom-iceberg-binding --test dependency_boundary`, `binding_contract`, `binding_handoff`, `mismatch_fail_closed`, `cargo test -p loom-core --test artifact_verifier`, and `cargo check -p loom-iceberg-binding`. Keep direct dependency guards proving no default `iceberg` dependency and no Iceberg SDK dependency outside the adapter; keep cargo-tree guards for `loom-core`, `loom-ffi`, `loom-source-ingress`, and `loom-cli` against Iceberg, object-store, catalog, credential, DuckDB route, and StarRocks markers per D-01/D-09/D-10/D-15. Keep public-surface scans over FFI headers, DuckDB extension code, and CLI for route/API creep. Add a duplicate-dependency guard that fails if `cargo tree -d` shows duplicate Arrow or Parquet families caused by accidental SDK adoption. Use filtered `awk`/`rg` checks rather than bare unfiltered `grep -c` gates.</action>
  <verify>
    <automated>bash -n scripts/iceberg-binding-test.sh && bash scripts/iceberg-binding-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Focused gate passes from the repository root.
    - Focused gate runs every Phase 28 adapter test and `loom-core` artifact verifier test.
    - Focused gate fails on missing summaries, missing report markers, missing metadata/sidecar/source-evidence/mismatch fixtures, default `iceberg` dependency, duplicate Arrow/Parquet dependency families, public route/API creep, or catalog/credential markers.
    - Guard checks avoid unfiltered `grep -c`.
  </acceptance_criteria>
  <done>The focused Phase 28 gate is complete and passes before main verifier wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 28 after Phase 27 and before DuckDB smoke</name>
  <read_first>
    - `scripts/mvp0-verify.sh`
    - `scripts/iceberg-binding-test.sh`
    - `scripts/lance-parquet-ingress-test.sh`
    - `scripts/source-ingress-contract-test.sh`
  </read_first>
  <files>scripts/mvp0-verify.sh</files>
  <action>Invoke `scripts/iceberg-binding-test.sh` from `scripts/mvp0-verify.sh` after `scripts/lance-parquet-ingress-test.sh` and before `scripts/duckdb-smoke-test.sh` per D-14. Update the nearby ordering comment so the late release sequence is Phase 24 DuckDB native integration, Phase 25 native hardening, Phase 26 source ingress contract, Phase 27 Lance/Parquet ingress, Phase 28 Iceberg binding, then DuckDB SQL smoke. Do not add public SQL functions, public C ABI symbols, DuckDB extension behavior, CLI commands, StarRocks code, catalog controls, or object-store credentials per D-01/D-10.</action>
  <verify>
    <automated>bash -n scripts/mvp0-verify.sh && rg -q "iceberg-binding-test\.sh" scripts/mvp0-verify.sh && python3 -c 'from pathlib import Path; text=Path("scripts/mvp0-verify.sh").read_text(); order=["scripts/source-ingress-contract-test.sh","scripts/lance-parquet-ingress-test.sh","scripts/iceberg-binding-test.sh","scripts/duckdb-smoke-test.sh"]; pos=[text.index(x) for x in order]; assert pos == sorted(pos), pos'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mvp0-verify.sh` invokes `scripts/iceberg-binding-test.sh`.
    - Order check confirms Phase 28 runs after Phase 27 and before DuckDB smoke.
    - No route-specific Iceberg SQL, CLI, DuckDB, C ABI, StarRocks, catalog, or credential surface is added.
  </acceptance_criteria>
  <done>The main release verifier runs Phase 28 in the required order.</done>
</task>

<task type="auto">
  <name>Task 3: Record final release evidence and run closeout verification</name>
  <read_first>
    - `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-01-SUMMARY.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-02-SUMMARY.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-03-SUMMARY.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-04-SUMMARY.md`
    - `scripts/iceberg-binding-test.sh`
    - `scripts/mvp0-verify.sh`
  </read_first>
  <files>.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md</files>
  <action>Update `28-ICEBERG-BINDING-REPORT.md` `Release Gate Evidence` with final command names and pass/fail status for `bash -n scripts/iceberg-binding-test.sh`, `bash scripts/iceberg-binding-test.sh`, `bash -n scripts/mvp0-verify.sh`, the direct order check, and `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh`. Confirm the report still includes Binding Schema, Accepted Unsupported Rejected Matrix, Mismatch Fail-Closed Matrix, Source Evidence, Verifier Evidence, Oracle Evidence, Dependency and API Boundary, Current-Phase Tradeoffs, Non-Goals, and Phase 29 Handoff. Confirm Source Evidence and Oracle Evidence name the accepted evidence artifact, list row count, table UUID, schema ID, snapshot ID, artifact SHA-256, source accepted status, and decoded-row/oracle accepted status as checked fields, and state that sidecar accepted flags are not sufficient. Confirm Mismatch Fail-Closed Matrix includes stale source evidence and forged decoded-row oracle evidence. Run the focused gate first, then the main verifier with `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`. If disk exhaustion occurs from ignored build artifacts as in Phase 27, remove only ignored generated build outputs and rerun the same verification command; record the environment cleanup in the summary, not as a product deviation. Do not edit `.planning/STATE.md` in this plan unless the execute workflow separately requires state closure.</action>
  <verify>
    <automated>bash scripts/iceberg-binding-test.sh && bash -n scripts/mvp0-verify.sh && python3 -c 'from pathlib import Path; text=Path("scripts/mvp0-verify.sh").read_text(); order=["scripts/source-ingress-contract-test.sh","scripts/lance-parquet-ingress-test.sh","scripts/iceberg-binding-test.sh","scripts/duckdb-smoke-test.sh"]; pos=[text.index(x) for x in order]; assert pos == sorted(pos), pos' && LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh && rg -q "Release Gate Evidence" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 28 gate passes.
    - Main release verifier syntax and ordering checks pass.
    - Main release verifier passes with `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`.
    - Final report records exact release evidence, concrete source/oracle evidence-artifact validation, stale/forged evidence mismatch coverage, and current-phase tradeoffs.
    - Final report states Phase 29 may consume the binding contract but query surfaces remain unimplemented here.
  </acceptance_criteria>
  <done>Phase 28 is release-gated and documented without widening into Phase 29 or production Iceberg catalog scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| focused gate -> main release gate | Phase 28 evidence becomes part of one-command verification only after focused checks pass. |
| main release ordering -> downstream trust | Incorrect ordering could run DuckDB smoke without Iceberg binding evidence. |
| final report -> Phase 29 handoff | Report must not imply query-surface or catalog implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Tampering | `scripts/iceberg-binding-test.sh` | mitigate | Final focused gate runs parser, handoff, mismatch, dependency, public-surface, duplicate Arrow/Parquet, report marker, and artifact verifier checks per D-13/D-15. |
| T-28-05-02 | Repudiation | `scripts/mvp0-verify.sh` ordering | mitigate | Syntax, marker, and Python order checks enforce Phase 26 -> Phase 27 -> Phase 28 -> DuckDB smoke per D-14. |
| T-28-05-03 | Elevation of Privilege | public SQL/C ABI/DuckDB/CLI route creep | mitigate | Focused gate scans guarded public and host surfaces and Plan 28-05 action adds no public routes per D-01. |
| T-28-05-04 | Information Disclosure | catalog/object-store credentials | mitigate | Focused gate scans for REST/catalog/warehouse/AWS/secret/credential markers outside adapter-local test fixtures per D-10/D-15. |
| T-28-05-05 | Tampering | SDK dependency leakage | mitigate | Final gate rejects default `iceberg` dependency and duplicate Arrow/Parquet families per D-09. |
| T-28-05-06 | Spoofing | report handoff to Phase 29 | mitigate | Final report distinguishes binding evidence from query-engine, catalog, and full Iceberg semantic compatibility claims per D-11/D-16. |
| T-28-05-07 | Spoofing | source/oracle evidence closeout | mitigate | Final gate and report require `accepted-table-source-evidence.json`, stale source evidence, and forged oracle evidence checks so accepted flags alone cannot become release evidence. |
</threat_model>

<verification>
Overall plan checks:
- `bash -n scripts/iceberg-binding-test.sh && bash scripts/iceberg-binding-test.sh`
- `bash -n scripts/mvp0-verify.sh`
- `rg -q "iceberg-binding-test\\.sh" scripts/mvp0-verify.sh`
- `python3 -c 'from pathlib import Path; text=Path("scripts/mvp0-verify.sh").read_text(); order=["scripts/source-ingress-contract-test.sh","scripts/lance-parquet-ingress-test.sh","scripts/iceberg-binding-test.sh","scripts/duckdb-smoke-test.sh"]; pos=[text.index(x) for x in order]; assert pos == sorted(pos), pos'`
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh`
- `rg -q "Release Gate Evidence" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
</verification>

<success_criteria>
Plan 28-05 is complete when the focused Phase 28 gate passes, the main release verifier invokes it after Phase 27 and before DuckDB smoke, the full release verifier passes with managed native-tool skip allowed, and the final report records release evidence, concrete source/oracle evidence-artifact validation, stale/forged evidence mismatch coverage, tradeoffs, non-goals, and Phase 29 handoff without adding query, catalog, credential, SDK, or public API scope.
</success_criteria>

<output>
Create `.planning/phases/28-iceberg-ref-table-binding/28-05-SUMMARY.md` when done.
</output>
