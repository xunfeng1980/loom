---
phase: 28-iceberg-ref-table-binding
plan: 04
type: execute
wave: 4
depends_on:
  - 28-03
files_modified:
  - crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
  - crates/loom-iceberg-binding/tests/fixtures/local/mismatch-schema-sidecar.json
  - crates/loom-iceberg-binding/tests/fixtures/local/mismatch-snapshot-sidecar.json
  - crates/loom-iceberg-binding/tests/fixtures/local/manifest-only-sidecar.json
  - crates/loom-iceberg-binding/tests/fixtures/local/stale-source-evidence.json
  - crates/loom-iceberg-binding/tests/fixtures/local/forged-oracle-evidence.json
  - scripts/iceberg-binding-test.sh
  - .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
autonomous: true
requirements:
  - PHASE-28
must_haves:
  truths:
    - "D-08/D-15: schema, snapshot, artifact hash, verifier status, source evidence, oracle evidence, stale/forged source-oracle artifacts, and manifest-only mismatch cases fail closed."
    - "D-12/D-16: `28-ICEBERG-BINDING-REPORT.md` records binding schema, accepted/unsupported/rejected matrix, evidence, non-goals, tradeoffs, and Phase 29 handoff."
    - "D-01/D-10/D-11: report and tests do not introduce query surfaces, catalog operations, object-store credentials, broad Iceberg semantics, pushdown, splits, or native kernels."
  artifacts:
    - path: crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
      provides: "Executable mismatch and manifest-only fail-closed matrix"
    - path: crates/loom-iceberg-binding/tests/fixtures/local/mismatch-schema-sidecar.json
      provides: "Schema mismatch sidecar fixture"
    - path: crates/loom-iceberg-binding/tests/fixtures/local/mismatch-snapshot-sidecar.json
      provides: "Snapshot mismatch sidecar fixture"
    - path: crates/loom-iceberg-binding/tests/fixtures/local/manifest-only-sidecar.json
      provides: "Manifest-only trust bypass fixture"
    - path: crates/loom-iceberg-binding/tests/fixtures/local/stale-source-evidence.json
      provides: "Stale source evidence fixture with mismatched identity/hash/row-count fields"
    - path: crates/loom-iceberg-binding/tests/fixtures/local/forged-oracle-evidence.json
      provides: "Forged decoded-row oracle fixture whose accepted flag is not sufficient"
    - path: .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
      provides: "Phase 28 binding evidence report"
  key_links:
    - from: crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
      to: crates/loom-iceberg-binding/src/binding_contract.rs
      via: "negative binding validation calls"
      pattern: "bind_iceberg_ref_from_paths"
    - from: crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs
      to: crates/loom-iceberg-binding/tests/fixtures/local/stale-source-evidence.json
      via: "stale/forged source-oracle evidence fixture paths"
      pattern: "stale-source-evidence|forged-oracle-evidence"
    - from: scripts/iceberg-binding-test.sh
      to: .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md
      via: "report marker checks"
      pattern: "28-ICEBERG-BINDING-REPORT"
---

<objective>
Add the fail-closed mismatch matrix and write the Phase 28 Iceberg binding report.

Purpose: prove that accepted Iceberg bindings are bounded evidence claims and that stale or metadata-only claims cannot pass, then document the binding schema, evidence, tradeoffs, and non-goals for Phase 29.
Output: mismatch tests/fixtures, report marker checks in the focused gate, and `28-ICEBERG-BINDING-REPORT.md`.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-iceberg-ref-table-binding/28-CONTEXT.md
@.planning/phases/28-iceberg-ref-table-binding/28-RESEARCH.md
@.planning/phases/28-iceberg-ref-table-binding/28-PATTERNS.md
@.planning/phases/28-iceberg-ref-table-binding/28-01-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-02-SUMMARY.md
@.planning/phases/28-iceberg-ref-table-binding/28-03-SUMMARY.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-VERIFICATION.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md
@crates/loom-iceberg-binding/src/binding_contract.rs
@scripts/iceberg-binding-test.sh
</context>

## Artifacts This Phase Produces

- Files: `crates/loom-iceberg-binding/Cargo.toml`, `crates/loom-iceberg-binding/src/lib.rs`, `crates/loom-iceberg-binding/src/binding_contract.rs`, `crates/loom-iceberg-binding/tests/dependency_boundary.rs`, `crates/loom-iceberg-binding/tests/binding_contract.rs`, `crates/loom-iceberg-binding/tests/binding_handoff.rs`, `crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs`.
- Fixtures: `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-metadata.json`, `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-loom-binding.json`, `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-source-evidence.json`, and local unsupported/rejected/mismatch/source-oracle JSON fixtures used by tests.
- Shared/gate files: `scripts/iceberg-binding-test.sh`, `scripts/mvp0-verify.sh`, `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`.
- Symbols: `IcebergBindingStatus`, `IcebergTableRefIdentity`, `IcebergBindingFacts`, `IcebergBindingEvidence`, `IcebergBindingReport`, `IcebergBindingAcceptedArtifact`, `iceberg_binding_facts_from_paths`, `source_ingress_report_from_iceberg_metadata_path`, `bind_iceberg_ref_from_paths`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable mismatch and manifest-only fail-closed matrix</name>
  <read_first>
    - `crates/loom-iceberg-binding/tests/binding_handoff.rs`
    - `crates/loom-iceberg-binding/tests/binding_contract.rs`
    - `crates/loom-iceberg-binding/src/binding_contract.rs`
    - `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-metadata.json`
    - `crates/loom-iceberg-binding/tests/fixtures/local/accepted-table-loom-binding.json`
  </read_first>
  <files>crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs, crates/loom-iceberg-binding/tests/fixtures/local/mismatch-schema-sidecar.json, crates/loom-iceberg-binding/tests/fixtures/local/mismatch-snapshot-sidecar.json, crates/loom-iceberg-binding/tests/fixtures/local/manifest-only-sidecar.json, crates/loom-iceberg-binding/tests/fixtures/local/stale-source-evidence.json, crates/loom-iceberg-binding/tests/fixtures/local/forged-oracle-evidence.json</files>
  <behavior>
    - Schema ID mismatch returns no accepted binding.
    - Snapshot ID/ref target mismatch returns no accepted binding.
    - Artifact SHA-256 mismatch returns no accepted binding.
    - Sidecar verifier-status mismatch or verifier-rejected artifact returns no accepted binding.
    - Missing source evidence, missing oracle evidence, stale source evidence, forged decoded-row oracle evidence, and manifest-only sidecar claims return no accepted binding.
    - Remote/catalog/object-store/credential-bearing metadata remains unsupported or rejected.
  </behavior>
  <action>Add `mismatch_fail_closed.rs` with focused negative tests for each D-08/D-15 mismatch dimension: schema identity, snapshot identity, table UUID/name, artifact path/hash, sidecar verifier status, live verifier rejection using malformed or mutated artifact bytes, missing source evidence, missing oracle evidence, stale source evidence JSON, forged decoded-row oracle evidence JSON, manifest-only sidecar, remote metadata location, REST catalog markers, object-store credential markers, and public route scope markers. Create fixture JSON files for reusable static sidecars and source-oracle evidence artifacts: `stale-source-evidence.json` must mismatch one or more of row count, table UUID, schema ID, snapshot ID, or artifact SHA-256; `forged-oracle-evidence.json` must include a descriptive accepted flag but fail an independent decoded-row/oracle status or identity/hash check. Generate mutated artifact bytes in temp files inside tests. Each test must assert the API returns `Err(IcebergBindingReport)` or non-accepted status, `IcebergBindingAcceptedArtifact` is not returned, artifact byte length is absent, and verifier/oracle evidence is not accepted per D-06/D-08. Do not add new source adapters, query execution, predicate pushdown, split execution, or native kernels per D-11.</action>
  <verify>
    <automated>cargo test -p loom-iceberg-binding --test mismatch_fail_closed</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p loom-iceberg-binding --test mismatch_fail_closed` passes.
    - Tests cover at minimum schema ID, snapshot ID, artifact hash, verifier status, missing source evidence, missing oracle evidence, stale source evidence JSON, forged decoded-row oracle evidence JSON, manifest-only, and remote/credential creep cases.
    - Every mismatch assertion proves no accepted bytes are returned.
  </acceptance_criteria>
  <done>The fail-closed matrix is executable and covers every required mismatch dimension.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Iceberg binding evidence report</name>
  <read_first>
    - `.planning/phases/28-iceberg-ref-table-binding/28-CONTEXT.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-RESEARCH.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-PATTERNS.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-01-SUMMARY.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-02-SUMMARY.md`
    - `.planning/phases/28-iceberg-ref-table-binding/28-03-SUMMARY.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md`
    - `.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md`
  </read_first>
  <files>.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md</files>
  <action>Write `28-ICEBERG-BINDING-REPORT.md` with sections named exactly: Executive Summary, Implemented Artifacts, Binding Schema, Accepted Unsupported Rejected Matrix, Mismatch Fail-Closed Matrix, Source Evidence, Verifier Evidence, Oracle Evidence, Dependency and API Boundary, Current-Phase Tradeoffs, Non-Goals, Release Gate Evidence, and Phase 29 Handoff. Document the standalone sidecar/reference model per D-02, bounded identity fields per D-03, descriptive-not-trust model per D-07, accepted binding rule per D-05/D-08, local fixture parser and no-default-`iceberg` dependency decision per D-09/D-10, and the current-phase tradeoffs per D-16. In Source Evidence and Oracle Evidence, name `accepted-table-source-evidence.json`, list the checked fields row count, table UUID, schema ID, snapshot ID, artifact SHA-256, source accepted status, and decoded-row/oracle accepted status, and state that sidecar accepted flags are descriptive only. In Mismatch Fail-Closed Matrix, include stale source evidence and forged decoded-row oracle evidence rows. State that `Release Gate Evidence` remains focused-gate evidence only until Plan 28-05 wires `scripts/mvp0-verify.sh` per D-13. Include explicit non-goals for StarRocks/DuckDB query surfaces, production catalogs, REST auth, object-store credentials, branch/tag mutation, snapshot lifecycle management, embedding Loom bytes, broad Iceberg type coverage, predicate pushdown, split execution, new native kernels, and full Vortex compatibility per D-10/D-11.</action>
  <verify>
    <automated>test -f .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md && rg -q "Binding Schema" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md && rg -q "Mismatch Fail-Closed Matrix" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md && rg -q "Current-Phase Tradeoffs" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Report exists at `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`.
    - Report contains all required section names.
    - Report states binding metadata is not a trust token and accepted bindings recompute hash, rerun verifier, and read/check the source-oracle evidence artifact.
    - Report records focused test command names and current pass/fail status available by the end of Plan 28-04.
    - Report lists current-phase tradeoffs and non-goals.
  </acceptance_criteria>
  <done>The Phase 28 report records binding schema, evidence, mismatch matrix, tradeoffs, and scoped handoff without making Phase 29 claims.</done>
</task>

<task type="auto">
  <name>Task 3: Expand focused gate with mismatch and report evidence checks</name>
  <read_first>
    - `scripts/iceberg-binding-test.sh`
    - `.planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
    - `crates/loom-iceberg-binding/tests/mismatch_fail_closed.rs`
    - `scripts/lance-parquet-ingress-test.sh`
  </read_first>
  <files>scripts/iceberg-binding-test.sh</files>
  <action>Update `scripts/iceberg-binding-test.sh` to run `cargo test -p loom-iceberg-binding --test mismatch_fail_closed`, check the mismatch fixture JSON files plus `accepted-table-source-evidence.json`, `stale-source-evidence.json`, and `forged-oracle-evidence.json`, and require report markers for Executive Summary, Implemented Artifacts, Binding Schema, Accepted Unsupported Rejected Matrix, Mismatch Fail-Closed Matrix, Source Evidence, Verifier Evidence, Oracle Evidence, Dependency and API Boundary, Current-Phase Tradeoffs, Non-Goals, Release Gate Evidence, and Phase 29 Handoff per D-12/D-16. Add a report language guard that fails if manifest-only, sidecar-only, metadata-only, verifier-status-only, source-evidence-only, or oracle-accepted-flag-only wording is described as accepted/success/proof/evidence without negating language. Preserve no-default-`iceberg` SDK, public-surface, catalog, credential, and query-surface guards per D-01/D-09/D-10/D-15. Do not wire into `scripts/mvp0-verify.sh` in this plan.</action>
  <verify>
    <automated>bash -n scripts/iceberg-binding-test.sh && bash scripts/iceberg-binding-test.sh && ! rg -q "iceberg-binding-test\.sh" scripts/mvp0-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - Focused gate runs mismatch tests and report marker checks.
    - Focused gate fails if the report treats manifest-only, sidecar-only, source-evidence-only, or oracle-accepted-flag-only metadata as successful proof.
    - `scripts/mvp0-verify.sh` remains unwired until Plan 28-05.
  </acceptance_criteria>
  <done>The focused gate now covers mismatch evidence and the final report while remaining outside the main release verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mismatch fixtures -> accepted report | Negative fixtures model stale or malicious metadata that must not produce bytes. |
| report wording -> downstream planning | Overstated evidence can mislead Phase 29 query-surface planning. |
| focused gate -> report acceptance | The gate verifies evidence markers before main release wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Tampering | schema/snapshot/hash mismatch | mitigate | Executable mismatch tests assert no accepted bytes or accepted evidence for every stale identity dimension per D-08/D-15. |
| T-28-04-02 | Spoofing | manifest-only and sidecar-only claims | mitigate | Negative tests and report language guard reject metadata-only success claims per D-08. |
| T-28-04-03 | Repudiation | final binding evidence report | mitigate | Report records exact supported slice, evidence commands, mismatch matrix, tradeoffs, and non-goals per D-12/D-16. |
| T-28-04-04 | Information Disclosure | object-store/catalog credential creep | mitigate | Mismatch tests and gate scans cover REST/catalog/warehouse/credential markers per D-10/D-15. |
| T-28-04-05 | Elevation of Privilege | public API and Phase 29 query-surface creep | mitigate | Report non-goals and gate scans forbid SQL/CLI/C ABI/DuckDB/StarRocks route markers per D-01. |
| T-28-04-06 | Denial of Service | over-broad compatibility scope | mitigate | Report limits coverage to local metadata, sidecar binding, and Phase 27 primitive/table artifact evidence per D-11. |
</threat_model>

<verification>
Overall plan checks:
- `cargo test -p loom-iceberg-binding --test mismatch_fail_closed`
- `cargo test -p loom-iceberg-binding --test binding_handoff`
- `test -f .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
- `rg -q "Binding Schema" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
- `rg -q "Mismatch Fail-Closed Matrix" .planning/phases/28-iceberg-ref-table-binding/28-ICEBERG-BINDING-REPORT.md`
- `bash -n scripts/iceberg-binding-test.sh && bash scripts/iceberg-binding-test.sh`
- `! rg -q "iceberg-binding-test\\.sh" scripts/mvp0-verify.sh`
</verification>

<success_criteria>
Plan 28-04 is complete when every required mismatch dimension has executable fail-closed coverage, the Phase 28 report exists with binding schema/evidence/tradeoff/non-goal sections, the focused gate checks the mismatch matrix and report markers, and the main release verifier is still unwired until final closeout.
</success_criteria>

<output>
Create `.planning/phases/28-iceberg-ref-table-binding/28-04-SUMMARY.md` when done.
</output>
