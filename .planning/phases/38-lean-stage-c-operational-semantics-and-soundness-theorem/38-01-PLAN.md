---
phase: 38-lean-stage-c-operational-semantics-and-soundness-theorem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formal/lean/LoomCore.lean
autonomous: true
requirements:
  - LINEAGE-05
must_haves:
  truths:
    - "D-38-01: Lean defines modeled execution semantics for the current Phase 37 checker surface."
    - "D-38-02: Inputs and builder outputs are abstract, proof-friendly modeled values/events, not byte-accurate Arrow buffers."
    - "D-38-03: Termination is explicit through maxRows/fuel; no unbounded semantics are added."
    - "D-38-04: failClosed is a safe terminal/failure state."
    - "D-38-12: lean formal/lean/LoomCore.lean remains the primary proof gate."
---

<objective>
Add proof-friendly Lean operational semantics for the modeled L2Core executor,
covering the current Phase 37 statement surface while preserving deterministic
Lean compilation and the existing correspondence output.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add modeled input, state, event, and terminal-status types</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>.planning/phases/38-lean-stage-c-operational-semantics-and-soundness-theorem/38-CONTEXT.md</file>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-02-SUMMARY.md</file>
    <file>formal/lean/LoomCore.lean</file>
  </read_first>
  <action>Add Lean model types for abstract input assumptions, modeled scalar/runtime state, typed builder events, row-budget/fuel accounting, and terminal status. Keep this model small: enough to state safe reads, well-typed builder events, termination within `maxRows`, and fail-closed behavior without modeling concrete Arrow buffers or source bytes.</action>
  <acceptance_criteria>
    <criterion>`rg -n "ModeledInput|ModeledState|ModeledEvent|ExecutionStatus|failClosed|modeled executor" formal/lean/LoomCore.lean` shows the modeled execution vocabulary.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement total modeled statement/body execution</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/src/l2_core.rs</file>
    <file>crates/loom-core/src/full_verifier.rs</file>
  </read_first>
  <action>Add total Lean execution functions or relations for `readInput`, `letScalar`, `appendValue`, `appendNull`, `forRange`, `cursorLoop`, and `failClosed`. The implementation may be fueled or structurally bounded, but it must terminate by construction and preserve the Phase 37 checker/correspondence definitions. Read behavior may use abstract values; it must expose enough facts to prove in-bounds access under declared capabilities.</action>
  <acceptance_criteria>
    <criterion>`rg -n "execStmt|execBody|execProgram|readInput|appendValue|appendNull|cursorLoop|forRange" formal/lean/LoomCore.lean` shows all modeled execution entry points.</criterion>
    <criterion>`lean formal/lean/LoomCore.lean` exits 0.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add modeled safety predicates and examples</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>formal/lean/LoomCore.lean</file>
    <file>.planning/phases/36-verified-lineage-contract-and-tcb-declaration/36-VERIFIED-LINEAGE-CONTRACT.md</file>
  </read_first>
  <action>Add explicit modeled safety predicates for no out-of-bounds modeled reads, well-typed builder events, termination within `maxRows`, and Arrow well-formedness by construction. Add small Lean examples showing accepted modeled execution is safe and `failClosed` terminates safely.</action>
  <acceptance_criteria>
    <criterion>`rg -n "NoOutOfBoundsRead|BuilderEventsWellTyped|TerminatesWithinMaxRows|ArrowWellFormedByConstruction|ModeledExecutionSafe" formal/lean/LoomCore.lean` shows the four safety obligations and combined predicate.</criterion>
    <criterion>`lean formal/lean/LoomCore.lean` exits 0 with no `sorry`.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
lean formal/lean/LoomCore.lean
rg -n "ModeledInput|ModeledState|ModeledEvent|ExecutionStatus|modeled executor" formal/lean/LoomCore.lean
rg -n "execStmt|execBody|execProgram|readInput|appendValue|appendNull|cursorLoop|forRange" formal/lean/LoomCore.lean
rg -n "NoOutOfBoundsRead|BuilderEventsWellTyped|TerminatesWithinMaxRows|ArrowWellFormedByConstruction|ModeledExecutionSafe" formal/lean/LoomCore.lean
! rg -n "\\bsorry\\b" formal/lean/LoomCore.lean
git diff --check
</verification>

<success_criteria>
Plan 38-01 is complete when Lean contains a total modeled operational semantics,
modeled safety predicates for the roadmap obligations, examples compile, and no
`sorry` appears.
</success_criteria>

<output>
Create `38-01-SUMMARY.md` when done.
</output>

