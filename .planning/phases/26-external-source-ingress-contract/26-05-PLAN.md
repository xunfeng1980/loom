---
phase: 26-external-source-ingress-contract
plan: 05
type: execute
wave: 5
depends_on:
  - 26-04
files_modified:
  - scripts/source-ingress-contract-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md
  - .planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md
autonomous: true
requirements:
  - PHASE-26
must_haves:
  truths:
    - "The main release gate runs the Phase 26 source-ingress contract gate after Phase 25 native hardening and before DuckDB SQL smoke."
    - "Phase 26 closeout records contract artifacts, test evidence, dependency/API guard evidence, tradeoffs, and Phase 27 Lance/Parquet handoff assumptions."
    - "ROADMAP.md and STATE.md are not edited by this plan; Phase 26 closeout stays in local phase docs/reports."
  artifacts:
    - path: scripts/source-ingress-contract-test.sh
      provides: "Complete Phase 26 source-ingress release gate"
    - path: scripts/mvp0-verify.sh
      provides: "Main release-gate wiring for Phase 26"
    - path: .planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md
      provides: "Final evidence and Phase 27 handoff report"
    - path: .planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md
      provides: "Plan 26-05 execution summary and self-check"
  key_links:
    - from: scripts/mvp0-verify.sh
      to: scripts/source-ingress-contract-test.sh
      via: "release gate invocation"
      pattern: "source-ingress-contract-test\\.sh"
    - from: .planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md
      to: .planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md
      via: "final evidence measured against contract obligations"
      pattern: "Release Gate Evidence|Phase 27 Handoff|Non-Goals"
---

<objective>
Finish the Phase 26 release gate and local closeout docs for Phase 27 handoff.

Purpose: make the external source-ingress contract part of the one-command verification path without expanding public APIs or editing global planning state in this plan.
Output: complete `source-ingress-contract-test.sh`, `mvp0-verify.sh` wiring, final report evidence, and `26-05-SUMMARY.md`.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/26-external-source-ingress-contract/26-CONTEXT.md
@.planning/phases/26-external-source-ingress-contract/26-RESEARCH.md
@.planning/phases/26-external-source-ingress-contract/26-PATTERNS.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md
@.planning/phases/25-native-equivalence-cache-and-fallback-hardening/25-NATIVE-HARDENING-REPORT.md
@scripts/source-ingress-contract-test.sh
@scripts/mvp0-verify.sh
</context>

<source_audit>
SOURCE | ID | Requirement | Plan | Status | Notes
GOAL | PHASE-26-GOAL | Define a source-neutral ingress contract for facts, diagnostics, support classification, emission disposition, dependency isolation, verifier-routed LMC1/LMP1/LMT1 emission, oracle evidence, and fail-closed unsupported/rejected behavior before new sources | 26-01..26-05 | COVERED | Split into contract crate, Vortex mapping, verifier/oracle handoff, report/creep guard, and release gate.
REQ | PHASE-26 | Roadmap phase requirement for External Source Ingress Contract | 26-01..26-05 | COVERED | Every plan declares PHASE-26.
RESEARCH | Dependency-light generic crate | Create `loom-source-ingress` with no source SDK dependencies; keep `loom-core`, `loom-ffi`, DuckDB, and generic contract source-specific-dependency free | 26-01, 26-04, 26-05 | COVERED | Final gate checks the boundary.
RESEARCH | Vortex mapping without API breakage | Map existing Vortex reader facts, coverage, diagnostics, and report vocabulary into the generic contract while preserving current Vortex APIs | 26-02 | COVERED | Final gate runs Vortex mapping tests.
RESEARCH | Artifact verifier and oracle handoff | Accepted single-column/table cases require verifier-accepted LMC1 plus oracle evidence; unsupported valid and malformed rejected cases fail closed | 26-03 | COVERED | Final gate runs handoff and verifier tests.
RESEARCH | Reviewer docs and non-goals | Final contract/report must list generic model, Vortex mapping, adapter obligations, non-goals, tradeoffs, and Phase 27 handoff | 26-04, 26-05 | COVERED | This plan finalizes report evidence and summary.
RESEARCH | Release gate | Add `scripts/source-ingress-contract-test.sh` and wire it into `scripts/mvp0-verify.sh` after Phase 25 and before DuckDB smoke | 26-04, 26-05 | COVERED | This plan wires the main gate.
CONTEXT | Contract Shape | Generic source identity, facts, diagnostics, support, emission, oracle, and lowering vocabulary; no Vortex public names in generic API | 26-01, 26-02 | COVERED | Gate checks generic markers and neutrality.
CONTEXT | Dependency Boundary | Source-specific SDKs stay in source-specific crates; no Lance/Parquet/Iceberg/MCAP/Zarr/LeRobot/object-store deps in generic/core/ffi/DuckDB | 26-01, 26-04, 26-05 | COVERED | Gate checks cargo trees and manifests.
CONTEXT | Emission And Verification | Accepted emission is verifier-routed LMC1 wrapping LMP1 or LMT1 only; unsupported valid emits no bytes; rejected has no trusted facts | 26-03 | COVERED | Gate runs source_ingress_handoff tests.
CONTEXT | Oracle And Equivalence Evidence | Adapter declares oracle strategy and evidence; Vortex source-native scan is the first real adapter proof | 26-03 | COVERED | Gate runs oracle evidence checks.
CONTEXT | Diagnostics And Reports | Stable code/path/message diagnostics and final report required | 26-01, 26-04, 26-05 | COVERED | Summary and report include self-checks.
CONTEXT | the agent's Discretion | Choose crate home and use adapters only where they reduce duplication | 26-01, 26-02 | COVERED | Handoff records new crate choice and avoids plugin framework.
CONTEXT | Deferred Ideas | Lance/Parquet implementation, Iceberg, MCAP/Zarr/LeRobot, host-engine integration, object-store credentials, predicate pushdown, parallel split execution, public SQL/API changes, and new native kernels | none | EXCLUDED | Summary must confirm these remain deferred.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Phase 26 source-ingress release gate</name>
  <files>scripts/source-ingress-contract-test.sh</files>
  <action>Finalize `scripts/source-ingress-contract-test.sh` so it runs all Phase 26 focused checks: `cargo test -p loom-source-ingress`, `cargo test -p loom-vortex-ingress --test source_ingress_contract`, `cargo test -p loom-vortex-ingress --test source_ingress_handoff`, existing Vortex reader/single-column/table tests, and `cargo test -p loom-core --test artifact_verifier`. Keep dependency/API creep checks from Plan 26-04. Add report marker checks for Release Gate Evidence, Current-Phase Tradeoffs, Non-Goals, and Phase 27 Handoff. Do not add package installs, Lance/Parquet/Iceberg/MCAP/Zarr/LeRobot implementation, object-store credentials, host-engine integration, predicate pushdown, parallel split execution, public SQL/API changes, or new native kernels.</action>
  <verify>
    <automated>bash -n scripts/source-ingress-contract-test.sh && bash scripts/source-ingress-contract-test.sh</automated>
  </verify>
  <done>The Phase 26 gate passes as a standalone release gate and covers contract, mapping, verifier/oracle handoff, docs, and dependency/API boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 26 into the main MVP0 release gate</name>
  <files>scripts/mvp0-verify.sh</files>
  <action>Add the Phase 26 source-ingress contract gate immediately after `scripts/native-hardening-test.sh` and before `scripts/duckdb-smoke-test.sh`. Preserve all prior gates and inherited environment behavior such as `LOOM_ALLOW_NATIVE_TOOL_SKIP`. Add an order assertion in comments or script logic consistent with existing gate style: Phase 24 DuckDB native integration, Phase 25 native hardening, Phase 26 source ingress contract, then DuckDB smoke. Do not add public SQL functions or host-engine source-ingress behavior.</action>
  <verify>
    <automated>bash -n scripts/mvp0-verify.sh && bash -n scripts/source-ingress-contract-test.sh && rg -q "source-ingress-contract-test\\.sh" scripts/mvp0-verify.sh && python3 - <<'PY'
from pathlib import Path
text = Path("scripts/mvp0-verify.sh").read_text()
order = [
    "scripts/duckdb-native-integration-test.sh",
    "scripts/native-hardening-test.sh",
    "scripts/source-ingress-contract-test.sh",
    "scripts/duckdb-smoke-test.sh",
]
positions = [text.index(item) for item in order]
assert positions == sorted(positions), positions
PY
bash scripts/source-ingress-contract-test.sh</automated>
  </verify>
  <done>`mvp0-verify.sh` invokes `source-ingress-contract-test.sh` in the correct order without weakening earlier gates.</done>
</task>

<task type="auto">
  <name>Task 3: Close local Phase 26 report and summary for Phase 27 handoff</name>
  <files>.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md, .planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md</files>
  <action>Update the report with final release-gate evidence and create `26-05-SUMMARY.md` following the standard per-plan summary style. Include completed artifacts, commands run, self-check, dependency/API boundary results, current-phase tradeoffs, and Phase 27 handoff assumptions. State that Phase 27 Lance/Parquet adapters must implement the generic contract, declare oracle strategy, emit only verifier-accepted `LMC1`/`LMP1` or `LMC1`/`LMT1`, keep source SDK dependencies outside `loom-core`/`loom-ffi`/DuckDB/generic contract, and preserve unsupported/rejected fail-closed behavior. Do not edit `.planning/ROADMAP.md` or `.planning/STATE.md` in this plan.</action>
  <verify>
    <automated>bash scripts/source-ingress-contract-test.sh && rg -q "Self-Check: PASSED" .planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md && rg -q "Phase 27 Handoff" .planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md</automated>
  </verify>
  <done>Phase 26 has local closeout evidence and a clear Phase 27 handoff without global planning-state edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| focused Phase 26 gate -> main release gate | Source-ingress contract evidence becomes part of the one-command release path. |
| final report -> Phase 27 implementation | Downstream source adapters will use the report as a contract and could inherit overclaims. |
| release script -> public API boundary | Release wiring must not accidentally add public source/query/native controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Tampering | mvp0 release gate order | mitigate | Wire Phase 26 after Phase 25 and before DuckDB smoke; syntax-check both scripts and run the focused gate. |
| T-26-05-02 | Repudiation | Phase 26 summary/report | mitigate | Record exact commands, self-check, evidence matrix, tradeoffs, non-goals, and Phase 27 assumptions. |
| T-26-05-03 | Information Disclosure | public API/source controls | mitigate | Gate blocks object-store credentials, source-specific public SQL/API, ArrowArrayStream exposure, predicate pushdown, parallel split execution, and native-kernel markers. |
| T-26-SC | Tampering | package installs/dependencies | mitigate | No package installs are planned; final report and gate must state no new external packages/source SDKs were added to generic/core/ffi/DuckDB surfaces. |
</threat_model>

<verification>
Run:
- bash -n scripts/source-ingress-contract-test.sh && bash -n scripts/mvp0-verify.sh
- bash scripts/source-ingress-contract-test.sh
- LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh
</verification>

<success_criteria>
Phase 26 is release-gated through the main verifier path and locally closed with bounded reports that hand Phase 27 a generic, verifier-routed source-ingress contract.
</success_criteria>

<output>
Create `.planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md` when done.
</output>
