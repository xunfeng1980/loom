---
phase: 26-external-source-ingress-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - crates/loom-vortex-ingress/Cargo.toml
  - crates/loom-vortex-ingress/src/lib.rs
  - crates/loom-vortex-ingress/src/source_contract.rs
  - crates/loom-vortex-ingress/tests/source_ingress_contract.rs
autonomous: true
requirements:
  - PHASE-26
must_haves:
  truths:
    - "Existing Vortex reader facts and coverage map into source-neutral facts without removing or renaming existing Vortex APIs."
    - "Vortex remains the first reference adapter, but the generic `loom-source-ingress` API exposes no Vortex-specific public vocabulary."
    - "Reader support, emission kind, emission disposition, and lowering disposition are preserved exactly across the Vortex-to-generic mapping."
  artifacts:
    - path: crates/loom-vortex-ingress/src/source_contract.rs
      provides: "Vortex-to-source-ingress mapping layer"
    - path: crates/loom-vortex-ingress/tests/source_ingress_contract.rs
      provides: "Mapping tests for supported, unsupported, and rejected Vortex reader facts"
    - path: crates/loom-vortex-ingress/Cargo.toml
      provides: "Adapter dependency on the generic contract crate"
  key_links:
    - from: crates/loom-vortex-ingress/src/source_contract.rs
      to: crates/loom-source-ingress/src/lib.rs
      via: "mapping VortexReaderFacts and VortexEncodingCoverage into SourceFacts and SourceCoverage"
      pattern: "SourceFacts|SourceCoverage|SourceIngressReport"
---

<objective>
Map the existing Vortex ingress/reader facts into the generic source-ingress contract.

Purpose: prove the new contract is source-neutral while preserving the working Vortex reader API and evidence surface.
Output: a narrow Vortex adapter mapping module and focused tests for facts, coverage, diagnostics, and API compatibility.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/26-external-source-ingress-contract/26-CONTEXT.md
@.planning/phases/26-external-source-ingress-contract/26-RESEARCH.md
@.planning/phases/26-external-source-ingress-contract/26-PATTERNS.md
@.planning/phases/18-complete-vortex-reader/18-READER-CONTRACT.md
@.planning/phases/21-expanded-vortex-encoding-coverage/21-COVERAGE-MATRIX.md
@crates/loom-source-ingress/src/lib.rs
@crates/loom-vortex-ingress/src/lib.rs
@crates/loom-vortex-ingress/tests/reader_facts_contract.rs
</context>

<source_audit>
SOURCE | ID | Requirement | Plan | Status | Notes
GOAL | PHASE-26-GOAL | Define a source-neutral ingress contract for facts, diagnostics, support classification, emission disposition, dependency isolation, verifier-routed LMC1/LMP1/LMT1 emission, oracle evidence, and fail-closed unsupported/rejected behavior before new sources | 26-01..26-05 | COVERED | Split into contract crate, Vortex mapping, verifier/oracle handoff, report/creep guard, and release gate.
REQ | PHASE-26 | Roadmap phase requirement for External Source Ingress Contract | 26-01..26-05 | COVERED | Every plan declares PHASE-26.
RESEARCH | Dependency-light generic crate | Create `loom-source-ingress` with no source SDK dependencies; keep `loom-core`, `loom-ffi`, DuckDB, and generic contract source-specific-dependency free | 26-01, 26-04, 26-05 | COVERED | Plan 26-01 chooses the new crate per research recommendation.
RESEARCH | Vortex mapping without API breakage | Map existing Vortex reader facts, coverage, diagnostics, and report vocabulary into the generic contract while preserving current Vortex APIs | 26-02 | COVERED | Vortex remains the reference adapter, not the generic vocabulary.
RESEARCH | Artifact verifier and oracle handoff | Accepted single-column/table cases require verifier-accepted LMC1 plus oracle evidence; unsupported valid and malformed rejected cases fail closed | 26-03 | COVERED | Plan 26-03 owns positive/negative contract handoff tests.
RESEARCH | Reviewer docs and non-goals | Final contract/report must list generic model, Vortex mapping, adapter obligations, non-goals, tradeoffs, and Phase 27 handoff | 26-04, 26-05 | COVERED | Reports record current-phase tradeoffs explicitly.
RESEARCH | Release gate | Add `scripts/source-ingress-contract-test.sh` and wire it into `scripts/mvp0-verify.sh` after Phase 25 and before DuckDB smoke | 26-04, 26-05 | COVERED | Plan 26-05 wires the main gate without editing ROADMAP.md or STATE.md.
CONTEXT | Contract Shape | Generic source identity, facts, diagnostics, support, emission, oracle, and lowering vocabulary; no Vortex public names in generic API | 26-01, 26-02 | COVERED | Plan 26-02 maps Vortex into Source* names.
CONTEXT | Dependency Boundary | Source-specific SDKs stay in source-specific crates; no Lance/Parquet/Iceberg/MCAP/Zarr/LeRobot/object-store deps in generic/core/ffi/DuckDB | 26-01, 26-04, 26-05 | COVERED | Vortex dependencies remain only in `loom-vortex-ingress`.
CONTEXT | Emission And Verification | Accepted emission is verifier-routed LMC1 wrapping LMP1 or LMT1 only; unsupported valid emits no bytes; rejected has no trusted facts | 26-03 | COVERED | Plan 26-02 maps facts only; Plan 26-03 accepts artifacts.
CONTEXT | Oracle And Equivalence Evidence | Adapter declares oracle strategy and evidence; Vortex source-native scan is the first real adapter proof | 26-03 | COVERED | Plan 26-02 prepares Vortex facts for oracle evidence.
CONTEXT | Diagnostics And Reports | Stable code/path/message diagnostics and final report required | 26-01, 26-04, 26-05 | COVERED | Vortex diagnostic codes map into source-neutral families.
CONTEXT | the agent's Discretion | Choose crate home and use adapters only where they reduce duplication | 26-01, 26-02 | COVERED | Mapping module is narrow; no plugin framework.
CONTEXT | Deferred Ideas | Lance/Parquet implementation, Iceberg, MCAP/Zarr/LeRobot, host-engine integration, object-store credentials, predicate pushdown, parallel split execution, public SQL/API changes, and new native kernels | none | EXCLUDED | Deferred ideas must not appear as implementation tasks.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Vortex-to-source facts mapping</name>
  <files>crates/loom-vortex-ingress/Cargo.toml, crates/loom-vortex-ingress/src/lib.rs, crates/loom-vortex-ingress/src/source_contract.rs</files>
  <behavior>
    - Test 1: supported non-null primitive Vortex facts map to generic source facts with accepted reader support, `LMP1`, `canonical-raw`, and `production-lowering-supported`.
    - Test 2: supported primitive struct/table facts map to generic source facts with accepted reader support, `LMT1`, `canonical-table`, and `production-lowering-supported`.
    - Test 3: UTF-8 unsupported-valid facts map to unsupported support, emission `none`, and lowering `fail-closed/deferred`.
  </behavior>
  <action>Add `loom-source-ingress` as a local path dependency of `loom-vortex-ingress`. Create a narrow `source_contract` module that converts `VortexReaderFacts`, `VortexEncodingCoverage`, `VortexReaderDiagnostic`, and `VortexIngressReport` into the generic source contract. Export adapter helpers such as `source_facts_from_vortex_buffer`, `source_facts_from_vortex_path`, and conversion helpers from `lib.rs` while leaving existing `reader_facts_from_vortex_buffer`, `inspect_vortex_buffer`, and `emit_supported_lmc1_from_vortex_buffer` signatures intact. This task maps facts and reader support only; Plan 26-03 owns verifier-accepted artifact handoff.</action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress --test source_ingress_contract --no-run</automated>
  </verify>
  <done>Vortex reader facts can be viewed through generic Source* facts without changing existing Vortex APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve diagnostics and rejected semantics</name>
  <files>crates/loom-vortex-ingress/src/source_contract.rs, crates/loom-vortex-ingress/tests/source_ingress_contract.rs</files>
  <behavior>
    - Test 1: Vortex open/read/schema/layout/support/conversion/verification/oracle diagnostics map to source-neutral diagnostic families with stable code/path/message fields.
    - Test 2: malformed Vortex buffers map to rejected source reports with diagnostics and no facts.
    - Test 3: unsupported valid Vortex inputs keep facts but cannot expose accepted artifact or oracle evidence.
  </behavior>
  <action>Add mapping tests that copy the existing Phase 18 fail-closed split: rejected input produces diagnostics and no trusted facts; valid unsupported input keeps facts and emission `none`. Use source-neutral diagnostic families while preserving original Vortex diagnostic detail in optional message/detail fields only. Do not expose raw Vortex Rust types, sessions, array handles, file handles, or lifetimes in any generic source-ingress type.</action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress --test source_ingress_contract</automated>
  </verify>
  <done>Generic diagnostics preserve stable source-neutral code/path/message semantics and reject malformed sources without facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard existing Vortex API compatibility and generic neutrality</name>
  <files>crates/loom-vortex-ingress/tests/source_ingress_contract.rs, crates/loom-vortex-ingress/tests/reader_facts_contract.rs</files>
  <behavior>
    - Test 1: existing Vortex reader contract tests still pass unchanged.
    - Test 2: existing single-column/table emission tests still compile against current Vortex API names.
    - Test 3: `loom-source-ingress` source files contain no Vortex-specific public type names.
  </behavior>
  <action>Add API compatibility assertions by importing both old Vortex-specific functions and new source-contract helpers in the new test file. Do not rename or remove old Vortex types; Phase 26 is an abstraction layer, not a breaking API cleanup. Add an automated check that the generic crate does not contain `Vortex`/`vortex` identifiers.</action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress --test reader_facts_contract && cargo test -p loom-vortex-ingress --test single_column_to_loom --test table_to_loom && ! rg -n "Vortex|vortex" crates/loom-source-ingress/src crates/loom-source-ingress/tests</automated>
  </verify>
  <done>Existing Vortex APIs remain usable and the generic contract stays source-neutral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VortexReaderFacts -> SourceFacts | Source-specific facts are translated into generic reviewer-visible data. |
| Vortex diagnostics -> SourceDiagnostic | Source-specific error details cross into source-neutral diagnostic families. |
| Vortex API -> generic API | The adapter must not break current Vortex callers or leak Vortex names into the generic crate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | Vortex-to-source mapping | mitigate | Mapping tests assert support/emission/lowering strings exactly match Phase 18/21 vocabulary. |
| T-26-02-02 | Information Disclosure | generic SourceFacts | mitigate | Public generic types contain only Loom-owned strings/primitive fields; no Vortex handles or SDK types. |
| T-26-02-03 | Repudiation | diagnostics mapping | mitigate | Preserve source-neutral code family plus original path/message in tests for accepted, unsupported, and rejected cases. |
| T-26-SC | Tampering | package installs/dependencies | mitigate | No package installs are planned; the only new dependency is the local `loom-source-ingress` crate from `loom-vortex-ingress`. |
</threat_model>

<verification>
Run:
- cargo test -p loom-vortex-ingress --test source_ingress_contract
- cargo test -p loom-vortex-ingress --test reader_facts_contract
- cargo test -p loom-vortex-ingress --test single_column_to_loom --test table_to_loom
- ! rg -n "Vortex|vortex" crates/loom-source-ingress/src crates/loom-source-ingress/tests
</verification>

<success_criteria>
The existing Vortex reader is mapped into the generic source-ingress contract without API breakage, dependency leakage, or Vortex vocabulary in the generic crate.
</success_criteria>

<output>
Create `.planning/phases/26-external-source-ingress-contract/26-02-SUMMARY.md` when done.
</output>
