---
phase: 23
plan: 23-05
title: Backend release gate, report, docs, and Phase 24 handoff
type: execute
wave: 5
depends_on:
  - 23-04
files_modified:
  - scripts/production-backend-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/23-production-native-backend-implementation/23-BACKEND-REPORT.md
  - .planning/phases/23-production-native-backend-implementation/23-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
---

<objective>
Close Phase 23 with a repeatable backend gate, final report, public/planning
documentation, and a precise Phase 24 DuckDB integration handoff.
</objective>

<context>
Phase 23 is complete only when backend behavior is documented, release-gated, and
ready for a host adapter. Phase 24 should consume a backend report and stable
diagnostics, not rediscover backend policy inside DuckDB.
</context>

<threat_model>
- Threat: backend success is local-only and not wired into the release gate.
  Mitigation: add `scripts/production-backend-test.sh` and call it from
  `scripts/mvp0-verify.sh`.
- Threat: docs claim ABI stability or broad native support too early.
  Mitigation: final report must list supported kernels, deferred paths, and
  unfrozen ABI status.
- Threat: Phase 24 lacks a clear adapter contract.
  Mitigation: handoff maps runtime/backend concepts to DuckDB lifecycle names.
</threat_model>

<tasks>
  <task id="1" type="execute">
    <title>Add the production backend gate script</title>
    <read_first>
      <file>scripts/mvp0-verify.sh</file>
      <file>scripts/melior-jit-test.sh</file>
      <file>scripts/production-native-lowering-test.sh</file>
    </read_first>
    <files>scripts/production-backend-test.sh, scripts/mvp0-verify.sh</files>
    <action>
      Add `scripts/production-backend-test.sh` to run Phase 23 focused tests,
      strict/skip toolchain checks, ODS manifest checks, backend pipeline tests,
      and JIT equivalence tests. Wire it into `scripts/mvp0-verify.sh`.
    </action>
    <verify>
      <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 scripts/production-backend-test.sh</automated>
      <automated>LOOM_ALLOW_NATIVE_TOOL_SKIP=1 scripts/mvp0-verify.sh</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Backend gate fails on missing focused tests or backend diagnostics drift.</criterion>
      <criterion>Toolchain skip remains explicit through `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`.</criterion>
      <criterion>Top-level release gate invokes the backend gate.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <title>Write final backend report and summary</title>
    <read_first>
      <file>.planning/phases/23-production-native-backend-implementation/23-BACKEND-CONTRACT.md</file>
      <file>.planning/phases/23-production-native-backend-implementation/23-RESEARCH.md</file>
      <file>.planning/phases/22-host-native-runtime-abi-and-execution-policy/22-RUNTIME-ABI-REPORT.md</file>
    </read_first>
    <files>.planning/phases/23-production-native-backend-implementation/23-BACKEND-REPORT.md, .planning/phases/23-production-native-backend-implementation/23-SUMMARY.md</files>
    <action>
      Document the implemented backend surface, supported kernels, ODS evidence,
      pipeline/JIT evidence, strict-toolchain behavior, diagnostics, cache identity
      fields, cancellation support, open risks, and explicit non-goals.
    </action>
    <verify>
      <automated>rg -n "RuntimePlan|RuntimeCacheKey|ODS|LLVM|JIT|Cancellation|Backend Identity|Unfrozen|DuckDB handoff" .planning/phases/23-production-native-backend-implementation/23-BACKEND-REPORT.md .planning/phases/23-production-native-backend-implementation/23-SUMMARY.md</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Report states public C ABI remains unfrozen.</criterion>
      <criterion>Report lists supported and deferred native kernel paths.</criterion>
      <criterion>Report includes exact verification commands and outcomes.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <title>Update roadmap/state for Phase 24 handoff</title>
    <read_first>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/STATE.md</file>
      <file>.planning/phases/23-production-native-backend-implementation/23-BACKEND-REPORT.md</file>
    </read_first>
    <files>.planning/ROADMAP.md, .planning/STATE.md</files>
    <action>
      Mark Phase 23 complete and update Phase 24 to consume the backend report.
      Preserve Phase 24 as DuckDB adapter work over the runtime/backend contract,
      not a second ABI or backend implementation phase.
    </action>
    <verify>
      <automated>rg -n "Phase 23 complete|Phase 24|DuckDB|adapter|runtime/backend|loom_runtime.h" .planning/ROADMAP.md .planning/STATE.md</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Phase 24 handoff names DuckDB bind/init/local-init/function mapping to runtime/backend lifecycle.</criterion>
      <criterion>Phase 25 remains cache/fallback hardening, not Phase 23 spillover.</criterion>
      <criterion>Planning docs do not claim arbitrary Vortex or stable ABI completion.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<must_haves>
- `scripts/production-backend-test.sh` is wired into the release gate.
- Final report records backend identity, cancellation, strict toolchain, and
  supported kernel evidence.
- Phase 24 handoff is concrete and host-adapter-shaped.
</must_haves>

<verification>
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 scripts/production-backend-test.sh`
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
Phase 23 is release-gated, documented, and ready for Phase 24 DuckDB native
integration.
</success_criteria>
