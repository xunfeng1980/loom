---
phase: 18
plan: 18-05
title: CLI report, release gate, and closeout docs
type: execute
wave: 5
depends_on:
  - 18-04
files_modified:
  - crates/loom-cli/src/main.rs
  - scripts/complete-vortex-reader-test.sh
  - scripts/mvp0-verify.sh
  - README.md
  - README-zh.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/18-complete-vortex-reader/18-READER-REPORT.md
  - .planning/phases/18-complete-vortex-reader/18-SUMMARY.md
autonomous: true
requirements: []
---

<objective>
Close Phase 18 with reviewer-visible complete-reader reports, a release-gated test script, public/planning documentation, and explicit handoff to Phase 19 solver-backed full artifact verifier work.
</objective>

<context>
By this point Phase 18 should have complete reader facts, recursive inspection, single-column conversion, and table conversion. The closeout must prove the boundary is usable from the CLI and release gate without overclaiming arbitrary Vortex support, solver discharge, production native lowering, or host-engine native execution.
</context>

<threat_model>
- Threat: docs claim complete support for all Vortex files.
  Mitigation: final report distinguishes complete reader boundary from finite accepted emission matrix.
- Threat: release gate misses unsupported/malformed cases.
  Mitigation: add a Phase 18 script with accepted, unsupported, malformed, CLI, artifact verifier, and dependency-boundary checks.
- Threat: Phase 19 starts without usable facts.
  Mitigation: final report includes the exact facts/obligations Phase 19 should consume.
- Threat: README becomes command-heavy.
  Mitigation: README updates stay concise and feature-focused.
</threat_model>

<tasks>
  <task id="1" type="execute">
    <title>Finalize CLI complete-reader reporting</title>
    <read_first>
      <file>crates/loom-cli/src/main.rs</file>
      <file>ingress/loom-vortex-ingress/src/lib.rs</file>
      <file>.planning/phases/18-complete-vortex-reader/18-READER-CONTRACT.md</file>
    </read_first>
    <files>crates/loom-cli/src/main.rs</files>
    <action>
      Ensure `loom ingest-vortex --inspect` prints deterministic complete-reader facts: status, row count, root dtype, layout tree or layout count, segment facts summary, statistics presence, support status, emission kind, artifact verifier status where applicable, and diagnostics. Ensure `loom ingest-vortex --emit-loom` writes supported `LMC1`/`LMT1` artifacts and fails closed for unsupported shapes.
    </action>
    <verify>
      <automated>cargo run --bin loom -- ingest-vortex --inspect fixtures/vortex/int32-flat.vortex</automated>
      <automated>cargo run --bin loom -- --help | rg -q "ingest-vortex"</automated>
      <automated>cargo test -p loom-cli</automated>
    </verify>
    <acceptance_criteria>
      <criterion>CLI inspect output includes reader facts and support/emission status.</criterion>
      <criterion>CLI emit writes supported artifacts and rejects unsupported shapes without output.</criterion>
      <criterion>CLI diagnostics are stable and include code/path/message.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <title>Add complete Vortex reader release gate</title>
    <read_first>
      <file>scripts/vortex-ingress-test.sh</file>
      <file>scripts/artifact-verifier-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>ingress/loom-vortex-ingress/tests/single_column_to_loom.rs</file>
      <file>ingress/loom-vortex-ingress/tests/table_to_loom.rs</file>
    </read_first>
    <files>scripts/complete-vortex-reader-test.sh, scripts/mvp0-verify.sh</files>
    <action>
      Add `scripts/complete-vortex-reader-test.sh` with `set -euo pipefail`. The script must check Phase 18 docs, run `cargo test -p loom-vortex-ingress`, run focused reader-facts/single-column/table tests, generate deterministic real Vortex fixtures, run CLI inspect/emit, verify emitted artifacts through `loom verify-artifact` or Rust artifact verifier tests, run malformed/unsupported negative checks, and call dependency-boundary guards. Wire it into `scripts/mvp0-verify.sh` after the Phase 17 artifact verifier gate or after the Phase 15 ingress gate, whichever keeps gate ordering clearest.
    </action>
    <verify>
      <automated>bash scripts/complete-vortex-reader-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>`scripts/complete-vortex-reader-test.sh` exists and exits 0.</criterion>
      <criterion>`scripts/mvp0-verify.sh` invokes the Phase 18 gate.</criterion>
      <criterion>Gate covers accepted, unsupported, malformed, CLI, artifact verifier, and dependency-boundary cases.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <title>Write final report and public/planning docs</title>
    <read_first>
      <file>.planning/phases/18-complete-vortex-reader/18-RESEARCH.md</file>
      <file>.planning/phases/18-complete-vortex-reader/18-CONTEXT.md</file>
      <file>.planning/phases/18-complete-vortex-reader/18-READER-CONTRACT.md</file>
      <file>README.md</file>
      <file>README-zh.md</file>
      <file>.planning/PROJECT.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/STATE.md</file>
    </read_first>
    <files>README.md, README-zh.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/18-complete-vortex-reader/18-READER-REPORT.md, .planning/phases/18-complete-vortex-reader/18-SUMMARY.md</files>
    <action>
      Write `18-READER-REPORT.md` with sections named `Scope`, `Complete Reader Boundary`, `Facts Model`, `Supported Emission Matrix`, `Artifact Verifier Handoff`, `Oracle Evidence`, `CLI and Release Gate`, `Commands Run`, `Deferred Work`, and `Phase 19 Handoff`. Write `18-SUMMARY.md` with shipped files, commands, deviations, residual risks, and `Self-Check: PASSED` only if final verification passed. Update README/README-zh concisely to say MVP1 has reached Phase 18 complete-reader boundary if Phase 18 is complete. Update PROJECT/ROADMAP/STATE consistently.
    </action>
    <verify>
      <automated>rg -n "Complete Reader Boundary|Facts Model|Supported Emission Matrix|Artifact Verifier Handoff|Oracle Evidence|Phase 19 Handoff|Self-Check" .planning/phases/18-complete-vortex-reader/18-READER-REPORT.md .planning/phases/18-complete-vortex-reader/18-SUMMARY.md</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Final report states complete reader boundary, not arbitrary Vortex support.</criterion>
      <criterion>Final report states solver-backed verification remains Phase 19.</criterion>
      <criterion>ROADMAP progress table marks Phase 18 `5/5 | Complete | 2026-06-08` only after all gates pass.</criterion>
      <criterion>STATE completed phases increments to `18 / 26` after the later Phase 21 insertion and only after all gates pass.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="execute">
    <title>Run final Phase 18 verification</title>
    <read_first>
      <file>scripts/complete-vortex-reader-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>.planning/phases/18-complete-vortex-reader/18-READER-REPORT.md</file>
    </read_first>
    <files>.planning/phases/18-complete-vortex-reader/18-READER-REPORT.md, .planning/phases/18-complete-vortex-reader/18-SUMMARY.md</files>
    <action>
      Run and record final verification commands: `cargo test --workspace`, `cargo test -p loom-vortex-ingress`, `cargo test -p loom-core --test artifact_verifier`, `bash scripts/complete-vortex-reader-test.sh`, `bash scripts/mvp0-verify.sh`, and `git diff --check`. Record optional Phase 16 MLIR/JIT skips accurately if inherited by the full gate.
    </action>
    <verify>
      <automated>cargo test --workspace</automated>
      <automated>cargo test -p loom-vortex-ingress</automated>
      <automated>cargo test -p loom-core --test artifact_verifier</automated>
      <automated>bash scripts/complete-vortex-reader-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>All final verification commands exit 0.</criterion>
      <criterion>`18-READER-REPORT.md` records exact commands run.</criterion>
      <criterion>Optional toolchain skips are recorded as skips, not production passes.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<must_haves>
- Public docs stay concise and feature-focused.
- Final report clearly distinguishes complete-reader boundary from arbitrary Vortex support.
- Phase 18 gate is wired into the one-command release gate.
- Phase 19 handoff names solver-backed full artifact verifier inputs.
</must_haves>

<verification>
- `cargo test --workspace`
- `cargo test -p loom-vortex-ingress`
- `cargo test -p loom-core --test artifact_verifier`
- `bash scripts/complete-vortex-reader-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Phase 18 has complete-reader facts, supported single-column/table emissions, CLI visibility, release-gate coverage, and a Phase 19 solver handoff.
</success_criteria>

## Artifacts this plan produces

| Symbol / File | Kind | Location |
|---|---|---|
| `scripts/complete-vortex-reader-test.sh` | release gate | `scripts/` |
| `18-READER-REPORT.md` | final report | `.planning/phases/18-complete-vortex-reader/` |
| `18-SUMMARY.md` | phase summary | `.planning/phases/18-complete-vortex-reader/` |
