---
phase: 10-additional-l2-kernels-and-numeric-compression-coverage
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/loom-fixtures/src/oracle.rs
  - crates/loom-fixtures/src/vortex_reader.rs
  - crates/loom-fixtures/tests/alp_roundtrip.rs
  - crates/loom-fixtures/tests/descriptor_roundtrip.rs
  - crates/loom-ffi/tests/roundtrip.rs
  - crates/loom-ffi/tests/buffer_layout.rs
autonomous: true
requirements: [COV-01]
must_haves:
  truths:
    - "D-10: use dual oracle strategy"
    - "D-11: prefer exact bit equality; fixed tolerance only if documented"
    - "D-13: cover normal decimals, negatives, zero, repeats, nulls"
    - "D-14: NaN/Inf/subnormal edge suite is out of scope"
    - "Vortex 0.74.0 lacks an exposed ALP array; use Vortex primitive float oracle plus synthetic ALP params"
  artifacts:
    - path: "crates/loom-fixtures/tests/alp_roundtrip.rs"
      provides: "ALP Float32/Float64 oracle and synthetic fixture tests"
      contains: "alp"
---

<objective>
Add deterministic ALP Float32/Float64 fixture coverage using synthetic ALP params and a Vortex-native primitive float oracle for row-value truth.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Float32/Float64 oracle helpers</name>
  <files>crates/loom-fixtures/src/oracle.rs, crates/loom-fixtures/src/vortex_reader.rs</files>
  <read_first>
    - crates/loom-fixtures/src/oracle.rs
    - crates/loom-fixtures/src/vortex_reader.rs
    - .planning/phases/10-additional-l2-kernels-and-numeric-compression-coverage/10-RESEARCH.md
  </read_first>
  <action>
    Add Vortex primitive Float32 and Float64 oracle helpers that execute Vortex primitive arrays through Vortex's own execution path and return `(Vec<f32>, Vec<bool>)` / `(Vec<f64>, Vec<bool>)`, where the boolean vector is true for null. Do not introduce file-backed Vortex APIs and do not expose Vortex types from `loom-core`.
  </action>
  <verify>
    <automated>cargo test -p loom-fixtures oracle</automated>
  </verify>
  <acceptance_criteria>
    - Float32 and Float64 oracle helpers preserve null flags.
    - Existing i32/u32/bool/utf8 oracle tests still pass.
    - `rg -n 'vortex_file|vortex-file|\\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures` finds no forbidden file-backed APIs.
  </acceptance_criteria>
  <done>Vortex-native primitive float oracle is available for Phase 10 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add ALP synthetic known-value fixtures</name>
  <files>crates/loom-fixtures/tests/alp_roundtrip.rs</files>
  <read_first>
    - crates/loom-core/src/alp_params.rs
    - crates/loom-core/src/l2_kernel_registry.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-fixtures/src/oracle.rs
  </read_first>
  <action>
    Create ALP roundtrip tests for Float32 and Float64. Build small `AlpParams` fixtures with finite decimals, negatives, zero, repeated values, and nulls. Decode through `loom_core::decode_layout_to_array_data` and compare to both synthetic expected values and Vortex primitive oracle rows. Use exact equality for chosen representable values; if any value requires tolerance, use one fixed helper with a comment naming the reason and fixture.
  </action>
  <verify>
    <automated>cargo test -p loom-fixtures alp</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover Float32 and Float64 ALP kernel escapes.
    - Tests cover at least one null in each type.
    - Tests cover negative, zero, repeated, and normal decimal values.
    - Test comments explicitly state that Vortex 0.74.0 has no exposed ALP array API, so the Vortex oracle is primitive float row truth.
  </acceptance_criteria>
  <done>ALP fixture tests prove Loom's kernel output matches known values and Vortex primitive row truth.</done>
</task>

<task type="auto">
  <name>Task 3: Extend FFI roundtrip tests for float payloads</name>
  <files>crates/loom-ffi/tests/roundtrip.rs, crates/loom-ffi/tests/buffer_layout.rs</files>
  <read_first>
    - crates/loom-ffi/tests/roundtrip.rs
    - crates/loom-ffi/tests/buffer_layout.rs
    - crates/loom-core/src/alp_params.rs
    - crates/loom-core/src/layout_codec.rs
  </read_first>
  <action>
    Add FFI tests showing ALP Float32 and Float64 payloads cross `loom_decode` as Arrow arrays with the expected type, length, values, and nulls. Add buffer-layout assertions for fixed-width float Arrow arrays where useful, mirroring the existing Int32/Utf8 checks.
  </action>
  <verify>
    <automated>cargo test -p loom-ffi roundtrip</automated>
    <automated>cargo test -p loom-ffi buffer_layout</automated>
  </verify>
  <acceptance_criteria>
    - `loom_decode` returns success for Float32 and Float64 ALP payloads.
    - Reconstructed Arrow arrays have Float32/Float64 data types and expected null counts.
    - Existing Int32 and Utf8 FFI tests still pass.
  </acceptance_criteria>
  <done>ALP float payloads survive the Rust FFI boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vortex fixture crate -> Loom core | Vortex remains an oracle/fixture producer only. |
| FFI caller -> decoded Arrow | Float Arrow arrays must retain type/null/value integrity across C Data Interface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-05 | Spoofing | oracle source | mitigate | Test comments name the Vortex primitive oracle boundary and lack of Vortex ALP API. |
| T-10-06 | Tampering | null handling | mitigate | Include nulls in both Float32 and Float64 fixtures and FFI tests. |
| T-10-07 | Repudiation | float equality | mitigate | Prefer exact fixtures; document any fixed tolerance. |

No high open threats remain if oracle boundaries are explicit and null/value checks pass.
</threat_model>

<verification>
- `cargo test -p loom-fixtures alp`
- `cargo test -p loom-fixtures oracle`
- `cargo test -p loom-ffi roundtrip`
- `cargo test -p loom-ffi buffer_layout`
- `rg -n 'vortex_file|vortex-file|\\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures`
</verification>

<success_criteria>
- Float32 and Float64 ALP fixtures decode correctly through Loom.
- Vortex primitive oracle comparison is present and documented.
- FFI tests prove float Arrow arrays cross the ABI boundary.
- Fixture crate still avoids file-backed Vortex APIs.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `decode_f32_oracle` / `decode_f64_oracle` | oracle helpers | `crates/loom-fixtures/src/oracle.rs` |
| `alp_roundtrip.rs` | fixture tests | `crates/loom-fixtures/tests/alp_roundtrip.rs` |
| ALP FFI roundtrips | integration tests | `crates/loom-ffi/tests/roundtrip.rs` |

