---
phase: 10-additional-l2-kernels-and-numeric-compression-coverage
plan: "03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/loom-cli/src/main.rs
  - crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
  - duckdb-ext/loom_extension.cpp
  - scripts/duckdb-smoke-test.sh
  - scripts/mvp0-verify.sh
autonomous: true
requirements: [COV-01]
must_haves:
  truths:
    - "D-12: ALP Float32/Float64 fixtures enter DuckDB SQL gate"
    - "D-15: loom inspect shows kernel name/id/output/count/concise params summary"
    - "D-17: loom decode supports finite Float32/Float64 plain decimal output and NULL"
    - "D-18: no ALP timing output in Phase 10"
  artifacts:
    - path: "scripts/duckdb-smoke-test.sh"
      provides: "ALP Float32/Float64 SQL acceptance gate"
      contains: "alp-f32"
---

<objective>
Expose ALP Float32/Float64 through reviewer CLI output and DuckDB SQL smoke coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI inspect/decode for ALP and floats</name>
  <files>crates/loom-cli/src/main.rs</files>
  <read_first>
    - crates/loom-cli/src/main.rs
    - crates/loom-core/src/alp_params.rs
    - crates/loom-core/src/l2_kernel_registry.rs
  </read_first>
  <action>
    Teach `loom inspect` and `loom decode` about Float32 and Float64. For `KernelEscape`, display kernel name (`fsst` or `alp`), kernel id, output type, count, and concise params summary when params decode successfully. For ALP, the summary should include output type, row count, exponent, value count, validity presence, and params byte length, but not a full byte dump. `loom decode` should print finite Float32/Float64 values as stable plain decimal strings and print `NULL` for nulls.
  </action>
  <verify>
    <automated>cargo test -p loom-cli</automated>
  </verify>
  <acceptance_criteria>
    - `loom inspect` output contains `kernel=alp` or equivalent plus `kernel_id=1`, output type, count, and params summary.
    - `loom decode` prints Float32/Float64 rows and `NULL`.
    - Existing Boolean/Int32/Int64/Utf8 CLI behavior remains unchanged.
  </acceptance_criteria>
  <done>Reviewer CLI can inspect and decode ALP float payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Emit deterministic ALP DuckDB payloads</name>
  <files>crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs</files>
  <read_first>
    - crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
    - crates/loom-core/src/alp_params.rs
    - crates/loom-core/src/layout_codec.rs
  </read_first>
  <action>
    Add `alp-f32.loom` and `alp-f64.loom` to the deterministic DuckDB fixture emitter. Use a small finite matrix covering normal decimals, negatives, zero, repeats, and nulls. Update the manifest with rows, count, non-null count, sum, min, and max where stable.
  </action>
  <verify>
    <automated>cargo run -p loom-fixtures --bin emit_duckdb_payloads</automated>
    <automated>test -f target/loom-duckdb-fixtures/alp-f32.loom && test -f target/loom-duckdb-fixtures/alp-f64.loom</automated>
  </verify>
  <acceptance_criteria>
    - ALP Float32 and Float64 payload files are generated.
    - Manifest contains `alp-f32` and `alp-f64`.
    - Existing payload generation still emits all previous fixture files.
  </acceptance_criteria>
  <done>Deterministic ALP payloads are available for CLI and DuckDB gates.</done>
</task>

<task type="auto">
  <name>Task 3: Add Float32/Float64 support to DuckDB extension and smoke test</name>
  <files>duckdb-ext/loom_extension.cpp, scripts/duckdb-smoke-test.sh, scripts/mvp0-verify.sh</files>
  <read_first>
    - duckdb-ext/loom_extension.cpp
    - scripts/duckdb-smoke-test.sh
    - scripts/mvp0-verify.sh
    - crates/loom-core/src/layout_codec.rs
  </read_first>
  <action>
    Extend the DuckDB extension dtype parser, bind schema, and scan switch for Float32 and Float64. Map Float32 to DuckDB `FLOAT` and Float64 to `DOUBLE`, and reuse fixed-width Arrow buffer population with `float`/`double`. Update `scripts/duckdb-smoke-test.sh` to assert ALP Float32 and Float64 row output and stable aggregate queries. Leave `scripts/mvp0-verify.sh` as the one-command release gate that reaches the new SQL coverage through the smoke test.
  </action>
  <verify>
    <automated>bash scripts/duckdb-smoke-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Smoke test checks `alp-f32` and `alp-f64` file existence.
    - Smoke test validates rows and aggregates for both ALP payloads.
    - Existing bitpack/FOR/dict/RLE/FSST/dict-FSST/mixed-table SQL checks still pass.
    - No ALP timing output is added.
  </acceptance_criteria>
  <done>ALP Float32/Float64 are accepted by the end-to-end DuckDB SQL gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Payload bytes -> DuckDB bind | C++ header parser must map new dtype tags exactly and reject unknown tags. |
| Arrow buffers -> DuckDB vectors | Fixed-width float buffers must be read with the correct native type and validity bitmap. |
| CLI output -> reviewer | Inspect output must be informative without dumping params bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-08 | Tampering | DuckDB dtype parser | mitigate | Append-only Float32/Float64 tags and explicit unknown-tag errors. |
| T-10-09 | Information disclosure | CLI params display | mitigate | Show concise params summary, not full params bytes. |
| T-10-10 | Denial of Service | Arrow buffer scan | mitigate | Reuse buffer count/null checks before reading float values. |

No high open threats remain if DuckDB SQL gates prove row and aggregate correctness for both float types.
</threat_model>

<verification>
- `cargo run -p loom-fixtures --bin emit_duckdb_payloads`
- `cargo run -p loom-cli --bin loom -- inspect target/loom-duckdb-fixtures/alp-f32.loom`
- `cargo run -p loom-cli --bin loom -- decode target/loom-duckdb-fixtures/alp-f64.loom`
- `bash scripts/duckdb-smoke-test.sh`
</verification>

<success_criteria>
- CLI inspect/decode supports ALP Float32/Float64.
- Deterministic ALP payloads are generated.
- DuckDB extension binds and scans Float32/Float64 payloads.
- DuckDB smoke test includes ALP Float32/Float64 SQL acceptance.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| ALP CLI inspect/decode output | user surface | `crates/loom-cli/src/main.rs` |
| `alp-f32.loom`, `alp-f64.loom` | deterministic fixtures | `target/loom-duckdb-fixtures/` |
| Float32/Float64 DuckDB scan support | SQL surface | `duckdb-ext/loom_extension.cpp` |
