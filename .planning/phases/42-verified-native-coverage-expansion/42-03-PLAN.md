---
phase: 42-verified-native-coverage-expansion
plan: 42-03
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
requirements:
  - COV2-03
autonomous: true
files_expected:
  - scripts/verified-native-coverage-expansion-test.sh
  - .planning/phases/42-verified-native-coverage-expansion/42-COVERAGE-MATRIX.md
  - .planning/phases/42-verified-native-coverage-expansion/42-03-SUMMARY.md
---

# 42-03 Plan: Living Matrix Gate and Closeout

## Objective

Create the Phase 42 focused gate, wire it into the broad verifier, and close the
living coverage matrix so Phase 43/44 can consume a known supported surface.

## Tasks

1. Add `scripts/verified-native-coverage-expansion-test.sh`.
   - Run Vortex, Lance, and Parquet Phase 42 matrix tests.
   - Run `scripts/verified-lineage-test.sh`.
   - Check matrix/report markers for native-supported, interpreter-only,
     canonicalized, and fail-closed/deferred rows.

2. Wire the focused gate into the broad verifier.
   - Add Phase 42 gate invocation to the MVP1/MVP2 broad script that currently
     owns source/native lineage gates.
   - Keep native toolchain skip behavior bounded to existing scripts; Phase 42
     must not infer native support from a skip.

3. Close docs and state.
   - Finalize `42-COVERAGE-MATRIX.md` and create `42-03-SUMMARY.md`.
   - Mark COV2 requirements complete.
   - Update ROADMAP, STATE, PROJECT, README/README-zh as needed.

## Acceptance Criteria

- `bash scripts/verified-native-coverage-expansion-test.sh` passes.
- Broad verifier including the Phase 42 gate passes.
- `git diff --check` passes.

## Handoff

Phase 43 consumes this matrix to test a second engine against the supported
surface. Phase 44 consumes it when freezing the ABI.
