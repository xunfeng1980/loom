---
phase: 14-mlir-native-lowering-spike
plan: "03"
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - crates/loom-core/src/native_lowering.rs
  - crates/loom-core/tests/native_lowering.rs
  - scripts/native-lowering-test.sh
autonomous: true
requirements: [LOWER-03, LOWER-04]
must_haves:
  truths:
    - "D-14-04: Preserve Arrow builder semantics"
    - "D-14-05: Native/JIT execution is optional evidence"
    - "Equivalence tests are regression evidence, not a formal compiler-correctness proof"
  artifacts:
    - path: "crates/loom-core/src/native_lowering.rs"
      provides: "textual MLIR emitter from 14-02"
      contains: "lower_to_textual_mlir"
---

<objective>
Add a focused reference/equivalence gate for the supported bounded Int32 copy slice and an optional MLIR toolchain validation script that skips explicitly when tools are unavailable.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add supported-slice reference execution</name>
  <files>crates/loom-core/src/native_lowering.rs</files>
  <read_first>
    - crates/loom-core/src/native_lowering.rs
    - crates/loom-core/src/l2_core.rs
    - crates/loom-core/src/full_verifier.rs
  </read_first>
  <action>
    Add a tiny supported-slice reference helper for bounded Int32 copy, such as `execute_supported_copy_i32(program, report, input: &[i32]) -> Result<Vec<i32>, LoweringSupportReport>`. It must call support validation first and use verifier facts for row bound/capacity. It must not construct Arrow arrays or bypass existing builder semantics beyond typed primitive comparison for the spike.
  </action>
  <verify>
    <automated>rg -n "execute_supported_copy_i32|row_count_bound|Vec<i32>|LoweringSupportReport" crates/loom-core/src/native_lowering.rs</automated>
  </verify>
  <acceptance_criteria>
    - Reference execution is limited to the supported copy subset.
    - Input length/row bound mismatch rejects rather than panics.
    - Helper exists only as Phase 14 equivalence evidence, not a general interpreter.
  </acceptance_criteria>
  <done>Supported-slice output can be compared without Arrow raw-buffer generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add equivalence tests</name>
  <files>crates/loom-core/tests/native_lowering.rs</files>
  <read_first>
    - crates/loom-core/tests/native_lowering.rs
    - crates/loom-core/src/native_lowering.rs
  </read_first>
  <action>
    Add tests comparing the supported sample against known input/output vectors. Assert that text lowering and reference execution agree on row count and supported shape. Add a negative input-length test to prove range/capacity checks fail closed.
  </action>
  <verify>
    <automated>cargo test -p loom-core native_lowering</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Supported sample returns the expected Int32 vector.
    - Input length mismatch rejects with a stable diagnostic.
    - Existing support and textual MLIR tests still pass.
  </acceptance_criteria>
  <done>Phase 14 has regression evidence that the supported lowered slice matches the intended copy semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional native-lowering gate script</name>
  <files>scripts/native-lowering-test.sh</files>
  <read_first>
    - scripts/full-verifier-test.sh
    - scripts/safety-proof-test.sh
    - scripts/mvp0-verify.sh
  </read_first>
  <action>
    Add `scripts/native-lowering-test.sh` that runs focused native-lowering Rust tests and checks planning docs for `LOWER-01` through `LOWER-04`. If `mlir-opt` is available, run a small validation path over emitted/sample MLIR if the implementation exposes a convenient artifact; otherwise print an explicit skipped optional-toolchain message. Do not fail the script solely because `mlir-opt` is absent.
  </action>
  <verify>
    <automated>bash scripts/native-lowering-test.sh</automated>
    <automated>rg -n "mlir-opt|optional|skip|native_lowering|LOWER-04" scripts/native-lowering-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script passes without system MLIR installed.
    - Script reports optional MLIR validation clearly when skipped.
    - Script covers focused Rust lowering tests.
  </acceptance_criteria>
  <done>Native-lowering spike has an executable gate that respects optional toolchain availability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reference helper -> proof claim | Equivalence test must not be described as full compiler proof. |
| optional toolchain -> release gate | Missing MLIR tools must be explicit, not silent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-14-06 | Spoofing | equivalence evidence | mitigate | Docs/script label equivalence as spike regression evidence only. |
| T-14-07 | Denial of service | local MLIR absence | mitigate | Optional `mlir-opt` path skips with a clear message. |

No high open threats remain if tests are explicit about scope and optional tools are non-blocking.
</threat_model>

<verification>
- `rg -n "execute_supported_copy_i32|row_count_bound|Vec<i32>|LoweringSupportReport" crates/loom-core/src/native_lowering.rs`
- `cargo test -p loom-core native_lowering`
- `bash scripts/native-lowering-test.sh`
- `rg -n "mlir-opt|optional|skip|native_lowering|LOWER-04" scripts/native-lowering-test.sh`
- `git diff --check`
</verification>

<success_criteria>
- Supported bounded Int32 copy has typed primitive equivalence coverage.
- Negative capacity/range cases fail closed.
- Optional MLIR toolchain validation is explicit and non-blocking.
- Focused native-lowering gate script exists.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `execute_supported_copy_i32` | supported-slice reference helper | `crates/loom-core/src/native_lowering.rs` |
| equivalence tests | regression coverage | `crates/loom-core/tests/native_lowering.rs` |
| `native-lowering-test.sh` | focused gate | `scripts/native-lowering-test.sh` |
