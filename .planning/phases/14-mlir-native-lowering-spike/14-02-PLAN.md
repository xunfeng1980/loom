---
phase: 14-mlir-native-lowering-spike
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/loom-core/src/native_lowering.rs
  - crates/loom-core/tests/native_lowering.rs
  - .planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md
autonomous: true
requirements: [LOWER-02]
must_haves:
  truths:
    - "D-14-02: Start with textual MLIR"
    - "D-14-03: Initial supported subset is tiny"
    - "Textual MLIR emission must remain pure Rust and deterministic"
  artifacts:
    - path: "crates/loom-core/src/native_lowering.rs"
      provides: "lowering support predicate from 14-01"
      contains: "LoweringSupportReport"
---

<objective>
Emit deterministic textual MLIR for the single supported bounded Int32 copy subset, using standard dialect names and no mandatory MLIR/LLVM dependency.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add lowering artifact model and MLIR text emitter</name>
  <files>crates/loom-core/src/native_lowering.rs</files>
  <read_first>
    - crates/loom-core/src/native_lowering.rs
    - crates/loom-core/src/l2_core.rs
    - .planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md
  </read_first>
  <action>
    Add `LoweringArtifact` and a function such as `lower_to_textual_mlir(program, report) -> Result<LoweringArtifact, LoweringSupportReport>`. The emitted text should contain a single `func.func @loom_l2core_copy_i32`, `arith.constant` indices, `scf.for`, `memref.load`, `memref.store`, and `return`. Use facts/support validation before emission.
  </action>
  <verify>
    <automated>rg -n "LoweringArtifact|lower_to_textual_mlir|func.func @loom_l2core_copy_i32|scf.for|memref.load|memref.store" crates/loom-core/src/native_lowering.rs</automated>
  </verify>
  <acceptance_criteria>
    - Emitter calls or reuses support checking before creating an artifact.
    - MLIR text is deterministic for the sample program.
    - The artifact records backend name, entry symbol, and verifier/facts linkage metadata.
  </acceptance_criteria>
  <done>Core can produce a deterministic textual MLIR artifact for the supported slice.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot-test textual MLIR shape</name>
  <files>crates/loom-core/tests/native_lowering.rs</files>
  <read_first>
    - crates/loom-core/tests/native_lowering.rs
    - crates/loom-core/src/native_lowering.rs
    - crates/loom-core/tests/full_verifier.rs
  </read_first>
  <action>
    Add tests asserting that the bounded Int32 copy sample emits expected MLIR markers and exact stable entry symbol. Negative cases from 14-01 must still reject before emission. Keep the snapshot inline in the test to avoid fixture churn.
  </action>
  <verify>
    <automated>cargo test -p loom-core native_lowering</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Textual MLIR contains `func`, `arith`, `scf`, and `memref` operations.
    - Unsupported programs return lowering diagnostics rather than partial MLIR.
    - No system MLIR command is required for the test.
  </acceptance_criteria>
  <done>Textual MLIR emission has deterministic regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Update contract with emitted dialect stack</name>
  <files>.planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md</files>
  <read_first>
    - .planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md
    - crates/loom-core/src/native_lowering.rs
  </read_first>
  <action>
    Update the lowering contract to record the Phase 14 textual MLIR dialect stack: `func`, `arith`, `scf`, `memref`, with LLVM lowering and custom Loom dialect explicitly deferred. Document that the textual artifact is design evidence plus regression artifact, not proof of production native execution.
  </action>
  <verify>
    <automated>rg -n "func|arith|scf|memref|LLVM lowering|custom Loom dialect|textual artifact" .planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Contract names the emitted dialect stack.
    - Contract distinguishes textual lowering from production JIT/native execution.
  </acceptance_criteria>
  <done>Documentation matches the emitted textual MLIR shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| support predicate -> MLIR text | Emitter must not produce partial artifacts for unsupported programs. |
| textual artifact -> production claim | Docs must not claim JIT/native performance from textual MLIR alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-14-04 | Tampering | MLIR artifact | mitigate | Deterministic inline snapshot/marker tests. |
| T-14-05 | Repudiation | native claims | mitigate | Contract states textual MLIR is not production native execution. |

No high open threats remain if emission is gated and docs preserve scope.
</threat_model>

<verification>
- `rg -n "LoweringArtifact|lower_to_textual_mlir|func.func @loom_l2core_copy_i32|scf.for|memref.load|memref.store" crates/loom-core/src/native_lowering.rs`
- `cargo test -p loom-core native_lowering`
- `rg -n "func|arith|scf|memref|LLVM lowering|custom Loom dialect|textual artifact" .planning/phases/14-mlir-native-lowering-spike/14-LOWERING-CONTRACT.md`
- `git diff --check`
</verification>

<success_criteria>
- Supported bounded Int32 copy sample emits deterministic textual MLIR.
- Unsupported programs still fail closed before emission.
- No mandatory MLIR/LLVM dependency is added.
- Contract records dialect stack and scope limits.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| `LoweringArtifact` | lowering output model | `crates/loom-core/src/native_lowering.rs` |
| `lower_to_textual_mlir` | textual MLIR emitter | `crates/loom-core/src/native_lowering.rs` |
| textual MLIR tests | regression coverage | `crates/loom-core/tests/native_lowering.rs` |
