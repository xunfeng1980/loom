---
phase: 28-full-lance-parquet-vortex-semantic-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - ingress/loom-vortex-ingress/src/lib.rs
  - ingress/loom-vortex-ingress/tests/semantic_compatibility_matrix.rs
  - scripts/vortex-semantic-compatibility-test.sh
autonomous: true
requirements:
  - PHASE-28
must_haves:
  truths:
    - "Existing Phase 21 coverage rows are represented by executable semantic compatibility assertions."
    - "Unsupported valid shapes emit no bytes and cannot be marked accepted by the semantic matrix."
    - "The focused gate starts as a drift/no-overclaim gate before final release wiring."
  artifacts:
    - path: scripts/vortex-semantic-compatibility-test.sh
      provides: "Focused Phase 28 semantic compatibility gate seed"
    - path: ingress/loom-vortex-ingress/tests/semantic_compatibility_matrix.rs
      provides: "Executable matrix assertions for Phase 21 rows"
---

<objective>
Convert the compatibility matrix into executable drift and no-overclaim checks.

Purpose: prevent Phase 28 from passing through documentation-only support claims.
Output: matrix tests covering existing Phase 21 rows and a focused gate script
that fails on missing evidence or public-surface creep.
</objective>

<context>
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-CONTEXT.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-RESEARCH.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-PATTERNS.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-01-PLAN.md
@scripts/vortex-encoding-coverage-test.sh
@scripts/native-hardening-test.sh
</context>

<threat_model>
T-28-02: Documentation drift. A report or gate could list support rows that are
not backed by source assertions. Mitigation: the gate greps for concrete symbols
and runs focused cargo tests for every matrix class.
</threat_model>

## Tasks

<task type="tdd">
  <name>Task 1: Assert existing Phase 21 row classes</name>
  <read_first>
    - ingress/loom-vortex-ingress/src/lib.rs
    - ingress/loom-vortex-ingress/tests/semantic_compatibility_matrix.rs
    - .planning/phases/21-expanded-vortex-encoding-coverage/21-COVERAGE-REPORT.md
  </read_first>
  <action>Extend `semantic_compatibility_matrix.rs` with tests for non-null primitive accepted native-supported rows, primitive table accepted native-supported rows, nullable primitive unsupported rows, chunked primitive canonical raw rows, dictionary/run-end canonical raw rows, bitpack/FOR canonical raw rows, UTF-8 unsupported rows, and ALP/PCodec deferred rows.</action>
  <acceptance_criteria>
    - Test `phase21_rows_have_semantic_compatibility_entries` covers at least eight `shape_id` values.
    - The dictionary/run-end and bitpack/FOR assertions require `AcceptedCanonicalized`, not structured support.
    - `cargo test -p loom-vortex-ingress --test semantic_compatibility_matrix phase21_rows_have_semantic_compatibility_entries` passes.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add focused Phase 28 gate seed</name>
  <read_first>
    - scripts/vortex-encoding-coverage-test.sh
    - scripts/native-hardening-test.sh
    - scripts/iceberg-binding-test.sh
  </read_first>
  <action>Create `scripts/vortex-semantic-compatibility-test.sh` with strict bash settings. The script must check for `VortexSemanticCompatibilityRow`, run `cargo test -p loom-vortex-ingress --test semantic_compatibility_matrix`, require `28-CONTEXT.md`, `28-RESEARCH.md`, `28-PATTERNS.md`, and fail if `28-LANCE-PARQUET-VORTEX-SEMANTIC-COMPATIBILITY-REPORT.md` contains `StarRocks evidence complete` or `dual-query evidence complete`.</action>
  <acceptance_criteria>
    - `scripts/vortex-semantic-compatibility-test.sh` contains `set -euo pipefail`.
    - `scripts/vortex-semantic-compatibility-test.sh` runs `semantic_compatibility_matrix`.
    - `bash scripts/vortex-semantic-compatibility-test.sh` passes after Plan 28-02.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Add no-overclaim marker checks</name>
  <read_first>
    - scripts/vortex-semantic-compatibility-test.sh
    - .planning/phases/30-starrocks-duckdb-dual-query-surface/30-SKIPPED.md
    - .planning/debug/native-query-zero-buffer.md
  </read_first>
  <action>Extend the focused gate to require marker strings `canonical-raw-overclaim`, `native-evidence-missing`, `Phase 29 starts without`, and `native-execution-engine-output` in their relevant source or planning artifacts. The gate must reject `native-raw-copy-output` and `toolchain-skipped.*native success` patterns.</action>
  <acceptance_criteria>
    - Gate fails if `native-raw-copy-output` appears in `crates/loom-ffi`, `crates/loom-native-melior`, or `scripts`.
    - Gate requires `native-execution-engine-output`.
    - `bash scripts/vortex-semantic-compatibility-test.sh` passes.
  </acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `phase21_rows_have_semantic_compatibility_entries`
- `scripts/vortex-semantic-compatibility-test.sh`

## Verification

```bash
cargo test -p loom-vortex-ingress --test semantic_compatibility_matrix
bash scripts/vortex-semantic-compatibility-test.sh
```
