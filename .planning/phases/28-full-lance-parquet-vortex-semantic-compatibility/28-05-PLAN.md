---
phase: 28-full-lance-parquet-vortex-semantic-compatibility
plan: 05
type: execute
wave: 5
depends_on:
  - 28-04
files_modified:
  - scripts/vortex-semantic-compatibility-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-LANCE-PARQUET-VORTEX-SEMANTIC-COMPATIBILITY-REPORT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PHASE-28
must_haves:
  truths:
    - "The final report lists accepted, unsupported, rejected, canonicalized, interpreter-only, native-supported, and deferred rows without conflating them."
    - "The release gate is wired into `mvp0-verify.sh` only after the focused gate passes."
    - "The final closeout records that Phase 29 dual-query evidence is skipped/deferred and unavailable as support."
  artifacts:
    - path: .planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-LANCE-PARQUET-VORTEX-SEMANTIC-COMPATIBILITY-REPORT.md
      provides: "Final Phase 28 compatibility evidence, unsupported matrix, native disposition, and tradeoffs"
    - path: scripts/vortex-semantic-compatibility-test.sh
      provides: "Final focused Phase 28 release gate"
    - path: scripts/mvp0-verify.sh
      provides: "Main release gate wiring"
---

<objective>
Finalize Phase 28 gate wiring, report, roadmap/state closeout, and milestone handoff.

Purpose: make full Vortex semantic compatibility reproducible and honest from a
clean checkout while preserving explicit unsupported/deferred rows.
Output: final report, focused gate, main verifier wiring, and updated planning state.
</objective>

<context>
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-CONTEXT.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-RESEARCH.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-PATTERNS.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-SKIPPED.md
@.planning/debug/native-query-zero-buffer.md
@scripts/mvp0-verify.sh
@scripts/vortex-semantic-compatibility-test.sh
</context>

<threat_model>
T-28-05: Release-gate false positive. The main verifier could run a partial
gate or the report could claim unsupported rows as complete. Mitigation: final
gate checks report markers, focused tests, native marker requirements, and no
Phase 29 overclaim language before `mvp0-verify.sh` wiring.
</threat_model>

## Tasks

<task type="execute">
  <name>Task 1: Finalize the focused semantic compatibility gate</name>
  <read_first>
    - scripts/vortex-semantic-compatibility-test.sh
    - scripts/native-hardening-test.sh
    - scripts/vortex-encoding-coverage-test.sh
  </read_first>
  <action>Finalize `scripts/vortex-semantic-compatibility-test.sh` so it checks required Phase 28 artifacts, runs `semantic_compatibility_matrix`, `nullable_semantic_compatibility`, and `structured_encoding_semantics`, rejects native raw-copy/fallback success markers, rejects Phase 29 dual-query completion claims, and requires report sections `Accepted Matrix`, `Unsupported Matrix`, `Native Disposition`, `Phase 30 Tradeoff`, and `Release Gate Evidence` once the report exists.</action>
  <acceptance_criteria>
    - `bash scripts/vortex-semantic-compatibility-test.sh` passes before editing `mvp0-verify.sh`.
    - The gate rejects `native-raw-copy-output`.
    - The gate rejects `dual-query evidence complete`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Write the final compatibility report</name>
  <read_first>
    - .planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-CONTEXT.md
    - .planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-RESEARCH.md
    - .planning/phases/21-expanded-vortex-encoding-coverage/21-COVERAGE-REPORT.md
    - .planning/debug/native-query-zero-buffer.md
  </read_first>
  <action>Create `28-LANCE-PARQUET-VORTEX-SEMANTIC-COMPATIBILITY-REPORT.md` with sections `Scope`, `Accepted Matrix`, `Unsupported Matrix`, `Rejected Matrix`, `Canonicalized Rows`, `Native Disposition`, `Phase 30 Tradeoff`, `Release Gate Evidence`, `Commands Run`, `Residual Risks`, and `Phase/Milestone Handoff`. The report must state that Phase 30 StarRocks work is deferred and that second-host proof is unavailable.</action>
  <acceptance_criteria>
    - Report contains `Phase 30 Tradeoff`.
    - Report contains `native-execution-engine-output`.
    - Report does not contain `StarRocks evidence complete` or `dual-query evidence complete`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Wire main verifier and close planning state</name>
  <read_first>
    - scripts/mvp0-verify.sh
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>After the focused Phase 28 gate passes, invoke `scripts/vortex-semantic-compatibility-test.sh` from `scripts/mvp0-verify.sh` after prior Vortex/source/table/native gates and before final DuckDB smoke. Update `.planning/ROADMAP.md` Phase 28 checkbox/status and `.planning/STATE.md` progress only after focused and main gates pass.</action>
  <acceptance_criteria>
    - `scripts/mvp0-verify.sh` contains `vortex-semantic-compatibility-test.sh`.
    - `bash scripts/vortex-semantic-compatibility-test.sh` passes.
    - `bash scripts/mvp0-verify.sh` passes or records an explicit environment/toolchain failure that is not counted as Phase 28 complete.
    - ROADMAP Phase 28 is marked complete only after verification passes.
  </acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-LANCE-PARQUET-VORTEX-SEMANTIC-COMPATIBILITY-REPORT.md`
- final `scripts/vortex-semantic-compatibility-test.sh`
- `mvp0-verify.sh` Phase 28 gate wiring

## Verification

```bash
bash scripts/vortex-semantic-compatibility-test.sh
bash scripts/mvp0-verify.sh
```
