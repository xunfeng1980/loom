---
phase: 28-full-lance-parquet-vortex-semantic-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 28-02
files_modified:
  - crates/loom-vortex-ingress/src/lib.rs
  - crates/loom-vortex-ingress/tests/nullable_semantic_compatibility.rs
  - crates/loom-core/tests/artifact_verifier.rs
  - scripts/vortex-semantic-compatibility-test.sh
autonomous: true
requirements:
  - PHASE-28
must_haves:
  truths:
    - "Nullable primitive Vortex rows either emit verifier-accepted Loom artifacts that preserve null positions, or remain unsupported with no emitted bytes."
    - "Accepted nullable rows require Vortex oracle evidence for values and null bitmap semantics."
    - "The compatibility matrix records the nullable result without weakening non-null primitive native evidence."
  artifacts:
    - path: crates/loom-vortex-ingress/tests/nullable_semantic_compatibility.rs
      provides: "Nullable primitive semantic compatibility tests"
    - path: scripts/vortex-semantic-compatibility-test.sh
      provides: "Focused gate coverage for nullable semantic compatibility"
---

<objective>
Close or explicitly preserve the nullable primitive semantic compatibility gap.

Purpose: convert Phase 21 nullable facts into either accepted verifier/oracle
evidence or a hard unsupported row with no artifact emission.
Output: nullable semantic tests and matrix rows that prove null positions cannot
be silently dropped.
</objective>

<context>
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-CONTEXT.md
@.planning/phases/28-full-lance-parquet-vortex-semantic-compatibility/28-RESEARCH.md
@crates/loom-vortex-ingress/tests/nullable_primitive_coverage.rs
@crates/loom-ffi/tests/buffer_layout.rs
@crates/loom-core/src/layout_codec.rs
@crates/loom-core/src/table_codec.rs
</context>

<threat_model>
T-28-03: Null loss. A nullable Vortex input could become a non-null Loom artifact
with the same values but wrong SQL/count semantics. Mitigation: oracle tests must
assert both values and null positions, and unsupported rows must emit no bytes.
</threat_model>

## Tasks

<task type="tdd">
  <name>Task 1: Add nullable semantic compatibility tests</name>
  <read_first>
    - crates/loom-vortex-ingress/tests/nullable_primitive_coverage.rs
    - crates/loom-vortex-ingress/src/lib.rs
    - crates/loom-core/tests/artifact_verifier.rs
  </read_first>
  <action>Create `nullable_semantic_compatibility.rs` with tests for nullable `i32`, `i64`, `f32`, and `f64` fixture coverage. Each test must assert either accepted artifact bytes plus verifier/oracle null equality, or unsupported support with `VortexReaderEmissionKind::None` and no emitted bytes.</action>
  <acceptance_criteria>
    - Test names include `nullable_i32_semantics_are_accepted_or_explicitly_unsupported`, `nullable_i64_semantics_are_accepted_or_explicitly_unsupported`, `nullable_f32_semantics_are_accepted_or_explicitly_unsupported`, and `nullable_f64_semantics_are_accepted_or_explicitly_unsupported`.
    - Unsupported nullable tests assert `emission_kind == VortexReaderEmissionKind::None`.
    - `cargo test -p loom-vortex-ingress --test nullable_semantic_compatibility` passes.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Implement accepted nullable emission only if verifier-safe</name>
  <read_first>
    - crates/loom-vortex-ingress/src/lib.rs
    - crates/loom-core/src/layout_codec.rs
    - crates/loom-core/src/table_codec.rs
    - crates/loom-core/tests/artifact_verifier.rs
  </read_first>
  <action>If existing Loom payload codecs can represent nullable primitive validity for real-ingress artifacts, implement nullable `LMP1`/`LMT1` emission through the existing verifier path. If not, keep nullable rows unsupported and add a stable deferral reason `nullable-validity-emission-deferred`. Do not emit non-null raw artifacts for nullable inputs.</action>
  <acceptance_criteria>
    - No code path emits `canonical-raw` for a nullable input unless null positions are represented and oracle-tested.
    - Unsupported nullable rows contain deferral reason `nullable-validity-emission-deferred`.
    - `cargo test -p loom-vortex-ingress --test nullable_semantic_compatibility` passes.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Add nullable checks to focused gate</name>
  <read_first>
    - scripts/vortex-semantic-compatibility-test.sh
    - crates/loom-vortex-ingress/tests/nullable_semantic_compatibility.rs
  </read_first>
  <action>Extend `scripts/vortex-semantic-compatibility-test.sh` to run `cargo test -p loom-vortex-ingress --test nullable_semantic_compatibility` and require either nullable accepted markers or `nullable-validity-emission-deferred` in source/tests/report.</action>
  <acceptance_criteria>
    - Gate invokes `nullable_semantic_compatibility`.
    - Gate requires `nullable-validity-emission-deferred` unless accepted nullable support markers are present.
    - `bash scripts/vortex-semantic-compatibility-test.sh` passes.
  </acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `crates/loom-vortex-ingress/tests/nullable_semantic_compatibility.rs`
- `nullable-validity-emission-deferred`

## Verification

```bash
cargo test -p loom-vortex-ingress --test nullable_semantic_compatibility
bash scripts/vortex-semantic-compatibility-test.sh
```
