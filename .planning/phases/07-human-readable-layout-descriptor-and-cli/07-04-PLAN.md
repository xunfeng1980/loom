---
phase: 07-human-readable-layout-descriptor-and-cli
plan: "04"
subsystem: verification
tags: [fixtures, timing, docs, release-gate]
requirements: [DX-02, DX-04]
wave: 3
requires:
  - phase: 07-human-readable-layout-descriptor-and-cli
    plan: "02"
    provides: descriptor fixture roundtrips
  - phase: 07-human-readable-layout-descriptor-and-cli
    plan: "03"
    provides: CLI inspect/decode
---

# Phase 07-04 Plan: Expanded Fixtures, Timing, Docs, and Final Gate

## Purpose

Close Phase 7 by broadening demo coverage, adding illustrative timing output, and documenting descriptor/CLI workflows.

## Tasks

1. Expand fixture matrix:
   - multiple sample columns per supported encoding where practical;
   - include null-heavy and edge-value cases;
   - keep generated outputs deterministic.
2. Add illustrative timing output:
   - compare Loom interpreter decode vs Vortex oracle decode for generated fixtures;
   - label output clearly as wall-clock illustration, not benchmark proof;
   - avoid flaky pass/fail thresholds.
3. Update docs:
   - README and README-zh include CLI examples;
   - Phase 7 summaries record descriptor format choice and limitations;
   - update requirements and roadmap only after verification passes.
4. Run full final verification:
   - descriptor tests;
   - CLI smoke;
   - full release gate.

## Acceptance Criteria

- At least two supported encodings have multiple sample columns in the harness.
- Timing output is available without making performance claims or failing on speed variance.
- README documents `loom inspect` and `loom decode`.
- `bash scripts/mvp0-verify.sh` passes after descriptor and CLI changes.

## Verification

- `cargo test --workspace`
- `bash scripts/mvp0-verify.sh`
- CLI inspect/decode smoke commands from README
- `git diff --check`
