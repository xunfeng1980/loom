---
phase: 34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - duckdb-ext/loom_extension.cpp
autonomous: true
requirements:
  - PHASE-34
must_haves:
  truths:
    - "D-34-04: DuckDB loom_scan(path) binds default LMC2(LMA1) as named multi-column SQL output."
    - "D-34-05: projected scans decode/populate only requested Arrow semantic columns where practical."
    - "D-34-06: direct LMA1 remains a regression bridge, not the default source path."
  artifacts:
    - path: duckdb-ext/loom_extension.cpp
      provides: "DuckDB bind/init/scan integration for Arrow semantic artifacts"
---

<objective>
Teach `loom_scan(path)` to bind and scan default `LMC2(LMA1)` Arrow semantic
artifacts through the new internal FFI handle.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-CONTEXT.md
@.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-RESEARCH.md
@.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-01-SUMMARY.md
@duckdb-ext/loom_extension.cpp
@crates/loom-ffi/include/loom_duckdb_internal.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route bind-time Arrow semantic artifacts through the internal handle</name>
  <files>duckdb-ext/loom_extension.cpp</files>
  <read_first>
    - duckdb-ext/loom_extension.cpp
    - crates/loom-ffi/include/loom_duckdb_internal.h
  </read_first>
  <action>Update `PopulateColumnSpecs` or the surrounding bind path so `LMC2(LMA1)` and direct `LMA1` artifacts are identified by the internal Arrow semantic helper, not by one-column public `loom_decode`. Preserve Arrow field names and map supported primitive/UTF8/Boolean kinds to existing DuckDB logical types.</action>
  <acceptance_criteria>
    - Default `LMC2` bytes bind as multiple named columns.
    - Direct `LMA1` bytes still bind for regression.
    - Unsupported logical/nested kinds produce stable `loom_scan:` diagnostics instead of silently becoming `value`.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build duckdb-ext/build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Decode projected Arrow semantic columns during init</name>
  <files>duckdb-ext/loom_extension.cpp</files>
  <read_first>
    - duckdb-ext/loom_extension.cpp
  </read_first>
  <action>Update `LoomInit` so Arrow semantic plans create the internal handle once, export only projected source columns into `state.arrow_arrays` / `state.arrow_schemas`, and preserve the existing release-on-teardown discipline. Keep legacy raw/table `LMC1` decode behavior unchanged.</action>
  <acceptance_criteria>
    - Projection over one Arrow semantic column succeeds without requiring all SQL columns to be populated.
    - Empty projection or count-style scans keep row-count behavior correct.
    - Existing `LoomScanState` destructor releases every exported Arrow C Data object exactly once.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/duckdb-smoke-test.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Keep runtime/native diagnostics truthful</name>
  <files>duckdb-ext/loom_extension.cpp</files>
  <read_first>
    - duckdb-ext/loom_extension.cpp
    - crates/loom-ffi/src/duckdb_runtime.rs
  </read_first>
  <action>Ensure Arrow semantic SQL support is labeled as interpreter-backed queryability. Do not route `LMC2(LMA1)` through a native execution claim. Existing runtime-plan diagnostics for legacy `LMC1` paths must remain unchanged.</action>
  <acceptance_criteria>
    - Existing native integration gates still pass.
    - Error messages do not describe Arrow semantic `LMC2` SQL as native execution.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/duckdb-native-integration-test.sh</automated>
  </verify>
</task>

</tasks>

<verification>
bash scripts/duckdb-smoke-test.sh
bash scripts/duckdb-native-integration-test.sh
git diff --check
</verification>

<success_criteria>
Plan 34-02 is complete when `loom_scan(path)` can bind and scan default
`LMC2(LMA1)` multi-column primitive/nullable artifacts while preserving legacy
DuckDB behavior and direct `LMA1` regression support.
</success_criteria>

<output>
Create `.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-02-SUMMARY.md` when done.
</output>

