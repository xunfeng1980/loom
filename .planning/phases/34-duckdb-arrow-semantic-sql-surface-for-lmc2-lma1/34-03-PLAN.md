---
phase: 34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1
plan: 03
type: execute
wave: 3
depends_on:
  - 34-02
files_modified:
  - scripts/duckdb-lmc2-sql-surface-test.sh
  - scripts/duckdb-source-e2e-test.sh
autonomous: true
requirements:
  - PHASE-34
must_haves:
  truths:
    - "D-34-07: focused SQL gate queries default LMC2(LMA1) artifacts directly."
    - "D-34-08: source e2e product path no longer depends on direct LMA1 bridge files."
  artifacts:
    - path: scripts/duckdb-lmc2-sql-surface-test.sh
      provides: "Focused DuckDB SQL acceptance gate for LMC2(LMA1)"
---

<objective>
Add the focused DuckDB `LMC2(LMA1)` SQL gate and cut the source e2e product
query path from direct `LMA1` bridge files to default wrapped artifacts.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-CONTEXT.md
@.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-02-SUMMARY.md
@scripts/duckdb-source-e2e-test.sh
@scripts/duckdb-smoke-test.sh
@scripts/lmc2-arrow-semantic-container-test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create focused LMC2 SQL surface gate</name>
  <files>scripts/duckdb-lmc2-sql-surface-test.sh</files>
  <read_first>
    - scripts/duckdb-source-e2e-test.sh
    - scripts/duckdb-smoke-test.sh
    - scripts/toolchain-common.sh
  </read_first>
  <action>Create a shell gate that builds or locates the DuckDB extension, generates one wrapped multi-column primitive/nullable Arrow semantic artifact, queries it through `loom_scan(path)`, and asserts projection, filter, aggregate, null, and direct `LMA1` regression behavior.</action>
  <acceptance_criteria>
    - The gate checks wrapped artifact magic `LMC2`.
    - The gate runs at least one query that projects a subset of columns.
    - The gate proves nulls survive into SQL.
    - The direct `LMA1` check is labeled regression or bridge.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/duckdb-lmc2-sql-surface-test.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Cut source e2e product queries over to default LMC2 artifacts</name>
  <files>scripts/duckdb-source-e2e-test.sh</files>
  <read_first>
    - scripts/duckdb-source-e2e-test.sh
  </read_first>
  <action>Update source e2e SQL queries so Parquet, Lance, and Vortex accepted source artifacts are queried directly as `LMC2(LMA1)`. Keep bridge fixture checks only as explicitly named regression evidence where still useful.</action>
  <acceptance_criteria>
    - The script queries `parquet.loom`, `lance.loom`, and `vortex.loom` default outputs directly through DuckDB.
    - Bridge paths, if retained, include `bridge` or `regression` wording.
    - The script fails if default outputs are not `LMC2`.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/duckdb-source-e2e-test.sh</automated>
  </verify>
</task>

</tasks>

<verification>
bash scripts/duckdb-lmc2-sql-surface-test.sh
bash scripts/duckdb-source-e2e-test.sh
git diff --check
</verification>

<success_criteria>
Plan 34-03 is complete when default source `LMC2(LMA1)` artifacts are the
queried DuckDB product path and direct `LMA1` is retained only as explicit
regression evidence.
</success_criteria>

<output>
Create `.planning/phases/34-duckdb-arrow-semantic-sql-surface-for-lmc2-lma1/34-03-SUMMARY.md` when done.
</output>

