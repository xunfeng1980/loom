---
phase: 30-starrocks-duckdb-dual-query-surface
plan: 03
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - crates/loom-dual-query-surface/src/lib.rs
  - crates/loom-dual-query-surface/src/duckdb_evidence.rs
  - crates/loom-dual-query-surface/src/bin/emit_dual_query_fixture.rs
  - crates/loom-dual-query-surface/tests/duckdb_evidence.rs
  - scripts/dual-query-surface-test.sh
autonomous: true
requirements:
  - PHASE-30
must_haves:
  truths:
    - "D-01/D-07/D-18: DuckDB evidence is executable through the existing loadable extension and public `loom_scan(path)` SQL."
    - "D-03/D-04/D-16: DuckDB queries run against the same generated Phase 29 accepted Loom artifact used by the descriptor contract and check real row/aggregate values."
    - "D-11/D-15: the focused gate adds no `loom_scan_iceberg`, `loom_scan_starrocks`, public C ABI, or CLI route."
  artifacts:
    - path: crates/loom-dual-query-surface/src/duckdb_evidence.rs
      provides: "DuckDB query evidence model over canonical Phase 30 query results"
    - path: crates/loom-dual-query-surface/src/bin/emit_dual_query_fixture.rs
      provides: "Deterministic fixture/descriptor emitter for shell gates"
    - path: crates/loom-dual-query-surface/tests/duckdb_evidence.rs
      provides: "Rust-side evidence model tests for DuckDB SQL expectations"
    - path: scripts/dual-query-surface-test.sh
      provides: "Initial focused Phase 30 gate with executable DuckDB query checks"
  key_links:
    - from: scripts/dual-query-surface-test.sh
      to: crates/loom-dual-query-surface/src/bin/emit_dual_query_fixture.rs
      via: "fixture generation before DuckDB query"
      pattern: "emit_dual_query_fixture"
    - from: scripts/dual-query-surface-test.sh
      to: scripts/duckdb-smoke-test.sh
      via: "DuckDB CLI/download/build pattern reuse"
      pattern: "DUCKDB_VERSION"
    - from: scripts/dual-query-surface-test.sh
      to: duckdb-ext/loom_extension.cpp
      via: "existing `loom_scan(path)` loadable extension"
      pattern: "loom_scan"
---

<objective>
Add executable DuckDB query evidence for the Phase 30 accepted artifact.

Purpose: ensure the DuckDB half of the dual-surface proof is a real SQL execution over the existing extension and not a descriptor-only or route-diagnostic claim.
Output: DuckDB evidence helpers, deterministic fixture emitter, Rust evidence tests, and the first focused Phase 30 gate.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-CONTEXT.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-RESEARCH.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-PATTERNS.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-02-SUMMARY.md
@.planning/debug/native-query-zero-buffer.md
@crates/loom-dual-query-surface/src/fixture_bundle.rs
@crates/loom-dual-query-surface/src/query_surface.rs
@scripts/duckdb-smoke-test.sh
@scripts/iceberg-binding-test.sh
@scripts/mvp0-verify.sh
@duckdb-ext/loom_extension.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model DuckDB SQL evidence for the canonical matrix</name>
  <files>crates/loom-dual-query-surface/src/lib.rs, crates/loom-dual-query-surface/src/duckdb_evidence.rs, crates/loom-dual-query-surface/tests/duckdb_evidence.rs</files>
  <behavior>
    - DuckDB evidence SQL uses only `loom_scan(path)`.
    - SQL cases cover ordered rows, projection, predicate `id >= 0`, count, and sum.
    - Expected outputs match the accepted artifact values: ordered rows `-1,7,42`, predicate rows `7,42`, count `3`, and sum `48`.
    - Evidence records the artifact path and accepted binding identity so DuckDB evidence cannot be detached from the shared input.
  </behavior>
  <action>Add `duckdb_evidence.rs` with a small evidence model that maps the Plan 30-02 canonical query matrix to exact DuckDB SQL strings using public `loom_scan(path)` only per D-01/D-07/D-18. Include expected CSV output for the focused gate, but keep typed canonical results as the source of truth per D-04/D-16. Tests should assert that no SQL string contains route-specific names such as `loom_scan_iceberg` or `loom_scan_starrocks`, no SQL string requires a new DuckDB function, and each SQL case carries the same accepted binding identity fields from Plan 30-02 per D-03/D-06/D-11.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test duckdb_evidence</automated>
  </verify>
  <done>DuckDB evidence expectations are exact, bounded, and tied to accepted binding identity while preserving `loom_scan(path)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit shell-gate fixtures and descriptor JSON</name>
  <files>crates/loom-dual-query-surface/src/bin/emit_dual_query_fixture.rs, crates/loom-dual-query-surface/src/lib.rs, crates/loom-dual-query-surface/tests/duckdb_evidence.rs</files>
  <behavior>
    - Running the binary writes an output directory containing the generated metadata, sidecar, source/oracle evidence, source marker, accepted `.loom` artifact, StarRocks-compatible descriptor JSON, and DuckDB expected-output JSON or text.
    - The binary prints stable machine-readable paths for the artifact and descriptor files.
    - The binary fails if Phase 29 binding acceptance fails.
  </behavior>
  <action>Create `src/bin/emit_dual_query_fixture.rs` as a deterministic gate helper per D-03/D-04/D-16. It should accept an output directory argument or default to `target/loom-dual-query-surface`, call the Plan 30-02 fixture bundle and descriptor builders, write descriptor/result files with stable filenames, and print the artifact path and descriptor path in parseable `KEY=value` lines. It must not shell out to DuckDB, create StarRocks runtime state, or write checked-in binary fixtures. Tests should run the binary or call its underlying helper enough to prove emitted files exist and are tied to an accepted binding.</action>
  <verify>
    <automated>cargo run -p loom-dual-query-surface --bin emit_dual_query_fixture -- target/loom-dual-query-surface-test && test -f target/loom-dual-query-surface-test/demo-events.lmc1.loom && test -f target/loom-dual-query-surface-test/starrocks-descriptors.json</automated>
  </verify>
  <done>The focused gate can generate a verifier-accepted artifact and descriptors deterministically from a clean checkout.</done>
</task>

<task type="auto">
  <name>Task 3: Create the initial focused dual-query gate</name>
  <files>scripts/dual-query-surface-test.sh</files>
  <action>Create `scripts/dual-query-surface-test.sh` with strict Bash following the Phase 29 gate style per D-14/D-16. The gate must check required Phase 30 context/research/pattern files, required Plan 30-01 and 30-02 summaries, the adapter crate files, and the fixture emitter. It must run `cargo test -p loom-dual-query-surface --test dependency_boundary`, `query_surface_contract`, and `duckdb_evidence`, then run the fixture emitter, build `loom-ffi` and `duckdb-ext` using the existing `scripts/duckdb-smoke-test.sh` DuckDB CLI/cache/build pattern, load `loom.duckdb_extension`, and execute the exact Plan 30-03 SQL cases through `loom_scan('${artifact_path}')`. Assert real query outputs: ordered rows `-1\n7\n42`, predicate rows `7\n42`, count `3`, and sum `48`. Because `.planning/debug/native-query-zero-buffer.md` records a resolved native-route issue, the gate must check row and aggregate values directly, not only route diagnostics. Do not wire the gate into `scripts/mvp0-verify.sh` yet; Plan 30-05 owns release-gate wiring.</action>
  <verify>
    <automated>bash -n scripts/dual-query-surface-test.sh && bash scripts/dual-query-surface-test.sh</automated>
  </verify>
  <done>The focused Phase 30 gate proves executable DuckDB query evidence over the same accepted artifact used by StarRocks-compatible descriptors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture emitter -> shell gate | Generated paths and descriptors are untrusted until the gate confirms files and accepted binding evidence. |
| DuckDB CLI -> evidence report | SQL output must match exact rows/aggregates, not just successful process exit. |
| existing DuckDB extension -> Phase 30 proof | Phase 30 may query through `loom_scan(path)` but must not widen DuckDB public SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Spoofing | fixture emitter output | mitigate | The emitter calls accepted binding/descriptor builders and the gate checks required generated files before DuckDB execution per D-03/D-05. |
| T-30-03-02 | Tampering | DuckDB SQL results | mitigate | The gate compares exact row and aggregate output for all supported query cases per D-04/D-07/D-16. |
| T-30-03-03 | Repudiation | native route/debug evidence | mitigate | Gate assertions use public SQL values, addressing the resolved native zero-buffer debug note rather than route text alone. |
| T-30-03-04 | Elevation of Privilege | public DuckDB route creep | mitigate | SQL strings and gate scans use only `loom_scan(path)` and reject route-specific public names per D-01/D-11/D-15. |
| T-30-SC | Tampering | Cargo/build dependencies | mitigate | No new package installs; existing DuckDB CLI download/cache pattern remains in the shell gate and Plan 30-01 dependency tests continue to guard manifests. |
</threat_model>

<verification>
Overall plan checks:
- `cargo test -p loom-dual-query-surface --test duckdb_evidence`
- `cargo run -p loom-dual-query-surface --bin emit_dual_query_fixture -- target/loom-dual-query-surface-test`
- `bash -n scripts/dual-query-surface-test.sh`
- `bash scripts/dual-query-surface-test.sh`
</verification>

<success_criteria>
Plan 30-03 is complete when Phase 30 has executable DuckDB evidence through the existing extension and public `loom_scan(path)`, the focused gate verifies real row/count/sum results from the generated Phase 29 accepted artifact, and no new public DuckDB route or ABI surface is added.
</success_criteria>

<output>
Create `.planning/phases/30-starrocks-duckdb-dual-query-surface/30-03-SUMMARY.md` when done.
</output>
