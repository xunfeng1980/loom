---
phase: 30-starrocks-duckdb-dual-query-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - crates/loom-dual-query-surface/src/lib.rs
  - crates/loom-dual-query-surface/src/query_surface.rs
  - crates/loom-dual-query-surface/src/fixture_bundle.rs
  - crates/loom-dual-query-surface/tests/query_surface_contract.rs
autonomous: true
requirements:
  - PHASE-30
must_haves:
  truths:
    - "D-03/D-05: every query-surface descriptor starts from a Phase 29 accepted binding, never metadata or sidecar flags alone."
    - "D-04/D-06/D-07: StarRocks-compatible descriptors record the same identity and bounded query matrix that DuckDB will execute later."
    - "D-08/D-16: deterministic fixture generation and descriptor validation fail closed on stale identity or unsupported query features."
  artifacts:
    - path: crates/loom-dual-query-surface/src/fixture_bundle.rs
      provides: "Deterministic local accepted binding fixture generator for Phase 30 tests and gates"
    - path: crates/loom-dual-query-surface/src/query_surface.rs
      provides: "Accepted binding to canonical query matrix and StarRocks descriptor construction"
    - path: crates/loom-dual-query-surface/tests/query_surface_contract.rs
      provides: "Positive contract tests for accepted binding identity, query matrix, and descriptor validation"
  key_links:
    - from: crates/loom-dual-query-surface/src/fixture_bundle.rs
      to: crates/loom-iceberg-binding/src/binding_contract.rs
      via: "`bind_iceberg_ref_from_paths` generated bundle acceptance"
      pattern: "bind_iceberg_ref_from_paths"
    - from: crates/loom-dual-query-surface/src/query_surface.rs
      to: crates/loom-core/src/table_codec.rs
      via: "decode accepted LMC1/LMT1 table bytes into canonical rows"
      pattern: "decode_table_to_array_data"
    - from: crates/loom-dual-query-surface/tests/query_surface_contract.rs
      to: crates/loom-dual-query-surface/src/query_surface.rs
      via: "query matrix descriptor assertions"
      pattern: "StarRocksQueryDescriptor"
---

<objective>
Build the accepted-binding query matrix and StarRocks-compatible descriptor contract.

Purpose: prove the second query surface is derived from the exact Phase 29 accepted artifact identity and canonical row evidence rather than a separate artifact format or sidecar-only metadata.
Output: deterministic fixture bundle generation, canonical query/result helpers, accepted StarRocks-compatible descriptors, and positive contract tests.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-CONTEXT.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-RESEARCH.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-PATTERNS.md
@.planning/phases/29-iceberg-ref-table-binding/29-ICEBERG-BINDING-REPORT.md
@.planning/phases/30-starrocks-duckdb-dual-query-surface/30-01-SUMMARY.md
@crates/loom-dual-query-surface/src/lib.rs
@crates/loom-dual-query-surface/src/query_surface.rs
@crates/loom-iceberg-binding/src/binding_contract.rs
@crates/loom-iceberg-binding/tests/binding_handoff.rs
@crates/loom-core/src/table_codec.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate a deterministic accepted binding bundle</name>
  <files>crates/loom-dual-query-surface/src/lib.rs, crates/loom-dual-query-surface/src/fixture_bundle.rs, crates/loom-dual-query-surface/tests/query_surface_contract.rs</files>
  <behavior>
    - A test writes a temporary local metadata JSON, sidecar JSON, source/oracle evidence JSON, source marker file, and verifier-accepted `LMC1(LMT1)` artifact.
    - The generated artifact contains one non-null Int32 table column named `id` with values `7, -1, 42`.
    - `bind_iceberg_ref_from_paths` accepts the bundle, returns non-empty artifact bytes, and exposes table UUID `9f1a03d0-61f7-4f6d-a7a4-3d8b983cbe30`, table name `demo.events`, schema ID `7`, snapshot ID `314159`, row count `3`, and recomputed artifact SHA-256 evidence.
  </behavior>
  <action>Create `fixture_bundle.rs` with a deterministic Phase 30 fixture helper modeled on Phase 29 `binding_handoff.rs` per D-03/D-05/D-16. The helper should write local files under a caller-provided or temporary directory, build an `LMC1(LMT1)` table artifact with `id` values `7, -1, 42`, compute SHA-256 using the existing project pattern, write matching metadata/sidecar/source-oracle evidence, and call `bind_iceberg_ref_from_paths` before returning any accepted bundle. Export only Phase 30 fixture helpers needed by crate tests and the later script; do not add checked-in binary fixture artifacts. Tests must prove that accepted bundle creation fails if the Phase 29 binding function does not accept the generated files.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test query_surface_contract accepted_fixture_bundle_is_bound_by_phase29_evidence</automated>
  </verify>
  <done>Phase 30 has deterministic local accepted binding fixture generation that is itself gated by Phase 29 binding acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the canonical query matrix from accepted bytes</name>
  <files>crates/loom-dual-query-surface/src/query_surface.rs, crates/loom-dual-query-surface/tests/query_surface_contract.rs</files>
  <behavior>
    - Canonical materialized rows for the generated accepted artifact are `-1, 7, 42` when ordered by `id`.
    - Projection over `id` preserves the same typed value set and records projection `id`.
    - Predicate `id >= 0` returns `7, 42`.
    - Aggregate evidence records `COUNT(*) = 3` and `SUM(id) = 48`.
    - Canonical result digests are deterministic and derived from typed rows, not display CSV formatting.
  </behavior>
  <action>Implement accepted-artifact decoding and canonical query-matrix construction per D-03/D-04/D-07. Decode the accepted `LMC1(LMT1)` bytes through existing `loom-core` table/container APIs and the MVP0 registry, support the current non-null Int32 table slice, and return unsupported diagnostics for shapes outside that slice rather than fabricating results per D-08/D-12. Represent the matrix as typed `CanonicalQueryResult` values for materialized rows ordered by `id`, projection `id`, predicate `id >= 0`, count, and sum. Compute result digests from stable typed canonical lines so later DuckDB and StarRocks-compatible checks compare evidence rather than display-only CSV strings.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test query_surface_contract canonical_query_matrix_matches_accepted_artifact_values</automated>
  </verify>
  <done>The crate can derive the required bounded query matrix from verifier-accepted Loom artifact bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit and validate StarRocks-compatible descriptors</name>
  <files>crates/loom-dual-query-surface/src/query_surface.rs, crates/loom-dual-query-surface/tests/query_surface_contract.rs</files>
  <behavior>
    - StarRocks-compatible descriptors contain table UUID, table name, schema ID, snapshot ID, artifact SHA-256, row count, projected columns, query kind, typed expected result digest, and SQL text.
    - Generated SQL is tiny and deterministic: selected rows/projection/predicate/count/sum over `demo.events`, with no DDL, catalog creation, credentials, external table registration, pushdown hint, join, or freeform accepted SQL.
    - Descriptor validation rejects any identity mismatch between descriptor and accepted binding facts.
  </behavior>
  <action>Add descriptor construction and validation functions per D-02/D-04/D-06/D-08. For each supported query matrix entry, emit a StarRocks-compatible descriptor with typed intent plus SQL text such as row/projection selection from `demo.events`, `WHERE id >= 0`, `COUNT(*)`, and `SUM(id)`. Validation must confirm the descriptor identity fields match the accepted Phase 29 binding facts exactly: table UUID, table name, schema ID, snapshot ID, artifact SHA-256, row count, and projection. Unsupported StarRocks runtime routes, remote catalogs, credentials, external table DDL, joins, nested fields, nullable expansion, and pushdown hints must be represented as unsupported diagnostics, not accepted descriptors per D-08/D-09/D-12.</action>
  <verify>
    <automated>cargo test -p loom-dual-query-surface --test query_surface_contract starrocks_descriptors_preserve_binding_identity_and_query_matrix</automated>
  </verify>
  <done>StarRocks-compatible offline descriptors are accepted only when they preserve the Phase 29 binding identity and bounded query matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture files -> accepted binding | Local metadata/sidecar/evidence are untrusted until Phase 29 binding acceptance succeeds. |
| accepted artifact bytes -> canonical query results | Decoding must use Loom verifier-backed artifact bytes, not sidecar display values. |
| StarRocks-compatible descriptor -> evidence claim | Descriptor SQL text must remain secondary to typed intent and identity validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Spoofing | generated sidecar/evidence files | mitigate | Fixture helper calls `bind_iceberg_ref_from_paths` and returns no accepted bundle unless Phase 29 validates hash, verifier, identity, and source/oracle evidence per D-03/D-05. |
| T-30-02-02 | Tampering | canonical query matrix | mitigate | Decode accepted bytes through `loom-core` table/container APIs and compare typed rows/digests, not sidecar text per D-04/D-16. |
| T-30-02-03 | Repudiation | StarRocks descriptor evidence | mitigate | Descriptors carry typed query kind, identity fields, expected digests, and SQL text; validation rejects identity drift per D-06. |
| T-30-02-04 | Elevation of Privilege | unsupported SQL features | mitigate | Query support is an allowlist; joins, DDL, remote catalogs, credentials, pushdown hints, and nested/nullable expansion return unsupported diagnostics per D-08/D-09/D-12. |
| T-30-SC | Tampering | Cargo dependency graph | mitigate | Plan continues using existing workspace/local dependencies only; Plan 30-01 boundary tests remain the install/dependency guard. |
</threat_model>

<verification>
Overall plan checks:
- `cargo test -p loom-dual-query-surface --test query_surface_contract`
- `cargo test -p loom-dual-query-surface --lib`
- `cargo check -p loom-dual-query-surface`
</verification>

<success_criteria>
Plan 30-02 is complete when the crate can generate a deterministic Phase 29-accepted local fixture bundle, decode the accepted artifact into the required query matrix, emit StarRocks-compatible descriptors that preserve binding identity, and reject unsupported shapes/features without accepting sidecar-only or descriptor-only evidence.
</success_criteria>

<output>
Create `.planning/phases/30-starrocks-duckdb-dual-query-surface/30-02-SUMMARY.md` when done.
</output>
