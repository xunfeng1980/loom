---
phase: 31-full-arrow-semantic-source-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/l1_model.rs
  - crates/loom-core/src/layout_codec.rs
  - crates/loom-core/src/descriptor.rs
  - crates/loom-core/src/lib.rs
  - .planning/phases/31-full-arrow-semantic-source-compatibility/31-ARROW-SEMANTIC-CONTRACT.md
autonomous: true
requirements:
  - PHASE-31
---

<objective>
Reset the source-compatibility substrate around Arrow semantics and remove the
superseded `NullableRaw` WIP path.
</objective>

<context>
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-CONTEXT.md
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-RESEARCH.md
@crates/loom-core/src/l1_model.rs
@crates/loom-core/src/layout_codec.rs
@crates/loom-core/src/descriptor.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clean superseded WIP and document the new substrate</name>
  <action>Remove any uncommitted `NullableRaw` implementation left from the abandoned core-80 attempt unless it is fully replaced by the Arrow semantic substrate in this phase. Add `31-ARROW-SEMANTIC-CONTRACT.md` defining `LMC2`/`LMA1`, accepted/rejected semantics, source-reader boundary, non-goals, and the fact that old `LMP1`/`LMT1` are legacy narrow payloads.</action>
  <acceptance_criteria>
    - `rg -n "NullableRaw" crates/loom-core` returns no result after cleanup, or every occurrence is explicitly part of the new Arrow semantic implementation and documented.
    - The contract states that arbitrary source compatibility is Arrow semantic compatibility, not `Raw` layout expansion.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add module scaffolds and dependency guards</name>
  <action>Add empty or minimal `arrow_semantic`, `arrow_semantic_codec`, and `arrow_semantic_verifier` modules in `loom-core`. Add tests/guards proving `loom-core` remains Lance/Parquet/Vortex-SDK-free while allowing Arrow crates already in the workspace.</action>
  <verify>
    <automated>cargo test -p loom-core arrow_semantic</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core arrow_semantic
</verification>
