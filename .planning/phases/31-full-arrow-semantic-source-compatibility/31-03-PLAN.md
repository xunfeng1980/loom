---
phase: 31-full-arrow-semantic-source-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 31-02
files_modified:
  - crates/loom-parquet-ingress/src/source_contract.rs
  - crates/loom-parquet-ingress/tests/full_arrow_schema_compatibility.rs
  - scripts/full-arrow-semantic-compatibility-test.sh
autonomous: true
requirements:
  - PHASE-31
---

<objective>
Replace Parquet's narrow primitive/table emission path with arbitrary Arrow
schema semantic artifact emission and equality tests.
</objective>

<context>
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-CONTEXT.md
@crates/loom-parquet-ingress/src/source_contract.rs
@crates/loom-core/src/arrow_semantic.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Parquet batches into `LMA1`</name>
  <action>Change Parquet accepted emission to read all materialized `RecordBatch` values through parquet-rs and encode them as Arrow semantic artifacts. Remove hard-coded `is_supported_primitive`, nullable rejection, string/nested/logical rejection, and per-type raw byte extraction from the accepted path. Keep malformed/unreadable files rejected.</action>
  <acceptance_criteria>
    - Nullable, string, binary, logical, and nested Parquet schemas are accepted when parquet-rs materializes them as Arrow.
    - Rejected files still carry diagnostics and no accepted bytes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Parquet full-schema fixture matrix</name>
  <action>Add generated Parquet fixtures for primitive, boolean, nullable, utf8/large_utf8, binary, decimal, date/time/timestamp, list, large_list, fixed_size_list, struct, map, dictionary, empty, all-null, and nested nullable combinations. For each fixture, compare source Arrow batches to Loom decoded Arrow batches.</action>
  <verify>
    <automated>cargo test -p loom-parquet-ingress --test full_arrow_schema_compatibility</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-parquet-ingress --test full_arrow_schema_compatibility
</verification>
