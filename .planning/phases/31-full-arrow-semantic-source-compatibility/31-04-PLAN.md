---
phase: 31-full-arrow-semantic-source-compatibility
plan: 04
type: execute
wave: 3
depends_on:
  - 31-02
files_modified:
  - ingress/loom-lance-ingress/src/source_contract.rs
  - ingress/loom-lance-ingress/tests/full_arrow_schema_compatibility.rs
  - scripts/full-arrow-semantic-compatibility-test.sh
autonomous: true
requirements:
  - PHASE-31
---

<objective>
Replace Lance's narrow primitive/table emission path with arbitrary Arrow schema
semantic artifact emission while preserving Lance field metadata evidence.
</objective>

<context>
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-CONTEXT.md
@ingress/loom-lance-ingress/src/source_contract.rs
@crates/loom-core/src/arrow_semantic.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Lance scanner batches into `LMA1`</name>
  <action>Change Lance accepted emission to scan materialized `RecordBatch` values and encode them as Arrow semantic artifacts. Remove hard-coded non-null primitive support restrictions from the accepted path. Preserve Lance schema field IDs, parent IDs, dataset version, fragment facts, and source identity as source facts/metadata in the report.</action>
  <acceptance_criteria>
    - Lance nullable, string, binary, logical, and nested schemas are accepted when the Lance reader materializes them as Arrow.
    - Lance field identity metadata is preserved as facts.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Lance full-schema fixture matrix</name>
  <action>Add generated Lance datasets for primitive, boolean, nullable, utf8/large_utf8, binary, decimal where Lance supports it, temporal, list, large_list, fixed_size_list/vector-like fields, struct, map where supported, empty, all-null, and nested nullable combinations. Compare Lance scan Arrow batches to Loom decoded Arrow batches.</action>
  <verify>
    <automated>cargo test -p loom-lance-ingress --test full_arrow_schema_compatibility</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-lance-ingress --test full_arrow_schema_compatibility
</verification>
