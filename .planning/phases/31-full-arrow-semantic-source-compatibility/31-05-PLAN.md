---
phase: 31-full-arrow-semantic-source-compatibility
plan: 05
type: execute
wave: 4
depends_on:
  - 31-02
  - 31-03
  - 31-04
files_modified:
  - ingress/loom-vortex-ingress/src/lib.rs
  - ingress/loom-vortex-ingress/tests/full_arrow_dtype_semantic_compatibility.rs
  - scripts/full-arrow-semantic-compatibility-test.sh
autonomous: true
requirements:
  - PHASE-31
---

<objective>
Upgrade Vortex compatibility from bounded canonical raw coverage to arbitrary
Vortex DType semantic compatibility through materialized Arrow oracle batches.
</objective>

<context>
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-CONTEXT.md
@ingress/loom-vortex-ingress/src/lib.rs
@crates/loom-core/src/arrow_semantic.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Vortex-to-Arrow semantic emission path</name>
  <action>Add a Vortex adapter path that materializes supported Vortex files/arrays to Arrow through Vortex reader APIs, encodes the resulting Arrow semantic artifact, verifies it, and compares decoded Loom Arrow output to Vortex oracle output. Keep Vortex SDK types isolated to `loom-vortex-ingress`.</action>
  <acceptance_criteria>
    - Vortex primitive, bool, utf8, binary, list, fixed-size-list, struct, decimal, and nullable dtypes are accepted when Vortex materializes them as Arrow.
    - Vortex layout/encoding facts remain recorded separately from semantic acceptance.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Vortex dtype fixture matrix</name>
  <action>Add fixtures/tests for Vortex root primitive, root bool, root utf8/binary, root list/fixed-size-list, root struct/table, nullable/all-null cases, and existing encoded layouts such as dictionary/run-end/bitpack/FOR when materializable. Compare materialized Vortex Arrow oracle to Loom decoded Arrow output.</action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress --test full_arrow_dtype_semantic_compatibility</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-vortex-ingress --test full_arrow_dtype_semantic_compatibility
</verification>
