---
phase: 31-full-arrow-semantic-source-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - crates/loom-core/src/arrow_semantic.rs
  - crates/loom-core/src/arrow_semantic_codec.rs
  - crates/loom-core/src/arrow_semantic_verifier.rs
  - crates/loom-core/tests/arrow_semantic_roundtrip.rs
autonomous: true
requirements:
  - PHASE-31
---

<objective>
Implement `LMA1` Arrow semantic payload encode/decode and verifier support for
full Arrow `ArrayData`/record-batch trees.
</objective>

<context>
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-CONTEXT.md
@.planning/phases/31-full-arrow-semantic-source-compatibility/31-RESEARCH.md
@crates/loom-core/src/table_codec.rs
@crates/loom-core/src/container_codec.rs
@crates/loom-core/src/artifact_verifier.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Arrow semantic artifact model</name>
  <action>Create Loom-owned structs for schema, record batch, array tree, buffers, children, dictionaries, source metadata hash, and verifier summary. Prefer direct `arrow_schema::Schema` and `arrow_data::ArrayData` at runtime while using a deterministic encoded representation for payload bytes.</action>
  <acceptance_criteria>
    - Model can represent primitive, boolean, string/binary, decimal, temporal, list, large list, fixed-size list, struct, map, dictionary, run-end, and union Arrow families.
    - Model preserves field metadata and schema metadata.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement codec and verifier</name>
  <action>Implement `encode_arrow_semantic_payload`, `decode_arrow_semantic_payload`, and `verify_arrow_semantic_payload`. Verification must call Arrow full validation where available and add Loom checks for row counts, column lengths, child offsets, dictionary key bounds, union type ids, and metadata hash consistency.</action>
  <verify>
    <automated>cargo test -p loom-core --test arrow_semantic_roundtrip</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add representative Arrow roundtrip tests</name>
  <action>Add roundtrip tests for null, bool, signed/unsigned integers, floats, utf8/large_utf8, binary/large_binary, decimal, date/time/timestamp/duration, list, large_list, fixed_size_list, struct, map, dictionary, run-end, dense/sparse union where supported by arrow-rs builders, sliced arrays with non-zero offset, and all-null/empty batches.</action>
  <verify>
    <automated>cargo test -p loom-core --test arrow_semantic_roundtrip</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test arrow_semantic_roundtrip
</verification>
