---
phase: 12-formal-verifier-safety-proof-mvp
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - crates/loom-core/tests/safety_contract.rs
  - crates/loom-ffi/tests/ffi_contract.rs
  - .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md
autonomous: true
requirements: [PROOF-02, PROOF-03]
must_haves:
  truths:
    - "D-12-07: stabilize existing verifier contract rather than rewrite APIs"
    - "D-12-08: malformed payloads must fail closed and not panic"
    - "D-12-10: termination proof scope is limited to current parser/interpreter loops"
  artifacts:
    - path: "crates/loom-core/tests/safety_contract.rs"
      provides: "focused no-panic/fail-closed safety contract tests"
      contains: "catch_unwind"
---

<objective>
Add focused executable evidence that malformed container, raw payload, descriptor, verifier, decode, and FFI inputs fail closed through typed errors/diagnostics instead of panicking.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add core safety contract tests for malformed bytes</name>
  <files>crates/loom-core/tests/safety_contract.rs</files>
  <read_first>
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md
    - crates/loom-core/src/container_codec.rs
    - crates/loom-core/src/layout_codec.rs
    - crates/loom-core/src/table_codec.rs
    - crates/loom-core/src/verifier.rs
  </read_first>
  <action>
    Add integration tests that use `std::panic::catch_unwind` around public core surfaces for curated malformed byte families: truncated `LMC1`, unsupported version, unknown required feature, malformed section offset/length, truncated `LMP1`, truncated `LMT1`, and invalid descriptor-derived payloads where practical. Assert that each case returns `Err` or a failing `VerificationReport`, and that no panic escapes.
  </action>
  <verify>
    <automated>cargo test -p loom-core --test safety_contract</automated>
  </verify>
  <acceptance_criteria>
    - Tests exercise container decode, raw payload decode, and verifier entry points.
    - Every curated malformed input returns a typed failure or failing report.
    - No test relies on exact prose error messages when diagnostic codes are available.
  </acceptance_criteria>
  <done>Core malformed input behavior has explicit no-panic/fail-closed regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add verifier and decode-before-Arrow contract coverage</name>
  <files>crates/loom-core/tests/safety_contract.rs</files>
  <read_first>
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/arrow_builder_output.rs
    - crates/loom-core/src/verifier.rs
    - scripts/verifier-negative-test.sh
  </read_first>
  <action>
    Add tests proving invalid verifier reports stop decode before Arrow output. Cover at least invalid bit width, raw byte length mismatch, validity length mismatch, non-monotonic run ends, unknown kernel, and table row-count mismatch. Use existing data model constructors or payload encode helpers rather than ad hoc byte offsets when structured APIs are available.
  </action>
  <verify>
    <automated>cargo test -p loom-core --test safety_contract</automated>
  </verify>
  <acceptance_criteria>
    - Test names map to `OBL-12-04`, `OBL-12-05`, and `OBL-12-06`.
    - Tests assert verifier diagnostics include stable codes/paths for representative failures.
    - Decode attempts fail before a successful Arrow `ArrayData` is returned.
  </acceptance_criteria>
  <done>Verifier failures are mechanically tied to decode fail-closed behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add FFI malformed-input contract coverage</name>
  <files>crates/loom-ffi/tests/ffi_contract.rs</files>
  <read_first>
    - crates/loom-ffi/src/ffi.rs
    - crates/loom-ffi/tests/roundtrip.rs
    - include/loom.h
    - scripts/check-core-invariants.sh
  </read_first>
  <action>
    Add or extend FFI tests so malformed bytes passed to `loom_decode` return `DecodeFailed` and the existing panic sentinel returns `Panicked`. Keep C ABI shape unchanged. Verify that no malformed byte path returns success or aborts the process.
  </action>
  <verify>
    <automated>cargo test -p loom-ffi ffi_contract</automated>
  </verify>
  <acceptance_criteria>
    - Malformed `LMC1`/`LMP1` bytes map to `LoomError::DecodeFailed`.
    - Panic sentinel still maps to `LoomError::Panicked`.
    - The test does not require DuckDB to run.
  </acceptance_criteria>
  <done>FFI ingress is covered as part of the safety proof, not only the happy path.</done>
</task>

<task type="auto">
  <name>Task 4: Update obligation matrix with executable evidence</name>
  <files>.planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - crates/loom-core/tests/safety_contract.rs
    - crates/loom-ffi/tests/ffi_contract.rs
  </read_first>
  <action>
    Mark obligations covered by the new tests as implemented evidence. Leave only true residual gaps for the script/docs closure plans.
  </action>
  <verify>
    <automated>rg -n "safety_contract|ffi_contract|OBL-12-0" .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - New tests are referenced by obligation ID.
    - Matrix statuses reflect actual coverage after tests pass.
  </acceptance_criteria>
  <done>Executable evidence is traceable from proof obligations.</done>
</task>

</tasks>

<verification>
- `cargo test -p loom-core --test safety_contract`
- `cargo test -p loom-ffi ffi_contract`
- `cargo test -p loom-core verifier container_codec`
- `git diff --check`
</verification>

<success_criteria>
- Curated malformed input families do not panic in core tests.
- Verifier failures prevent successful Arrow output.
- FFI malformed input returns typed error codes.
- Obligation matrix references the new executable evidence.
</success_criteria>

