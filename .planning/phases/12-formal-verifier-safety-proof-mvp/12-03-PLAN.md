---
phase: 12-formal-verifier-safety-proof-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/safety-proof-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md
autonomous: true
requirements: [PROOF-02, PROOF-04]
must_haves:
  truths:
    - "D-12-13: add a dedicated safety-proof gate"
    - "D-12-14: wire the safety-proof gate into scripts/mvp0-verify.sh"
    - "D-12-15: the gate must remain practical for local execution"
  artifacts:
    - path: "scripts/safety-proof-test.sh"
      provides: "dedicated Phase 12 release-gate component"
      contains: "OBL-12-01"
---

<objective>
Add a practical safety-proof gate that checks proof documentation consistency, static safety invariants, focused tests, and existing malformed-input gates, then wire it into the one-command release gate.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add safety proof gate script</name>
  <files>scripts/safety-proof-test.sh</files>
  <read_first>
    - scripts/mvp0-verify.sh
    - scripts/verifier-negative-test.sh
    - scripts/container-negative-test.sh
    - scripts/check-core-invariants.sh
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md
  </read_first>
  <action>
    Create `scripts/safety-proof-test.sh` following existing shell style. The script should verify required proof docs exist, every `OBL-12-01` through `OBL-12-09` appears in the obligation matrix, `loom-core` contains `#![forbid(unsafe_code)]`, `loom-ffi` contains `catch_unwind`, and focused tests/negative gates pass. Keep this script focused; do not duplicate the full workspace test already run by `mvp0-verify.sh`.
  </action>
  <verify>
    <automated>bash scripts/safety-proof-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script fails if proof docs or obligation IDs are missing.
    - Script runs the Phase 12 focused Rust tests and existing negative gates.
    - Script reports clear `[PASS]` or `[FAIL]` lines consistent with existing gate style.
  </acceptance_criteria>
  <done>Phase 12 proof evidence is executable from one dedicated command.</done>
</task>

<task type="auto">
  <name>Task 2: Wire safety proof gate into MVP0 release gate</name>
  <files>scripts/mvp0-verify.sh</files>
  <read_first>
    - scripts/mvp0-verify.sh
    - scripts/safety-proof-test.sh
  </read_first>
  <action>
    Invoke `bash scripts/safety-proof-test.sh` from `scripts/mvp0-verify.sh` after workspace tests and before DuckDB SQL smoke. Avoid duplicating existing negative gate invocations if the new safety proof script already runs them; keep output readable and avoid running the same expensive command unnecessarily.
  </action>
  <verify>
    <automated>bash scripts/mvp0-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mvp0-verify.sh` fails if `safety-proof-test.sh` fails.
    - Existing dependency guard, fixture hygiene, and DuckDB smoke checks still run.
    - Duplicate negative-gate execution is removed or explicitly justified.
  </acceptance_criteria>
  <done>The main release gate enforces Phase 12 safety proof evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Update proof matrix with gate evidence</name>
  <files>.planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - scripts/safety-proof-test.sh
    - scripts/mvp0-verify.sh
  </read_first>
  <action>
    Add the new script and main release gate as evidence for `OBL-12-09`, and update any obligations whose gate coverage now comes from the dedicated script.
  </action>
  <verify>
    <automated>rg -n "safety-proof-test.sh|mvp0-verify.sh|OBL-12-09" .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `OBL-12-09` is marked covered by both dedicated and main release gates.
    - Matrix remains consistent with actual script behavior.
  </acceptance_criteria>
  <done>Gate evidence is reflected in the proof matrix.</done>
</task>

</tasks>

<verification>
- `bash scripts/safety-proof-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Dedicated safety proof gate exists and passes.
- Main MVP0 release gate invokes the safety proof gate.
- The gate checks docs, obligation IDs, static invariants, focused tests, and malformed-input negative gates.
</success_criteria>

