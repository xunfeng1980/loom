---
phase: 12-formal-verifier-safety-proof-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md
  - .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md
autonomous: true
requirements: [PROOF-01, PROOF-03, PROOF-05]
must_haves:
  truths:
    - "D-12-01: proof target is the current implemented boundary only"
    - "D-12-03: correctness remains out of scope beyond existing oracle tests"
    - "D-12-04: proof artifact is obligation matrix + executable gates + written safety argument"
    - "D-12-08: contract is typed failure, fail closed before Arrow output, and no panic on malformed payloads"
  artifacts:
    - path: ".planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md"
      provides: "reviewer-visible obligation matrix"
      contains: "OBL-12-01"
---

<objective>
Define the Phase 12 safety contract and proof-obligation matrix for the current implemented `LMC1`/`LMP1`/`LMT1` decode boundary, without expanding into future Loom IR or native lowering proofs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write the narrow safety contract</name>
  <files>.planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md</files>
  <read_first>
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-CONTEXT.md
    - crates/loom-core/src/error.rs
    - crates/loom-core/src/verifier.rs
    - crates/loom-ffi/src/ffi.rs
  </read_first>
  <action>
    Document the exact Phase 12 safety contract: typed parse/decode/verifier failures, fail-closed behavior before Arrow output, no panic on attacker-controlled malformed input, `loom-core` unsafe-code prohibition, FFI panic containment, and correctness exclusions. Make the contract explicit about supported public surfaces: core decode helpers, verifier entry points, CLI inspect/decode, FFI `loom_decode`, and DuckDB `loom_scan`.
  </action>
  <verify>
    <automated>rg -n "implemented boundary|fail closed|typed|panic|Arrow output|out of scope" .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Contract names the current byte-to-Arrow boundary and all public ingress surfaces.
    - Contract explicitly excludes future L2 language proof, MLIR/native lowering, real Vortex file ingress, signatures, attestation, and correctness beyond oracle tests.
    - Contract distinguishes stable diagnostic code/path categories from non-stable prose message wording.
  </acceptance_criteria>
  <done>Reviewers can tell exactly what Phase 12 is claiming and not claiming.</done>
</task>

<task type="auto">
  <name>Task 2: Create proof-obligation matrix</name>
  <files>.planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md</files>
  <read_first>
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-RESEARCH.md
    - crates/loom-core/src/container_codec.rs
    - crates/loom-core/src/layout_codec.rs
    - crates/loom-core/src/table_codec.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/verifier.rs
    - scripts/verifier-negative-test.sh
    - scripts/container-negative-test.sh
  </read_first>
  <action>
    Add `OBL-12-01` through `OBL-12-09` as a matrix with claim, boundary, source evidence, executable evidence, release-gate evidence, status, and known gaps. Use planned statuses for obligations that need tests or gate wiring in later plans.
  </action>
  <verify>
    <automated>for id in OBL-12-01 OBL-12-02 OBL-12-03 OBL-12-04 OBL-12-05 OBL-12-06 OBL-12-07 OBL-12-08 OBL-12-09; do rg -q "$id" .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Every obligation has at least one source evidence entry.
    - Every obligation has a planned or existing executable evidence entry.
    - Gaps are explicitly marked rather than hidden.
  </acceptance_criteria>
  <done>The safety argument has stable IDs that implementation/tests/scripts can target.</done>
</task>

<task type="auto">
  <name>Task 3: Audit loop bounds and unsafe boundaries</name>
  <files>.planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md, .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md</files>
  <read_first>
    - crates/loom-core/src/lib.rs
    - crates/loom-core/src/container_codec.rs
    - crates/loom-core/src/verifier.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/l1_model/bitpack.rs
    - crates/loom-core/src/l2_kernel_registry.rs
    - crates/loom-core/src/fsst_params.rs
    - crates/loom-core/src/alp_params.rs
    - crates/loom-ffi/src/lib.rs
    - crates/loom-ffi/src/ffi.rs
  </read_first>
  <action>
    Add a loop-bound/unsafe-boundary section to the proof docs. Record each relevant parser/interpreter/kernel loop family and the finite input value that bounds it. Record that `loom-core` forbids unsafe code and that FFI unsafety is isolated to `loom-ffi`.
  </action>
  <verify>
    <automated>rg -n "section_count|row_count|column count|buffer length|forbid\\(unsafe_code\\)|catch_unwind|OBL-12-06|OBL-12-01" .planning/phases/12-formal-verifier-safety-proof-mvp</automated>
  </verify>
  <acceptance_criteria>
    - Termination argument covers container sections, table columns, verifier recursion, bitpack chunks, RLE runs, dictionary codes, FSST rows, and ALP rows.
    - Unsafe argument points to `loom-core` and `loom-ffi` crate-level boundaries.
    - Any unresolved loop-bound uncertainty becomes a follow-up in the matrix.
  </acceptance_criteria>
  <done>The proof docs explain why current loops are finite and where unsafe code is allowed.</done>
</task>

</tasks>

<verification>
- `rg -n "OBL-12-0[1-9]" .planning/phases/12-formal-verifier-safety-proof-mvp/12-PROOF-OBLIGATIONS.md`
- `rg -n "fail closed|Arrow output|catch_unwind|forbid\\(unsafe_code\\)" .planning/phases/12-formal-verifier-safety-proof-mvp`
- `git diff --check`
</verification>

<success_criteria>
- Phase 12 has a concrete safety contract.
- Phase 12 has stable proof-obligation IDs.
- The obligation matrix identifies current evidence and remaining executable-coverage gaps.
- Future formal proof work remains explicitly out of scope.
</success_criteria>

