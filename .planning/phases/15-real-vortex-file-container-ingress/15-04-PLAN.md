---
phase: 15-real-vortex-file-container-ingress
plan: "04"
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - crates/loom-cli/Cargo.toml
  - crates/loom-cli/src/main.rs
  - scripts/mvp0-verify.sh
  - scripts/vortex-ingress-test.sh
  - README.md
  - README-zh.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-REPORT.md
  - .planning/phases/15-real-vortex-file-container-ingress/15-01-SUMMARY.md
  - .planning/phases/15-real-vortex-file-container-ingress/15-02-SUMMARY.md
  - .planning/phases/15-real-vortex-file-container-ingress/15-03-SUMMARY.md
  - .planning/phases/15-real-vortex-file-container-ingress/15-04-SUMMARY.md
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, INGEST-05]
must_haves:
  truths:
    - "Phase 15 is a narrow real-ingress boundary, not a complete Vortex reader"
    - "Phase 16 remains the full melior/LLVM/JIT backend integration placeholder"
    - "Real ingress must stay verifier-gated through LMC1 where supported"
  artifacts:
    - path: "scripts/vortex-ingress-test.sh"
      provides: "focused Phase 15 gate"
      contains: "vortex-ingress"
    - path: ".planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md"
      provides: "Phase 15 ingress contract"
      contains: "Dependency Boundary"
---

<objective>
Expose real Vortex ingress through CLI inspection, wire the focused gate into
the release gate, document the narrow scope, and close Phase 15 planning state
after all verification passes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI real-ingress inspection</name>
  <files>crates/loom-cli/Cargo.toml, crates/loom-cli/src/main.rs</files>
  <read_first>
    - crates/loom-cli/src/main.rs
    - ingress/loom-vortex-ingress/src/lib.rs
    - crates/loom-core/src/container_codec.rs
  </read_first>
  <action>
    Add CLI support such as `loom ingest-vortex --inspect <path>` and, if 15-03 emission is stable, `loom ingest-vortex --emit-loom <input.vortex> <output.loom>`. The CLI may depend on `loom-vortex-ingress` but must not depend on `vortex-file` directly. Output should include status, diagnostics, row count, dtype summary, layout summary, segment summary, statistics presence, and whether a supported Loom payload can be emitted.
  </action>
  <verify>
    <automated>cargo run -p loom-cli -- ingest-vortex --help</automated>
    <automated>cargo run -p loom-cli -- ingest-vortex --inspect fixtures/vortex/*</automated>
    <automated>rg -n "ingest-vortex|VortexIngressReport|emit-loom|inspect" crates/loom-cli/src/main.rs crates/loom-cli/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - CLI prints ingress diagnostics without panicking.
    - CLI can inspect the supported generated real Vortex fixture.
    - CLI direct dependencies do not include `vortex-file`.
  </acceptance_criteria>
  <done>Reviewers can inspect real Vortex ingress from the CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release gate and docs</name>
  <files>scripts/mvp0-verify.sh, README.md, README-zh.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - scripts/mvp0-verify.sh
    - scripts/vortex-ingress-test.sh
    - README.md
    - README-zh.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Wire `scripts/vortex-ingress-test.sh` into `scripts/mvp0-verify.sh` after the native-lowering gate and before DuckDB smoke. Update README and README-zh to state that Phase 15 adds a narrow real Vortex ingress boundary with isolated `vortex-file` usage, stable facts/diagnostics, and one supported `.vortex` -> `LMC1` slice. Mark `INGEST-01` through `INGEST-05` complete in requirements. Mark Phase 15 complete in ROADMAP and STATE while keeping Phase 16 as the next placeholder.
  </action>
  <verify>
    <automated>rg -n "Phase 15|real Vortex ingress|vortex-ingress-test|INGEST-0|Phase 16" README.md README-zh.md scripts/mvp0-verify.sh .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Public docs do not claim arbitrary Vortex layout support.
    - Planning docs identify Phase 16 as the full `melior`/LLVM/JIT placeholder.
    - Requirements and roadmap agree on Phase 15 completion.
  </acceptance_criteria>
  <done>Phase 15 docs and release gate are wired.</done>
</task>

<task type="auto">
  <name>Task 3: Write final ingress report and summaries</name>
  <files>.planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-REPORT.md, .planning/phases/15-real-vortex-file-container-ingress/15-01-SUMMARY.md, .planning/phases/15-real-vortex-file-container-ingress/15-02-SUMMARY.md, .planning/phases/15-real-vortex-file-container-ingress/15-03-SUMMARY.md, .planning/phases/15-real-vortex-file-container-ingress/15-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md
    - .planning/phases/15-real-vortex-file-container-ingress/15-RESEARCH.md
    - ingress/loom-vortex-ingress/src/lib.rs
    - scripts/vortex-ingress-test.sh
  </read_first>
  <action>
    Create `15-INGRESS-REPORT.md` summarizing the final supported scope, dependency boundary, report fields, stable diagnostics, real fixture evidence, oracle comparison, CLI behavior, and deferred work. Write plan summaries with changed files, verification commands, decisions, and follow-ups.
  </action>
  <verify>
    <automated>rg -n "dependency boundary|VortexIngressReport|diagnostic|fixture|oracle|CLI|deferred" .planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Report states Phase 15 is not a complete Vortex reader.
    - Report records exact verification commands and final gate result.
    - Summaries are sufficient for future Phase 16 planning.
  </acceptance_criteria>
  <done>Phase 15 has final reviewer-readable closeout docs.</done>
</task>

<task type="auto">
  <name>Task 4: Run final verification</name>
  <files>verification output only</files>
  <read_first>
    - scripts/vortex-ingress-test.sh
    - scripts/mvp0-verify.sh
  </read_first>
  <action>
    Run the full final gate and record any notable local toolchain facts in the final report. Required commands: `cargo test --workspace`, `bash scripts/vortex-ingress-test.sh`, `bash scripts/check-core-invariants.sh`, `bash scripts/mvp0-verify.sh`, and `git diff --check`.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
    <automated>bash scripts/vortex-ingress-test.sh</automated>
    <automated>bash scripts/check-core-invariants.sh</automated>
    <automated>bash scripts/mvp0-verify.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - All final gates pass.
    - Any skipped optional evidence is explicitly documented.
    - No untracked generated junk remains outside intended fixtures/docs.
  </acceptance_criteria>
  <done>Phase 15 is verified and ready for commit/review.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace`
- `bash scripts/vortex-ingress-test.sh`
- `bash scripts/check-core-invariants.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<rollback>
Remove CLI wiring and release-gate integration, leaving ingress crate tests and
docs for follow-up repair.
</rollback>
