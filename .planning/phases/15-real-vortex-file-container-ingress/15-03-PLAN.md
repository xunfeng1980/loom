---
phase: 15-real-vortex-file-container-ingress
plan: "03"
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - crates/loom-vortex-ingress/src/lib.rs
  - crates/loom-vortex-ingress/tests/real_file_to_loom.rs
  - crates/loom-vortex-ingress/src/bin/emit_vortex_ingress_fixtures.rs
  - fixtures/vortex/
  - fixtures/loom/
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "D-15-04: Supported conversion slice is intentionally small"
    - "D-15-05: Vortex scan is oracle evidence, not the implementation"
    - "Supported real files must enter the existing LMC1 verifier/decode path"
  artifacts:
    - path: "crates/loom-vortex-ingress/src/lib.rs"
      provides: "real Vortex inspection from 15-02"
      contains: "inspect_vortex_buffer"
---

<objective>
Add one supported real `.vortex` -> `LMC1` conversion slice and prove Loom's
decoded output matches Vortex oracle rows for that real file.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement supported conversion API</name>
  <files>crates/loom-vortex-ingress/src/lib.rs</files>
  <read_first>
    - crates/loom-vortex-ingress/src/lib.rs
    - crates/loom-fixtures/src/vortex_reader.rs
    - crates/loom-core/src/layout_codec.rs
    - crates/loom-core/src/container_codec.rs
    - crates/loom-core/src/verifier.rs
  </read_first>
  <action>
    Add a narrow API such as `emit_supported_lmc1_from_vortex_buffer(bytes: &[u8]) -> Result<Vec<u8>, VortexIngressReport>` and a support predicate in the ingress crate. The first supported slice should be a generated local real Vortex file whose scanned array can be translated to an existing `LayoutDescription` or `TableDescription` without adding new Loom core layout variants. Unsupported dtypes/layouts/validity must return the report with unsupported diagnostics and no partial payload. The emitted bytes must be an existing `LMC1` container wrapping `LMP1` or `LMT1`.
  </action>
  <verify>
    <automated>rg -n "emit_supported_lmc1_from_vortex_buffer|LMC1|encode_layout_payload|wrap_layout_payload|VortexIngressReport" crates/loom-vortex-ingress/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - Emission is gated by explicit support checks.
    - Emission uses existing Loom container/payload codecs.
    - Unsupported files cannot emit partial `.loom` bytes.
  </acceptance_criteria>
  <done>The ingress crate can emit an existing Loom container for one supported real Vortex slice.</done>
</task>

<task type="auto">
  <name>Task 2: Add real file to Loom roundtrip tests</name>
  <files>crates/loom-vortex-ingress/tests/real_file_to_loom.rs</files>
  <read_first>
    - crates/loom-vortex-ingress/src/lib.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/container_codec.rs
    - crates/loom-core/src/verifier.rs
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vortex-file-0.74.0/src/tests.rs
  </read_first>
  <action>
    Add tests that generate the supported real Vortex file in memory, inspect it, emit `LMC1`, verify the emitted container with `verify_container`, decode it through Loom, and compare row values/nulls to Vortex scan oracle output. Add at least one unsupported real file shape test that returns unsupported diagnostics and no `LMC1`.
  </action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress real_file_to_loom</automated>
    <automated>bash scripts/vortex-ingress-test.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Supported real Vortex fixture decodes through Loom with oracle equality.
    - Emitted bytes are container-wrapped and pass the existing verifier.
    - Unsupported real Vortex fixture fails closed before Loom decode.
  </acceptance_criteria>
  <done>Phase 15 has real file to Loom evidence for one narrow slice.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic fixture emitter</name>
  <files>crates/loom-vortex-ingress/src/bin/emit_vortex_ingress_fixtures.rs, fixtures/vortex/, fixtures/loom/</files>
  <read_first>
    - crates/loom-fixtures/src/bin/emit_duckdb_payloads.rs
    - crates/loom-vortex-ingress/src/lib.rs
  </read_first>
  <action>
    Add an emitter binary that writes the supported real `.vortex` fixture under `fixtures/vortex/` and the emitted `LMC1` payload under `fixtures/loom/` or the repo's existing generated fixture location. The emitter should be deterministic and should print the fact summary. Do not commit large generated files if existing project practice regenerates fixtures during gates; otherwise keep them tiny and reviewer-readable by CLI output.
  </action>
  <verify>
    <automated>cargo run -p loom-vortex-ingress --bin emit_vortex_ingress_fixtures</automated>
    <automated>rg -n "Vortex ingress|row_count|LMC1|supported" crates/loom-vortex-ingress/src/bin/emit_vortex_ingress_fixtures.rs</automated>
  </verify>
  <acceptance_criteria>
    - Fixture emitter is deterministic.
    - Generated fixture is tiny enough for repo review or is explicitly regenerated by gate.
    - The emitted `.loom` fixture can be inspected/decoded by existing CLI commands.
  </acceptance_criteria>
  <done>Supported real ingress fixtures can be generated repeatably.</done>
</task>

</tasks>

<verification>
- `cargo test -p loom-vortex-ingress`
- `cargo run -p loom-vortex-ingress --bin emit_vortex_ingress_fixtures`
- `bash scripts/vortex-ingress-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<rollback>
Remove the conversion API/tests/emitter while preserving metadata-only
inspection from 15-02.
</rollback>
