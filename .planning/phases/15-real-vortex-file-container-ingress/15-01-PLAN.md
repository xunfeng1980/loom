---
phase: 15-real-vortex-file-container-ingress
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md
  - Cargo.toml
  - crates/loom-vortex-ingress/Cargo.toml
  - crates/loom-vortex-ingress/src/lib.rs
  - scripts/check-core-invariants.sh
  - scripts/mvp0-verify.sh
autonomous: true
requirements: [INGEST-01, INGEST-02]
must_haves:
  truths:
    - "D-15-01: Isolate real Vortex file APIs"
    - "D-15-02: Replace the old global no-vortex-file guard"
    - "D-15-03: Facts first, conversion second"
  artifacts:
    - path: ".planning/phases/15-real-vortex-file-container-ingress/15-RESEARCH.md"
      provides: "Phase 15 research recommendation"
      contains: "VortexFileFacts"
    - path: ".planning/phases/15-real-vortex-file-container-ingress/15-CONTEXT.md"
      provides: "locked Phase 15 decisions"
      contains: "D-15-01"
---

<objective>
Define the Phase 15 ingress contract, introduce the isolated ingress crate, and
replace the obsolete global `vortex-file` ban with scoped dependency and API
guards that preserve the Loom core proof boundary.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write the ingress contract</name>
  <files>.planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md</files>
  <read_first>
    - .planning/phases/15-real-vortex-file-container-ingress/15-RESEARCH.md
    - .planning/phases/15-real-vortex-file-container-ingress/15-CONTEXT.md
    - .planning/phases/11-distribution-container-v0/11-CONTAINER-SPEC.md
    - .planning/phases/12-formal-verifier-safety-proof-mvp/12-SAFETY-CONTRACT.md
  </read_first>
  <action>
    Create `15-INGRESS-CONTRACT.md` with sections named `Scope`, `Dependency Boundary`, `Ingress Report Schema`, `Stable Diagnostics`, `Supported Conversion Slice`, `Verifier Routing`, `Oracle Use`, and `Non-Goals`. State that `vortex-file` is allowed only in the ingress layer, that `loom-core` and `loom-ffi` stay Vortex-free, and that unsupported real files produce diagnostics rather than partial `.loom` output.
  </action>
  <verify>
    <automated>rg -n "Scope|Dependency Boundary|Ingress Report Schema|Stable Diagnostics|Supported Conversion Slice|Verifier Routing|Oracle Use|Non-Goals" .planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Contract states real Vortex APIs are isolated from `loom-core` and `loom-ffi`.
    - Contract states facts/diagnostics exist even for unsupported files.
    - Contract states Phase 15 does not add native lowering or arbitrary Vortex layout support.
  </acceptance_criteria>
  <done>Phase 15 has a reviewer-visible ingress contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add isolated ingress crate and report skeleton</name>
  <files>Cargo.toml, crates/loom-vortex-ingress/Cargo.toml, crates/loom-vortex-ingress/src/lib.rs</files>
  <read_first>
    - Cargo.toml
    - crates/loom-fixtures/Cargo.toml
    - crates/loom-fixtures/src/vortex_reader.rs
    - rust-toolchain.toml
  </read_first>
  <action>
    Add `crates/loom-vortex-ingress` to the workspace. The crate may depend on `vortex-file = 0.74.0` and supporting Vortex crates, and may depend on `loom-core`; no reverse dependency from `loom-core` is allowed. Add stable public types such as `VortexIngressStatus`, `VortexIngressDiagnosticCode`, `VortexIngressDiagnostic`, `VortexFileFacts`, `VortexIngressReport`, and a placeholder `inspect_vortex_buffer(bytes: &[u8]) -> VortexIngressReport` that returns an unsupported/not-yet-inspected diagnostic until 15-02 implements real opening.
  </action>
  <verify>
    <automated>rg -n "loom-vortex-ingress|vortex-file|VortexIngressReport|VortexFileFacts|VortexIngressDiagnosticCode" Cargo.toml crates/loom-vortex-ingress/Cargo.toml crates/loom-vortex-ingress/src/lib.rs</automated>
    <automated>cargo test -p loom-vortex-ingress</automated>
  </verify>
  <acceptance_criteria>
    - The new crate compiles independently.
    - `loom-core` does not depend on the new crate.
    - The report/diagnostic API uses Loom-owned structs and stable string codes.
  </acceptance_criteria>
  <done>An isolated ingress crate and report model exist.</done>
</task>

<task type="auto">
  <name>Task 3: Replace global vortex-file ban with scoped guards</name>
  <files>scripts/check-core-invariants.sh, scripts/mvp0-verify.sh</files>
  <read_first>
    - scripts/check-core-invariants.sh
    - scripts/mvp0-verify.sh
    - .planning/phases/15-real-vortex-file-container-ingress/15-CONTEXT.md
  </read_first>
  <action>
    Update the invariant scripts so `vortex-file` is no longer globally forbidden in `Cargo.lock`. Add scoped checks: `cargo tree -p loom-core` and `cargo tree -p loom-ffi` must contain zero Vortex/FastLanes dependencies; only `crates/loom-vortex-ingress/Cargo.toml` may directly name `vortex-file`; file-backed Vortex API tokens are allowed only in `crates/loom-vortex-ingress` and narrowly in CLI wiring once 15-04 adds it. Keep existing fixture hygiene until 15-03 deliberately routes real files through the ingress crate.
  </action>
  <verify>
    <automated>bash scripts/check-core-invariants.sh</automated>
    <automated>bash scripts/mvp0-verify.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The release gate permits `vortex-file` only through the new ingress boundary.
    - `loom-core` and `loom-ffi` dependency guards remain fail-closed.
    - Existing MVP0 tests still pass after the dependency boundary change.
  </acceptance_criteria>
  <done>The dependency policy now matches Phase 15 without weakening core isolation.</done>
</task>

</tasks>

<verification>
- `cargo test -p loom-vortex-ingress`
- `bash scripts/check-core-invariants.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<rollback>
Remove `crates/loom-vortex-ingress`, remove it from workspace members, restore
the old scripts, and leave `15-INGRESS-CONTRACT.md` as planning-only context.
</rollback>
