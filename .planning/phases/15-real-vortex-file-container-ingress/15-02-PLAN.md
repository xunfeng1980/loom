---
phase: 15-real-vortex-file-container-ingress
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - ingress/loom-vortex-ingress/src/lib.rs
  - ingress/loom-vortex-ingress/tests/ingress_facts.rs
  - scripts/vortex-ingress-test.sh
autonomous: true
requirements: [INGEST-02, INGEST-03]
must_haves:
  truths:
    - "D-15-03: Facts first, conversion second"
    - "Unsupported or malformed real files fail closed with stable diagnostics"
  artifacts:
    - path: "ingress/loom-vortex-ingress/src/lib.rs"
      provides: "ingress report model from 15-01"
      contains: "VortexIngressReport"
    - path: ".planning/phases/15-real-vortex-file-container-ingress/15-INGRESS-CONTRACT.md"
      provides: "stable ingress contract"
      contains: "Stable Diagnostics"
---

<objective>
Implement real Vortex buffer/path inspection that emits deterministic
Loom-owned metadata facts and stable fail-closed diagnostics without converting
files to `.loom` yet.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement real buffer/path inspection</name>
  <files>ingress/loom-vortex-ingress/src/lib.rs</files>
  <read_first>
    - ingress/loom-vortex-ingress/src/lib.rs
    - .planning/phases/15-real-vortex-file-container-ingress/15-RESEARCH.md
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vortex-file-0.74.0/src/open.rs
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vortex-file-0.74.0/src/file.rs
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vortex-file-0.74.0/src/footer/mod.rs
  </read_first>
  <action>
    Implement `inspect_vortex_buffer(bytes: &[u8]) -> VortexIngressReport` and `inspect_vortex_path(path: &Path) -> VortexIngressReport`. Use a local Vortex session with default encodings, `VortexOpenOptions::open_buffer` for deterministic buffer tests, and `open_path` for local path inspection. Populate facts including row count, dtype summary, layout summary, segment count, segment byte ranges, alignment summary, statistics presence, footer approximate byte size, source kind, and a supported/unsupported classification. Convert all Vortex errors into stable diagnostics; do not panic.
  </action>
  <verify>
    <automated>rg -n "inspect_vortex_buffer|inspect_vortex_path|open_buffer|open_path|row_count|segment_ranges|statistics_present" ingress/loom-vortex-ingress/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - Valid real Vortex buffers produce `status=accepted` or `status=unsupported` with facts present.
    - Invalid/malformed buffers produce diagnostics and no facts.
    - No Vortex type appears in the public report fields.
  </acceptance_criteria>
  <done>Real Vortex files can be opened and summarized as Loom-owned facts.</done>
</task>

<task type="auto">
  <name>Task 2: Add positive and negative ingress fact tests</name>
  <files>ingress/loom-vortex-ingress/tests/ingress_facts.rs</files>
  <read_first>
    - ingress/loom-vortex-ingress/src/lib.rs
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vortex-file-0.74.0/src/tests.rs
    - crates/loom-fixtures/tests/wave0_checks.rs
  </read_first>
  <action>
    Add tests that generate a tiny in-memory Vortex file using Vortex 0.74.0 writer APIs and inspect it through `inspect_vortex_buffer`. Assert deterministic facts: source kind, positive row count, dtype summary, segment count, and statistics presence field. Add negative tests for empty input, bad magic, truncated EOF/postscript, and mutated version or postscript length where practical. All negative cases must return diagnostics instead of panicking.
  </action>
  <verify>
    <automated>cargo test -p loom-vortex-ingress ingress_facts</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - At least one generated real Vortex buffer opens successfully.
    - At least three malformed inputs fail closed with stable diagnostic codes.
    - Tests do not write persistent fixture files yet.
  </acceptance_criteria>
  <done>Ingress facts and malformed file handling are covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused ingress gate script</name>
  <files>scripts/vortex-ingress-test.sh</files>
  <read_first>
    - scripts/native-lowering-test.sh
    - scripts/full-verifier-test.sh
    - scripts/mvp0-verify.sh
  </read_first>
  <action>
    Add `scripts/vortex-ingress-test.sh` using the existing gate script style. It should run focused ingress crate tests, assert the ingress contract exists, assert scoped dependency guards are present in `scripts/check-core-invariants.sh`, and assert Phase 15 docs mention fail-closed real Vortex ingress. Do not wire it into `mvp0-verify.sh` until 15-04.
  </action>
  <verify>
    <automated>bash scripts/vortex-ingress-test.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The focused gate passes independently.
    - The gate fails if ingress facts tests are skipped.
    - The gate does not require DuckDB or native lowering beyond existing release gates.
  </acceptance_criteria>
  <done>Phase 15 has a focused ingress verification script.</done>
</task>

</tasks>

<verification>
- `cargo test -p loom-vortex-ingress ingress_facts`
- `bash scripts/vortex-ingress-test.sh`
- `bash scripts/check-core-invariants.sh`
- `git diff --check`
</verification>

<rollback>
Revert the inspection implementation and tests while keeping the 15-01 crate
boundary in place.
</rollback>
