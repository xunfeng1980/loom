---
phase: 37-lean-stage-b-lean-rust-verifier-correspondence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formal/lean/LoomCore.lean
autonomous: true
requirements:
  - LINEAGE-03
must_haves:
  truths:
    - "D-37-01: Lean gains ScalarExpr and LetScalar concepts that mirror the current Rust verifier slice."
    - "D-37-02: Lean statements carry scalar expressions where Rust does instead of relying on the current flattened Nat projection."
    - "D-37-03: builder_events_typed derives append value types from expressions through a scalar type environment."
    - "D-37-04: Lean models LetScalar insertion and UnknownVariable rejection."
    - "D-37-05: Overflow and solver-only obligations remain delegated to existing Rust/Bitwuzla evidence."
---

<objective>
Enrich the Lean L2Core checker so the covered static verifier slice mirrors the
executable Rust verifier's `ScalarExpr` / `LetScalar` typing behavior instead of
the current lossy `Nat`-grounded projection.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Replace the flattened Lean AST projection with Rust-parity scalar expression forms</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-CONTEXT.md</file>
    <file>.planning/phases/36-verified-lineage-contract-and-tcb-declaration/36-VERIFIED-LINEAGE-CONTRACT.md</file>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/src/l2_core.rs</file>
    <file>crates/loom-core/src/full_verifier.rs</file>
  </read_first>
  <action>Update `formal/lean/LoomCore.lean` to add Lean equivalents for the Rust scalar value/expression slice: constants, variables, arithmetic expressions, min/max, and comparisons. Add a `letScalar` statement constructor. Change `readInput`, `appendValue`, `forRange`, and `cursorLoop` so offsets, widths, append values, loop bounds, limits, and progress use scalar expressions where Rust uses `ScalarExpr`. Preserve existing capability and program structures unless a small local adjustment is needed for parity.</action>
  <acceptance_criteria>
    <criterion>`rg -n "inductive ScalarValue|inductive ScalarExpr|letScalar|appendValue .*ScalarExpr|readInput .*ScalarExpr" formal/lean/LoomCore.lean` shows the new scalar-expression AST surface.</criterion>
    <criterion>The opening Lean file comments no longer describe the covered checker as a pre-resolved `Nat`-grounded AST projection for `ScalarExpr` / `LetScalar` parity.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Lean scalar type environment and expression-derived append typing</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/src/full_verifier.rs</file>
  </read_first>
  <action>Add a simple scalar type environment and Lean `typeOfExpr?` checker that mirrors Rust `verify_expr` / `type_of_const` behavior for the covered slice. Thread the environment through statement checking. `letScalar` must insert a derived type when its expression is known. `appendValue` must compare the builder's expected scalar type against `typeOfExpr?` for the appended value expression rather than accepting an explicit type argument.</action>
  <acceptance_criteria>
    <criterion>`rg -n "typeOfExpr|ScalarEnv|UnknownVariable|builder.*expected|letScalar" formal/lean/LoomCore.lean` shows explicit scalar typing and unknown-variable handling.</criterion>
    <criterion>Lean examples or check definitions cover a valid `letScalar` -> `appendValue` path and an unknown-variable rejection path.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve bounds, authority, and solver-delegation semantics under the enriched AST</name>
  <files>formal/lean/LoomCore.lean</files>
  <read_first>
    <file>formal/lean/LoomCore.lean</file>
    <file>crates/loom-core/src/full_verifier.rs</file>
    <file>.planning/phases/19-solver-backed-full-artifact-verifier/19-SOLVER-CONTRACT.md</file>
  </read_first>
  <action>Adapt `checkAuthority`, `checkBounds`, `builder_events_typed`, `no_ambient_authority`, `finite_bounds`, `Verified`, and existing theorems to the enriched AST. Keep finite constant loop and cursor checks aligned with Rust `const_u64` / `is_monotone_progress`. Document that overflow/range proof obligations emitted by Rust remain SMT/Bitwuzla evidence, not Lean-proven obligations in this plan.</action>
  <acceptance_criteria>
    <criterion>`lean formal/lean/LoomCore.lean` exits 0.</criterion>
    <criterion>`rg -n "NonMonotoneCursorLoop|InvalidLoopBounds|ResourceBudgetExceeded|MissingInputCapability|MissingOutputBuilder|OutputTypeMismatch|OutputNullabilityMismatch|UnknownVariable" formal/lean/LoomCore.lean` shows the relevant diagnostic vocabulary remains visible.</criterion>
    <criterion>`rg -n "Bitwuzla|SMT|overflow|delegated" formal/lean/LoomCore.lean` shows overflow/solver obligations remain explicitly delegated.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
lean formal/lean/LoomCore.lean
rg -n "inductive ScalarValue|inductive ScalarExpr|letScalar|appendValue .*ScalarExpr|readInput .*ScalarExpr" formal/lean/LoomCore.lean
rg -n "typeOfExpr|ScalarEnv|UnknownVariable|builder.*expected|letScalar" formal/lean/LoomCore.lean
rg -n "NonMonotoneCursorLoop|InvalidLoopBounds|ResourceBudgetExceeded|MissingInputCapability|MissingOutputBuilder|OutputTypeMismatch|OutputNullabilityMismatch|UnknownVariable" formal/lean/LoomCore.lean
rg -n "Bitwuzla|SMT|overflow|delegated" formal/lean/LoomCore.lean
git diff --check
</verification>

<success_criteria>
Plan 37-01 is complete when Lean compiles with `ScalarExpr` / `LetScalar`
typing parity for the covered static verifier slice, derives append value types
from expressions, models unknown variables, and keeps SMT-only arithmetic
obligations explicitly outside Lean parity claims.
</success_criteria>

<output>
Create `37-01-SUMMARY.md` when done.
</output>

