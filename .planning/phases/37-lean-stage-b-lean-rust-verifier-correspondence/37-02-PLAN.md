---
phase: 37-lean-stage-b-lean-rust-verifier-correspondence
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - crates/loom-core/tests/full_verifier.rs
  - formal/lean/LoomCore.lean
  - scripts/full-verifier-test.sh
  - scripts/mvp0-verify.sh
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - LINEAGE-03
  - LINEAGE-04
must_haves:
  truths:
    - "D-37-06: The Lean/Rust differential harness uses the current full verifier matrix plus deterministic bounded fuzz cases."
    - "D-37-07: The harness compares accept/reject and stable reject classification."
    - "D-37-09: Any divergence fails closed."
    - "D-37-11: A focused correspondence gate is wired into the verifier/release path."
    - "D-37-14: Phase 37 claims correspondence evidence only, not soundness or executor correctness."
---

<objective>
Add deterministic Lean<->Rust differential evidence for the current L2Core
verifier slice, wire it into the release gate, and close LINEAGE-03/LINEAGE-04
without expanding Rust L2Core or claiming soundness.
</objective>

<tasks>
<task type="tdd">
  <name>Task 1: Define the shared correspondence corpus and required reject-code coverage</name>
  <files>crates/loom-core/tests/full_verifier.rs, formal/lean/LoomCore.lean</files>
  <read_first>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-CONTEXT.md</file>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-01-SUMMARY.md</file>
    <file>crates/loom-core/tests/full_verifier.rs</file>
    <file>crates/loom-core/tests/l2_core_model.rs</file>
    <file>formal/lean/LoomCore.lean</file>
  </read_first>
  <action>Create or extend a deterministic verifier correspondence corpus that includes the current full verifier fixture matrix plus bounded generated cases. The corpus must include accept/reject rows for `MissingInputCapability`, `MissingOutputBuilder`, `InvalidLoopBounds`, `NonMonotoneCursorLoop`, and `ResourceBudgetExceeded`; include `UnknownVariable`, `OutputTypeMismatch`, `OutputNullabilityMismatch`, and `ConstraintBudgetExceeded` when practical. Choose a shared representation that both Rust tests and Lean checks can consume or audit.</action>
  <acceptance_criteria>
    <criterion>`rg -n "MissingInputCapability|MissingOutputBuilder|InvalidLoopBounds|NonMonotoneCursorLoop|ResourceBudgetExceeded" crates/loom-core/tests/full_verifier.rs formal/lean/LoomCore.lean` shows all required reject-code coverage.</criterion>
    <criterion>`rg -n "UnknownVariable|OutputTypeMismatch|OutputNullabilityMismatch" crates/loom-core/tests/full_verifier.rs formal/lean/LoomCore.lean` shows additional parity coverage or the summary explains why a code remains deferred.</criterion>
    <criterion>The corpus generation is deterministic; if seeded, the seed is visible in code or script output.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test full_verifier</automated>
    <automated>lean formal/lean/LoomCore.lean</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed Lean/Rust classification comparison</name>
  <files>crates/loom-core/tests/full_verifier.rs, formal/lean/LoomCore.lean</files>
  <read_first>
    <file>crates/loom-core/src/full_verifier.rs</file>
    <file>crates/loom-core/tests/full_verifier.rs</file>
    <file>formal/lean/LoomCore.lean</file>
  </read_first>
  <action>Add a correspondence comparison that records, for each corpus case, the Rust verifier classification and the Lean checker classification. It must fail if one side accepts and the other rejects, if both reject with different codes, or if a required case is missing. Keep comparison output stable and concise so divergence is actionable.</action>
  <acceptance_criteria>
    <criterion>`cargo test -p loom-core --test full_verifier` exits 0 and includes the correspondence corpus check.</criterion>
    <criterion>`lean formal/lean/LoomCore.lean` exits 0 and includes Lean-side expected classifications for the same corpus or an auditable shared subset.</criterion>
    <criterion>`rg -n "correspondence|divergence|reject_code|reject code|accept/reject" crates/loom-core/tests/full_verifier.rs formal/lean/LoomCore.lean` shows explicit comparison semantics.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test full_verifier</automated>
    <automated>lean formal/lean/LoomCore.lean</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire the correspondence gate into verifier and release scripts</name>
  <files>scripts/full-verifier-test.sh, scripts/mvp0-verify.sh</files>
  <read_first>
    <file>scripts/full-verifier-test.sh</file>
    <file>scripts/mvp0-verify.sh</file>
    <file>.planning/phases/36-verified-lineage-contract-and-tcb-declaration/36-VERIFIED-LINEAGE-CONTRACT.md</file>
  </read_first>
  <action>Wire the correspondence comparison into the existing verifier/release path. Prefer keeping `scripts/full-verifier-test.sh` as the focused gate unless a new `scripts/lean-rust-correspondence-test.sh` makes the evidence clearer; if a new focused script is added, call it from `scripts/full-verifier-test.sh` or the release path. The gate must fail closed on divergence and must not be optional.</action>
  <acceptance_criteria>
    <criterion>`bash scripts/full-verifier-test.sh` exits 0 and runs the Lean/Rust correspondence evidence.</criterion>
    <criterion>`rg -n "full-verifier-test|lean-rust|correspondence" scripts/mvp0-verify.sh scripts/full-verifier-test.sh scripts/*.sh` shows the gate is reachable from the release path.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/full-verifier-test.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Close Phase 37 requirements, docs, roadmap, and summary</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-02-SUMMARY.md</files>
  <read_first>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-CONTEXT.md</file>
    <file>.planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-01-SUMMARY.md</file>
    <file>.planning/phases/36-verified-lineage-contract-and-tcb-declaration/36-VERIFIED-LINEAGE-CONTRACT.md</file>
    <file>.planning/PROJECT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <action>Add or complete `LINEAGE-03` for Lean AST/type parity and `LINEAGE-04` for Lean/Rust differential gate evidence. Mark Phase 37 complete in roadmap/state only after verification passes. Update project/state wording to say Phase 37 establishes Lean/Rust verifier correspondence evidence only, not operational semantics, soundness, interpreter correctness, native correctness, or source correctness. Create `37-02-SUMMARY.md` recording files changed, verification commands, required reject-code coverage, residual risks, and Phase 38 handoff.</action>
  <acceptance_criteria>
    <criterion>`rg -n "LINEAGE-03|LINEAGE-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-02-SUMMARY.md` shows both requirements represented and closed.</criterion>
    <criterion>`rg -n "Phase 37.*Complete|Phase 38.*READY|Lean.*Rust.*correspondence" .planning/STATE.md .planning/ROADMAP.md` shows Phase 37 complete and Phase 38 ready/current.</criterion>
    <criterion>`rg -n "not.*soundness|no.*soundness|correspondence evidence only|not.*native|not.*source correctness" .planning/PROJECT.md .planning/STATE.md .planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-02-SUMMARY.md` shows non-claims remain explicit.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
cargo test -p loom-core --test full_verifier
cargo test -p loom-core --test l2_core_model
lean formal/lean/LoomCore.lean
bash scripts/full-verifier-test.sh
rg -n "MissingInputCapability|MissingOutputBuilder|InvalidLoopBounds|NonMonotoneCursorLoop|ResourceBudgetExceeded" crates/loom-core/tests/full_verifier.rs formal/lean/LoomCore.lean
rg -n "correspondence|divergence|reject_code|reject code|accept/reject" crates/loom-core/tests/full_verifier.rs formal/lean/LoomCore.lean scripts/full-verifier-test.sh
rg -n "LINEAGE-03|LINEAGE-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/37-lean-stage-b-lean-rust-verifier-correspondence/37-02-SUMMARY.md
rg -n "Phase 37.*Complete|Phase 38.*READY|Lean.*Rust.*correspondence" .planning/STATE.md .planning/ROADMAP.md
git diff --check
</verification>

<success_criteria>
Plan 37-02 is complete when the current full verifier matrix plus deterministic
bounded fuzz corpus has zero Lean/Rust accept/reject and reject-code divergence,
the correspondence check is wired into the verifier/release path, LINEAGE-03 and
LINEAGE-04 are closed, and the phase summary preserves correspondence-only
non-claims.
</success_criteria>

<output>
Create `37-02-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

- Lean `ScalarExpr` / `LetScalar` typing parity in `formal/lean/LoomCore.lean`
- Lean/Rust verifier correspondence corpus and comparison evidence
- Release-gated verifier correspondence check
- Requirement IDs: `LINEAGE-03`, `LINEAGE-04`
