---
phase: 35-native-arrow-semantic-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/src/lib.rs
  - crates/loom-core/tests/native_arrow_semantic.rs
autonomous: true
requirements:
  - PHASE-35
must_haves:
  truths:
    - "D-35-01: Native Arrow semantic execution accepts verifier-backed LMC2(LMA1) before producing output."
    - "D-35-02: Supported primitive nullable columns are copied into a new Arrow RecordBatch, not borrowed as the decoded reference."
    - "D-35-03: Unsupported Arrow shapes fail closed with stable diagnostics and no output."
---

<objective>
Implement the engine-neutral native Arrow semantic executor for the first
fixed-width primitive nullable slice.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add positive and negative native Arrow semantic tests</name>
  <files>crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Add tests for wrapped `LMC2(LMA1)` nullable Int32/Int64/Float64/Boolean native execution, direct `LMA1` bridge execution, Utf8 unsupported, Date32 unsupported, Struct unsupported, multi-batch unsupported, and malformed/verifier-rejected input.</action>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement native Arrow semantic support and execution reports</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/src/lib.rs</files>
  <action>Add a new module that verifies artifact bytes, decodes `LMC2(LMA1)` or direct `LMA1`, requires exactly one batch, copies supported primitive nullable columns via typed Arrow builders, and returns stable support/execution diagnostics.</action>
  <acceptance_criteria>
    - The backend string is `loom-native-arrow-semantic`.
    - Positive output is a newly-created `RecordBatch`.
    - Nulls and Boolean value semantics are preserved.
    - Unsupported shapes expose deterministic diagnostic codes and paths.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic</automated>
    <automated>cargo test -p loom-core --test artifact_verifier arrow_semantic</automated>
  </verify>
</task>

</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic
cargo test -p loom-core --test artifact_verifier arrow_semantic
git diff --check
</verification>

<success_criteria>
Plan 35-01 is complete when `loom-core` can produce native equivalent Arrow
output for verifier-accepted one-batch primitive nullable `LMC2(LMA1)` artifacts
and fail closed for unsupported Arrow semantic shapes.
</success_criteria>

<output>
Create `35-01-SUMMARY.md` when done.
</output>
