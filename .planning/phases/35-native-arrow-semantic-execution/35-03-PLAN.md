---
phase: 35-native-arrow-semantic-execution
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - crates/loom-core/src/runtime_abi.rs
  - crates/loom-core/tests/runtime_execution_policy.rs
  - crates/loom-core/tests/runtime_cache_key.rs
  - crates/loom-core/tests/native_arrow_semantic.rs
autonomous: true
requirements:
  - PHASE-35
must_haves:
  truths:
    - "D-35-06: Runtime/cache evidence can identify native Arrow semantic execution without host-engine vocabulary."
    - "D-35-07: Unsupported Arrow semantic shapes remain fail-closed or interpreter-policy controlled, never positive native evidence."
---

<objective>
Connect native Arrow semantic evidence to host-neutral runtime/cache vocabulary.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add native Arrow semantic route/cache identity evidence</name>
  <files>crates/loom-core/src/runtime_abi.rs, crates/loom-core/tests/runtime_execution_policy.rs, crates/loom-core/tests/runtime_cache_key.rs, crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Add or reuse host-neutral backend identity/cache-key helpers so native Arrow semantic execution can be keyed by backend, artifact bytes, projection, and policy without naming DuckDB.</action>
  <verify>
    <automated>cargo test -p loom-core --test runtime_execution_policy</automated>
    <automated>cargo test -p loom-core --test runtime_cache_key</automated>
    <automated>cargo test -p loom-core --test native_arrow_semantic cache</automated>
  </verify>
</task>
</tasks>

<verification>
cargo test -p loom-core --test runtime_execution_policy
cargo test -p loom-core --test runtime_cache_key
cargo test -p loom-core --test native_arrow_semantic
git diff --check
</verification>

<success_criteria>
Plan 35-03 is complete when the native Arrow semantic backend has deterministic
engine-neutral route/cache evidence and unsupported shapes cannot seed positive
native cache entries.
</success_criteria>

<output>
Create `35-03-SUMMARY.md` when done.
</output>
