---
phase: 35-native-arrow-semantic-execution
plan: 02
type: execute
wave: 1
depends_on: [35-01]
files_modified:
  - crates/loom-core/src/native_arrow_semantic.rs
  - crates/loom-core/tests/native_arrow_semantic.rs
autonomous: true
requirements:
  - PHASE-35
must_haves:
  truths:
    - "D-35-04: Native Arrow semantic equivalence is explicit evidence, not inferred from successful execution."
    - "D-35-05: Mismatch evidence is fail-closed and distinguishable from unsupported-shape diagnostics."
---

<objective>
Add explicit native/reference equivalence reporting for Arrow semantic execution.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add equivalence report and mismatch diagnostics</name>
  <files>crates/loom-core/src/native_arrow_semantic.rs, crates/loom-core/tests/native_arrow_semantic.rs</files>
  <action>Expose a comparison helper that decodes the reference batch, compares native output arrays by column, and returns accepted equivalence evidence or `native-output-mismatch` diagnostics.</action>
  <verify>
    <automated>cargo test -p loom-core --test native_arrow_semantic equivalence</automated>
  </verify>
</task>
</tasks>

<verification>
cargo test -p loom-core --test native_arrow_semantic
git diff --check
</verification>

<success_criteria>
Plan 35-02 is complete when positive native execution includes explicit
equivalence evidence and an injected mismatch fails closed.
</success_criteria>

<output>
Create `35-02-SUMMARY.md` when done.
</output>
