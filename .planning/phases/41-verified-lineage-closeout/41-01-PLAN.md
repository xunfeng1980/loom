# Phase 41-01 Plan: Combined Verified-Lineage Gate

**Status:** Planned
**Date:** 2026-06-09
**Requirements:** LINEAGE-11

## Goal

Add one closeout gate, `scripts/verified-lineage-test.sh`, that runs and checks
the full MVP1.5 lineage evidence matrix fail-closed.

## Tasks

1. Add `scripts/verified-lineage-test.sh`.
2. Run:
   - `lean formal/lean/LoomCore.lean`;
   - no-`sorry` scan for `formal/lean/LoomCore.lean`;
   - `bash scripts/lean-rust-correspondence-test.sh`;
   - `bash scripts/model-rust-interpreter-consistency-test.sh`;
   - `bash scripts/native-model-validation-test.sh`.
3. Add marker checks for:
   - `accepted_program_safe`;
   - `ModeledExecutionSafe`;
   - `readSafety` and `inBounds := false`;
   - `native-model-trace-mismatch`;
   - `per-run-validation;mlir-llvm-native-lowering-tcb`;
   - Phase 36 verified-lineage contract non-claims.
4. Wire the new gate into `scripts/full-verifier-test.sh`.
5. Update requirements and planning state for LINEAGE-11.
6. Add `41-01-SUMMARY.md`.

## Verification

```sh
bash scripts/verified-lineage-test.sh
bash scripts/full-verifier-test.sh
git diff --check
```

## Non-Goals

- No new proof theorem.
- No new source format or execution coverage.
- No verified-compilation claim.
- No artifact record implementation; that is `41-02`.

Self-Check: READY
