---
phase: 20
plan: 20-05
title: MLIR validation gate, report, and closeout
type: execute
wave: 5
depends_on:
  - 20-04
files_modified:
  - crates/loom-native-melior/src/pipeline.rs
  - crates/loom-native-melior/tests/production_pipeline.rs
  - scripts/production-native-lowering-test.sh
  - scripts/mvp0-verify.sh
  - README.md
  - README-zh.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-NATIVE-LOWERING-REPORT.md
  - .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-SUMMARY.md
autonomous: true
requirements: []
---

<objective>
Close Phase 20 with optional/strict MLIR 22 validation, release-gate evidence, public/planning docs, and a clear Phase 21/22 handoff.
</objective>

<context>
By this point Phase 20 should have a production lowering gate, `loom.decode` dialect text, primitive Arrow/raw-buffer builder lowering, and primitive native kernel expansion. The closeout must validate evidence without claiming host-engine execution or arbitrary Vortex coverage.
</context>

<threat_model>
- Threat: optional MLIR absence is reported as success.
  Mitigation: normal mode prints explicit `[SKIP]`; strict mode fails when MLIR 22 validation is unavailable.
- Threat: docs overclaim production native compiler or host execution.
  Mitigation: final report states Phase 20 is a production lowering surface, not Phase 22/23 runtime integration.
- Threat: Phase 21/22 consume unsupported matrix implicitly.
  Mitigation: final report records exact supported and deferred matrix.
</threat_model>

<tasks>
  <task id="1" type="execute">
    <title>Add production MLIR validation evidence</title>
    <read_first>
      <file>crates/loom-native-melior/src/pipeline.rs</file>
      <file>crates/loom-native-melior/src/toolchain.rs</file>
      <file>crates/loom-core/src/decode_dialect.rs</file>
      <file>crates/loom-core/src/arrow_buffer_lowering.rs</file>
    </read_first>
    <files>crates/loom-native-melior/src/pipeline.rs, crates/loom-native-melior/tests/production_pipeline.rs</files>
    <action>
      Extend optional MLIR validation to cover the Phase 20 standard-MLIR text emitted from the production builder lowering path. Keep validation skip-aware in default tests and strict in explicit mode. Do not require a compiled `loom.decode` dialect unless 20-02 deliberately stabilized and implemented one.
    </action>
    <verify>
      <automated>cargo test -p loom-native-melior production_pipeline</automated>
      <automated>cargo test -p loom-native-melior pipeline</automated>
      <automated>rg -n "production|decode|mlir-opt|EXPECTED_MLIR_MAJOR|toolchain-version-mismatch" crates/loom-native-melior/src crates/loom-native-melior/tests/production_pipeline.rs</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Production standard-MLIR text can be validated when compatible MLIR 22 tools are available.</criterion>
      <criterion>Missing/incompatible toolchain remains visible and skip-aware in normal mode.</criterion>
      <criterion>Strict mode treats missing/incompatible validation as failure.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <title>Create Phase 20 gate script</title>
    <read_first>
      <file>scripts/melior-jit-test.sh</file>
      <file>scripts/solver-verifier-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
    </read_first>
    <files>scripts/production-native-lowering-test.sh, scripts/mvp0-verify.sh</files>
    <action>
      Add `scripts/production-native-lowering-test.sh`. It should run the Phase 20 core tests, production kernel tests, and optional MLIR validation. Use `LOOM_REQUIRE_PRODUCTION_NATIVE=1` for strict mode. Wire the script into `scripts/mvp0-verify.sh` after the Phase 19 solver gate and before host/duckdb smoke checks.
    </action>
    <verify>
      <automated>bash scripts/production-native-lowering-test.sh</automated>
      <automated>LOOM_REQUIRE_PRODUCTION_NATIVE=1 bash scripts/production-native-lowering-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>rg -n "production-native-lowering-test|LOOM_REQUIRE_PRODUCTION_NATIVE|Phase 20" scripts/production-native-lowering-test.sh scripts/mvp0-verify.sh</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Normal Phase 20 gate exits 0 and prints explicit pass/skip evidence.</criterion>
      <criterion>Strict gate requires compatible MLIR 22 validation on machines where strict mode is requested.</criterion>
      <criterion>`scripts/mvp0-verify.sh` invokes the Phase 20 gate.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <title>Write final native lowering report and docs</title>
    <read_first>
      <file>.planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-RESEARCH.md</file>
      <file>.planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-LOWERING-CONTRACT.md</file>
      <file>.planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-DECODE-DIALECT.md</file>
      <file>README.md</file>
      <file>README-zh.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/STATE.md</file>
    </read_first>
    <files>README.md, README-zh.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-NATIVE-LOWERING-REPORT.md, .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-SUMMARY.md</files>
    <action>
      Write `20-NATIVE-LOWERING-REPORT.md` with sections named `Scope`, `Trust Boundary`, `Dialect Contract`, `Supported Matrix`, `Arrow Builder Lowering`, `MLIR Validation`, `Commands Run`, `Deferred Work`, `Phase 21 Handoff`, and `Phase 22 Handoff`. Write `20-SUMMARY.md` with shipped files, commands, deviations, residual risks, and `Self-Check: PASSED` only after final verification. Update public docs concisely to say Phase 20 adds production native-lowering surface evidence, not host native execution.
    </action>
    <verify>
      <automated>rg -n "Trust Boundary|Dialect Contract|Supported Matrix|Arrow Builder Lowering|MLIR Validation|Phase 21 Handoff|Phase 22 Handoff|Self-Check" .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-NATIVE-LOWERING-REPORT.md .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-SUMMARY.md</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>Final report records exact supported primitive/native matrix.</criterion>
      <criterion>Final report states Phase 21 owns expanded Vortex encoding coverage.</criterion>
      <criterion>Final report states Phase 22 owns host runtime ABI/execution policy.</criterion>
      <criterion>Docs do not claim production host execution, arbitrary Vortex support, or checked proof objects.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="execute">
    <title>Run final Phase 20 verification</title>
    <read_first>
      <file>scripts/production-native-lowering-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>.planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-NATIVE-LOWERING-REPORT.md</file>
    </read_first>
    <files>.planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-NATIVE-LOWERING-REPORT.md, .planning/phases/20-production-decode-dialect-and-native-kernel-expansion/20-SUMMARY.md</files>
    <action>
      Run and record final verification commands: `cargo test -p loom-core --test production_native_lowering`, `cargo test -p loom-core --test decode_dialect`, `cargo test -p loom-core --test arrow_buffer_lowering`, `cargo test -p loom-core --test production_native_kernels`, `cargo test -p loom-native-melior production_pipeline`, `bash scripts/production-native-lowering-test.sh`, `bash scripts/mvp0-verify.sh`, and `git diff --check`.
    </action>
    <verify>
      <automated>cargo test -p loom-core --test production_native_lowering</automated>
      <automated>cargo test -p loom-core --test decode_dialect</automated>
      <automated>cargo test -p loom-core --test arrow_buffer_lowering</automated>
      <automated>cargo test -p loom-core --test production_native_kernels</automated>
      <automated>cargo test -p loom-native-melior production_pipeline</automated>
      <automated>bash scripts/production-native-lowering-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>All final non-strict verification commands exit 0.</criterion>
      <criterion>Strict MLIR/native validation result is recorded accurately as pass or unavailable/fail.</criterion>
      <criterion>`20-SUMMARY.md` records `Self-Check: PASSED` only after final commands pass.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<must_haves>
- Phase 20 closeout is release-gated.
- Optional MLIR evidence is visibly pass/skip and strict mode is fail-closed.
- Final docs avoid host-execution overclaims.
- Phase 21 and Phase 22 handoffs are explicit.
</must_haves>

<verification>
- `cargo test -p loom-core --test production_native_lowering`
- `cargo test -p loom-core --test decode_dialect`
- `cargo test -p loom-core --test arrow_buffer_lowering`
- `cargo test -p loom-core --test production_native_kernels`
- `cargo test -p loom-native-melior production_pipeline`
- `bash scripts/production-native-lowering-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Phase 20 closes with a verifier-backed production native-lowering surface, optional/strict MLIR 22 validation, exact supported matrix documentation, and clean handoffs to Phase 21 and Phase 22.
</success_criteria>

