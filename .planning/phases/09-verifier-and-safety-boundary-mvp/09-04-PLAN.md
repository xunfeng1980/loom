---
phase: 09-verifier-and-safety-boundary-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - README.md
  - README-zh.md
  - .planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, VERIFY-06]
must_haves:
  truths:
    - "D-16: stale FOR-over-non-BitPack todo is audited and closed or updated with implementation evidence"
    - "Phase 9 must not claim formal totality proof"
    - "scripts/mvp0-verify.sh is the final release gate"
  artifacts:
    - path: "README.md"
      provides: "Verifier MVP documentation"
      contains: "verification"
    - path: ".planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md"
      provides: "todo closure/update source if still pending"
      contains: "cr-02"
---

<objective>
Close Phase 9 with documentation, stale todo cleanup, final release verification, and planning-state updates only after all verifier gates pass.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Document verifier MVP behavior</name>
  <files>README.md, README-zh.md</files>
  <read_first>
    - README.md
    - README-zh.md
    - .planning/phases/09-verifier-and-safety-boundary-mvp/09-CONTEXT.md
    - scripts/mvp0-verify.sh
    - scripts/verifier-negative-test.sh
  </read_first>
  <action>
    Update README and README-zh to describe Phase 9's implemented verifier as a first-pass structural verifier, not a formal totality proof. Document that `loom inspect` shows `verification: pass|fail`, and that `bash scripts/mvp0-verify.sh` includes curated negative verifier coverage. Keep language factual and avoid implying the full Loom safety model is complete.
  </action>
  <verify>
    <automated>rg -n 'verification|verifier|structural' README.md README-zh.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'structural verifier|verification:' README.md` returns a line.
    - `rg -n 'structural verifier|verification:' README-zh.md` returns a line, or the Chinese equivalent appears with `verification:`.
    - README states or implies that formal totality proof remains future work.
  </acceptance_criteria>
  <done>Public docs describe the verifier MVP accurately and do not overclaim formal safety.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and close/update stale FOR todo</name>
  <files>.planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md, .planning/todos/resolved/cr-02-decode-for-non-bitpack-reference.md</files>
  <read_first>
    - .planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/verifier.rs
    - crates/loom-core/tests/verifier_decode_authority.rs
  </read_first>
  <action>
    Audit `cr-02-decode-for-non-bitpack-reference.md` against current code and Phase 9 tests. If Phase 9 includes a passing FOR-over-non-BitPack regression test proving the reference is applied, move the todo from `.planning/todos/pending/` to `.planning/todos/resolved/` and add a short resolution note with the command that passed. If the test reveals the issue is still real, keep it pending and update the todo with current evidence and the next phase target. Do not silently delete the todo.
  </action>
  <verify>
    <automated>test -f .planning/todos/resolved/cr-02-decode-for-non-bitpack-reference.md || test -f .planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one of `.planning/todos/resolved/cr-02-decode-for-non-bitpack-reference.md` or `.planning/todos/pending/cr-02-decode-for-non-bitpack-reference.md` exists.
    - The existing todo text contains a Phase 9 resolution/update note.
    - The note names the verifier or FOR-over-non-BitPack test command used as evidence.
  </acceptance_criteria>
  <done>The historical FOR warning is no longer ambiguous for future planning.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 9 verification and close requirements</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/09-verifier-and-safety-boundary-mvp/09-CONTEXT.md
    - scripts/mvp0-verify.sh
  </read_first>
  <action>
    Run the final Phase 9 gates: `cargo test --workspace`, `bash scripts/verifier-negative-test.sh`, `bash scripts/mvp0-verify.sh`, `cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}'`, and `git diff --check`. Only after all commands pass, update `.planning/REQUIREMENTS.md` to mark `SAFE-01`, `SAFE-02`, `SAFE-03`, `SAFE-04`, and `VERIFY-06` complete; update `.planning/ROADMAP.md` Phase 9 plan checkboxes/status; update `.planning/STATE.md` current position; and write Phase 9 summaries as required by execute-phase conventions.
  </action>
  <verify>
    <automated>cargo test --workspace && bash scripts/verifier-negative-test.sh && bash scripts/mvp0-verify.sh && test "$(cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}')" = "0" && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0.
    - `bash scripts/verifier-negative-test.sh` exits 0.
    - `bash scripts/mvp0-verify.sh` exits 0.
    - `cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}'` prints `0`.
    - `git diff --check` exits 0.
    - Requirements `SAFE-01`, `SAFE-02`, `SAFE-03`, `SAFE-04`, and `VERIFY-06` are marked complete only after the commands above pass.
  </acceptance_criteria>
  <done>Phase 9 is verified, documented, and ready for commit/closeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation -> user expectations | Docs must not overclaim formal safety from a structural verifier. |
| Planning state -> future work | Requirements and todo state guide future phases and must match verified reality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-11 | Repudiation | docs | mitigate | State explicitly that Phase 9 is structural verifier MVP, not formal totality proof. |
| T-09-12 | Tampering | planning state | mitigate | Mark requirements complete only after final commands pass. |
| T-09-13 | Repudiation | stale todo | mitigate | Move/update todo with evidence rather than deleting it. |

No high open threats remain if docs and planning state match final verification.
</threat_model>

<verification>
- `rg -n 'verification|verifier|structural' README.md README-zh.md`
- `cargo test --workspace`
- `bash scripts/verifier-negative-test.sh`
- `bash scripts/mvp0-verify.sh`
- `cargo tree -p loom-core | awk '/vortex|fastlanes/{c++} END{print c+0}'`
- `git diff --check`
</verification>

<success_criteria>
- README and README-zh document verifier MVP behavior without formal-safety overclaim.
- Stale FOR todo is resolved or updated with Phase 9 evidence.
- Final release gate passes.
- Phase 9 requirements and roadmap status are updated only after verification passes.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---|---|---|
| verifier MVP docs | documentation | `README.md`, `README-zh.md` |
| CR-02 resolution/update note | planning todo | `.planning/todos/pending/` or `.planning/todos/resolved/` |
| Phase 9 requirement closure | planning state | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` |
| Phase 9 summaries | planning summaries | `.planning/phases/09-verifier-and-safety-boundary-mvp/09-*-SUMMARY.md` |

