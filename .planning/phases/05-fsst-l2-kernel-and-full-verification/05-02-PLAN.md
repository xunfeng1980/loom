---
phase: 05-fsst-l2-kernel-and-full-verification
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/loom-fixtures/src/vortex_reader.rs
  - crates/loom-fixtures/src/oracle.rs
  - crates/loom-fixtures/src/lib.rs
  - crates/loom-fixtures/tests/fsst_roundtrip.rs
  - crates/loom-fixtures/tests/dict_fsst_roundtrip.rs
autonomous: true
requirements: [L2-02, L2-03, VERIFY-01, VERIFY-02]
must_haves:
  truths:
    - "D-01: Vortex APIs remain isolated to loom-fixtures"
    - "D-04: vortex-fsst remains a fixture/oracle dependency only"
    - "D-11: Loom output is compared row-for-row with Vortex oracle output across FSST and dict-over-FSST"
    - "D-12: Required FSST edge cases are empty strings, escape-heavy strings, 8-byte symbol strings, and null routing"
    - "D-13: Fixture matrix is controlled MVP0 coverage, not exhaustive combinatorial coverage"
    - "D-16: Fixture bridges may canonicalize Vortex-specific representations only when tests still compare against Vortex's live oracle"
  artifacts:
    - path: "crates/loom-fixtures/src/vortex_reader.rs"
      provides: "FSST to KernelEscape bridge"
      contains: "from_fsst"
    - path: "crates/loom-fixtures/src/oracle.rs"
      provides: "Utf8 oracle helper"
      contains: "decode_utf8_oracle"
---

<objective>
Extend the Vortex fixture/oracle layer so real `vortex-fsst` arrays are bridged
into Loom-owned FSST params and verified row-for-row against Vortex's own decode
path. This plan proves both top-level FSST and dict-over-FSST behavior at the
Rust fixture level without adding Vortex dependencies to `loom-core`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Bridge vortex-fsst arrays into LayoutNode::KernelEscape params</name>
  <files>crates/loom-fixtures/src/vortex_reader.rs, crates/loom-fixtures/src/lib.rs, crates/loom-core/src/fsst_params.rs</files>
  <read_first>
    - crates/loom-fixtures/src/vortex_reader.rs
    - crates/loom-fixtures/src/lib.rs
    - crates/loom-core/src/fsst_params.rs
    - .planning/phases/05-fsst-l2-kernel-and-full-verification/05-RESEARCH.md "FSST API Findings"
    - .planning/phases/05-fsst-l2-kernel-and-full-verification/05-PATTERNS.md "Fixture and Oracle Patterns"
    - ~/.cargo/registry/src/index.crates.io-*/vortex-fsst-0.74.0/src/array.rs
    - ~/.cargo/registry/src/index.crates.io-*/vortex-fsst-0.74.0/src/compress.rs
  </read_first>
  <action>
    Import `vortex_fsst::{FSST, FSSTArrayExt}` and related accessors in `vortex_reader.rs`. Add a bridge helper such as `pub fn from_fsst_array(arr: &vortex_fsst::FSSTArray) -> LayoutNode` plus a view helper used by `from_array_ref`. The helper must extract symbols, symbol lengths, compressed codes bytes, codes offsets, uncompressed lengths, validity, and count from the Vortex FSST array, convert them to `loom_core::fsst_params::FsstParams::encode(...)`, and return `LayoutNode::KernelEscape { kernel_id: 0, params, count }`. Extend `from_array_ref` so `array.as_opt::<vortex_fsst::FSST>()` routes to this helper. No Vortex type may appear in the returned `LayoutNode`.
  </action>
  <verify>
    <automated>cargo test -p loom-fixtures fsst_bridge && cargo tree -p loom-core | grep -c -E 'vortex|fastlanes'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'vortex_fsst' crates/loom-fixtures/src/vortex_reader.rs` returns a line.
    - `grep -n 'from_fsst' crates/loom-fixtures/src/vortex_reader.rs` returns a line.
    - `cargo test -p loom-fixtures fsst_bridge_emits_kernel_escape_id_zero` exits 0.
    - `cargo tree -p loom-core | grep -c -E 'vortex|fastlanes'` prints `0`.
  </acceptance_criteria>
  <done>loom-fixtures can convert a real Vortex FSST array into a Loom KernelEscape carrying validated FSST params.</done>
</task>

<task type="auto">
  <name>Task 2: Add Utf8 Vortex oracle helper</name>
  <files>crates/loom-fixtures/src/oracle.rs, crates/loom-fixtures/tests/fsst_roundtrip.rs</files>
  <read_first>
    - crates/loom-fixtures/src/oracle.rs
    - crates/loom-fixtures/tests/dict_roundtrip.rs
    - .planning/phases/05-fsst-l2-kernel-and-full-verification/05-RESEARCH.md "Fixture and Oracle Findings"
    - ~/.cargo/registry/src/index.crates.io-*/vortex-fsst-0.74.0/src/canonical.rs
    - ~/.cargo/registry/src/index.crates.io-*/vortex-array-0.74.0/src/arrays/varbin*
  </read_first>
  <action>
    Extend `oracle.rs` with a Utf8/string oracle helper, for example `pub fn decode_utf8_oracle(array: &ArrayRef) -> (Vec<Option<String>>, Vec<bool>)`. The helper must use Vortex's execution/canonical path, collect string values for valid rows, and produce null flags where `true` means null. If Vortex executes FSST to a VarBin/VarBinView array rather than a direct Arrow StringArray, use Vortex accessor or scalar APIs to collect strings; keep the helper entirely inside `loom-fixtures`. Add focused helper tests that construct a small FSST-compressed Utf8 array and assert the oracle returns the expected strings/nulls.
  </action>
  <verify>
    <automated>cargo test -p loom-fixtures utf8_oracle</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'decode_utf8_oracle' crates/loom-fixtures/src/oracle.rs` returns a line.
    - `cargo test -p loom-fixtures utf8_oracle_decodes_fsst_strings` exits 0.
    - `cargo test -p loom-fixtures utf8_oracle_preserves_nulls` exits 0.
  </acceptance_criteria>
  <done>Vortex Utf8 oracle output can be collected as values plus null flags for FSST tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add FSST and dict-over-FSST row-for-row fixture tests</name>
  <files>crates/loom-fixtures/tests/fsst_roundtrip.rs, crates/loom-fixtures/tests/dict_fsst_roundtrip.rs, crates/loom-fixtures/src/vortex_reader.rs, crates/loom-fixtures/src/oracle.rs</files>
  <read_first>
    - crates/loom-fixtures/tests/dict_roundtrip.rs
    - crates/loom-fixtures/tests/kernel_escape_roundtrip.rs
    - crates/loom-fixtures/src/vortex_reader.rs
    - crates/loom-fixtures/src/oracle.rs
    - crates/loom-core/src/l1_model.rs
    - crates/loom-core/src/l2_kernel_registry.rs
    - .planning/phases/05-fsst-l2-kernel-and-full-verification/05-CONTEXT.md "Verification Corpus Scope"
    - .planning/phases/05-fsst-l2-kernel-and-full-verification/05-RESEARCH.md "Fixture and Oracle Findings"
  </read_first>
  <action>
    Add `fsst_roundtrip.rs` tests that build in-memory Vortex FSST arrays using `vortex_fsst::fsst_train_compressor` and `vortex_fsst::fsst_compress` or `fsst_compress_iter`. Include rows for empty strings, escape-heavy strings that force raw-byte escape behavior, strings composed of max-length 8-byte symbols, and nulls. For each fixture, collect Vortex oracle strings/nulls, bridge the array to `LayoutNode::KernelEscape`, decode through `decode_layout_to_array_data` with `L2KernelRegistry::default_for_mvp0()`, and compare Arrow Utf8 values/nulls row-for-row. Add `dict_fsst_roundtrip.rs` that builds `DictArray::try_new(codes.into_array(), fsst_values.into_array())`, bridges through `from_dict_array`, decodes with the registry-aware helper from Plan 05-01, and compares to the Vortex oracle row-for-row.
  </action>
  <verify>
    <automated>cargo test -p loom-fixtures --test fsst_roundtrip && cargo test -p loom-fixtures --test dict_fsst_roundtrip && cargo test -p loom-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p loom-fixtures --test fsst_roundtrip fsst_edge_cases_match_vortex_oracle` exits 0.
    - `cargo test -p loom-fixtures --test fsst_roundtrip fsst_nulls_match_vortex_oracle` exits 0.
    - `cargo test -p loom-fixtures --test dict_fsst_roundtrip dict_over_fsst_matches_vortex_oracle` exits 0.
    - `cargo test -p loom-fixtures` exits 0.
    - `rg -n 'vortex_file|vortex-file|\\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures` returns no matches.
  </acceptance_criteria>
  <done>FSST and dict-over-FSST fixtures compare Loom output to Vortex's own oracle row-for-row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vortex FSST -> FsstParams | Fixture bridge extracts Vortex internals and converts them to plain Loom params. |
| Oracle -> acceptance | Oracle output drives correctness; null flags and values must both be compared. |
| Fixture construction -> project hygiene | Tests must remain in-memory and avoid file-backed Vortex IO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | FSST bridge | mitigate | Decode bridged params through Loom and compare row-for-row with Vortex oracle. |
| T-05-07 | Repudiation | Utf8 null behavior | mitigate | Oracle helpers return explicit null flags; tests assert every row's null state. |
| T-05-08 | Information Disclosure | fixture IO | mitigate | Keep fixtures in memory and grep for forbidden file-backed Vortex APIs. |

No high open threats remain if fixture tests and dependency isolation checks pass.
</threat_model>

<verification>
- `cargo test -p loom-fixtures --test fsst_roundtrip` exits 0.
- `cargo test -p loom-fixtures --test dict_fsst_roundtrip` exits 0.
- `cargo test -p loom-fixtures` exits 0.
- `cargo tree -p loom-core | grep -c -E 'vortex|fastlanes'` prints `0`.
- `rg -n 'vortex_file|vortex-file|\\.vortex|VortexFile|from_path|read_file' crates/loom-fixtures` returns no matches.
</verification>

<success_criteria>
- Vortex FSST arrays bridge to `LayoutNode::KernelEscape { kernel_id: 0, ... }`.
- Top-level FSST edge cases match Vortex oracle row-for-row (`VERIFY-01`, `VERIFY-02`).
- Dict-over-FSST matches Vortex oracle row-for-row (`L2-03`, `VERIFY-02`).
- Vortex dependencies remain isolated to `loom-fixtures`.
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `from_fsst_array` / `from_fsst_view` | fn | `vortex_reader.rs` |
| `decode_utf8_oracle` | fn | `oracle.rs` |
| `crates/loom-fixtures/tests/fsst_roundtrip.rs` | new test file | loom-fixtures |
| `fsst_edge_cases_match_vortex_oracle` | test | `fsst_roundtrip.rs` |
| `fsst_nulls_match_vortex_oracle` | test | `fsst_roundtrip.rs` |
| `crates/loom-fixtures/tests/dict_fsst_roundtrip.rs` | new test file | loom-fixtures |
| `dict_over_fsst_matches_vortex_oracle` | test | `dict_fsst_roundtrip.rs` |
