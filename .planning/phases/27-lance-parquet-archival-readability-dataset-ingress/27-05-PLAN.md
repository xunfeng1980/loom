---
phase: 27-lance-parquet-archival-readability-dataset-ingress
plan: 05
type: execute
wave: 4
depends_on:
  - 27-04
files_modified:
  - scripts/lance-parquet-ingress-test.sh
  - scripts/mvp0-verify.sh
  - .planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md
autonomous: true
requirements:
  - PHASE-27
must_haves:
  truths:
    - "D-13: Phase 27 has a focused release gate wired into the main release path after the Phase 26 source-ingress contract gate and before DuckDB smoke."
    - "D-07/D-11/D-12/D-16: the final report and gate prove current-version evidence plus actual older-version Lance and Parquet fixture files paired with verifier-accepted Loom artifacts, oracle evidence, row-equivalence evidence, and current rewrite proof for both source families."
    - "D-05: dependency/API/scope guards prove Lance and Parquet do not leak into generic/core/ffi/DuckDB/CLI/public surfaces."
  artifacts:
    - path: scripts/lance-parquet-ingress-test.sh
      provides: "Focused Phase 27 release gate"
    - path: scripts/mvp0-verify.sh
      provides: "Main release gate wiring"
    - path: .planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md
      provides: "Final Phase 27 evidence, tradeoffs, non-goals, and Phase 28 handoff"
    - path: ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.parquet
      provides: "Actual older-version Parquet fixture required by closeout gate"
    - path: ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.lance/
      provides: "Actual older-version Lance fixture required by closeout gate"
  key_links:
    - from: scripts/mvp0-verify.sh
      to: scripts/lance-parquet-ingress-test.sh
      via: "release gate invocation"
      pattern: "lance-parquet-ingress-test\\.sh"
    - from: scripts/lance-parquet-ingress-test.sh
      to: adapter crates
      via: "focused cargo tests"
      pattern: "loom-lance-ingress|loom-parquet-ingress"
    - from: 27-ARCHIVAL-READABILITY-REPORT.md
      to: adapter handoff tests
      via: "documented verification command output and evidence matrices"
      pattern: "Verifier Evidence|Oracle Evidence|Legacy Readability|Actual Older-Version Fixtures"
---

<objective>
Finalize Phase 27 release gating, report evidence, and closeout wiring.

Purpose: make Lance and Parquet archival-readability evidence reproducible from one focused gate and from the main release verifier while documenting the exact supported slice and non-goals.
Output: final `scripts/lance-parquet-ingress-test.sh`, main `mvp0-verify.sh` wiring, and `27-ARCHIVAL-READABILITY-REPORT.md`.
</objective>

<scope_note>
Scope warning accepted for 27-05 dependencies on 27-04 evidence: the closeout plan stays small in files modified, while its verification intentionally reaches into actual older-version fixture paths created by 27-04. No extra plan split is needed because this plan edits only the focused gate, main verifier wiring, and final report.
</scope_note>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-CONTEXT.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-RESEARCH.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-PATTERNS.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-REPORT.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-01-SUMMARY.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-02-SUMMARY.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-03-SUMMARY.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-04-SUMMARY.md
@scripts/lance-parquet-ingress-test.sh
@scripts/source-ingress-contract-test.sh
@scripts/mvp0-verify.sh
</context>

## Artifacts This Phase Produces

- Files: `ingress/loom-lance-ingress/Cargo.toml`, `ingress/loom-lance-ingress/src/lib.rs`, `ingress/loom-lance-ingress/src/source_contract.rs`, `ingress/loom-lance-ingress/tests/dependency_boundary.rs`, `ingress/loom-lance-ingress/tests/source_ingress_contract.rs`, `ingress/loom-lance-ingress/tests/source_ingress_handoff.rs`, `ingress/loom-lance-ingress/tests/legacy_readability.rs`, `ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.lance/`, `ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.loom`, `ingress/loom-lance-ingress/tests/fixtures/legacy/MANIFEST.md`.
- Files: `ingress/loom-parquet-ingress/Cargo.toml`, `ingress/loom-parquet-ingress/src/lib.rs`, `ingress/loom-parquet-ingress/src/source_contract.rs`, `ingress/loom-parquet-ingress/tests/dependency_boundary.rs`, `ingress/loom-parquet-ingress/tests/source_ingress_contract.rs`, `ingress/loom-parquet-ingress/tests/source_ingress_handoff.rs`, `ingress/loom-parquet-ingress/tests/legacy_readability.rs`, `ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.parquet`, `ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.loom`, `ingress/loom-parquet-ingress/tests/fixtures/legacy/MANIFEST.md`.
- Shared/gate files: `scripts/lance-parquet-ingress-test.sh`, `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md`.
- Symbols: `SourceIngressAcceptedArtifact`, `lance_source_facts_from_path`, `lance_native_oracle_batches_from_path`, `emit_source_ingress_lmc1_from_lance_path`, `parquet_source_facts_from_path`, `parquet_arrow_oracle_batches_from_path`, `emit_source_ingress_lmc1_from_parquet_path`.

<tasks>

<task type="auto">
  <name>Task 1: Finalize focused Lance/Parquet ingress gate</name>
  <read_first>
    - `scripts/lance-parquet-ingress-test.sh`
    - `scripts/source-ingress-contract-test.sh`
    - `Cargo.toml`
    - `ingress/loom-lance-ingress/Cargo.toml`
    - `ingress/loom-parquet-ingress/Cargo.toml`
    - `crates/loom-core/Cargo.toml`
    - `crates/loom-ffi/Cargo.toml`
    - `ingress/loom-source-ingress/Cargo.toml`
  </read_first>
  <files>scripts/lance-parquet-ingress-test.sh</files>
  <action>Expand `scripts/lance-parquet-ingress-test.sh` into the final Phase 27 focused gate per D-13. It must check required Phase 27 plan/research/context/report artifacts, require actual legacy fixture paths `ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.parquet`, `ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.loom`, `ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.lance/`, and `ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.loom`, run `cargo test -p loom-source-ingress`, `cargo test -p loom-parquet-ingress --test dependency_boundary`, `source_ingress_contract`, `source_ingress_handoff`, `legacy_readability`, the matching Lance tests, and `cargo test -p loom-core --test artifact_verifier`. Keep dependency/API creep guards from Plan 27-01 and add checks that Lance/Parquet direct dependencies are limited to adapter manifests, `object_store` and credential markers do not appear outside adapter internals, and public/DuckDB/CLI surfaces contain no `loom_scan_lance`, `loom_scan_parquet`, `loom_ingest_lance`, `loom_ingest_parquet`, manifest/footer embedding controls, predicate pushdown controls, split worker controls, or new native-kernel public markers per D-05. Add report marker checks for Supported Slice, Unsupported and Rejected Matrix, Current-Version Evidence, Actual Older-Version Fixtures, Legacy Readability, Verifier Evidence, Oracle Evidence, Dependency and API Boundary, Tradeoffs, Non-Goals, and Phase 28 Handoff. The gate must fail if the report describes a manifest-only or record-only artifact as successful legacy evidence.</action>
  <verify>
    <automated>bash -n scripts/lance-parquet-ingress-test.sh && bash scripts/lance-parquet-ingress-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Focused adapter tests for both source families pass from the script.
    - The script fails if any actual older-version source fixture or paired Loom artifact is missing.
    - The script fails if the final report is missing required evidence markers.
    - The script fails if the final report treats a manifest-only record as a passing legacy proof.
    - The script fails if Lance/Parquet/object-store/credential/public route/native-kernel markers leak into guarded surfaces.
    - Guard checks avoid unfiltered `grep -c` and avoid matching their own forbidden-token literals.
  </acceptance_criteria>
  <done>The focused Phase 27 gate is complete and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 27 gate into the main release verifier</name>
  <read_first>
    - `scripts/mvp0-verify.sh`
    - `scripts/source-ingress-contract-test.sh`
    - `scripts/lance-parquet-ingress-test.sh`
  </read_first>
  <files>scripts/mvp0-verify.sh</files>
  <action>Invoke `scripts/lance-parquet-ingress-test.sh` from `scripts/mvp0-verify.sh` after `scripts/source-ingress-contract-test.sh` and before `scripts/duckdb-smoke-test.sh` per D-13. Update the nearby ordering comment so the late gate sequence is Phase 24 DuckDB native integration, Phase 25 native hardening, Phase 26 source ingress contract, Phase 27 Lance/Parquet ingress, then DuckDB SQL smoke. Do not add public SQL/API, DuckDB extension behavior, or CLI route changes per D-05.</action>
  <verify>
    <automated>bash -n scripts/mvp0-verify.sh && rg -q "lance-parquet-ingress-test\.sh" scripts/mvp0-verify.sh && python3 -c 'from pathlib import Path; text=Path("scripts/mvp0-verify.sh").read_text(); order=["scripts/native-hardening-test.sh","scripts/source-ingress-contract-test.sh","scripts/lance-parquet-ingress-test.sh","scripts/duckdb-smoke-test.sh"]; pos=[text.index(x) for x in order]; assert pos == sorted(pos), pos'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mvp0-verify.sh` invokes `scripts/lance-parquet-ingress-test.sh`.
    - The Python order check confirms Phase 27 runs after Phase 26 and before DuckDB smoke.
    - No public route-specific Lance/Parquet SQL or CLI command is added.
  </acceptance_criteria>
  <done>The main release verifier runs the Phase 27 gate in the required order.</done>
</task>

<task type="auto">
  <name>Task 3: Write archival readability report and run closeout verification</name>
  <read_first>
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-CONTEXT.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-RESEARCH.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-PATTERNS.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-01-SUMMARY.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-02-SUMMARY.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-03-SUMMARY.md`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-04-SUMMARY.md`
    - `scripts/lance-parquet-ingress-test.sh`
    - `scripts/mvp0-verify.sh`
  </read_first>
  <files>.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md, scripts/lance-parquet-ingress-test.sh</files>
  <action>Write `27-ARCHIVAL-READABILITY-REPORT.md` per D-13. Include sections named exactly: Executive Summary, Implemented Artifacts, Supported Slice, Unsupported and Rejected Matrix, Current-Version Evidence, Actual Older-Version Fixtures, Legacy Readability, Verifier Evidence, Oracle Evidence, Dependency and API Boundary, Current-Phase Tradeoffs, Non-Goals, Release Gate Evidence, and Phase 28 Handoff. The report must state that accepted evidence is limited to local files/datasets and non-null primitive Int32/Int64/Float32/Float64 single-column/simple-table shapes per D-01/D-02, that unsupported valid sources are fact-bearing but byte-free, that malformed sources expose no trusted facts, that actual older-version Lance and Parquet fixture files are present at the required paths with generator versions, hashes, paired Loom artifact hashes, and current rewrite proof per D-07/D-16, and that Loom artifacts are paired rather than embedded per D-08. A manifest may appear only as provenance for actual fixture files and paired Loom artifacts; it must not be described as a substitute or successful legacy proof without actual older-version source fixture files. Run the focused gate and the main verifier with `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`, then record the exact command names and pass/fail status in the report.</action>
  <verify>
    <automated>test -f ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.parquet && test -f ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.loom && test -d ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.lance && test -f ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.loom && rg -q "Actual Older-Version Fixtures" .planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md && bash scripts/lance-parquet-ingress-test.sh && LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `27-ARCHIVAL-READABILITY-REPORT.md` exists and contains all required section names.
    - The report records supported, unsupported, rejected, current, actual older-version fixture, legacy, verifier, oracle, dependency, tradeoff, non-goal, and Phase 28 handoff evidence.
    - The report lists required actual older-version fixture paths, generator versions, source fixture hashes/tree hashes, paired Loom artifact hashes, and current rewrite proof for both source families.
    - The report does not count a manifest-only or deterministic record as a successful legacy proof.
    - The focused gate passes.
    - The main release verifier passes with `LOOM_ALLOW_NATIVE_TOOL_SKIP=1`.
  </acceptance_criteria>
  <done>The final report and release evidence close Phase 27 without widening public surfaces or source compatibility claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| focused gate -> release claim | A passing script becomes the reviewer-visible evidence for Phase 27. |
| final report -> archival-readability claim | Report wording can overstate source compatibility beyond the verified slice. |
| main release verifier -> downstream phases | Incorrect ordering could let DuckDB smoke run without source-ingress evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | `scripts/lance-parquet-ingress-test.sh` | mitigate | Script runs focused tests plus dependency/API/report marker guards and fails closed on missing evidence. |
| T-27-05-02 | Repudiation | `27-ARCHIVAL-READABILITY-REPORT.md` | mitigate | Report records exact verification commands, supported matrices, actual older-version fixture paths/hashes, paired Loom artifact hashes, tradeoffs, non-goals, and rewrite proof. |
| T-27-05-03 | Elevation of Privilege | Public surfaces | mitigate | Gate scans public headers, DuckDB extension, and CLI for source-specific SQL/API and native-kernel markers. |
| T-27-05-04 | Information Disclosure | Credential/object-store creep | mitigate | Gate scans guarded surfaces for object-store and credential markers; Lance default features remain disabled. |
| T-27-05-05 | Tampering | Main release ordering | mitigate | Syntax and Python order checks enforce Phase 26 -> Phase 27 -> DuckDB smoke sequence. |
| T-27-05-06 | Spoofing | Archival-readability wording | mitigate | Report states exact local-file, primitive/table, verifier/oracle-supported scope and lists non-goals explicitly. |
</threat_model>

<verification>
Overall plan checks:
- `bash -n scripts/lance-parquet-ingress-test.sh && bash scripts/lance-parquet-ingress-test.sh`
- `bash -n scripts/mvp0-verify.sh`
- `test -f ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.parquet && test -f ingress/loom-parquet-ingress/tests/fixtures/legacy/legacy-v1.loom && test -d ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.lance && test -f ingress/loom-lance-ingress/tests/fixtures/legacy/legacy-v1.loom`
- `rg -q "Actual Older-Version Fixtures" .planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md`
- `python3 -c 'from pathlib import Path; text=Path("scripts/mvp0-verify.sh").read_text(); order=["scripts/native-hardening-test.sh","scripts/source-ingress-contract-test.sh","scripts/lance-parquet-ingress-test.sh","scripts/duckdb-smoke-test.sh"]; pos=[text.index(x) for x in order]; assert pos == sorted(pos), pos'`
- `LOOM_ALLOW_NATIVE_TOOL_SKIP=1 bash scripts/mvp0-verify.sh`
</verification>

<success_criteria>
Plan 27-05 is complete when the focused Phase 27 gate passes, the main verifier invokes it after Phase 26 and before DuckDB smoke, the final archival readability report records bounded current and actual older-version fixture evidence for both source families, each older-version fixture is paired with verifier-accepted Loom artifact evidence and current rewrite proof, and no public/API/dependency scope creep is present. Missing actual older-version Lance or Parquet fixture files fail closeout.
</success_criteria>

<output>
Create `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-05-SUMMARY.md` when done.
</output>
