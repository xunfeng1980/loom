---
phase: 27-lance-parquet-archival-readability-dataset-ingress
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - ingress/loom-lance-ingress/src/lib.rs
  - ingress/loom-lance-ingress/src/source_contract.rs
  - ingress/loom-lance-ingress/tests/source_ingress_contract.rs
autonomous: true
requirements:
  - PHASE-27
must_haves:
  truths:
    - "D-03: Lance schema, version, and fragment metadata map into source-neutral facts."
    - "D-15: Lance adapter APIs are async-only and tested with `#[tokio::test]`."
    - "D-05/D-06: Lance SDK objects, dataset handles, readers, credentials, and object-store state do not escape the adapter crate."
  artifacts:
    - path: ingress/loom-lance-ingress/src/source_contract.rs
      provides: "`lance_source_facts_from_path` and Lance classification diagnostics"
    - path: ingress/loom-lance-ingress/tests/source_ingress_contract.rs
      provides: "Lance facts, unsupported, and rejected contract tests"
  key_links:
    - from: ingress/loom-lance-ingress/src/source_contract.rs
      to: ingress/loom-source-ingress/src/lib.rs
      via: "`SourceIdentity`, `SourceFacts`, `SourceCoverage`, and `SourceIngressReport`"
      pattern: "SourceFacts|SourceCoverage|SourceIngressReport"
    - from: ingress/loom-lance-ingress/tests/source_ingress_contract.rs
      to: Lance local dataset writer/reader APIs
      via: "temporary local dataset directories"
      pattern: "Dataset::write|Dataset::open|try_into_stream"
---

<objective>
Implement Lance fact extraction and source-ingress mapping.

Purpose: let the Lance adapter inspect local Lance datasets through the Phase 26 source-ingress contract without emitting Loom bytes or exposing Lance SDK objects outside the adapter.
Output: async source-neutral Lance facts, support classification, stable diagnostics, and focused tests for valid supported, valid unsupported, fragment metadata, and malformed/non-dataset local paths.
</objective>

<execution_context>
@/Users/macintoshhd/.codex/gsd-core/workflows/execute-plan.md
@/Users/macintoshhd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-CONTEXT.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-RESEARCH.md
@.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-PATTERNS.md
@.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md
@.planning/phases/26-external-source-ingress-contract/26-05-SUMMARY.md
@ingress/loom-source-ingress/src/lib.rs
@ingress/loom-lance-ingress/Cargo.toml
@ingress/loom-lance-ingress/src/lib.rs
@ingress/loom-vortex-ingress/src/source_contract.rs
@ingress/loom-vortex-ingress/tests/source_ingress_contract.rs
</context>

## Artifacts This Phase Produces

- Files: `ingress/loom-lance-ingress/Cargo.toml`, `ingress/loom-lance-ingress/src/lib.rs`, `ingress/loom-lance-ingress/src/source_contract.rs`, `ingress/loom-lance-ingress/tests/dependency_boundary.rs`, `ingress/loom-lance-ingress/tests/source_ingress_contract.rs`, `ingress/loom-lance-ingress/tests/source_ingress_handoff.rs`, `ingress/loom-lance-ingress/tests/legacy_readability.rs`, `ingress/loom-lance-ingress/tests/fixtures/legacy/MANIFEST.md`.
- Files: `ingress/loom-parquet-ingress/Cargo.toml`, `ingress/loom-parquet-ingress/src/lib.rs`, `ingress/loom-parquet-ingress/src/source_contract.rs`, `ingress/loom-parquet-ingress/tests/dependency_boundary.rs`, `ingress/loom-parquet-ingress/tests/source_ingress_contract.rs`, `ingress/loom-parquet-ingress/tests/source_ingress_handoff.rs`, `ingress/loom-parquet-ingress/tests/legacy_readability.rs`, `ingress/loom-parquet-ingress/tests/fixtures/legacy/MANIFEST.md`.
- Shared/gate files: `scripts/lance-parquet-ingress-test.sh`, `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-ARCHIVAL-READABILITY-REPORT.md`.
- Symbols: `SourceIngressAcceptedArtifact`, `lance_source_facts_from_path`, `lance_native_oracle_batches_from_path`, `emit_source_ingress_lmc1_from_lance_path`, `parquet_source_facts_from_path`, `parquet_arrow_oracle_batches_from_path`, `emit_source_ingress_lmc1_from_parquet_path`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map Lance dataset metadata into SourceFacts</name>
  <read_first>
    - `ingress/loom-lance-ingress/src/lib.rs`
    - `ingress/loom-lance-ingress/Cargo.toml`
    - `ingress/loom-source-ingress/src/lib.rs`
    - `ingress/loom-vortex-ingress/src/source_contract.rs`
    - `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-RESEARCH.md`
  </read_first>
  <files>ingress/loom-lance-ingress/src/lib.rs, ingress/loom-lance-ingress/src/source_contract.rs, ingress/loom-lance-ingress/tests/source_ingress_contract.rs</files>
  <behavior>
    - A local Lance dataset written by current Lance APIs yields `SourceFacts` with `SourceIdentity.source_kind = "lance"` and `format = "external-source"`.
    - Root schema facts include logical kind, nullable status, field count, field names, and an Arrow summary string.
    - Version and fragment metadata appear as source-neutral layout/split/segment-style facts without SDK handles.
  </behavior>
  <action>Create `ingress/loom-lance-ingress/src/source_contract.rs` and export async `lance_source_facts_from_path` from the crate root per D-15. Use `Dataset::open`, `Dataset::schema`, `Dataset::version_id`, `Dataset::count_fragments`, `Dataset::get_fragments`, and fragment metadata/count methods where available to inspect only local dataset paths per D-01 and D-03. Map facts into `SourceIdentity`, `SourceFacts`, `SourceSchemaFact`, `SourceLayoutFact`, `SourceSplitFact`, and `SourceCoverage`. Store version, fragment ids, row counts, physical row counts, data-file counts, validation summaries, and metadata summaries as strings, counts, booleans, or notes only; do not expose `Dataset`, `FileFragment`, object-store options, credentials, scanner streams, or Arrow ownership handles in public generic values per D-06.</action>
  <verify>
    <automated>cargo test -p loom-lance-ingress --test source_ingress_contract lance_facts_include_schema_version_and_fragment_metadata</automated>
  </verify>
  <acceptance_criteria>
    - The test writes a deterministic non-null local Lance dataset in a temporary directory and reads facts through `lance_source_facts_from_path`.
    - Facts include schema fact, row count `3`, coverage, version information, and at least one fragment/split/layout fact.
    - `rg -n "pub struct Lance|Dataset|FileFragment|object_store" ingress/loom-source-ingress crates/loom-core crates/loom-ffi` returns no matches except unrelated existing comments if any are reviewed and excluded by path.
  </acceptance_criteria>
  <done>Lance dataset metadata is represented as source-neutral facts without SDK leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify supported and unsupported Lance shapes</name>
  <read_first>
    - `ingress/loom-lance-ingress/src/source_contract.rs`
    - `ingress/loom-lance-ingress/tests/source_ingress_contract.rs`
    - `ingress/loom-source-ingress/src/lib.rs`
    - `.planning/phases/26-external-source-ingress-contract/26-SOURCE-INGRESS-CONTRACT.md`
  </read_first>
  <files>ingress/loom-lance-ingress/src/source_contract.rs, ingress/loom-lance-ingress/tests/source_ingress_contract.rs</files>
  <behavior>
    - Non-null Int32, Int64, Float32, and Float64 single-column datasets classify as the supported canonical raw slice.
    - Non-null primitive two-column datasets classify as the supported canonical table slice.
    - Nullable, string, nested, extension, deletion-vector/index-dependent, or unsupported Lance shapes return fact-bearing unsupported results with no artifact bytes.
  </behavior>
  <action>Add Lance shape classification helpers that set `SourceCoverage.support`, `SourceCoverage.emission_kind`, `SourceCoverage.emission_disposition`, and `SourceCoverage.lowering_disposition` without constructing an accepted `SourceIngressReport` in this plan. Supported single-column coverage uses `SourceEmissionKind::Lmp1` and `SourceEmissionDisposition::CanonicalRaw`; supported simple table coverage uses `SourceEmissionKind::Lmt1` and `SourceEmissionDisposition::CanonicalTable` per D-02. Unsupported valid shapes must preserve facts and produce `SourceDiagnosticCode::UnsupportedSchema`, `UnsupportedLayout`, or `UnsupportedConversion` with no artifact bytes and no oracle evidence per D-11. Do not implement remote IO, object-store credentials, Lance indices, namespace writes, SQL/DataFusion routes, predicate pushdown, parallel fragment execution, or public blocking wrappers per D-05 and D-15.</action>
  <verify>
    <automated>cargo test -p loom-lance-ingress --test source_ingress_contract lance_classifies_supported_and_unsupported_shapes</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover non-null Int32 single-column, non-null primitive table, nullable primitive, and string/nested/extension-style unsupported cases.
    - Unsupported reports have `SourceIngressStatus::Unsupported`, facts present, `SourceEmissionKind::None`, `SourceEmissionDisposition::None`, `SourceArtifactVerificationSummary::not_applicable()`, and `oracle_evidence = None`.
    - No test expects emitted `.loom` bytes in Plan 27-03.
  </acceptance_criteria>
  <done>Lance support status and diagnostics are source-contract compatible, with accepted bytes left to Plan 27-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject malformed or non-dataset Lance paths fail-closed</name>
  <read_first>
    - `ingress/loom-lance-ingress/src/source_contract.rs`
    - `ingress/loom-lance-ingress/tests/source_ingress_contract.rs`
    - `ingress/loom-vortex-ingress/tests/source_ingress_handoff.rs`
  </read_first>
  <files>ingress/loom-lance-ingress/src/source_contract.rs, ingress/loom-lance-ingress/tests/source_ingress_contract.rs</files>
  <behavior>
    - Non-dataset paths and unreadable local paths return rejected reports with diagnostics only.
    - Rejected reports carry no trusted facts, no artifact metadata, and no oracle evidence.
  </behavior>
  <action>Map Lance open/read/schema failures into `SourceIngressReport::rejected` with `SourceDiagnosticCode::OpenFailed`, `ReadFailed`, or `SchemaUnavailable` as appropriate. Diagnostic paths should use stable JSON-path-like strings such as `$`, `$.open`, `$.manifest`, `$.fragment`, and `$.schema`. Include source detail text only when it contains no credentials, object-store config, SDK handles, scanner state, or remote URI secrets per D-06. Keep tests async with `#[tokio::test]` per D-15.</action>
  <verify>
    <automated>cargo test -p loom-lance-ingress --test source_ingress_contract lance_non_dataset_paths_are_rejected_without_facts</automated>
  </verify>
  <acceptance_criteria>
    - Non-dataset path test asserts `SourceIngressStatus::Rejected`.
    - Rejected report has `facts = None`, `SourceEmissionKind::None`, `SourceArtifactVerificationSummary::not_applicable()`, and `oracle_evidence = None`.
    - Diagnostics include a stable code and path.
  </acceptance_criteria>
  <done>Malformed or non-dataset Lance input fails closed before facts or bytes become trusted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Lance dataset -> Lance SDK | Untrusted local dataset metadata is opened by Lance async APIs. |
| Lance metadata -> SourceFacts | Dataset/fragment facts are descriptive and do not authorize artifact bytes. |
| Lance schema -> support classification | Unsupported Lance features must not produce partial Loom output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | `lance_source_facts_from_path` | mitigate | Open/schema/fragment failures map to rejected reports with no facts. |
| T-27-03-02 | Information Disclosure | Lance diagnostics | mitigate | Diagnostics avoid credentials, object-store config, SDK handles, and scanner state. |
| T-27-03-03 | Tampering | Support classification | mitigate | Nullable, nested, index-dependent, and unsupported shapes are fact-bearing unsupported with no bytes. |
| T-27-03-04 | Elevation of Privilege | Async Lance API | mitigate | Keep async-only adapter APIs; no public blocking wrapper or host/SQL route is added. |
| T-27-03-05 | Information Disclosure | Lance cloud/provider features | mitigate | Default features remain disabled; object-store/credential markers are guarded outside adapter internals. |
| T-27-SC | Tampering | Cargo installs | mitigate | Package legitimacy audit in `27-RESEARCH.md` approves `lance`; no suspicious package is planned. |
</threat_model>

<verification>
Overall plan checks:
- `cargo test -p loom-lance-ingress --test source_ingress_contract`
- `cargo tree -p loom-core | awk '/lance|parquet|object_store/{found=1} END{exit found?1:0}'`
- `cargo tree -p loom-source-ingress | awk '/lance|parquet|object_store/{found=1} END{exit found?1:0}'`
</verification>

<success_criteria>
Plan 27-03 is complete when local Lance datasets produce source-neutral facts and classification, valid unsupported shapes are fact-bearing but byte-free, and malformed/non-dataset Lance input is rejected without trusted facts.
</success_criteria>

<output>
Create `.planning/phases/27-lance-parquet-archival-readability-dataset-ingress/27-03-SUMMARY.md` when done.
</output>
