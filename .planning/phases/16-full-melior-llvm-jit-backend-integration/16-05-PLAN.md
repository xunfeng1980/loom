---
phase: 16
plan: 16-05
title: Final docs, release-gate wiring, and roadmap/state closeout
type: execute
wave: 5
depends_on:
  - 16-04
files_modified:
  - README.md
  - README-zh.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md
  - .planning/phases/16-full-melior-llvm-jit-backend-integration/16-SUMMARY.md
  - scripts/melior-jit-test.sh
  - scripts/mvp0-verify.sh
autonomous: true
requirements: []
---

<objective>
Close Phase 16 with reviewer-visible backend evidence, public scope wording, release-gate wiring, and planning state updates that accurately describe optional `melior`/LLVM/JIT integration without claiming production native compiler completion.
</objective>

<context>
Phase 16 is a backend integration proof, not Phase 17 production decode dialect, Phase 18 full Vortex reader, or Phase 20 DuckDB native execution. Closeout docs must keep those boundaries sharp.
</context>

<threat_model>
- Threat: docs overclaim native speed, production readiness, or arbitrary Vortex/native lowering support.
  Mitigation: README and backend report explicitly state the supported slice and out-of-scope items.
- Threat: release gate status is ambiguous when JIT is skipped.
  Mitigation: backend report records local toolchain facts, whether JIT executed, and exact commands run.
- Threat: future sessions resume from stale planning state.
  Mitigation: ROADMAP, STATE, and PROJECT are updated together with Phase 16 completion and Phase 17 next-step boundary.
</threat_model>

<tasks>
  <task id="1" type="execute">
    <title>Write final backend report</title>
    <read_first>
      <file>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-RESEARCH.md</file>
      <file>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-CONTRACT.md</file>
      <file>scripts/melior-jit-test.sh</file>
      <file>crates/loom-native-melior/src/report.rs</file>
    </read_first>
    <files>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</files>
    <action>
      Write `16-BACKEND-REPORT.md` with sections for scope, local toolchain facts, backend crate boundary, verifier-gated artifact flow, programmatic MLIR evidence, MLIR validation evidence, JIT evidence or skip reason, fail-closed negative coverage, commands run, and explicit non-goals. State that Phase 16 does not implement custom Loom MLIR dialect, vectorization, Arrow raw-buffer native writes, DuckDB native execution, or complete Vortex reader support.
    </action>
    <verify>
      <automated>rg -n "toolchain facts|verify_l2_core|programmatic MLIR|JIT evidence|skip reason|non-goals|DuckDB native execution" .planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</automated>
    </verify>
    <acceptance_criteria>
      <criterion>`16-BACKEND-REPORT.md` contains `verify_l2_core`, `programmatic MLIR`, `JIT evidence`, `skip reason`, and `non-goals`.</criterion>
      <criterion>`16-BACKEND-REPORT.md` states Phase 16 does not implement `DuckDB native execution`.</criterion>
      <criterion>The report lists `bash scripts/melior-jit-test.sh` and `bash scripts/mvp0-verify.sh` under commands run.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <title>Update public docs without overclaiming</title>
    <read_first>
      <file>README.md</file>
      <file>README-zh.md</file>
      <file>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</file>
    </read_first>
    <files>README.md, README-zh.md</files>
    <action>
      Update the MVP1 scope tables or short feature sections to mention Phase 16 optional `melior`/LLVM/JIT backend evidence for the bounded Int32 copy slice. Keep wording concise. Explicitly state it is not a production native compiler, not arbitrary Vortex/native lowering, and not host-engine native execution.
    </action>
    <verify>
      <automated>rg -n "MVP1|melior|LLVM|JIT|bounded Int32|not a production native compiler|host-engine" README.md README-zh.md</automated>
    </verify>
    <acceptance_criteria>
      <criterion>`README.md` contains `melior`, `LLVM`, `JIT`, and `bounded Int32`.</criterion>
      <criterion>`README-zh.md` contains `melior`, `LLVM`, `JIT`, and `bounded Int32`.</criterion>
      <criterion>Both READMEs state Phase 16 is not host-engine native execution or equivalent wording.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <title>Update planning state and roadmap</title>
    <read_first>
      <file>.planning/PROJECT.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/STATE.md</file>
      <file>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</file>
    </read_first>
    <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/16-full-melior-llvm-jit-backend-integration/16-SUMMARY.md</files>
    <action>
      Mark Phase 16 complete in ROADMAP and STATE, set Phase 17 as the next placeholder focus, and update PROJECT active/validated decisions. Add `16-SUMMARY.md` summarizing shipped files, commands, deviations, and residual risks. Do not change Phase 17-23 ordering.
    </action>
    <verify>
      <automated>rg -n "Phase 16|Complete|Phase 17|melior|JIT|backend" .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/16-full-melior-llvm-jit-backend-integration/16-SUMMARY.md</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>ROADMAP progress table lists `16. Full melior/LLVM/JIT Backend Integration | 5/5 | Complete | 2026-06-08` after this plan executes.</criterion>
      <criterion>STATE completed phases increments from `15 / 23` to `16 / 23` after this plan executes.</criterion>
      <criterion>`16-SUMMARY.md` contains `Self-Check: PASSED` or records any failure explicitly.</criterion>
      <criterion>Phase 17 remains `Production Decode Dialect and Native Kernel Expansion`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="execute">
    <title>Run final Phase 16 verification</title>
    <read_first>
      <file>scripts/melior-jit-test.sh</file>
      <file>scripts/mvp0-verify.sh</file>
      <file>.planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</file>
    </read_first>
    <files>scripts/melior-jit-test.sh, scripts/mvp0-verify.sh, .planning/phases/16-full-melior-llvm-jit-backend-integration/16-BACKEND-REPORT.md</files>
    <action>
      Run final verification and record the exact outcome in `16-BACKEND-REPORT.md`: `cargo test --workspace`, `cargo test -p loom-native-melior`, `bash scripts/check-core-invariants.sh`, `bash scripts/melior-jit-test.sh`, `bash scripts/mvp0-verify.sh`, and `git diff --check`. If strict JIT mode is not runnable locally, record the reason and do not mark strict JIT as passed.
    </action>
    <verify>
      <automated>cargo test --workspace</automated>
      <automated>cargo test -p loom-native-melior</automated>
      <automated>bash scripts/check-core-invariants.sh</automated>
      <automated>bash scripts/melior-jit-test.sh</automated>
      <automated>bash scripts/mvp0-verify.sh</automated>
      <automated>git diff --check</automated>
    </verify>
    <acceptance_criteria>
      <criterion>All non-strict commands in this task exit 0.</criterion>
      <criterion>`16-BACKEND-REPORT.md` records whether strict `LOOM_REQUIRE_MELIOR_JIT=1` was run, skipped, or failed due to local toolchain mismatch.</criterion>
      <criterion>`git diff --check` exits 0.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<must_haves>
- Public docs describe Phase 16 as optional backend evidence, not production native execution.
- Final gates are recorded and skip semantics are explicit.
- ROADMAP/STATE/PROJECT remain consistent and Phase 17 is still the next productionization phase.
</must_haves>

<verification>
- `cargo test --workspace`
- `cargo test -p loom-native-melior`
- `bash scripts/check-core-invariants.sh`
- `bash scripts/melior-jit-test.sh`
- `bash scripts/mvp0-verify.sh`
- `git diff --check`
</verification>

<success_criteria>
- Phase 16 is fully documented and marked complete only after non-strict gates pass.
- The repository can still be verified on machines without compatible MLIR/LLVM.
- Future Phase 17 work has a clear handoff and no hidden Phase 16 overclaim.
</success_criteria>

## Artifacts this phase produces

- New report: `16-BACKEND-REPORT.md`
- New summary: `16-SUMMARY.md`
- Updated docs: `README.md`, `README-zh.md`
- Updated planning state: `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`
