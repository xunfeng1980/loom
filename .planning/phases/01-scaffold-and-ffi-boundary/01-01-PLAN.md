---
phase: 01-scaffold-and-ffi-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - .gitignore
  - crates/loom-core/Cargo.toml
  - crates/loom-core/src/lib.rs
  - crates/loom-ffi/Cargo.toml
  - crates/loom-ffi/src/lib.rs
  - crates/loom-fixtures/Cargo.toml
  - crates/loom-fixtures/src/lib.rs
autonomous: true
requirements: [CORE-01, CORE-02]
must_haves:
  truths:
    - "cargo build at the workspace root compiles all three member crates with zero warnings"
    - "cargo tree -d | grep arrow reports zero duplicate arrow-* crate instances"
    - "grep vortex-file Cargo.lock returns nothing (no .vortex file-format crate pulled in)"
    - "The release profile sets panic = abort and loom-ffi installs a System global allocator"
    - "loom-core has no vortex-* dependency; the dependency boundary is clean"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root: members list + [workspace.dependencies] arrow/vortex pins + [patch.crates-io] arrow-* unification + [profile.release] panic=abort"
      contains: "panic = \"abort\""
    - path: "rust-toolchain.toml"
      provides: "Pinned stable Rust toolchain (D-03)"
      contains: "channel"
    - path: "crates/loom-core/Cargo.toml"
      provides: "Pure-Rust decode library crate, zero FFI, zero vortex deps (D-01, D-02)"
    - path: "crates/loom-ffi/Cargo.toml"
      provides: "staticlib crate carrying the extern C surface (D-01)"
      contains: "staticlib"
    - path: "crates/loom-ffi/src/lib.rs"
      provides: "System global allocator declaration (CORE-02)"
      contains: "GlobalAlloc"
    - path: "crates/loom-fixtures/Cargo.toml"
      provides: "Fixtures/reference crate where vortex-* deps are allowed (D-01, D-02)"
  key_links:
    - from: "Cargo.toml [patch.crates-io]"
      to: "all arrow-* sub-crates"
      via: "version unification to 58.3.0"
      pattern: "patch.crates-io"
    - from: "crates/loom-ffi/src/lib.rs"
      to: "std::alloc::System"
      via: "#[global_allocator] static"
      pattern: "global_allocator"
---

<objective>
Create the Loom Cargo workspace as the load-bearing foundation: three member crates with a clean dependency boundary (D-01, D-02), a pinned toolchain (D-03), all arrow-* sub-crates unified to a single version (CORE-01), `panic = "abort"`, and a System global allocator (CORE-02). This plan does NOT create the FFI surface or cbindgen wiring — that is Plan 02. This plan proves the workspace compiles soundly and the version/allocator/panic invariants hold.

Purpose: Every later phase inherits these invariants. arrow-rs version skew, allocator mismatch, and panic-unwind cannot be retrofitted (PITFALLS P3, P4, P5, P8, P9) — they must be correct in the first commit.
Output: A compiling 3-crate workspace with the CORE-01 and CORE-02 invariants verifiable by command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-ffi-boundary/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols/files (the source-grounding pass must treat these as newly-created, not drift):

**Workspace files:**
- `Cargo.toml` (workspace root) — `[workspace] members`, `[workspace.dependencies]`, `[patch.crates-io]`, `[profile.release]`
- `rust-toolchain.toml` — pinned stable channel (D-03)
- `.gitignore` — Rust `/target` ignore

**Crates (D-01):**
- `loom-core` — pure-Rust decode library, ZERO FFI, ZERO vortex deps (path `crates/loom-core`)
- `loom-ffi` — `staticlib` crate; carries the System global allocator now and the `extern "C"` surface in Plan 02 (path `crates/loom-ffi`)
- `loom-fixtures` — fixtures/reference crate; the ONLY crate permitted to depend on `vortex-*` (path `crates/loom-fixtures`)

**Symbols introduced:**
- `loom-ffi`: a `#[global_allocator] static` of type `std::alloc::System` (CORE-02). cbindgen will ignore it (not `extern "C"`).
- `loom-core`: a placeholder library root (`lib.rs`) — module skeleton only, no decode logic yet (that is Phase 3).

Pinned versions (from STACK.md — use EXACTLY these): `arrow` 58.3.0 (feature `ffi`), `arrow-array` 58.3.0, `arrow-schema` 58.3.0, `arrow-data` 58.3.0, `vortex-array`/`vortex-fastlanes`/`vortex-dict`/`vortex-fsst` 0.74.0, `cbindgen` 0.29.3 (used in Plan 02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Workspace root, toolchain pin, and version unification</name>
  <read_first>
    - .planning/research/STACK.md (exact pinned versions: arrow 58.3.0 family, vortex 0.74.0, the [patch.crates-io] guidance, the "do not use" list)
    - .planning/research/PITFALLS.md (P8 vortex-file scope, P9 arrow-rs version skew, the "Looks Done But Isn't" checklist)
    - .planning/phases/01-scaffold-and-ffi-boundary/01-CONTEXT.md (D-01 members, D-02 vortex isolation, D-03 toolchain pin)
  </read_first>
  <files>Cargo.toml, rust-toolchain.toml, .gitignore</files>
  <action>
    Create the workspace root `Cargo.toml` with `resolver = "2"` and `[workspace] members = ["crates/loom-core", "crates/loom-ffi", "crates/loom-fixtures"]` (D-01). Add a `[workspace.dependencies]` table pinning every shared crate to the exact STACK.md versions: `arrow` 58.3.0 with `features = ["ffi"]`, `arrow-array` 58.3.0, `arrow-schema` 58.3.0, `arrow-data` 58.3.0, and the four `vortex-*` crates at 0.74.0 (these last four are declared here but only `loom-fixtures` will consume them, per D-02). Add a `[patch.crates-io]` section that forces all `arrow-*` sub-crates to resolve to 58.3.0 so the workspace cannot select two minor/patch instances (PITFALLS P9). Add `[profile.release]` with `panic = "abort"` (CORE-02). Do NOT add `vortex-file`, `vortex-serde`, `vortex-ipc`, `arrow2`, `cxx` anywhere (PITFALLS P8, do-not-use list). Create `rust-toolchain.toml` pinning a specific stable `channel` (use the latest stable known-good, e.g. a `1.8x.y` stable; record the exact version chosen in the SUMMARY) plus `components = ["rustfmt", "clippy"]` (D-03). Create `.gitignore` ignoring `/target`. Document in the SUMMARY the exact toolchain version pinned and the exact arrow patch version, since Phases 2-5 build against them.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 >/dev/null 2>&1; echo "metadata exit=$?"; grep -c 'panic = "abort"' Cargo.toml; grep -c 'channel' rust-toolchain.toml</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` parses: `cargo metadata --no-deps` exits 0 (after Task 2 crates exist; until then `cargo metadata` may report missing members — that is expected to clear in Task 2).
    - `grep 'panic = "abort"' Cargo.toml` returns exactly one line.
    - `grep 'patch.crates-io' Cargo.toml` returns a line.
    - `grep -E 'channel\s*=' rust-toolchain.toml` returns a line with a concrete stable version (not `stable` unqualified — a pinned `1.x.y`).
    - `grep -E 'vortex-file|vortex-serde|vortex-ipc|arrow2|"cxx"' Cargo.toml` returns nothing.
  </acceptance_criteria>
  <done>Workspace root pins all arrow-* to 58.3.0 via [patch.crates-io], sets panic=abort, toolchain is pinned to a concrete stable version, and no forbidden crates appear.</done>
</task>

<task type="auto">
  <name>Task 2: Three member crates with clean dependency boundary + System allocator</name>
  <read_first>
    - Cargo.toml (workspace root from Task 1 — for [workspace.dependencies] keys to reference with `.workspace = true`)
    - .planning/research/ARCHITECTURE.md (module decomposition; loom-core owns decode modules, loom-ffi owns the FFI shim, the build topology)
    - .planning/research/PITFALLS.md (P5 allocator mismatch: #[global_allocator] static A: System = System;)
    - .planning/phases/01-scaffold-and-ffi-boundary/01-CONTEXT.md (D-02: loom-core MUST NOT depend on vortex-*)
  </read_first>
  <files>crates/loom-core/Cargo.toml, crates/loom-core/src/lib.rs, crates/loom-ffi/Cargo.toml, crates/loom-ffi/src/lib.rs, crates/loom-fixtures/Cargo.toml, crates/loom-fixtures/src/lib.rs</files>
  <action>
    Create `loom-core` (`crates/loom-core/Cargo.toml`, `src/lib.rs`): a normal library crate. Depend on `arrow`, `arrow-array`, `arrow-schema`, `arrow-data` via `.workspace = true`. Do NOT add any `vortex-*` dependency (D-02 — this boundary is the honesty of the verification proof). `lib.rs` declares empty/placeholder module skeletons matching ARCHITECTURE.md (`arrow_builder_output`, `l1_model`, `l2_kernel_registry`) as `mod` stubs with no logic, or simply a documented empty lib — do not implement decode logic (that is Phase 3). Add `#![forbid(unsafe_code)]` to loom-core so the unsafe surface stays isolated in loom-ffi (D-01).
    Create `loom-ffi` (`crates/loom-ffi/Cargo.toml`, `src/lib.rs`): set `[lib] crate-type = ["staticlib", "rlib"]` (staticlib for the eventual link into the C++ extension per CORE-01; rlib so Rust unit tests in Plan 02 can call the inner functions). Depend on `loom-core` (path dep) and `arrow` via `.workspace = true`. In `src/lib.rs`, declare the System global allocator: `#[global_allocator] static GLOBAL: std::alloc::System = std::alloc::System;` (CORE-02, PITFALLS P5). No `extern "C"` function yet — that is Plan 02. Add a crate-level doc comment noting the FFI surface arrives in Plan 02.
    Create `loom-fixtures` (`crates/loom-fixtures/Cargo.toml`, `src/lib.rs`): the ONLY crate allowed to depend on `vortex-*` (D-02). Declare the four `vortex-*` deps via `.workspace = true` but feel free to gate them so an empty build still compiles; `src/lib.rs` is an empty placeholder for now (programmatic fixture builders arrive in Phase 3). Ensure NO `vortex-file`/`vortex-serde`/`vortex-ipc` (PITFALLS P8).
    After creating all three, run `cargo build` and `cargo build --release` at the workspace root and fix any warnings to zero (CORE-01 success criterion 4 requires no warnings).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tee /tmp/loom_build.log; echo "build exit=${PIPESTATUS[0]}"; cargo build --workspace --release 2>&1 | grep -c -E 'warning:' ; grep -rc 'global_allocator' crates/loom-ffi/src/lib.rs; cargo tree -d 2>/dev/null | grep -c arrow; grep -c vortex-file Cargo.lock 2>/dev/null || echo 0; cargo tree -p loom-core 2>/dev/null | grep -c vortex</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0.
    - `cargo build --workspace --release 2>&1 | grep -c -E 'warning:'` returns 0 (zero warnings — ROADMAP success criterion 4).
    - `cargo tree -d | grep arrow` returns zero lines (ROADMAP success criterion 1, PITFALLS P9).
    - `grep vortex-file Cargo.lock` returns nothing (ROADMAP success criterion 2, PITFALLS P8).
    - `cargo tree -p loom-core | grep vortex` returns nothing (D-02 boundary clean).
    - `grep 'global_allocator' crates/loom-ffi/src/lib.rs` returns a line and `grep 'staticlib' crates/loom-ffi/Cargo.toml` returns a line (CORE-01, CORE-02).
    - `grep 'forbid(unsafe_code)' crates/loom-core/src/lib.rs` returns a line (unsafe isolated to loom-ffi).
  </acceptance_criteria>
  <done>All three crates compile with zero warnings in debug and release; arrow has zero duplicates; loom-core has no vortex dependency; loom-ffi declares the System allocator and is a staticlib; vortex-file is absent from the lockfile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → workspace | crates.io packages resolved into the build; supply-chain surface |
| Rust staticlib → C++ extension (future) | this plan establishes the allocator invariant that boundary depends on; no live FFI yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Rust↔C++ heap ownership (allocator mismatch) | mitigate | Install `#[global_allocator] static GLOBAL: System = System;` in loom-ffi so Rust and DuckDB share the system allocator and no cross-allocator free occurs (PITFALLS P5). Verified by `grep global_allocator`. |
| T-01-02 | Tampering | arrow-rs duplicate type instances across FFI | mitigate | `[patch.crates-io]` unifies all arrow-* to 58.3.0; `cargo tree -d \| grep arrow` must return zero (PITFALLS P9). ABI-identical-but-Rust-distinct types are the failure mode being closed. |
| T-01-03 | Denial of Service | panic unwinding past a future extern "C" frame → process abort | mitigate | `[profile.release] panic = "abort"` set now so the invariant exists before Plan 02 adds the FFI surface (PITFALLS P3, CORE-02). catch_unwind wrapper itself lands in Plan 02. |
| T-01-04 | Tampering | scope-creep dependency (vortex-file/serde/ipc) dragging in unaudited file-format parsing | accept→mitigate | Forbid these crates in Cargo.toml/lock; `grep vortex-file Cargo.lock` returns nothing (PITFALLS P8). |
| T-01-SC | Tampering | npm/pip/cargo installs (supply chain) | mitigate | All deps are pinned to exact versions from STACK.md (researcher-vetted: vortex-data org, apache/arrow-rs, official cbindgen). No `[ASSUMED]`/`[SUS]` packages in STACK.md. Cargo.lock committed for reproducibility. |

No `high` open threats remain. Phase 1 has no runtime untrusted-input surface (no decode logic, no network).
</threat_model>

<verification>
Run at workspace root after both tasks:
- `cargo build --workspace --release` exits 0 with zero `warning:` lines.
- `cargo tree -d | grep arrow` → zero lines (CORE-01).
- `grep vortex-file Cargo.lock` → nothing (scope discipline).
- `cargo tree -p loom-core | grep vortex` → nothing (D-02).
- `grep 'panic = "abort"' Cargo.toml` and `grep 'global_allocator' crates/loom-ffi/src/lib.rs` → present (CORE-02).
</verification>

<success_criteria>
- 3-crate workspace (loom-core, loom-ffi, loom-fixtures) compiles in debug + release with zero warnings (D-01).
- arrow-* unified to 58.3.0, zero duplicates (CORE-01).
- panic=abort + System allocator present and verified (CORE-02).
- loom-core has zero vortex-* deps; vortex-file absent from lockfile (D-02, scope).
- Toolchain pinned to a concrete stable version (D-03).
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-and-ffi-boundary/01-01-SUMMARY.md` when done. MUST record: the exact pinned Rust toolchain version, the exact arrow patch version resolved, the three crate paths, and confirmation of the four invariant commands' outputs.
</output>
</content>
</invoke>
