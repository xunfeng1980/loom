# Rust build artifacts — never commit the target directory.
/target

# Project-local downloaded tool artifacts.
/.tools

# K Framework generated artifacts and temp files.
/contrib/kloom/.build
/contrib/kloom/.tmp
