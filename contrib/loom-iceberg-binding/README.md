# loom-iceberg-binding (contrib placeholder)

This crate previously held Phase 28 local-file Iceberg table/ref binding proof.

It has been moved to `contrib/` and reduced to a placeholder as part of the
MVP1 scope-tightening effort.  Iceberg binding is not on the current core track.

To restore: revive `src/`, `tests/`, and `tests/fixtures/` from git history.
